<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.3.1">Jekyll</generator><link href="https://hotshot824.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://hotshot824.github.io/" rel="alternate" type="text/html" /><updated>2024-03-19T12:50:54+00:00</updated><id>https://hotshot824.github.io/feed.xml</id><title type="html">Home</title><subtitle>Build Jekyll site with the GitBook style.
</subtitle><author><name>Benson Hsu</name></author><entry><title type="html">Compiler | ANTLR Guide</title><link href="https://hotshot824.github.io/jekylls/2024-03-08-ANTLR_guide.html" rel="alternate" type="text/html" title="Compiler | ANTLR Guide" /><published>2024-03-08T00:00:00+00:00</published><updated>2024-03-08T00:00:00+00:00</updated><id>https://hotshot824.github.io/jekylls/ANTLR_guide</id><content type="html" xml:base="https://hotshot824.github.io/jekylls/2024-03-08-ANTLR_guide.html"><![CDATA[<blockquote class="block-tip">
  <p>ANTLR (ANother Tool for Language Recognition) 是一個強大的 Parser Generator，可以用來產生語法分析器，並且可以用來產生語法分析樹，
並且在一些商業或開源程式中被使用，例如:</p>
  <ul>
    <li>Eclipse Modeling Framework, 就使用 ANTLR 來將程式碼轉換為 OCL (Object Constraint Language)</li>
    <li>PrestoDB 也使用 ANTLR 來解析 SQL</li>
  </ul>
</blockquote>

<h3 id="antlr-in-maven">ANTLR in Maven</h3>

<blockquote>
  <p><a href="https://www.antlr.org/api/maven-plugin/latest/usage.html">ANTLR v4 Maven</a> 這邊是 ANTLR v4 Maven 的官方文件，可以參考這個文件來使用</p>
</blockquote>

<p>Maven 中有幾個 ANTLR 相關的 Plugin 可以使用:</p>
<ul>
  <li>ANTLR 4 Maven Plugin: 最完整的 ANTLR Maven Plugin，可以在 Maven 中使用 ANTLR4</li>
  <li>ANTLR 4 Runtime Maven: 只包含 ANTLR 4 Runtime 的部分</li>
  <li>ANTLR 4 Tool</li>
</ul>

<p><strong>Maven Configuration:</strong><br />
下面的範例使用了 ANTLR 4 Maven Plugin，讓 ANTLR 加入到 Maven 的 Build 週期中</p>
<ul>
  <li>sourceDirectory: ANTLR 原始檔案的位置</li>
  <li>outputDirectory: ANTLR 產生的檔案的位置</li>
  <li>可以輸入 <code class="language-plaintext highlighter-rouge">mvn org.antlr:antlr4-maven-plugin:help -Ddetail=true</code> 來查看 ANTLR 4 Maven Plugin 的詳細資訊
    <ul>
      <li>這邊會列出 configuration 的詳細資訊，反而比 antlr4-maven-plugin 官方文件詳細</li>
    </ul>
  </li>
</ul>

<div class="language-xml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;plugins&gt;</span>
    <span class="nt">&lt;plugin&gt;</span>
        <span class="nt">&lt;groupId&gt;</span>org.antlr<span class="nt">&lt;/groupId&gt;</span>
        <span class="nt">&lt;artifactId&gt;</span>antlr4-maven-plugin<span class="nt">&lt;/artifactId&gt;</span>
        <span class="nt">&lt;version&gt;</span>4.13.1<span class="nt">&lt;/version&gt;</span>
        <span class="nt">&lt;configuration&gt;</span>
            <span class="nt">&lt;listener&gt;</span>true<span class="nt">&lt;/listener&gt;</span>
            <span class="nt">&lt;visitor&gt;</span>true<span class="nt">&lt;/visitor&gt;</span>
            <span class="nt">&lt;sourceDirectory&gt;</span>src/main/java<span class="nt">&lt;/sourceDirectory&gt;</span>
            <span class="nt">&lt;outputDirectory&gt;</span>src/main/generated-sources<span class="nt">&lt;/outputDirectory&gt;</span>
        <span class="nt">&lt;/configuration&gt;</span>
        <span class="nt">&lt;executions&gt;</span>
            <span class="nt">&lt;execution&gt;</span>
                <span class="nt">&lt;id&gt;</span>antlr<span class="nt">&lt;/id&gt;</span>
                <span class="nt">&lt;goals&gt;</span>
                    <span class="nt">&lt;goal&gt;</span>antlr4<span class="nt">&lt;/goal&gt;</span>
                <span class="nt">&lt;/goals&gt;</span>
            <span class="nt">&lt;/execution&gt;</span>
        <span class="nt">&lt;/executions&gt;</span>
    <span class="nt">&lt;/plugin&gt;</span>
<span class="nt">&lt;/plugins&gt;</span>
</code></pre></div></div>

<p><strong>ANTLR Command Line Options:</strong></p>
<ul>
  <li>如果不使用 Maven 提供的 configuration，可以直接在 pom.xml 中加入 ANTLR4 的參數
    <ul>
      <li>這部分就跟 <a href="https://gist.github.com/subchen/464b05ee611bce031984">ANTLR Command Line</a> 的參數輸出一樣，可以直接在這邊輸入</li>
    </ul>

    <div class="language-xml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;configuration&gt;</span>
    <span class="nt">&lt;arguments&gt;</span>
        <span class="nt">&lt;argument&gt;</span>-package<span class="nt">&lt;/argument&gt;</span>
        <span class="nt">&lt;argument&gt;</span>ocl.runner<span class="nt">&lt;/argument&gt;</span>
        <span class="nt">&lt;argument&gt;</span>-o<span class="nt">&lt;/argument&gt;</span>
        <span class="nt">&lt;argument&gt;</span>${project.basedir}/src/generated-sources<span class="nt">&lt;/argument&gt;</span>
    <span class="nt">&lt;/arguments&gt;</span>
<span class="nt">&lt;/configuration&gt;</span>
</code></pre></div>    </div>
  </li>
</ul>

<p>把上面設定好就可以再 Maven 的建置中使用 ANTLR 產生一個語法分析器了，通常會在 Java 中 import ANTLR 的相關工具，
所以建議是在 dependency 也加入 <code class="language-plaintext highlighter-rouge">antlr4-maven-plugin</code> 或 <code class="language-plaintext highlighter-rouge">antlr4-runtime</code>。</p>

<hr />

<h3 id="parser-generation-and-execution">Parser Generation and Execution</h3>

<p>通常不會把 ANTLR 產生的檔案與原始檔案放在一起，這樣在管理上會比較麻煩，所以通常會把 ANTLR 產生的檔案放在另一個 Directory。</p>
<ul>
  <li>這邊選擇把 ANTLR 產生的檔案放在 java 的同層目錄下的 <code class="language-plaintext highlighter-rouge">generated-sources</code> 中</li>
  <li>因為多了一個地方存放 Java source code，所以可以使用 <code class="language-plaintext highlighter-rouge">build-helper-maven-plugin</code> 來幫助 Maven 來找到這些 source code</li>
</ul>

<p>如果這些設定都完成了，就可以在 antlr4 的 sourceDirectory 中加入 grammar file 了，這邊以下面的語法為例:</p>

<pre><code class="language-antlr4">grammar Expr;		
prog:	expr EOF ;
expr:	expr ('*'|'/') expr
    |	expr ('+'|'-') expr
    |	INT
    |	'(' expr ')'
    ;
NEWLINE : [\r\n]+ -&gt; skip;
INT     : [0-9]+ ;
</code></pre>

<blockquote>
  <p>加入這個 grammar file 到 sourceDirectory 中，然後執行 <code class="language-plaintext highlighter-rouge">mvn clean install</code> 就可以看到 ANTLR 產生的語法分析器了</p>
</blockquote>

<p>在 java 中添加一個 main function 來測試 ANTLR 產生的語法分析器，如下:</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">class</span> <span class="nc">Expr</span> <span class="o">{</span>
    <span class="kd">public</span> <span class="kd">static</span> <span class="kt">void</span> <span class="nf">main</span><span class="o">(</span><span class="nc">String</span> <span class="o">[]</span> <span class="n">args</span><span class="o">)</span> <span class="kd">throws</span> <span class="nc">Exception</span>
    <span class="o">{</span>
        <span class="nc">CharStream</span> <span class="n">charStream</span> <span class="o">=</span> <span class="nc">CharStreams</span><span class="o">.</span><span class="na">fromString</span><span class="o">(</span><span class="s">"10+20*30"</span><span class="o">);</span>

        <span class="c1">// Make lexer</span>
        <span class="nc">ExprLexer</span> <span class="n">lexer</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ExprLexer</span><span class="o">(</span><span class="n">charStream</span><span class="o">);</span>
        <span class="c1">// Get a TokenStream on the lexer</span>
        <span class="nc">CommonTokenStream</span> <span class="n">tokens</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">CommonTokenStream</span><span class="o">(</span> <span class="n">lexer</span> <span class="o">);</span>
        <span class="c1">// Make a parser attached to the token stream</span>
        <span class="nc">ExprParser</span> <span class="n">parser</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ExprParser</span><span class="o">(</span> <span class="n">tokens</span> <span class="o">);</span>
        <span class="c1">// Get the top node (the root) of the parse tree then print it</span>
        <span class="nc">ParseTree</span> <span class="n">tree</span> <span class="o">=</span> <span class="n">parser</span><span class="o">.</span><span class="na">prog</span><span class="o">();</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="n">tree</span><span class="o">.</span><span class="na">toStringTree</span><span class="o">(</span><span class="n">parser</span><span class="o">));</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>執行 <code class="language-plaintext highlighter-rouge">mvn exec:java -Dexec.mainClass="{YourMainClass}"</code> 就可以看到 ANTLR 產生的語法分析樹了，也可以在 <code class="language-plaintext highlighter-rouge">pom.xml</code> 中加入 <code class="language-plaintext highlighter-rouge">exec-maven-plugin</code> 來執行這個 main function。</p>

<hr />

<h3 id="antlr-command-line">ANTLR Command Line</h3>

<p>ANTLR 也有提供一系列 Command Line 的指令，可以用來進行產生語法分析器跟 GUI 的 Parser Tree，下面是安裝步驟:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>wget https://www.antlr.org/download/antlr-4.13.1-complete.jar
<span class="nb">mv </span>antlr-4.13.1-complete.jar /opt/javalib/

vim ~/.bashrc 
<span class="c"># Join the following to the .bashrc file</span>
<span class="c"># export JAVA_HOME=/opt/jdk-17.0.10</span>
<span class="c"># export PATH=$JAVA_HOME/bin:$PATH</span>
<span class="c"># export CLASSPATH=/opt/javalib/antlr-4.13.1-complete.jar</span>
<span class="c"># alias antlr4='java -jar /opt/javalib/antlr-4.13.1-complete.jar'</span>
<span class="c"># alias grun='java org.antlr.v4.gui.TestRig'</span>

<span class="nb">source</span> ~/.bashrc

antlr4
<span class="c"># [benson@arch ~]$ antlr4 </span>
<span class="c"># ANTLR Parser Generator  Version 4.13.1</span>
<span class="c">#  -o ___              specify output directory where all output is generated</span>
<span class="c">#  -lib ___            specify location of grammars, tokens files</span>
<span class="c">#  ...</span>
<span class="c">#  -Xexact-output-dir  all output goes into -o dir regardless of paths/package</span>
</code></pre></div></div>

<blockquote class="block-warning">
  <h5 id="last-edit">Last Edit</h5>
  <p>03-19-2024 19:32</p>
</blockquote>]]></content><author><name>Benson Hsu</name></author><category term="Jekylls" /><category term="Compiler" /><summary type="html"><![CDATA[ANTLR (ANother Tool for Language Recognition) 是一個強大的 Parser Generator，可以用來產生語法分析器，並且可以用來產生語法分析樹， 並且在一些商業或開源程式中被使用，例如: Eclipse Modeling Framework, 就使用 ANTLR 來將程式碼轉換為 OCL (Object Constraint Language) PrestoDB 也使用 ANTLR 來解析 SQL]]></summary></entry><entry><title type="html">Note | Arch Linux Installation</title><link href="https://hotshot824.github.io/jekylls/2024-03-06-arch_linux_installation.html" rel="alternate" type="text/html" title="Note | Arch Linux Installation" /><published>2024-03-06T00:00:00+00:00</published><updated>2024-03-06T00:00:00+00:00</updated><id>https://hotshot824.github.io/jekylls/arch_linux_installation</id><content type="html" xml:base="https://hotshot824.github.io/jekylls/2024-03-06-arch_linux_installation.html"><![CDATA[<blockquote class="block-tip">
  <p>Notes how to install Arch Linux</p>
</blockquote>

<p>紀錄一下怎麼安裝 Arch Linux，非常推薦讀官方的 <a href="https://wiki.archlinux.org/title/Installation_guide">Installation guide</a> 這邊大概就是一個簡化版的安裝流程，不得不說 Arch 的自由度真的很高，
所以安裝的時候可以根據自己的需求來安裝。</p>

<h3 id="pre-installation">Pre-installation</h3>

<p><a href="#11-connect-to-the-internet">1.1 Connect to the internet</a><br />
<a href="#12-update-the-system-clock">1.2 Update the system clock</a><br />
<a href="#13-partition-the-disks">1.3 Partition the disks</a></p>

<h5 id="11-connect-to-the-internet">1.1 Connect to the internet</h5>

<p>這邊會透過 <a href="https://wiki.archlinux.org/title/Systemd-networkd">systemd-networkd</a> 來配置網路，如果要設置 Static IP:</p>
<ul>
  <li><code class="language-plaintext highlighter-rouge">nano /etc/systemd/network/20-wired.network</code>
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">[</span>Match]
<span class="nv">Name</span><span class="o">=</span>en<span class="k">*</span>

<span class="o">[</span>Network]
<span class="nv">DHCP</span><span class="o">=</span>no
<span class="nv">Address</span><span class="o">=</span>10.1.10.9/24
<span class="nv">Gateway</span><span class="o">=</span>10.1.10.1
<span class="nv">DNS</span><span class="o">=</span>10.1.10.1
<span class="c">#DNS=8.8.8.8</span>
</code></pre></div>    </div>
  </li>
  <li>配置後 <code class="language-plaintext highlighter-rouge">systemctl restart systemd-networkd</code> 重啟網路服務
    <ul>
      <li>檢查 <code class="language-plaintext highlighter-rouge">ip addr</code> 是否與配置一樣</li>
    </ul>
  </li>
</ul>

<h5 id="12-update-the-system-clock">1.2 Update the system clock</h5>

<p>使用 <a href="https://man.archlinux.org/man/timedatectl.1">timedatectl</a> 來設置系統時間，首先 <code class="language-plaintext highlighter-rouge">timedatectl set-ntp true</code> 啟動 NTP 服務:</p>
<ul>
  <li><code class="language-plaintext highlighter-rouge">timedatectl list-timezones | grep Asia/Taipei</code> 查詢時區是否存在</li>
  <li><code class="language-plaintext highlighter-rouge">timedatectl set-timezone Asia/Taipei</code> 設置時區</li>
  <li><code class="language-plaintext highlighter-rouge">timedatectl status</code> 查看目前時間是否正確</li>
</ul>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>root@archiso ~ <span class="c"># timedatectl status</span>
               Local <span class="nb">time</span>: Sun 2024-03-03 15:21:00 CST
           Universal <span class="nb">time</span>: Sun 2024-03-03 07:21:00 UTC
                 RTC <span class="nb">time</span>: Sun 2024-03-03 07:21:00
                Time zone: Asia/Taipei <span class="o">(</span>CST, +0800<span class="o">)</span>
System clock synchronized: <span class="nb">yes
              </span>NTP service: active
          RTC <span class="k">in </span><span class="nb">local </span>TZ: no
</code></pre></div></div>

<blockquote>
  <p>設置成功應該會看到以上的訊息</p>
</blockquote>

<h5 id="13-partition-the-disks">1.3 Partition the disks</h5>

<p><a href="https://wiki.archlinux.org/title/fdisk">fdisk</a> 是一個常用的分割硬碟的工具，這邊會使用 fdisk 來分割硬碟:</p>
<ol>
  <li>先使用 <code class="language-plaintext highlighter-rouge">fdisk -l</code> 查看硬碟分割情況</li>
  <li><code class="language-plaintext highlighter-rouge">fdisk /dev/sda</code> 開始分割硬碟
    <ul>
      <li>這邊我只分割一個 SWAP 跟一個主分割區，因為剩下的空間會使用 Btrfs 來管理</li>
    </ul>
  </li>
</ol>

<div align="center">
    <img src="/image/2024/03-06-arch_linux_installation/1.jpg" width="75%" height="75%" />
</div>

<ol>
  <li>使用 mkfs 來格式化分割區
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mkswap /dev/sda1
swapon /dev/sda1
mkfs.btrfs /dev/sda2
mount /dev/sda2 /mnt
</code></pre></div>    </div>
  </li>
</ol>

<div align="center">
    <img src="/image/2024/03-06-arch_linux_installation/2.jpg" width="75%" height="75%" />
</div>

<p>mount 之後操作就跟 Debian 一樣，唯一的差別是 arch 沒有 target 這個目錄，所以把操作的目錄都換成 /mnt 就好。</p>
<ul>
  <li>在最後的時候記得用 <code class="language-plaintext highlighter-rouge">genfstab -U /mnt &gt;&gt; /mnt/etc/fstab</code> 來生成 fstab</li>
</ul>

<blockquote class="block-warning">
  <p>延伸閱讀: 可以參考 <a href="./2024-01-05-debian_btrfs.html">Using btrfs on Debian</a> 來設置 Btrfs，或者官方說明 <a href="https://wiki.archlinux.org/title/btrfs">Arch - btrfs</a></p>
</blockquote>

<h3 id="install-linux">Install Linux</h3>

<p><a href="#21-select-the-mirrors">2.1 Select the mirrors</a><br />
<a href="#22-install-the-base-packages">2.2 Install the base packages</a></p>

<h5 id="21-select-the-mirrors">2.1 Select the mirrors</h5>

<p>這邊要選擇最快的鏡像，可以使用 <code class="language-plaintext highlighter-rouge">reflector</code> 來自動選擇最快的鏡像，或者手動編輯 <code class="language-plaintext highlighter-rouge">/etc/pacman.d/mirrorlist</code> 來選擇鏡像。</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">reflector --country Taiwan --age 12 --protocol https --sort rate --save /etc/pacman.d/mirrorlist</code>
    <ul>
      <li>選擇最近 12 小時內更新過位於台灣的鏡像，並且排序下載速度排序</li>
    </ul>
  </li>
  <li>官方也有一個提供的 <a href="https://archlinux.org/mirrorlist/">Mirrorlist</a></li>
</ul>

<blockquote class="block-warning">
  <p>這步驟也可以在安裝完系統後再執行，最好在進行前備份 <code class="language-plaintext highlighter-rouge">/etc/pacman.d/mirrorlist</code></p>
</blockquote>

<h5 id="22-install-the-base-packages">2.2 Install the base packages</h5>

<p>使用 pacstrap 來安裝基本的套件，<code class="language-plaintext highlighter-rouge">pacstrap /mnt base linux linux-firmware</code></p>
<ul>
  <li>如果想要安裝其他套件可以在後面加上套件名稱，例如: <code class="language-plaintext highlighter-rouge">pacstrap /mnt base linux linux-firmware vim openssh</code></li>
  <li>先使用 <code class="language-plaintext highlighter-rouge">pacman -Sy</code> 更新套件庫，就可以使用 <code class="language-plaintext highlighter-rouge">pacman -Ss</code> 搜尋套件名稱</li>
</ul>

<h3 id="configure-the-system">Configure the system</h3>

<p><a href="#32-chroot">3.2 Chroot</a><br />
<a href="#35-network-configuration">3.5 Network configuration</a><br />
<a href="#38-install-a-bootloader">3.8 Install a bootloader</a></p>

<p>剩下的部分官方文件都有詳細的說明，這邊就不再贅述，可以參考 <a href="https://wiki.archlinux.org/title/Installation_guide">Installation guide</a> 來進行安裝。</p>
<ul>
  <li>fstab 跟官方說明設置就好，然後記得查看是否有錯誤</li>
</ul>

<h5 id="32-chroot">3.2 Chroot</h5>

<ol>
  <li>chroot 可以讓你進入安裝好的系統，這樣就可以提前進行一些設置
    <ul>
      <li>如果在 pasctrap 少安裝了一些套件，可以在這邊安裝，例如: <code class="language-plaintext highlighter-rouge">pacman -S openssh</code></li>
      <li>安裝後記得到 <code class="language-plaintext highlighter-rouge">systemctl enable sshd</code> 啟動 sshd 服務</li>
    </ul>
  </li>
  <li>Time Zone
    <ul>
      <li>用 symbolic link 來設置時區，然後用 <code class="language-plaintext highlighter-rouge">hwclock --systohc</code> 來設置硬體時鐘</li>
    </ul>
  </li>
  <li>Localization</li>
  <li>Hostname</li>
</ol>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Time Zone</span>
<span class="nb">ln</span> <span class="nt">-sf</span> /usr/share/zoneinfo/Asia/Taipei /etc/localtime
hwclock <span class="nt">--systohc</span>

<span class="c"># Localization</span>
vim /etc/locale.gen
<span class="c"># Uncomment `en_US.UTF-8 UTF-8` and other needed locales</span>
locale-gen
<span class="nb">echo</span> <span class="s2">"LANG=en_US.UTF-8"</span> <span class="o">&gt;</span> /etc/locale.conf

<span class="c"># Hostname</span>
<span class="nb">echo</span> <span class="k">${</span><span class="nv">YOU_HOSTNAME</span><span class="k">}</span> <span class="o">&gt;</span> /etc/hostname
</code></pre></div></div>

<h5 id="35-network-configuration">3.5 Network configuration</h5>

<p>這邊可以參考 <a href="https://wiki.archlinux.org/title/Systemd-networkd">systemd-networkd</a> 來設置網路，或者使用其他的套件來設置網路。</p>

<ul>
  <li>下面是沿用 <a href="https://wiki.archlinux.org/title/Systemd-networkd">systemd-networkd</a> 的設置</li>
</ul>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>vim /etc/systemd/network/20-wired.network
<span class="c"># Wired network like following:</span>
<span class="c"># [Match]</span>
<span class="c"># Name=en*</span>

<span class="c"># [Network]</span>
<span class="c"># DHCP=no</span>
<span class="c"># Address=10.1.10.9/24</span>
<span class="c"># Gateway=10.1.10.1</span>
<span class="c"># DNS=10.1.10.1</span>
systemctl <span class="nb">enable </span>systemd-networkd
</code></pre></div></div>

<h5 id="38-install-a-bootloader">3.8 Install a bootloader</h5>

<blockquote class="block-warning">
  <p>這一步非常重要，因為如果沒有 bootloader 就無法開機</p>
</blockquote>

<p>在安裝 bootloader 前先把 initramfs 跟 passwd 設置好，然後再安裝 bootloader。</p>
<ul>
  <li>選擇 GRUB 作為 bootloader，Arch 這邊有其他 <a href="https://wiki.archlinux.org/title/Arch_boot_process#Boot_loader">bootloader</a> 的選擇，可以參考官方文件</li>
  <li>這裡沒有 UEFI 的安裝方式，如果要安裝 UEFI 可以參考官方文件 <a href="https://wiki.archlinux.org/title/GRUB#BIOS_systems">Arch - GRUB</a></li>
</ul>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Initramfs</span>
mkinitcpio <span class="nt">-P</span>
<span class="c"># Set root password</span>
passwd
<span class="c"># Install bootloader, os-prober can detect other OS</span>
pacman  <span class="nt">-Sy</span> grub efibootmgr os-prober
grub-install <span class="nt">--target</span><span class="o">=</span>i386-pc /dev/sda
grub-mkconfig <span class="nt">-o</span> /boot/grub/grub.cfg
</code></pre></div></div>

<p>這裡可以把剩下的部分都設置好，然後 exit 離開 chroot，官方是建議關機前 <code class="language-plaintext highlighter-rouge">umount -R /mnt</code> 卸載所有的分割區，然後再 <code class="language-plaintext highlighter-rouge">reboot</code>，
重啟之後如果進入 GRUB 引導到 Arch Linux 就代表安裝成功。</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">useradd -m -G wheel,users -s /bin/bash ${USER_NAME}</code> 新增使用者
    <ul>
      <li><code class="language-plaintext highlighter-rouge">passwd ${USER_NAME}</code> 設置密碼</li>
    </ul>
  </li>
</ul>

<h3 id="other">Other</h3>

<h5 id="41-firewall">4.1 Firewall</h5>

<p>Arch 通常沒有預設安裝防火牆，並且 iptables 是被 systemd 服務管理的，所以最好開啟 <code class="language-plaintext highlighter-rouge">systemctl enable iptables</code> 來啟動防火牆服務。
並且要設置好防火牆規則，這裡有一個簡單的腳本 <a href="https://gist.github.com/Hotshot824/5021fca047a65b6ff6df2139e59cf9e4#file-iptables-script-sh">iptables-script.sh</a></p>

<ul>
  <li>設定完記得保存規則
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>iptables-save <span class="o">&gt;</span> /etc/iptables/iptables.rules
ip6tables-save <span class="o">&gt;</span> /etc/iptables/ip6tables.rules
</code></pre></div>    </div>
  </li>
</ul>

<blockquote class="block-warning">
  <h5 id="last-edit">Last Edit</h5>
  <p>03-06-2024 15:21</p>
</blockquote>]]></content><author><name>Benson Hsu</name></author><category term="Jekylls" /><category term="Note" /><summary type="html"><![CDATA[Notes how to install Arch Linux]]></summary></entry><entry><title type="html">Note | Apache Maven Multi-Module Project Guide</title><link href="https://hotshot824.github.io/jekylls/2024-03-01-maven_multi_module_project.html" rel="alternate" type="text/html" title="Note | Apache Maven Multi-Module Project Guide" /><published>2024-03-01T00:00:00+00:00</published><updated>2024-03-01T00:00:00+00:00</updated><id>https://hotshot824.github.io/jekylls/maven_multi_module_project</id><content type="html" xml:base="https://hotshot824.github.io/jekylls/2024-03-01-maven_multi_module_project.html"><![CDATA[<blockquote class="block-tip">
  <p>在開發 Java 的時候一定會考量的是如何管理專案的結構，尤其是當專案規模大到一定程度的時候，這裡就是因為遇到了陳年舊 Code 把所有程式塞在同一個專案裡面，
為了對專案做更好的管理架構，使用 Module 來分割專案是一個不錯的選擇，並且也可以增加代碼的可維護性。</p>
</blockquote>

<blockquote>
  <p>Reference: <a href="https://books.sonatype.com/mvnex-book/reference/multimodule.html">Maven by Example - Chapter 6. A Multi-Module Project</a>, <a href="https://maven.apache.org/guides/mini/guide-multiple-modules.html">Guide to Working with Multiple Modules</a></p>
</blockquote>

<p>其實概念上就是依靠 POM.xml 來管理多個專案，這樣就會有一個頂層的 POM.xml 在此之下會有多個子專案的 POM.xml，這樣就可以透過頂層的 POM.xml 來管理所有的子專案，
Maven 把這樣的機制稱為 Reactor，Maven 會透過 Reactor 來做以下操作:</p>

<ul>
  <li>收集所有可用的 Module 來 Build</li>
  <li>依照 Module 之間的依賴關係跟順序來 Build</li>
</ul>

<h3 id="parent-pom">Parent POM</h3>

<p>這邊用 Eclipse 來建立一個 Maven Project 把 <code class="language-plaintext highlighter-rouge">POM.xml</code> 中的 <code class="language-plaintext highlighter-rouge">&lt;packaing&gt;</code> 設定為 <code class="language-plaintext highlighter-rouge">pom</code>，這樣就會變成一個 Parent POM，這樣就可以透過這個 POM.xml 來管理所有的子專案。</p>

<ol>
  <li>建立一個 Maven Project 使用 maven-archetype-quickstart 這個 Archetype</li>
  <li>把 <code class="language-plaintext highlighter-rouge">POM.xml</code> 中的 <code class="language-plaintext highlighter-rouge">&lt;packaing&gt;</code> 設定為 <code class="language-plaintext highlighter-rouge">pom</code>
    <ul>
      <li>設定為 <code class="language-plaintext highlighter-rouge">pom</code> 之後就會變成一個 Parent POM 其他的檔案都可以刪掉了</li>
      <li>POM.xml 的 artifactId 可以命名為 root</li>
    </ul>
  </li>
  <li>Module 的 dependency 可以放在 Parent POM 中，這樣所有的 Module 都可以共用這些 dependency
    <ul>
      <li>如果單獨放在 Module 的 POM.xml 中，那麼就代表是只有這個 Module 可以使用這些 dependency</li>
      <li>version, groupId 可以在 Parent POM 中設定代表所有的 Module 都共用這些設定</li>
    </ul>
  </li>
</ol>

<p>Parent POM:</p>
<div class="language-xml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;project&gt;</span>
  <span class="nt">&lt;modelVersion&gt;</span>4.0.0<span class="nt">&lt;/modelVersion&gt;</span>

  <span class="nt">&lt;groupId&gt;</span>com.example.myapp<span class="nt">&lt;/groupId&gt;</span>
  <span class="nt">&lt;artifactId&gt;</span>root<span class="nt">&lt;/artifactId&gt;</span>
  <span class="nt">&lt;version&gt;</span>0.0.1-SNAPSHOT<span class="nt">&lt;/version&gt;</span>
  <span class="nt">&lt;packaging&gt;</span>pom<span class="nt">&lt;/packaging&gt;</span>

  <span class="nt">&lt;name&gt;</span>root<span class="nt">&lt;/name&gt;</span>
  <span class="nt">&lt;url&gt;</span>http://maven.apache.org<span class="nt">&lt;/url&gt;</span>

  <span class="nt">&lt;properties&gt;</span>
    <span class="nt">&lt;project.build.sourceEncoding&gt;</span>UTF-8<span class="nt">&lt;/project.build.sourceEncoding&gt;</span>
  <span class="nt">&lt;/properties&gt;</span>

  <span class="nt">&lt;dependencies&gt;</span>
    <span class="nt">&lt;dependency&gt;</span>
      <span class="nt">&lt;groupId&gt;</span>junit<span class="nt">&lt;/groupId&gt;</span>
      <span class="nt">&lt;artifactId&gt;</span>junit<span class="nt">&lt;/artifactId&gt;</span>
      <span class="nt">&lt;version&gt;</span>3.8.1<span class="nt">&lt;/version&gt;</span>
      <span class="nt">&lt;scope&gt;</span>test<span class="nt">&lt;/scope&gt;</span>
    <span class="nt">&lt;/dependency&gt;</span>
  <span class="nt">&lt;/dependencies&gt;</span>
<span class="nt">&lt;/project&gt;</span>
</code></pre></div></div>

<p>Module POM:</p>
<div class="language-xml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;project&gt;</span>
	<span class="nt">&lt;modelVersion&gt;</span>4.0.0<span class="nt">&lt;/modelVersion&gt;</span>
	<span class="nt">&lt;parent&gt;</span>
		<span class="nt">&lt;groupId&gt;</span>com.example.my<span class="nt">&lt;/groupId&gt;</span>
		<span class="nt">&lt;artifactId&gt;</span>root<span class="nt">&lt;/artifactId&gt;</span>
		<span class="nt">&lt;version&gt;</span>0.0.1-SNAPSHOT<span class="nt">&lt;/version&gt;</span>
	<span class="nt">&lt;/parent&gt;</span>

	<span class="nt">&lt;artifactId&gt;</span>application<span class="nt">&lt;/artifactId&gt;</span>
	<span class="nt">&lt;name&gt;</span>application<span class="nt">&lt;/name&gt;</span>
	<span class="nt">&lt;url&gt;</span>http://maven.apache.org<span class="nt">&lt;/url&gt;</span>

	<span class="nt">&lt;properties&gt;</span>
		<span class="nt">&lt;project.build.sourceEncoding&gt;</span>UTF-8<span class="nt">&lt;/project.build.sourceEncoding&gt;</span>
	<span class="nt">&lt;/properties&gt;</span>

	<span class="nt">&lt;dependencies&gt;</span>
	<span class="nt">&lt;/dependencies&gt;</span>
<span class="nt">&lt;/project&gt;</span>
</code></pre></div></div>

<h3 id="module">Module</h3>

<p>Module 就是一個獨立的專案，跟一般的 Maven Project 一樣，如果 Module 之間有互相 dependency 的關係，
那就在 POM 加入對該 Module 的 dependency，這樣 Maven 就會依照順序來 Build。</p>

<p>如果 application module 依賴於 data-site 就可以在 application 的 POM.xml 中加入以下的設定:</p>
<div class="language-xml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;dependency&gt;</span>
    <span class="nt">&lt;groupId&gt;</span>com.example.my<span class="nt">&lt;/groupId&gt;</span>
    <span class="nt">&lt;artifactId&gt;</span>data-site<span class="nt">&lt;/artifactId&gt;</span>
    <span class="nt">&lt;version&gt;</span>0.0.1-SNAPSHOT<span class="nt">&lt;/version&gt;</span>
<span class="nt">&lt;/dependency&gt;</span>
</code></pre></div></div>

<p>全部設定完後使用 <code class="language-plaintext highlighter-rouge">mvn verify</code> 就可以驗證整個專案的 Build，Maven 會依照 Module 之間的依賴關係跟順序來 Build jar。
如果想要把所有專案的 jar 都打包成一個大的 jar 可以使用一些 Maven Plugin 來達成，例如 <code class="language-plaintext highlighter-rouge">maven-assembly-plugin</code>。</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">[</span>INFO] <span class="nt">------------------------------------------------------------------------</span>
<span class="o">[</span>INFO] Reactor Summary <span class="k">for </span>root 0.0.1-SNAPSHOT:
<span class="o">[</span>INFO]
<span class="o">[</span>INFO] root ............................................... SUCCESS <span class="o">[</span>  0.921 s]
<span class="o">[</span>INFO] data-site .......................................... SUCCESS <span class="o">[</span>  2.003 s]
<span class="o">[</span>INFO] application ........................................ SUCCESS <span class="o">[</span>  0.732 s]
<span class="o">[</span>INFO] <span class="nt">------------------------------------------------------------------------</span>
<span class="o">[</span>INFO] BUILD SUCCESS
<span class="o">[</span>INFO] <span class="nt">------------------------------------------------------------------------</span>
<span class="o">[</span>INFO] Total <span class="nb">time</span>:  3.793 s
<span class="o">[</span>INFO] Finished at: 2024-03-01T20:31:18+08:00
<span class="o">[</span>INFO] <span class="nt">------------------------------------------------------------------------</span>
</code></pre></div></div>

<blockquote>
  <p>mvn verify 成功後就會看到類似上面的訊息，代表整個專案都 Build 成功了</p>
</blockquote>

<blockquote class="block-warning">
  <h5 id="last-edit">Last Edit</h5>
  <p>03-01-2024 15:32</p>
</blockquote>]]></content><author><name>Benson Hsu</name></author><category term="Jekylls" /><category term="software" /><summary type="html"><![CDATA[在開發 Java 的時候一定會考量的是如何管理專案的結構，尤其是當專案規模大到一定程度的時候，這裡就是因為遇到了陳年舊 Code 把所有程式塞在同一個專案裡面， 為了對專案做更好的管理架構，使用 Module 來分割專案是一個不錯的選擇，並且也可以增加代碼的可維護性。]]></summary></entry><entry><title type="html">OS | Storage and File System</title><link href="https://hotshot824.github.io/jekyll/2024-02-03-storage_and_file_system.html" rel="alternate" type="text/html" title="OS | Storage and File System" /><published>2024-02-03T00:00:00+00:00</published><updated>2024-02-03T00:00:00+00:00</updated><id>https://hotshot824.github.io/jekyll/storage_and_file_system</id><content type="html" xml:base="https://hotshot824.github.io/jekyll/2024-02-03-storage_and_file_system.html"><![CDATA[<blockquote class="block-tip">
  <p>Operating System: Design and Implementation course notes from CCU, lecturer Shiwu-Lo.</p>
</blockquote>

<blockquote class="block-tip">
  <p>參考資料: Jserv <a href="https://www.youtube.com/watch?v=iWvkoJawxQA&amp;list=PL6ls5wvkiFxwnI39JfI63PbN0bgEGwqPA">Linux 核心設計: 作業系統術語及概念</a>, <a href="https://hackmd.io/@sysprog/linux-file-system">Linux 核心設計: 檔案系統概念及實作手法</a></p>
</blockquote>

<blockquote class="block-warning">
  <p>Everything is a file. 是一個 Unix 設計哲學，但是這個設計並沒有完全實現在 UNIX 和後續的 BSD 上，直到 Plan 9 才算是真正的 Everything is a file，
使用了 9P 這個 Protocol，而 Linux 也採用了 9P 應用在虛擬化技術上。</p>
</blockquote>

<p>如果從恐龍書來學習作業系統，那麼通常 File System 會被放在比較後面來學習，但是以 UNIX 的發展來看的話，
File System 是從第零版的 UNIX 手冊就已經包含了檔案系統，而不見得已經包含了 Scheduler，
這也代表 File System 是作業系統中一個非常關鍵的部分。</p>

<ul>
  <li>Block storage</li>
  <li>Hard drive Scheduling</li>
  <li>File system Overview</li>
  <li>File structure</li>
  <li>Directory structure</li>
  <li>Filesytem across hard drives</li>
</ul>

<hr />

<h3 id="file-system-overview">File System Overview</h3>

<p>不管是 HDD 或是 SSD 都是 Block device，也就是資料的最小儲存單位是 block，對於 OS 來說硬碟就是一連串的 block。</p>
<ul>
  <li>再給定 Logical block address (LBA) 就可以讀取或寫入資料
    <ul>
      <li>LBA 就是一連串的 block 並且有邏輯上的連續性</li>
    </ul>
  </li>
  <li>CS 的先驅們設計了 File 和 Directory 這樣的概念，把 block device 變成邏輯上有意義的資料的集合</li>
</ul>

<p>在 Linux 中為了讓不同的 File System 可以共存，所以設計了 VFS(Virtual File System) 這樣的機制，VFS 是一組檔案操作的抽象介面，
只要依循 VFS 開發的 File System 就可以在執行時期動態的掛載到 Linux 的 Kernel 上。</p>

<div align="center">
    <img src="https://developer.ibm.com/developer/default/tutorials/l-linux-filesystem/images/figure1.gif" width="75%" height="75%" />
</div>

<h5 id="931-directory-and-file">9.3.1 Directory and File</h5>

<ul>
  <li>實際上 Directory 也是一個 File，在傳統的 UNIX 中 Directory 也是一個 File
    <ul>
      <li>其中記錄了其他 File 的名稱和對應的檔案編號</li>
    </ul>
  </li>
  <li>透過檔案編號就可以找到對應的 control block，control block 中記錄了檔案的 block 分布於硬碟上的位置</li>
</ul>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>benson@101-debian:~<span class="nv">$ </span>tree <span class="nt">-L</span> 2 <span class="nt">--inodes</span>
<span class="o">[</span>     12]  <span class="nb">.</span>
├── <span class="o">[</span>  16506]  .bash_history
├── <span class="o">[</span>   2545]  .cache
├── <span class="o">[</span>   3631]  go
│   └── <span class="o">[</span>   3632]  pkg
└── <span class="o">[</span>   1582]  workspace
    ├── <span class="o">[</span>   1792]  Hotshot824.github.io
    └── <span class="o">[</span>  17040]  jekyll-gitbook
5 directories, 0 files
</code></pre></div></div>

<p>這裡我們使用 <code class="language-plaintext highlighter-rouge">tree -L 2 --inodes</code> 來查看目前目錄下的檔案結構，會發現 Directory 也有對應的 inode，這代表 Directory 也是一個 File。</p>

<p><strong>Block Size</strong></p>

<ul>
  <li>大部分 Block device 所支援的 block 大部分是 4KB，跟 OS 的 page size 相同
    <ul>
      <li>這樣可以讓 OS 在管理記憶體與 Block device 的時候更有效率</li>
    </ul>
  </li>
</ul>

<h5 id="932-universal-io-model">9.3.2 Universal I/O Model</h5>

<p>Linux 因為要秉持 UNIX 哲學 Everything is a file，因此不同種類的 I/O 都是透過 File Descriptor 來操作的，這樣就可以在這個一致的介面下操作。</p>
<ul>
  <li>我們可以使用 mount 來查看這些虛擬檔案系統
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>benson@debian:~<span class="nv">$ </span>mount
sysfs on /sys <span class="nb">type </span>sysfs <span class="o">(</span>rw,nosuid,nodev,noexec,relatime<span class="o">)</span>
proc on /proc <span class="nb">type </span>proc <span class="o">(</span>rw,nosuid,nodev,noexec,relatime<span class="o">)</span>
udev on /dev <span class="nb">type </span>devtmpfs <span class="o">(</span>rw,nosuid,relatime,size<span class="o">=</span>4042436k,nr_inodes<span class="o">=</span>1010609,mode<span class="o">=</span>755,inode64<span class="o">)</span>
devpts on /dev/pts <span class="nb">type </span>devpts <span class="o">(</span>rw,nosuid,noexec,relatime,gid<span class="o">=</span>5,mode<span class="o">=</span>620,ptmxmode<span class="o">=</span>000<span class="o">)</span>
...
</code></pre></div>    </div>
  </li>
</ul>

<hr />

<h3 id="file-structure">File Structure</h3>

<p>這裡會介紹幾種常見的 File Structure，例如: Contiguous Allocation, Linked List, Indexed Allocation，但真正的重點是 i-node，
這是目前 Linux 使用的 File Structure。</p>

<h5 id="941-contiguous-allocation">9.4.1 Contiguous Allocation</h5>

<p><img src="/image/2024/02-03-storage_and_file_system/1.jpg" alt="" height="100%" width="100%" /></p>

<p>Contiguous Allocation(連續配置) 是一種最簡單的檔案配置方式:</p>
<ul>
  <li>基本構造是依照一個識別方式來找到檔案的起始位置，然後再依照檔案的大小來找到檔案的結束位置。</li>
  <li>因為檔案是連續配置的，所以不管是循序存取或是隨機存取的效率都很高
    <ul>
      <li>例如: 存取 file1 的第 20 個 block 只要讀取起始位置加上 20 就可以了</li>
    </ul>
  </li>
  <li>但是檔案基本上無法繼續增長，適用於靜態的檔案系統，例如: CD-ROM, 磁帶機等等
    <ul>
      <li>再 Linux 上例如 root file system 每次開機都會掛載，並不會變化就可以使用 Contiguous allocation</li>
    </ul>
  </li>
</ul>

<blockquote class="block-tip">
  <p>Contiguous allocation 可以獲得比較高的效率，但是勢必會有 fragmentation 的問題</p>
</blockquote>

<p><strong>Extents</strong></p>

<p>Extents 是取得連續配置與 fragmentation 中取得平衡的方案，file system 會盡可能的分配連續的空間給檔案，這樣的連續空間被稱為 Extents。</p>
<ul>
  <li>如果 file1 的空間不夠用，就會分配一個的 Extent 給 file1 延伸</li>
  <li>後續的演算法都可以使用 Extents 來擴充，加入 Extents 的概念</li>
</ul>

<blockquote>
  <p>Extents 可以視為分配空間的單位，但是保證這個單位空間是連續的</p>
</blockquote>

<h5 id="942-linked-list">9.4.2 Linked List</h5>

<p><img src="/image/2024/02-03-storage_and_file_system/2.jpg" alt="" height="100%" width="100%" /></p>

<blockquote>
  <p>想要存取 jeep 就必須從 block 9 -&gt; 16 -&gt; 1 -&gt; 10 -&gt; 25 這樣去存取</p>
</blockquote>

<p>Linked List 就跟字面上的意思一樣，每個 block 都會記錄下一個 block 的位置，這樣就可以連結起來，
因此在空間上可以不連續，但是在邏輯上是連續的。</p>

<ul>
  <li>在效能上不會太好，尤其是 Random Access 的效能，如: SSD, HDD
    <ul>
      <li>即使是 DRAM 也是 sequential access 的效能比較好</li>
      <li>SSD 的效能可能會降低至 1/2，而在 HDD 效能會下降的非常多</li>
    </ul>
  </li>
</ul>

<p><strong>File Allocation Table(FAT)</strong></p>

<p><img src="/image/2024/02-03-storage_and_file_system/3.jpg" alt="" height="100%" width="100%" /></p>

<p>FAT 就是把 Linked List 的指標直接拿出來放在一個表格，這樣的好處是可以提前知道那些 block 是連續的，要讀取那些 block，這樣就可以提前 sequential access。</p>
<ul>
  <li>可以做部分的優化，sequential access 的效能比 random access 快很多
    <ul>
      <li>假如要讀取 jeep 就可以提前知道 jeep 的那些 block 是連續的，這樣就可以提前讀取</li>
      <li>多餘被讀取的 block 就先丟進 DRAM，之後再丟棄就可以</li>
    </ul>
  </li>
  <li>OS 可以提前讀進這些檔案的連結方式，也可以更快速的做 Random Access</li>
  <li>如果 Main Memory 夠大還可以把 FAT 放在 Main Memory，這樣就可以更快速的存取</li>
</ul>

<blockquote class="block-warning">
  <p>這裡要注意，FAT 所存放的是 block 的下一個 block 的位置，而不是直接指向該 block 的位置，因此跟 Index Allocation 有所不同</p>
</blockquote>

<h5 id="943-indexed-allocation">9.4.3 Indexed Allocation</h5>

<p><img src="/image/2024/02-03-storage_and_file_system/4.jpg" alt="" height="100%" width="100%" /></p>

<p>系統為每個檔案建立一個 index block，這個 index block 會記錄這個檔案的所有 block 的位置，這樣就可以直接存取這個 index block。</p>
<ul>
  <li>主要的問題是 index block 的大小，如果 index block 是 4K，那最多存儲 65536 個 data block，假如 data block 也是 4K 這個檔案系統最大為 256MB 的檔案
    <ul>
      <li>因此這裡可以向 MMU 一樣使用多層的 index block，這樣就可以存儲更大的檔案</li>
    </ul>
  </li>
</ul>

<blockquote>
  <p>缺點是假如我有一個很小的檔案如 4K，那麼這個檔案的 index block 會佔用很多的空間，假如有三層就要 12K 的 Index block 要維護</p>
</blockquote>

<h5 id="944-i-node">9.4.4 i-node</h5>

<blockquote class="block-tip">
  <p><a href="https://en.wikipedia.org/wiki/Inode">i-node</a> 是 Unix 和 Unix-like OS 中所使用的一種資料結構，用一個資料結構來記錄一個檔案 metadata，包含了檔案的大小、擁有者、權限、時間戳等等</p>
</blockquote>

<p><img src="/image/2024/02-03-storage_and_file_system/5.jpg" alt="" height="100%" width="100%" /></p>

<p>i-node 的這個設計在第一代的 UNIX(1960) 就已經使用，這個設計已經沿用了 60 年以上，很難想像一個檔案結構的觀念在這麼長的時間內都沒有被超越。</p>
<ul>
  <li>當然 i-node 有一些改善，例如控制權限從 <strong>owner, group, others</strong> 變成了 ACL(<a href="https://en.wikipedia.org/wiki/Access-control_list">Access control list</a>)
    <ul>
      <li>但是 i-node 的基本結構並沒有改變</li>
    </ul>
  </li>
</ul>

<p>i-node 的前 12 個 block 是直接指向 <strong>data block(Direct data block)</strong>，所以不用再去使用第二層的 index-block，這樣如果檔案小於 48KB 就可以直接使用 i-node 來紀錄，
節省了 index-block 的空間。</p>

<div align="center">
    <img src="https://upload.wikimedia.org/wikipedia/commons/thumb/0/09/Ext2-inode.svg/1200px-Ext2-inode.svg.png" width="60%" height="60%" />
</div>

<ul>
  <li>一個 i-node 本身通常是 128 bytes，所以一個 block 可以存放 32 個 i-node</li>
  <li>除了 Direct data block 之外還有 <strong>Single indirect block</strong> 和 <strong>Double indirect block</strong>
    <ul>
      <li>Single indirect: 就是第二層的 index block 可以指向編號 12 ~ 267 的 block，也就是 256 個 block,</li>
      <li>Double indirect: 就是第三層的 index block 可以指向編號 268 ~ 65535 的 block，也就是 65536 個 block</li>
    </ul>
  </li>
  <li>因此一共可以定址 12 + 256 + 256^2 + 256^3 = 16843020 個 block，也就是 64.25GB 的檔案
    <ul>
      <li>在此之上還可以使用 Extents 來擴充，這樣就會讓 Data block 的大小變得更大</li>
    </ul>
  </li>
</ul>

<p>因此如果假設 data block 是 4KB，要存取某個 block:</p>
<ul>
  <li>當 File size &lt; 48KB 就可以直接使用 i-node 兩次存取到 block
    <ul>
      <li>&lt; 1072KB 使用 Single indirect block，最多三次的 Access</li>
      <li>&lt; 263216 使用 Double indirect block，最多四次的 Access</li>
      <li>&lt; 64.25GB 最多五次的 Access</li>
    </ul>
  </li>
</ul>

<blockquote class="block-warning">
  <p>要注意這裡所提的都是最糟情況下，實際上已經存取過的 i-node 或 index 會被放在 DRAM 中，所以實際上的 Access 次數會比較少</p>
</blockquote>

<h5 id="945-hole">9.4.5 Hole</h5>

<p>Hole 是一個很重要的功能，如果一個檔案中並沒有實際的資料，那是否要真的要先分配空間給這個檔案，例如:</p>
<ul>
  <li>VMWare 的虛擬機器通常可能設定了 100GB 的硬碟，但是實際上只有 10GB 的資料，剩下的資料還沒有寫入
    <ul>
      <li>如果沒有 Hole 的機制，就要把這 90GB 的空間都分配出來，全部以某種方式填滿</li>
      <li>再有 Hole 的情況下這些空間都被視為 Logical 0，不占用實際的 Disk</li>
    </ul>
  </li>
</ul>

<blockquote class="block-warning">
  <p>關於 VMWare 的虛擬機器通常會提供 <strong><a href="https://docs.vmware.com/en/VMware-vSphere/7.0/com.vmware.vsphere.vm_admin.doc/GUID-4C0F4D73-82F2-4B81-8AA7-1DD752A8A5AC.html">Provisioning Policies</a></strong>(<a href="https://docs.vmware.com/tw/VMware-vSphere/7.0/com.vmware.vsphere.vm_admin.doc/GUID-4C0F4D73-82F2-4B81-8AA7-1DD752A8A5AC.html">佈建原則</a>) 給使用者選擇</p>
</blockquote>

<blockquote class="block-warning">
  <p>延伸閱讀: <a href="https://wen00072.github.io/blog/2013/12/31/linux-archives-hole/">Linux 檔案的hole</a>, <a href="https://en.wikipedia.org/wiki/Sparse_file">Sparse file</a></p>
</blockquote>

<h5 id="946-free-space-management">9.4.6 Free Space Management</h5>

<p>空的 block 也需要某種管理機制，讓 OS 能快速的找到空的 block 來分配給檔案，這裡有幾種常見的管理機制:</p>
<ul>
  <li><strong>Bit Vector</strong> 來管理空間，每個 block 用一個 bit 來表示是否被使用
    <ul>
      <li>這樣的好處是可以直接使用 Bitwise Operation 來操作，並快速的找到連續的空間</li>
      <li>但是操作 Bit Vector 會有一些 overhead，例如: 把 Bit Vector 放在 DRAM 中才能快速的操作，並且 Time Complexity 是 O(n)</li>
    </ul>
  </li>
  <li><strong>Linked List</strong> 來管理空間，每個 block 會記錄下一個空的 block
    <ul>
      <li>使用 Linked List 來管理的好處是找到空的 block 就放入 Linked List 就好，OS 只要知道 Head 就可以找到所有的空間</li>
      <li>缺點是比較難找到連續的空間</li>
      <li>實際的例子是 ext2, ext3 都是使用 Linked List 來管理空間</li>
    </ul>
  </li>
</ul>

<p><strong>btrfs</strong></p>

<ul>
  <li>在 btrfs 中所有使用中的 block 都由 extent 來管理，因此 btrfs 中的檔案都是由 contiguous block 構成</li>
  <li>btrfs 會使用一顆 B-tree 來管理被使用的 block
    <ul>
      <li>這樣也代表沒被放入 B-tree 的 block 就是空的 block</li>
      <li>例如兩個 node 分別記錄 <code class="language-plaintext highlighter-rouge">250~440</code> 與 <code class="language-plaintext highlighter-rouge">800~1000</code>，那麼 <code class="language-plaintext highlighter-rouge">441~799</code> 就是空的 block</li>
    </ul>
  </li>
</ul>

<hr />

<h3 id="directory-structure">Directory Structure</h3>

<p>會對 Directory structure 的操作有兩種:</p>
<ul>
  <li>已經給定的 path name，找出相對應的檔案，例如:
    <ul>
      <li><code class="language-plaintext highlighter-rouge">vim /home/benson/example.txt</code> 就是在 <code class="language-plaintext highlighter-rouge">/home/benson</code> 下找出 <code class="language-plaintext highlighter-rouge">example.txt</code></li>
    </ul>
  </li>
  <li>列出 Directory 下所有的檔案
    <ul>
      <li><code class="language-plaintext highlighter-rouge">ls /home/benson</code> 就是列出 <code class="language-plaintext highlighter-rouge">/home/benson</code> 下所有的檔案</li>
    </ul>
  </li>
</ul>

<h5 id="951-unix-directory">9.5.1 Unix Directory</h5>

<ul>
  <li>Directory 是一個特別的檔案
    <ul>
      <li>這個檔案紀錄了這個 Directory 下所有的的東西</li>
    </ul>
  </li>
  <li>Directory 可以用 opendir(), readdir() 操作
    <ul>
      <li>主要包含這些: <code class="language-plaintext highlighter-rouge">name</code>, <code class="language-plaintext highlighter-rouge">type</code>, <code class="language-plaintext highlighter-rouge">i-node number</code></li>
    </ul>
  </li>
</ul>

<p><strong>Directory Design Method</strong></p>

<ul>
  <li><strong>Linear List</strong>
    <ul>
      <li>每一個目錄檔案可以視作是一個特殊的文字檔案，裡面紀錄 <code class="language-plaintext highlighter-rouge">name</code>, <code class="language-plaintext highlighter-rouge">type</code>, <code class="language-plaintext highlighter-rouge">i-node number</code></li>
      <li>這種方式很適合列表功能，例如: <code class="language-plaintext highlighter-rouge">ls</code></li>
    </ul>
  </li>
  <li><strong>Data Structure for Fast Searching</strong>
    <ul>
      <li>可以使用檔案路徑名稱來快速的找到對應的 i-node，例如: hash, b-tree</li>
      <li>這種方式很適合指定路徑名稱的功能，例如: <code class="language-plaintext highlighter-rouge">vim /home/benson/example.txt</code></li>
    </ul>
  </li>
  <li><strong>Mixed</strong>
    <ul>
      <li>這兩種方式可以混合使用，這樣就可以快速列表跟快速搜尋，例如: btrfs</li>
    </ul>
  </li>
</ul>

<blockquote class="block-warning">
  <h5 id="last-edit">Last Edit</h5>
  <p>3-6-2024 12:58</p>
</blockquote>]]></content><author><name>Benson Hsu</name></author><category term="Jekyll" /><category term="OS" /><summary type="html"><![CDATA[Operating System: Design and Implementation course notes from CCU, lecturer Shiwu-Lo.]]></summary></entry><entry><title type="html">Note | Using btrfs on Debian</title><link href="https://hotshot824.github.io/jekylls/2024-01-05-debian_btrfs.html" rel="alternate" type="text/html" title="Note | Using btrfs on Debian" /><published>2024-01-05T00:00:00+00:00</published><updated>2024-01-05T00:00:00+00:00</updated><id>https://hotshot824.github.io/jekylls/debian_btrfs</id><content type="html" xml:base="https://hotshot824.github.io/jekylls/2024-01-05-debian_btrfs.html"><![CDATA[<blockquote class="block-tip">
  <p>Notes debian using btrfs as main file system</p>
</blockquote>

<p>想在 debian 上安裝 Btrfs，有很多種方式，這裡紀錄一下怎麼從頭開始安裝 debian，並且使用 Btrfs 作為主要的檔案系統。</p>

<h3 id="install-debian">Install Debian</h3>

<p>在安裝 Debian 的時候要進入 Advanced options，並且選擇 Expert install，這樣才能夠在安裝的時候選擇 Btrfs 作為檔案系統。
接著就跟標準的安裝方式一樣。</p>

<ul>
  <li>在這個過程中如果不確定的選項就直接按 Enter 就好，這樣就會使用預設的選項</li>
</ul>

<div style="display: flex; flex-direction: row; align-items: center;">
    <img src="/image/2024/01-05-debian_btrfs/1.png" width="50%" height="50%" />
    <img src="/image/2024/01-05-debian_btrfs/2.png" width="50%" height="50%" />
</div>

<p><br /></p>

<p><strong>Partition disks</strong></p>

<blockquote class="block-warning">
  <p>直到 Partition disks 的時候，才是這次要做的重點</p>
</blockquote>

<p>這邊要選擇 Manual，這樣才能夠自己選擇要安裝的檔案系統，並且自己分割硬碟，分割硬碟的時候只要分割出 SWAP 和 /，
其餘的都不做分割之後用 Btrfs 來管理 subvolume 就好。</p>

<ul>
  <li>Manual
    <ul>
      <li>gpt: 使用 UEFI 的啟動方式</li>
      <li>msdos: 傳統的 BIOS 啟動方式</li>
    </ul>
  </li>
</ul>

<p>分割好之後 alt + F2 進入 console，先 df -h 查看一下目前的狀況</p>

<div style="display: flex; flex-direction: row; align-items: center;">
    <img src="/image/2024/01-05-debian_btrfs/3.png" width="50%" height="50%" />
    <img src="/image/2024/01-05-debian_btrfs/4.png" width="50%" height="50%" />
</div>

<p><br /></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">df</span> <span class="nt">-h</span>

<span class="c"># Umount /target</span>
umount /target

<span class="c"># Mount /mnt</span>
mount /dev/<span class="o">{</span>partition<span class="o">}</span> /mnt

<span class="c"># Create subvolume and mount subvolume</span>
<span class="nb">cd</span> /mnt
<span class="nb">mv</span> @rootfs @
btrfs subvolume create @boot
btrfs subvolume create @home
btrfs subvolume create @snapshots

<span class="c"># Mount rootfs</span>
mount <span class="nt">-o</span> noatime,compress<span class="o">=</span>zstd:1,space_cache<span class="o">=</span>v2,ssd,discard<span class="o">=</span>async,subvol<span class="o">=</span>@ /dev/<span class="o">{</span>partition<span class="o">}</span> /target

<span class="nb">cd</span> /target
<span class="nb">mkdir</span> <span class="nt">-p</span> boot
<span class="nb">mkdir</span> <span class="nt">-p</span> home
<span class="nb">mkdir</span> <span class="nt">-p</span> .snapshots

mount <span class="nt">-o</span> noatime,compress<span class="o">=</span>zstd:1,space_cache<span class="o">=</span>v2,ssd,discard<span class="o">=</span>async,subvol<span class="o">=</span>@boot /dev/<span class="o">{</span>partition<span class="o">}</span> /target/boot
mount <span class="nt">-o</span> noatime,compress<span class="o">=</span>zstd:1,space_cache<span class="o">=</span>v2,ssd,discard<span class="o">=</span>async,subvol<span class="o">=</span>@home /dev/<span class="o">{</span>partition<span class="o">}</span> /target/home
mount <span class="nt">-o</span> noatime,compress<span class="o">=</span>zstd:1,space_cache<span class="o">=</span>v2,ssd,discard<span class="o">=</span>async,subvol<span class="o">=</span>@snapshots /dev/<span class="o">{</span>partition<span class="o">}</span> /target/.snapshots

<span class="c"># Umount /mnt</span>
umount /mnt

nano /target/etc/fstab
</code></pre></div></div>

<blockquote>
  <p>nano 裡面 ctrl + k 剪下整行，ctrl + u 貼上</p>
</blockquote>

<p>修改後的 fstab 應該會類似下面這樣，這樣就可以在重開機的時候自動掛載了</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># /etc/fstab: static file system information.
#
# Use 'blkid' to print the universally unique identifier for a
# device; this may be used with UUID= as a more robust way to name devices
# that works even if disks are added and removed. See fstab(5).
#
# systemd generates mount units based on this file, see systemd.mount(5).
# Please run 'systemctl daemon-reload' after making changes here.
#
# &lt;file system&gt; &lt;mount point&gt;   &lt;type&gt;  &lt;options&gt;       &lt;dump&gt;  &lt;pass&gt;
# / was on /dev/sda2 during installation
UUID=82197af0-96dc-4967-90f6-05072569ebf6 /               btrfs   noatime,space_cache=v2,compress=zstd:1,ssd,discard=async,subvol=@            0       0
UUID=82197af0-96dc-4967-90f6-05072569ebf6 /home           btrfs   noatime,space_cache=v2,compress=zstd:1,ssd,discard=async,subvol=@home        0       0
UUID=82197af0-96dc-4967-90f6-05072569ebf6 /boot           btrfs   noatime,space_cache=v2,compress=zstd:1,ssd,discard=async,subvol=@boot        0       0
UUID=82197af0-96dc-4967-90f6-05072569ebf6 /.snapshots     btrfs   noatime,space_cache=v2,compress=zstd:1,ssd,discard=async,subvol=@snapshots   0       0
# swap was on /dev/sda1 during installation
UUID=bf901835-ce39-47cd-a225-d3f9c4b25685 none            swap    sw              0       0
/dev/sr0        /media/cdrom0   udf,iso9660 user,noauto     0       0
</code></pre></div></div>

<blockquote>
  <p>延伸閱讀: <a href="https://man.archlinux.org/man/btrfs.5#MOUNT_OPTIONS">ArchWiki btrfs MOUNT OPTIONS</a>, <a href="https://btrfs.readthedocs.io/">BTRFS documentation</a><br />
上面的連結說明了 Mount Options 的意義，另外是官方的說明文件</p>
</blockquote>

<p>上面操作結束後就 ctrl + alt + F1 回到安裝畫面，把剩下的步驟都完成就可以了，全部完成重開機之後，使用 <code class="language-plaintext highlighter-rouge">findmnt | grep btrfs</code> 確認一下有沒有掛載成功。</p>

<h3 id="sub-volume--snapshot">Sub-Volume &amp; Snapshot</h3>

<p><strong>Sub-Volume</strong></p>
<ul>
  <li>要在已經存在的 Sub-Volume 下創造 Sub-Volume 的方式就使用 <code class="language-plaintext highlighter-rouge">btrfs subvolume create {path}</code>
    <ul>
      <li>這邊可以把這個操作當成跟建立目錄一樣去建立 subvolume</li>
    </ul>
  </li>
</ul>

<p><strong>Snapshot</strong></p>
<ul>
  <li>而建立 snapshot 的方式就是使用 <code class="language-plaintext highlighter-rouge">btrfs subvolume snapshot -r {path} {snapshot}</code>
    <ul>
      <li><code class="language-plaintext highlighter-rouge">path</code>: 目標的 subvolume 路徑</li>
      <li><code class="language-plaintext highlighter-rouge">snapshot</code>: snapshot 的存放路徑</li>
      <li><code class="language-plaintext highlighter-rouge">r</code>: read-only，避免 snapshot 之後被修改</li>
    </ul>
  </li>
</ul>

<p>如果想要還原的話也可以反向使用 snapshot 來還原，也就把上面的 <code class="language-plaintext highlighter-rouge">path</code> 和 <code class="language-plaintext highlighter-rouge">snapshot</code> 對調並且把 <code class="language-plaintext highlighter-rouge">r</code> 拿掉就可以。</p>

<blockquote>
  <p>延伸閱讀: <a href="https://wiki.archlinux.org/title/btrfs#Configuring_the_file_system">Btrfs - Arch: Configuring the file system</a></p>
</blockquote>

<blockquote class="block-warning">
  <p>這邊只講了很基礎的 btrfs 建立步驟，實際上 btrfs 還有很多需要調校的地方，例如: docker container 或是 tmp 這樣會頻繁寫入的地方，是否還需要使用 cow，這些都需要自己去調校，這部分之後要再研究一下。</p>
</blockquote>

<blockquote class="block-warning">
  <h5 id="last-edit">Last Edit</h5>
  <p>01-07-2024 14:59</p>
</blockquote>]]></content><author><name>Benson Hsu</name></author><category term="Jekylls" /><category term="OS" /><summary type="html"><![CDATA[Notes debian using btrfs as main file system]]></summary></entry><entry><title type="html">Compiler | Code Generation (Unfinished)</title><link href="https://hotshot824.github.io/jekylls/2023-12-26-code_generation.html" rel="alternate" type="text/html" title="Compiler | Code Generation (Unfinished)" /><published>2023-12-26T00:00:00+00:00</published><updated>2023-12-26T00:00:00+00:00</updated><id>https://hotshot824.github.io/jekylls/code_generation</id><content type="html" xml:base="https://hotshot824.github.io/jekylls/2023-12-26-code_generation.html"><![CDATA[<blockquote class="block-tip">
  <p>Compilers course notes from CCU, lecturer Nai-Wei Lin.</p>
</blockquote>

<ul>
  <li><strong>The target machine</strong></li>
  <li><strong>Instruction selection and register allocation</strong></li>
  <li><strong>Basic blocks and flow graphs</strong></li>
  <li>A simple code generator</li>
  <li>Peephole optimization</li>
  <li>Instruction selector generator</li>
  <li><strong>Graph-coloring register allocator</strong></li>
</ul>

<h3 id="the-target-machine">The target machine</h3>

<p>假設這是一台 Byte-addressable 的機器，每個指令由 4 個 bytes 組成，並且有 n 個 registers，這代表機器的最小尋址單位是 1 byte。</p>

<ul>
  <li>Two address instructions(雙地址指令)
    <ul>
      <li>opsource, destination</li>
    </ul>
  </li>
  <li>Six addressing modes(六種尋址模式)
    <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>absolute        M           M                       1
register        R           R                       0
indexed         c(R)        c+content(R)            1
ind register    *R          content(R)              0
ind indexed     *c(R)       content(c+content(R))   1
iteral          #c          c                       1
</code></pre></div>    </div>
    <p>上面的表格中，第一欄是尋址模式，第二欄是對應的表示方式，第三欄是對應的實際 bit 大小，最後一欄是對應的位址計算方式會需要幾個 word。</p>
  </li>
  <li>M: 使用到記憶體位置需要 1 個 word</li>
  <li>c: 使用到常數需要 1 個 word</li>
</ul>

<p><strong>Example</strong></p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>MOV R0, M       /* 2 words */
MOV 4 (R0), M   /* 3 words */
MOV *R0, M      /* 2 words */
MOV *4 (R0), M  /* 3 words */
MOV #1, R0      /* 2 words */
</code></pre></div></div>

<h5 id="61-instruction-costs">6.1 Instruction Costs</h5>

<ul>
  <li>一個指令的成本等於 1 加上尋址模式的成本</li>
  <li>這個成本會跟指令的長度成正比</li>
  <li>因此最小化指令長度也可以減少指令的執行時間</li>
</ul>

<blockquote>
  <p>這裡很直覺的，如果一個指令的長度越小，那麼執行的時間也會越短，因為每個指令都需要一個 clock cycle 來執行，所以指令長度越小，執行的時間也會越短。</p>
</blockquote>

<p><strong>Example</strong></p>

<p>假如有一個 a := b + c 的原始碼，這裡有不同種類的指令可以選擇:</p>

<p><img src="/image/2023/12-26-code_generation/1.png" alt="" height="100%" width="100%" /></p>

<ol>
  <li>搬移到 Reg 再運算，成本是 2 + 2 + 2 = 6</li>
  <li>直接以記憶體地址運算，成本是 3 + 3 = 6</li>
  <li>在某些情況下，如果 Reg 中已經存在 a, b, c 那麼成本是 1 + 1 = 2</li>
  <li>如果 Reg 中存在 b, c，成本是 1 + 2 = 3</li>
</ol>

<h3 id="graph-coloring">Graph Coloring</h3>

<p>我們可以了解到 Register 的分配對於程式的效能有很大影響，但是這個問題是一個 NP-Complete 的問題，這裡有一些 approximation 的方法可以解決這個問題。</p>

<h5 id="71-graph-coloring">7.1 Graph Coloring</h5>

<ol>
  <li>首先掃描一次程式碼，並且選擇好要使用的指令，此時可以任意分配 Symbolic register</li>
  <li>第二遍掃描時，使用 Graph coloring algorithm 來分配 Physical register 給 Symbolic register
    <ul>
      <li>在第二遍掃描分配時有可能會發生衝突，此時就需要進行 Spilling</li>
      <li>Spilling: 決定那些 Register 需要被 spill，並且將 spill 的資料存到 Memory 中(類似 virtual memory 的 swap)</li>
    </ul>
  </li>
</ol>

<p><strong>Interference Graph</strong></p>

<ul>
  <li>在每個 Basic block 中，都會去建立一個 interference graph</li>
  <li>圖中的每個 Node 代表一個 symbolic register</li>
  <li>如果一個 Node 在另一個 Node 被定義的時候仍然活躍，那麼這兩個 Node 之間會連接一條邊</li>
</ul>

<p><strong>K-colorable Graph</strong></p>

<ul>
  <li>一個圖如果每個 Node 都可以被賦予 K 個顏色，並且相鄰的 Node 顏色都不相同，那麼這個圖就是 K-colorable</li>
  <li>這裡的 Color 就是 Physical register</li>
  <li>想要確認一個圖是否是 K-colorable，是 NP-Complete 的問題
    <ul>
      <li>這裡會用 Greedy algorithm 來解決這個問題</li>
    </ul>
  </li>
</ul>

<p><strong>A Graph Coloring Algorithm</strong></p>

<ol>
  <li>如果一個 Node 及其邊的 Neighbor 數量小於 K，那麼就移除這個 Node</li>
  <li>重複上面的步驟直到所有的 Node 都被移除或者是留下的 Node 邊的數量大於 K</li>
  <li>如果有 Node 無法移除，那麼就需要進行 Spilled 的動作
    <ul>
      <li>刪除最多邊的 Node，之後重複上面的步驟，直到最後所有的 Node 都被移除</li>
    </ul>
  </li>
  <li>現在開始依照被移除的逆序來分配 Color 給 Node</li>
  <li>每個 Node 都要分配鄰居沒有使用過的 Color
    <ul>
      <li>被 Spilled 的 Node 可以使用任何 Color</li>
    </ul>
  </li>
</ol>

<p><strong>Example:</strong></p>

<p><img src="/image/2023/12-26-code_generation/2.png" alt="" height="100%" width="100%" /></p>

<p><img src="/image/2023/12-26-code_generation/3.png" alt="" height="100%" width="100%" /></p>

<blockquote class="block-warning">
  <h5 id="last-edit">Last Edit</h5>
  <p>1-4-2021 01:26</p>
</blockquote>]]></content><author><name>Benson Hsu</name></author><category term="Jekylls" /><category term="Compiler" /><summary type="html"><![CDATA[Compilers course notes from CCU, lecturer Nai-Wei Lin.]]></summary></entry><entry><title type="html">OS | Virtual Memory</title><link href="https://hotshot824.github.io/jekyll/2023-12-23-virtual_memory.html" rel="alternate" type="text/html" title="OS | Virtual Memory" /><published>2023-12-23T00:00:00+00:00</published><updated>2023-12-23T00:00:00+00:00</updated><id>https://hotshot824.github.io/jekyll/virtual_memory</id><content type="html" xml:base="https://hotshot824.github.io/jekyll/2023-12-23-virtual_memory.html"><![CDATA[<blockquote class="block-tip">
  <p>Operating System: Design and Implementation course notes from CCU, lecturer Shiwu-Lo.</p>
</blockquote>

<p>本章節會主要介紹 Virtual Memory 這是這章節的討論範圍</p>

<ul>
  <li>Virtual Memory Hardware and Software</li>
  <li>Virtual Memory Example</li>
  <li>Page Replacement</li>
  <li>Working Set</li>
  <li>open, read, writ vs. mmap
    <ul>
      <li>Differences between these system calls</li>
    </ul>
  </li>
  <li>TLB miss</li>
</ul>

<h5 id="mmu-main-function">MMU Main Function</h5>

<ul>
  <li>MMU 主要處理記憶體分配的問題
    <ul>
      <li>消除了 External fragmentation(外部碎片)</li>
      <li>由於 Page size 僅有 4KB，所以 Internal fragmentation(內部碎片)很輕微</li>
    </ul>
  </li>
  <li>如果 TLB miss
    <ul>
      <li>假設該 TLB 要去 Mapping 的 Page 在 Memory 中只要將 Virtual memory to Physical memory 的 Mapping 加入 TLB 即可</li>
      <li>上述的動作可以使用 Hardware 來完成，或使用 Software 來完成</li>
    </ul>
  </li>
  <li>如果 TLB miss 並且 Hardware 的表格也查不到呢
    <ul>
      <li>稱之為 Page fault</li>
    </ul>
  </li>
</ul>

<h3 id="what-is-virtual-memory">What is Virtual Memory</h3>

<h5 id="81-define-the-virtual-memory">8.1 Define the Virtual Memory</h5>

<p>嚴格的定義: <strong>由軟體重新定義 Page fault</strong></p>
<ul>
  <li>當 CPU 所要存取的 Memory 根本不存在於 DRAM 中時，就稱作為 Page fault
    <ul>
      <li>如果我們啟動 Software 此時該 Software 所要存取的 Memory 根本不存在於 DRAM 中，就稱作為 Page fault，例如 Segmentation fault</li>
    </ul>
  </li>
  <li>OS 設計者重新詮釋了 Page fault 的意義，例如:
    <ul>
      <li>OS 暫時還沒把該 Page 載入到 DRAM 中，重新載入就好
        <ul>
          <li>例如: Execl 假如有 500MB 的資料要放入 DRAM 中以供執行，但硬碟的讀取速度有他的極限例如 20MB/s，不可能每次執行 Execl 都要花 25 秒鐘去讀取資料</li>
          <li>先把必要的程式碼放入 DRAM 就好，等使用者真的需要使用其他功能時再去載入該功能</li>
        </ul>
      </li>
      <li>OS 讓不同的 Process 隱形共用內容相同的 Page
        <ul>
          <li>唯讀，不會有任何問題發生，還可以減少記憶體使用量，例如: fork() 使用 Copy-on-write 的技術來實作</li>
          <li>寫入，OS 必須確保每個 Process 在邏輯上都有自己的 Page，例如: 即使 fork() 使用同一份 Memory，但是要確保程式的 Logic address 上是不同的</li>
        </ul>
      </li>
    </ul>
  </li>
</ul>

<p>稍微寬鬆一點的定義: <strong>讓 Mapping 變得更加靈活</strong></p>
<ul>
  <li>透過設定 MMU，使 Memory Mapping 更加靈活的例子
    <ul>
      <li>Shard Memory，讓不同的 Process 可以共用同一份 Memory</li>
      <li>可以將檔案 Mapping 到 Process 的 Memory Space，讓 Process 可以直接存取檔案</li>
      <li>可以將部分 I/O 的記憶體 Mapping 到 Process 的 Memory Space，讓 Process 可以直接控制 I/O
        <ul>
          <li>Linux 中使用 mmap 存取 <code class="language-plaintext highlighter-rouge">/dev/mem</code></li>
        </ul>
      </li>
    </ul>
  </li>
</ul>

<h5 id="82-virtual-memory-components">8.2 Virtual Memory Components</h5>

<blockquote>
  <p>下圖是 Virtual Memory 中會需要的組件</p>
</blockquote>

<p><img src="/image/2023/12-23-virtual_memory/1.png" alt="" height="100%" width="100%" /></p>

<ul>
  <li>Hardware table(Page table)
    <ul>
      <li>這部分由 OS 來寫入硬體所設計的表格，由 MMU 來讀取，例如: x86 定義表格，Linux 依照 x86 的定義來實作</li>
      <li>每個 Process 都有自己的 Page table</li>
      <li>如果 TLB miss 硬體查詢 Page table 還是找不到，此時觸發 Page fault exception</li>
    </ul>
  </li>
  <li>Software table(mm_struct, kerne mapping table)
    <ul>
      <li>每個 Process 甚至 Linux Kernel 都需要一個查詢表，快速的查詢每個 Memory 是否正確</li>
      <li>如果正確還發生 Page fault 如何處理？</li>
    </ul>
  </li>
  <li>處理 Page fault
    <ul>
      <li>OS 必須有 Page fault handler(Interrupt service routine)</li>
      <li>並且要能紀錄: 錯誤的原因，錯誤的 Address</li>
    </ul>
  </li>
</ul>

<h3 id="virtual-memory-example">Virtual Memory Example</h3>

<p><a href="#83-shard-memory">8.3 Shard Memory</a><br />
<a href="#84-demand-paging">8.4 Demand Paging</a><br />
<a href="#85-linux-pure-demand-paging">8.5 Linux Pure Demand Paging</a><br />
<a href="#86-performance-of-demand-paging">8.6 Performance of Demand Paging</a>
<a href="#87-copy-on-write">8.7 Copy-on-Write</a><br />
<a href="#88-kernel-same-page-merging">8.8 Kernel same-page merging</a><br />
<a href="#89-page-table-entry-attributes">8.9 Page Table Entry Attributes</a></p>

<h5 id="83-shard-memory">8.3 Shard Memory</h5>

<p><img src="/image/2023/12-23-virtual_memory/2.png" alt="" height="100%" width="100%" /></p>

<ul>
  <li>stack, heap 這部分是程式自己獨有的</li>
  <li>黑色的部分是共享的:
    <ul>
      <li>code, libc: 這兩個是同一個程式但是開了兩個 Process
        <ul>
          <li>因為在執行時會去 Mapping 硬碟上的執行檔，這樣 OS 就能知道其實這兩個 Process 完全是一樣的</li>
          <li>並且這部分只能 Read，這樣就沒有 Locked 所造成的效能瓶頸</li>
        </ul>
      </li>
      <li>mmap: 這部分不是由 Kernel 去分享的，當 Shell 去呼叫 mmap syscall，後得到一個符號這樣就可以將該記憶體映射至自己的 Memory
        <ul>
          <li>shell-1 將資料傳給 shell-2 要傳的是資料在 mmap 中的 offset</li>
          <li>盡量不要去指定 mmap 要被映射的 Address</li>
        </ul>
      </li>
    </ul>
  </li>
  <li>Virtual Memory 允許 Code, Data 共享
    <ul>
      <li>多個程式使用相同的 Library，例如: libc</li>
      <li>應用程式透過 Shared memory 共享資料，例如: mmap</li>
      <li>Parent 和 Child 都沒呼叫 execv 的話，記憶體內容幾乎一樣，例如: Copy-on-Write</li>
    </ul>
  </li>
  <li>Linux 允許隱含式的共享
    <ul>
      <li>Linux kernel 可能會掃描 Memory，將內容一樣的 Page 隱含共享</li>
      <li>例如: 電腦的桌布跟登入畫面使用同一張圖片，記憶體內容是一樣的</li>
    </ul>
  </li>
</ul>

<h5 id="84-demand-paging">8.4 Demand Paging</h5>

<p><strong>Define of Demand paging</strong></p>

<ul>
  <li>作業系統不會立刻將使用者所需要的 Memory 配置給 User</li>
  <li>發生 Page fault 時，才會載入該段記憶體的資料</li>
</ul>

<p><strong>Demand paging advantage</strong></p>

<p>因為只需要載入需要的資料</p>
<ul>
  <li>Less I/O needed</li>
  <li>Less memory needed</li>
  <li>Faster response
    <ul>
      <li>啟動時間，只需要載入需要的資料就能啟動了</li>
    </ul>
  </li>
  <li>More users/programs
    <ul>
      <li>因為每個程式需要的記憶體較少，因此可以載入更多的應用程式</li>
    </ul>
  </li>
</ul>

<p><strong>Demand paging shortcoming</strong></p>

<ul>
  <li>對於 Disk system，Demand paging 雖然減少了讀取的量，但是會引發大量的 Random access
    <ul>
      <li>例如: 執行兩個程式，這兩個程式一共 100 MB，OS 可以驅動 Disk 使用兩秒鐘的時間來載入這兩個應用程式。</li>
      <li>如果使用 Pure demand paging，假設兩個程式輪流執行，一共需要 20 MB 5120 個 pages，並且執行的程式碼<strong>「隨機散布」</strong>，
目前 Disk 的 IOPS 約為 200，則載入兩個程式的時間變為 25.6 Sec</li>
    </ul>
  </li>
  <li>除了引發大量的 Random access 以外，Demand paging 也可能會造成執行期間有些 Lag
    <ul>
      <li>例如: 不希望玩遊戲時會有延遲，就要一次把遊戲所要用的資料全部載入</li>
    </ul>
  </li>
</ul>

<blockquote>
  <p>Random access 對 SSD 的速度大約是 Sequential access 的 1/2 ~ 1/3，HDD 則有可能從 200MB 降低到 1MB</p>
</blockquote>

<blockquote>
  <p>IOPS: Input/Output Operations Per Second(每秒輸入/輸出次數)</p>
</blockquote>

<p>Linux 在這方面的處理方式是假如要 1個 Page，就會一次載入周遭 8 個 Page，因為通常同一個程式會連續使用到相同的 Page，這樣就能減少 Random access 的問題。</p>

<p><img src="/image/2023/12-23-virtual_memory/3.png" alt="" height="100%" width="100%" /></p>

<p>上面張圖表示了一個程式配置到 DRAM 的情況</p>
<ul>
  <li>OS 會透過 process 的 kernel mapping table 來看要去哪裡找資料
    <ul>
      <li>code, libc: disk</li>
      <li>stack, mmap, heap: zero-out page pool</li>
    </ul>
  </li>
  <li>zero-out page pool: OS 會維護一個 pool 在這裡面都是已經預先初始化(0)的 page，用來分配給 stack, mmap 使用
    <ul>
      <li>這是為了避免資訊，例如: 上個程式的資料還留在 DRAM 中，下個程式可以從 DRAM 讀取到上個程式的資料</li>
    </ul>
  </li>
</ul>

<blockquote>
</blockquote>

<p><strong>Valid-invalid</strong></p>

<p>下圖是一個 x86 的 Page table entry，後面的 12 個 bit 就是 attribute</p>

<p><img src="/image/2023/12-23-virtual_memory/4.png" alt="" height="100%" width="100%" /></p>

<ul>
  <li>Present: 0 的話表示該 Page 不在 DRAM 中，此時可能代表 OS 還沒有將該 page 載入 DRAM 中，或者該 page 本來就不在 DRAM 中(程式寫錯)</li>
  <li>不管是在 page table 中的第一層或第二層，只要有一個是 0(invalid)，就會觸發 exception</li>
  <li>此時 OS 會去查詢 process 的軟體表格(Linux mm_struct)
    <ul>
      <li>查詢成功: 配置 physical page 給該 address，然後修改 page table 重新執行</li>
      <li>查詢失敗: 程式發生錯誤，例如: pointer 指向錯誤的 address，此時繼續錯誤處理(終止或呼叫 GDB)</li>
    </ul>
  </li>
</ul>

<blockquote>
  <p>大部分的 valid-invalid bit 都在 PTE 的最右邊，這是 CPU 設計時如果指向不存在的 page，在此時前變得 bit 都可以任意使用，
Linux 就使用這些 bit 來協助解決 backing storage 為 swap space 的 page fult</p>
</blockquote>

<h5 id="85-linux-pure-demand-paging">8.5 Linux Pure Demand Paging</h5>

<blockquote>
  <p>Pure demand paging 的意思是在執行新的程式時，並不會分配任何 page 給 task，直到該 task 索取</p>
</blockquote>

<p>以 Linux 為例子，使用 execve() 將執行檔放入當前 process 的 memory space</p>
<ol>
  <li>先在該 task 的 mm_struct 中設定 section 映射的資訊
    <ul>
      <li>code, data 對應到執行檔案</li>
      <li>bss section 對應到 zero-out page pool</li>
      <li>lib 對應到相對的 library</li>
      <li>只分配少量的 stack space(8KB) 給該 task</li>
    </ul>
  </li>
  <li>執行程式</li>
</ol>

<p>Linux 並沒有分配任何 page 先給該 task，因此是 pure demand paging</p>

<h5 id="86-performance-of-demand-paging">8.6 Performance of Demand Paging</h5>

<p>怎麼估算 Demand Paging 的效能，如下:</p>
<ul>
  <li>Page fault rate: 0 &lt;= p &lt;= 1
    <ul>
      <li>估算一個 Page fault 發生的機率</li>
    </ul>
  </li>
  <li>Effective Access Time(EAT)
$EAT = (1 - p) * memory\,access + p(page\,fault\,overhead + swap\,page\,out + swap\,page\,in + restart\,overhead)$
    <ul>
      <li>swap page out: Free space 不夠就需要將某些 page 從 DRAM 寫入 disk
        <ul>
          <li>優化方式: 無論如何都保持一定的 free space</li>
        </ul>
      </li>
      <li>swap page in: 當 page fault 發生時，需要將某些 page 從 disk 讀入 DRAM
        <ul>
          <li>常用的 page 就先放入 page cache 中，例如: <code class="language-plaintext highlighter-rouge">ls</code></li>
        </ul>
      </li>
      <li>page fault overhead, restart overhead: 這兩個 overhead 通常是硬體相關的問題，或 ISR 的部分 assembly 寫的夠不夠好</li>
    </ul>
  </li>
</ul>

<blockquote>
  <p>OS 上主要能優化的就是 swap page out/in 這兩種方式</p>
</blockquote>

<p><strong>Example</strong></p>

<ul>
  <li>Memory access time: 200 nanoseconds</li>
  <li>Average page-fault service time: 8 milliseconds
    <ul>
      <li>EAT = (1 - p) * 200 + p(8,000,000)<br />
= 200 + p * 7,999,800</li>
    </ul>
  </li>
  <li>如果每 1000 次 memory access 有 1 次 page fault，那麼速度將變為原本的 1/40</li>
</ul>

<blockquote>
  <p>以上的例子是說明，在可能的情況下盡量少用 swap space，這樣可能會造成 CPU 大量的時間都在 idle</p>
</blockquote>

<h5 id="87-copy-on-write">8.7 Copy-on-Write</h5>

<p>在 Physical memory 中也會使用 Copy-on-Write 的技術，例如: 在剛 fork() 出來的 child process，如果 child process 沒有修改任何資料，
就不會真的複製一份 parent process 的資料，而是共用同一份資料，這樣就能減少記憶體的使用量。</p>

<p><img src="/image/2023/12-23-virtual_memory/5.png" alt="" height="100%" width="100%" /></p>

<ul>
  <li>通常 fork 之後的 parent 與 child
    <ul>
      <li>在 register 之外都是一樣的，因此直接 copy parent 的 mm_struct 給 child</li>
      <li>將 parent 與 child 的 page table 都設定為 read-only</li>
    </ul>
  </li>
  <li>在 read-only 之後如果其中一個 task 去作出修改，就會觸發 page fault
    <ul>
      <li>Linux 把造成 page fault 的 page 改成 writeable，並且複製一份 page 給 child</li>
      <li>這樣 parent 與 child 就重新各自擁有一份可 write 的 page</li>
    </ul>
  </li>
</ul>

<blockquote>
  <p>這樣的技術也可以用在 File System 上，例如: Linux 的 Btrfs</p>
</blockquote>

<h5 id="88-kernel-same-page-merging">8.8 Kernel same-page merging</h5>

<ul>
  <li>Kernel virtual memory(KVM) 中會有許多不同的 VM 都會用到相同的 page
    <ul>
      <li>例如: 都是執行 Win10，那麼這些 VM 中的 OS Kernel 應該都是一樣的</li>
    </ul>
  </li>
  <li>在一般的 OS 也可能會有相同的 Page
    <ul>
      <li>例如: 桌面與登入畫面使用相同的圖片，這樣就會有相同的 Page</li>
    </ul>
  </li>
  <li>上面的情況都比較難有真正的事件發生，因此需要 OS 去主動掃描才能發現共用記憶體，節省記憶體的使用量</li>
</ul>

<h5 id="89-page-table-entry-attributes">8.9 Page Table Entry Attributes</h5>

<p>在 Main Memory 中介紹的是 PTE 的前 20 個 bit，這邊會更詳細的說明後 12 個 bit 的意義</p>

<p><img src="/image/2023/12-23-virtual_memory/4.png" alt="" height="100%" width="100%" /></p>

<ul>
  <li>Present: 是否在 DRAM 中</li>
  <li>Read/Write: 0 表示 read-only，1 表示 read/write</li>
  <li>User/Supervisor: 表示是否需要 kernel mode 權限才能存取</li>
  <li>Write-through: 決定 page 使用的是 write-back 或 write-through</li>
  <li>Cache Disable: 如果是 1 則禁止該 page 被 cache</li>
  <li>Accessed: 表示該 page 是否被存取過</li>
  <li>Dirty: 表示該 page 是否被修改過</li>
  <li>Page Table Attribute Index: 跟 Write-through/Cache Disable 有關，可以設置更細緻的 Cache 設定</li>
  <li>Global: 表示該 page 是否是 global page，例如: 共享的 library</li>
  <li>Available: 這 3 個 bit 可以讓 OS 自由使用</li>
</ul>

<p>而一個 4KB 的 Page table 可以放入 1024 個 PTE，然後在 Main memory 提過的 Hierarchical pading 在這裡可以更詳細的說明，
這樣的話架構就會如下(以 4 個 PTE 組成一層 Page table 為例):</p>

<p><img src="/image/2023/12-23-virtual_memory/6.png" alt="" height="100%" width="100%" /></p>

<p>這樣我們就能把 Page table, TLB, Virtual memory, Physical memory 組合起來，如下:</p>

<p><img src="/image/2023/12-23-virtual_memory/7.png" alt="" height="100%" width="100%" /></p>

<ol>
  <li>Virtual address 透過 TLB 轉換成 Physical address
    <ul>
      <li>TLB 紀錄 virtual address 與 physical address 的 mapping</li>
      <li>attribute 也會紀錄在 TLB 中</li>
    </ul>
  </li>
  <li>TLB miss 會觸發 page fault，透過 CR3 register 找到 page table</li>
  <li>Page table 紀錄 virtual address 與 physical address 的 mapping
    <ul>
      <li>page table 找到 physical address 後去更新 TLB，然後重新查詢</li>
      <li>如果 PTE 是 invalid，就會觸發 page fault，再去載入該 page 到 DRAM 中</li>
    </ul>
  </li>
</ol>

<blockquote>
  <p>到這裡為止就是完整的 Virtual memory 到 Physical memory 的運作方式</p>
</blockquote>

<hr />

<h3 id="page-replacement">Page Replacement</h3>

<blockquote class="block-tip">
  <p>在這之前所討論的都是在 DRAM 足夠滿足程式大小的情況，但是實際上 DRAM 並不一定能滿足程式所需要的大小</p>
</blockquote>

<p><a href="#810-why-need-page-replacement">8.10 Why Need Page Replacement</a><br />
<a href="#811-page-replacement-concept">8.11 Page Replacement Concept</a><br />
<a href="#812-page-replacement-algorithm">8.12 Page Replacement Algorithm</a><br />
<a href="#813-algorithm-evaluation-example">8.13 Algorithm Evaluation Example</a><br />
<a href="#814-linuxs-page-replacement">8.14 Linux’s Page Replacement</a></p>

<h5 id="810-why-need-page-replacement">8.10 Why Need Page Replacement</h5>

<p>主要的原因就是 DRAM 有限，例如: 使用者同時執行了 Excel, Word, Chrome，這三個程式的大小加起來超過了 DRAM 的大小，
或者是使用者正在看一部影片，但是這部影片的大小超過了 DRAM 的大小。</p>

<blockquote>
  <p>例如一個 6.2GB 的影片，使用者只有 6GB 的 DRAM，就先把 200MB 的資料寫入 swap space，需要時再從 swap space 讀取</p>
</blockquote>

<ul>
  <li>因為 Virtual memory 的存在，所以 OS 可以只載入目前所需要的資料
    <ul>
      <li>隨著程式的執行，過去所需要的資料會不斷累積，但不代表這些資料可以被丟棄</li>
      <li>因此當 DRAM 不足時，OS 要考慮是否把這些資料寫出到 secondary storage 或丟棄</li>
    </ul>
  </li>
</ul>

<blockquote>
  <p>例如: 一個 word 的程式用來初始化 word，但是接下來不會再使用，這時就可以把這些資料丟棄</p>
</blockquote>

<h5 id="811-page-replacement-concept">8.11 Page Replacement Concept</h5>

<p>在 memory 中我們怎麼找出最近不會使用到的 page，這就是 page replacement algorithm 的目的</p>
<ul>
  <li>這個 algorithm 設計的好可以讓使用者感覺不到 page replacement 的存在
    <ul>
      <li>當然也有可能設計的不好，造成使用者感覺到 lag</li>
    </ul>
  </li>
  <li>在不考慮 page-out/page-in overhead 的情況下，user 可以使用的 memory 遠超 DRAM 的大小
    <ul>
      <li>假設 user 執行的應用程式共需要 12GB 的記憶體，但 user 只有 4GB 的 DRAM</li>
      <li>依照未來會存取的順序來對 12GB 的記憶體做排序，就可以知道哪些 page 優先被放入 DRAM</li>
    </ul>
  </li>
  <li>為什麼只要仔入部分資料就可以
    <ul>
      <li>例如: 10秒鐘內，user 能存取的資料不會超過 4GB(locality)</li>
      <li>如果超過 4GB，那麼就把部分資料 page-out，然後 page-in 需要的資料</li>
    </ul>
  </li>
</ul>

<blockquote class="block-warning">
  <p>實際在 Linux 執行中可以使用 free -h 去查看使用的 Men, Swap，Linux 並不會真的把 Men 使用光，而是會保留一部分的 Men 做為 page cache，
available 則是在不使用 swap 的情況下可以分配多少記憶體</p>
</blockquote>

<p>那麼在 512MB 的電腦上執行 6.3 GB 的程式，會發生什麼</p>

<ul>
  <li>理論上系統的所有被使用的記憶體大小不能超過 DRAM + Swap</li>
  <li>OS kernel 本身是 non-swappable
    <ul>
      <li>所以 OS 本身所需要的記憶體都會直接配置在 DRAM 中，這裡並不是 kernel 不能被設計成 swappable，而是這樣的設計不合理</li>
    </ul>
  </li>
  <li>OS 會在必要時 kill 掉一些 task，稱為 OOM killer(out-of-memory killer)</li>
</ul>

<h5 id="812-page-replacement-algorithm">8.12 Page Replacement Algorithm</h5>

<p>這裡要討論的就是那些 page 應該被丟棄，這就需要 page replacement algorithm 來決定</p>

<ul>
  <li>Algorithm 的目標: 最小化 page fault 的數量
    <ul>
      <li>這裡要看優化的目標是什麼，在這裡討論的是最小化 page fault 的數量</li>
    </ul>
  </li>
  <li>在這裡會討論以下幾種 algorithm
    <ul>
      <li>FIFO: First-in-first-out</li>
      <li>Optimal algorithm
        <ul>
          <li>概念上的最佳化，必需假設知道未來的 Page access pattern</li>
        </ul>
      </li>
      <li>LRU: Least-recently-used
        <ul>
          <li>過去不曾用到的 page，未來也不太可能會用到</li>
          <li>實現時會增加硬體上的成本，因為要加入一個時間戳記</li>
        </ul>
      </li>
      <li><strong>Second-chance</strong>
        <ul>
          <li>有效且低成本的方式來實現接近 LRU 的效果</li>
        </ul>
      </li>
      <li>LFU: Least-frequently-used
        <ul>
          <li>過去最不常用的 page，未來也不太可能會用到</li>
        </ul>
      </li>
      <li>MFU: Most-frequently-used
        <ul>
          <li>過去最常用的 page，可能代表已經使用結束了，所以之後不會用到</li>
        </ul>
      </li>
      <li><strong>Linux’s page replacement</strong></li>
    </ul>
  </li>
</ul>

<p><strong>Basic assessment method</strong></p>

<p>在等等評估演算法的方式中，會使用以下的方式來評估:</p>
<ul>
  <li>在給定的 Reference string 中，計算 page fault 的數量</li>
  <li>Reference string:
    <ul>
      <li>7, 0, 1, 2, 0, 3, 0, 4, 2, 3, 0, 3, 2, 1, 2, 0, 1, 7, 0, 1</li>
      <li>這個 reference string 代表了一個程式的 page access pattern</li>
    </ul>
  </li>
  <li>在這裡假設系統只有 3 個 frame</li>
</ul>

<h5 id="813-algorithm-evaluation-example">8.13 Algorithm Evaluation Example</h5>

<p><strong>FIFO</strong></p>

<p><img src="/image/2023/12-23-virtual_memory/8.png" alt="" height="100%" width="100%" /></p>

<ul>
  <li>替換掉最早進入的 page
    <ul>
      <li>所以當 4, 0, 1, 2 時，將 4 Swap out</li>
    </ul>
  </li>
</ul>

<p><strong>Optimal algorithm</strong></p>

<p><img src="/image/2023/12-23-virtual_memory/9.png" alt="" height="100%" width="100%" /></p>

<ul>
  <li>提早替換掉未來最久不會使用到的 page
    <ul>
      <li>所以當 4, 0, 1, 2 時，發現未來不會使用到 1，因此將 1 Swap out</li>
      <li>接下來到 0, 3 時發現 0 還有三次 Access 才會使用，最久因此將 0 Swap out</li>
    </ul>
  </li>
  <li>實際上這樣的演算法是不可能的，因為我們不可能知道未來的 Access pattern</li>
</ul>

<p><strong>LRU</strong></p>

<blockquote class="block-tip">
  <p>我們無法得知未來的 Access pattern，但是可以知道過去的 Access pattern</p>
</blockquote>

<p><img src="/image/2023/12-23-virtual_memory/10.png" alt="" height="100%" width="100%" /></p>

<ul>
  <li>替換掉最久沒有使用的 page
    <ul>
      <li>所以當 4, 0, 1, 2 時，將 4 Swap out</li>
      <li>接下來到 0, 3 時發現 1 是最後使用的，因此將 1 Swap out</li>
    </ul>
  </li>
</ul>

<p><strong>Second-chance(clock algorithm)</strong></p>

<blockquote class="block-tip">
  <p>LRU 如果要在硬體上實現，需要一個時間戳記，但是這樣會增加硬體的成本，因此有了 Second-chance</p>
</blockquote>

<p><img src="/image/2023/12-23-virtual_memory/4.png" alt="" height="100%" width="100%" /></p>

<p>Second-chance 可以用 PTE 中的</p>
<ul>
  <li><strong>Accessed bit</strong>(也稱為 Reference bit)來實現，當 page 被存取時，OS 會將 Accessed bit 設為 1，OS 會定期去清除 Accessed bit 改為 0。</li>
  <li><strong>Dirty bit</strong> 則是在 page 被修改時，OS 會將 Dirty bit 設為 1，如果把 Dirty bit 為 1 的 page-out，就代表必須要跟 disk 做同步，因此會增加 page-out 的時間。</li>
  <li>如果以上兩個 bit 都是 0，就代表這個 page 是可以被 page-out 並且成本低的</li>
</ul>

<p><img src="/image/2023/12-23-virtual_memory/11.png" alt="" height="100%" width="100%" /></p>

<ul>
  <li>實際上剛載入的 memory 的 page accessed bit 都是 0，因此會先被替換掉
    <ul>
      <li>這樣假設 A 剛載入一個 page p, 此時 B ctx 他有可能會把 A 的 page p 替換掉，這樣回到 A 又要重新載入 p</li>
      <li>解決方式是在一些 OS 上會有 young bit，或者是在載入時就將 accessed bit 設為 1</li>
    </ul>
  </li>
  <li>紅色的字體就是剛 Access 的 page，在實作上可以有很多方式
    <ul>
      <li>這裡使用一個 Pointer 依序指向 page，當 page 被指向第二次時並且 accessed bit 為 0，這個可以 page-out</li>
    </ul>
  </li>
</ul>

<blockquote class="block-danger">
  <p>Second-chance 有很多種實作方式，這裡只是其中一種</p>
</blockquote>

<p><strong>LFU</strong></p>

<p><img src="/image/2023/12-23-virtual_memory/12.png" alt="" height="100%" width="100%" /></p>

<ul>
  <li>上方的橘色區塊代表該 page 被使用的次數
    <ul>
      <li>當需要 swap out 時，就會找到使用次數最少的 page page-out</li>
    </ul>
  </li>
  <li>如果使用次數都一樣這裡就假設 FIFO</li>
</ul>

<p><strong>MFU</strong></p>

<p><img src="/image/2023/12-23-virtual_memory/13.png" alt="" height="100%" width="100%" /></p>

<ul>
  <li>與 LFU 相反，找到使用次數最多的 page page-out</li>
</ul>

<p><strong>Conclusion</strong></p>

<ul>
  <li>Optimal algorithm: 理論上最佳化，但是不可能知道未來的 Access pattern</li>
  <li>LRU: 基於過去預測未來，實際上的效能不錯
    <ul>
      <li>但是要完全實現 LRU 需要增加硬體成本，例如: 紀錄 page 上次被存取的時間</li>
    </ul>
  </li>
  <li>Second-chance: 有效且低成本的方式來實現接近 LRU 的效果
    <ul>
      <li>如果經常被存取，那 Access bit 會一直被設為 1</li>
      <li>要 replancement 的時候會給有 Access bit 的 page 一個 second chance</li>
      <li>很接進 LRU 的方式，相當於使用 1 個 bit 來當作時間戳記</li>
    </ul>
  </li>
  <li>FIFO: 簡單，但是效能不好
    <ul>
      <li>因為放在 memory 中多久跟 page 是否會被使用沒有直接關係</li>
    </ul>
  </li>
  <li>LFU: 保留過去常用的 page，需要一些機制來消除累積的 page 次數，不然有可能造成某些 page 一直被留在 DRAM 中</li>
  <li>MFU: 提除過去常用的 page，通常是在特定情況下才會有較好的效果</li>
</ul>

<h5 id="814-linuxs-page-replacement">8.14 Linux’s Page Replacement</h5>

<blockquote class="block-tip">
  <p>Linux 使用了兩個 LRU list 來實現 page replacement</p>
</blockquote>

<p><img src="https://www.kernel.org/doc/gorman/html/understand/understand-html059.png" alt="" height="100%" width="100%" /></p>

<p>設計兩個 LRU 的好處是避免一些 page 去汙染 active list，例如: 一個一次性但很大的程式，那有可能就會把 active list 中真正需要的 page 踢出去，
有 inacitve list 就可以讓這些一次性的 page 保留在 inactive list 中，不會影響到 active list。</p>

<ul>
  <li>兩個 LRU list: active, inactive list
    <ul>
      <li>Active list: 最近比較活耀的 page</li>
      <li>Inactive list: 最近比較不活耀的 page
        <ul>
          <li>incative list 會定期的去檢查在一段時間內是否有 page 被存取過兩次，如果有就會被移動到 active list head</li>
        </ul>
      </li>
    </ul>
  </li>
</ul>

<blockquote>
  <p>從 Active/Inactive list 移除 page 使用的是 <strong>Second-chance algorithm</strong></p>
</blockquote>

<ul>
  <li>New page 通常會被放入 inactive list 的 head</li>
  <li>Inactive list 被填滿，就會將 tail 的部分 page 移到 swap space</li>
  <li>Active list 被填滿，就會將最 tail 的 page 移到 inactive list head
    <ul>
      <li>這樣的好處是，page 在 inactive list 中一直沒有被存取，就會慢慢移動到 tail，最後就會被移出 DRAM</li>
    </ul>
  </li>
  <li>page 離開 inactive list 時，會把該 page 紀錄在 table 中，這個 table 只會紀錄最近丟棄的 page
    <ul>
      <li>如果有 new page 進入時，就會檢查 table 中是否有相同的 page，如果有就把該 page 移到 active list head</li>
    </ul>
  </li>
</ul>

<p>如果最近讀入的 page 剛好在 swap-out table 中，就表示演算法有失誤，例如: 這個 page 會被常常使用，只是存取的間隔比 inactive list 的時間長。</p>

<ul>
  <li>所以這裡會選擇把這個 page 移到 active list
    <ul>
      <li>丟進 active list 可以給他比在 inactive list 更長的時間有機會被存取</li>
    </ul>
  </li>
  <li>這裡可以討論的是是否要記錄這個間隔有多長，要不要以此去調整 inactive list 的時間</li>
</ul>

<blockquote>
  <p>延伸閱讀: <a href="https://www.kernel.org/doc/gorman/html/understand/understand013.html">Linux Page Replacement</a></p>
</blockquote>

<ul>
  <li>關於這個 Swap-out table 的設計，他的靈感或許來自 「Adaptive Replacement Cache」(IBM)</li>
  <li>使用 Active + Inactive
    <ul>
      <li>融合了 LRU + LFU，只要常常被存取，即使不是最近被存取的也不太容易被移出 DRAM</li>
    </ul>
  </li>
</ul>

<blockquote>
  <p>在此之下可以思考的是，是否可以把 Active + Inactive 變成更多層的結構</p>
</blockquote>

<p><strong>Linux Page Replacement Steps</strong></p>

<p>要注意這裡所提到的 Active/Inactive list 是 OS 上的資料結構，所以真正的 Asecced bit 是在 page table 上，
這兩個 list 所儲存的是 DRAM 中的 physical address。</p>

<p>在 OS 上的資料結構如何去知道 Access bit 的變化，實際的 Access bit 是存在 task 的 page table 上:</p>
<ol>
  <li>掃描 Inactive list，要反向的從 physical page 去找出對應的 page tabe</li>
  <li>從 page table 中把該 Page 的 Access bit mapping 到 Inactive list 的 page</li>
</ol>

<p>因為 Linux 中的 Inactive list 是使用 Second-chance algorithm，因此是沿著 circular list 不斷往下掃描每一個 entry，
逐一去更新 entry 中的 Access bit 即可。</p>

<p>這樣移除的時候也要反向的去找出對應的 page table，然後更新 page table 中的 valid bit，然後就將該 page 移到 swap space，
mm_struct 中的 page table 會記錄這個 page 的使用情況是在 swap space 還是在 disk 中。</p>

<blockquote class="block-warning">
  <h5 id="last-edit">Last Edit</h5>
  <p>1-31-2023 21:14</p>
</blockquote>]]></content><author><name>Benson Hsu</name></author><category term="Jekyll" /><category term="OS" /><summary type="html"><![CDATA[Operating System: Design and Implementation course notes from CCU, lecturer Shiwu-Lo.]]></summary></entry><entry><title type="html">Testing | System Testing</title><link href="https://hotshot824.github.io/jekyll/2023-12-20-system_testing.html" rel="alternate" type="text/html" title="Testing | System Testing" /><published>2023-12-20T00:00:00+00:00</published><updated>2023-12-20T00:00:00+00:00</updated><id>https://hotshot824.github.io/jekyll/system_testing</id><content type="html" xml:base="https://hotshot824.github.io/jekyll/2023-12-20-system_testing.html"><![CDATA[<blockquote class="block-tip">
  <p>Software testing course notes from CCU, lecturer Nai-Wei Lin.<br />
前面講的部分都是如何去產生測試案例，這裡會介紹 Mutation Testing，這是一種可以評估測試案例的方法</p>
</blockquote>

<p>System Testing 通常是在 Integration Testing 之後，並且是內部測試的最後一個階段，因此此時通常會在一個完整的系統(包含軟體與硬體)上進行，
用來評估系統是否符合其需求規格。</p>

<p>系統測試通常是屬於黑箱測試的範疇，因此不需要對程式碼或邏輯的內部設計有所了解。</p>

<h3 id="system-testing">System Testing</h3>

<p><a href="#82-performance-testing">8.2 Performance Testing</a><br />
<a href="#83-load-testing">8.3 Load Testing</a><br />
<a href="#84-stress-testing">8.4 Stress Testing</a></p>

<h5 id="81-system-testing">8.1 System Testing</h5>

<ul>
  <li>系統測試所使用的輸入資料是所有已經成功完成集成測試的整合軟體組件。</li>
  <li>這是 User acceptance testing(使用者驗收測試)前的最後一個階段，也就是最後的內部測試階段。</li>
</ul>

<p>在這個階段測試可以採用一種接近破壞性的測試方式，這代表測試不僅僅侷限於驗證系統是否符合需求規格，也會去驗證系統是否能夠承受極端的使用情境，還是否能符合預期。</p>

<ul>
  <li>因此在這個階段測試軟體/硬體在要求規格中所定義的邊界，甚至超出邊界的情況</li>
</ul>

<p><strong>Types of System Testing</strong></p>

<ul>
  <li>性能測試(Performance testing)</li>
  <li>負載測試(Load testing)</li>
  <li>壓力測試(Stress testing)</li>
  <li>可用性測試(Usability testing)</li>
  <li>安全測試(Security testing)</li>
</ul>

<hr />

<h5 id="82-performance-testing">8.2 Performance Testing</h5>

<ul>
  <li>性能測試的目標不是發現錯誤，而是消除性能瓶頸並為未來的回歸測試建立基準</li>
  <li>進行性能測試代表需要一個可以謹慎控制的測量與分析的過程</li>
  <li>理想情況下是正在測試的軟體已經足夠穩定，使這個過程可以專注於性能問題</li>
</ul>

<p>以下會介紹性能測試的重要步驟</p>

<p><strong>Set of Expectations</strong></p>

<ul>
  <li>一套清晰定義的預期結果對於有意義的性能測試至關重要，如果測試者根本不知道在性能方面要達到什麼目標，
那麼在測試過程中所採取的任何行動都是毫無意義的</li>
  <li>例如: 對於一個網路應用程式，至少要知道兩件事
    <ol>
      <li>在一個給定的時間內，系統應該能夠處理多少個同時連線的使用者</li>
      <li>系統應該能夠在多少時間內回應使用者的請求</li>
    </ol>
  </li>
</ul>

<p><strong>Looking for Bottlenecks</strong></p>

<ul>
  <li>一旦已經了解想到達到的目標，就可以開始不斷增加系統的負載來開始測試，同時尋找系統中的瓶頸</li>
  <li>例如: 以之前的網路程式為例
    <ul>
      <li>這些瓶頸可以存在於多個層面上，為了找到這些瓶頸，可以使用各種工具來監控系統的狀態</li>
    </ul>
  </li>
</ul>

<p>這是一些工具的例子:</p>
<ul>
  <li><em>Application level:</em> Developer 可以使用一些分析工具來找出程式碼中的低效之處</li>
  <li><em>Database level:</em> 使用特定於資料庫的分析器與查詢優化</li>
  <li><em>Operating system level:</em> System Engineer 可以使用如 top, vmstat, iostat(on Unix) 或者 PerfMon(on Windows) 之類的工具來監控硬體資源，
CPU, memory, swap space, disk I/O, network I/O 等等…</li>
  <li><em>Network level:</em> Network Engineer 可以使用如 tcpdump, Wireshark, iptraf 之類的封包嗅探器，網路協議分析器如 ethereal，
以及各種工具如 netstat, MRTG, ntop, mii-tool 等等…</li>
</ul>

<p><strong>Performance Tuning</strong></p>

<ul>
  <li>當 Load testing 的結果顯示系統效能沒有達到預期目標時，就是進行調整的時候了，假設從 Application/Nework level 開始
    <ul>
      <li>這時就要確保程式碼中沒有低效之處</li>
      <li>確保程式碼中沒有不必要的 DB Query，並且 DB 在特定的 OS/Hardware 上的配置下是最佳化的</li>
    </ul>
  </li>
</ul>

<p><strong>JUnitPerf</strong></p>
<ul>
  <li>實踐 TDD(Test-Driven Development) 的開發者會發現，像是 Mike Clark 所寫的 JUnitPerf 這樣的工具在 JUnit 中是非常有用的，
他可以通過增加 Load testing 和增加計時測試功能來增強現有的單元測試代碼</li>
  <li>一旦重構某個特定函數或方法後，就可以使用 JUnitPerf 來確保其性能沒有受到影響，Mike Clark 稱其為 <strong>Continuous Performance Testing</strong>(持續性能測試)</li>
</ul>

<blockquote>
  <p>延伸閱讀: <a href="https://github.com/clarkware/junitperf">JUnitPerf</a></p>
</blockquote>

<p>當然有可能即使 Application level 的程式碼已經是最佳化的，但是系統的瓶頸還是存在於其他層面，因此系統的性能還是沒有達到預期目標，
那麼在前面討論過的所有 Level 上都還有許多調整工作可以進行</p>

<ul>
  <li>這裡列出一些可能的方法，這些方法都不局限於軟體的 Source code
    <ul>
      <li>Use Web cache mechanisms, such as the one provided by Squid.</li>
      <li>Publish highly-requested Web pages statically, so that they don’t hit the database.</li>
      <li>Scale the Web server farm horizontally via load balancing.</li>
      <li>Scale the database servers horizontally and split them into read/write servers and read-only servers, 
then load balance the read-only servers.</li>
      <li>Scale the Web and database servers vertically, by adding more hardware resources (CPU, RAM, disks).</li>
      <li>Increase the available network bandwidth.</li>
    </ul>
  </li>
</ul>

<p><strong>One Variable at a Time</strong></p>

<blockquote class="block-warning">
  <p>這裡就跟機器學習中的 Hyperparameter tuning 一樣，以列舉的方式來找出最佳的參數，並且一次只會調整一個參數</p>
</blockquote>

<ul>
  <li>進行性能測試時，有時更像是一種藝術而不是科學，因為有太多的變數需要考慮，而且這些變數之間的關係也很複雜
    <ul>
      <li>每次調整效能時，必須警慎的一次修改一個變數，並再次測量數據</li>
    </ul>
  </li>
</ul>

<p><strong>Staging Environment</strong></p>

<ul>
  <li>通常會有一個獨立的環境，用來進行性能測試，這個環境通常稱為 Staging Environment</li>
  <li>這個環境或許不能複製 Production Environment 的所有特性與效能，但是至少要能夠模擬 Production Environment 的行為
    <ul>
      <li>在這個預期之下系統的預期性能可以相應的進行縮放</li>
    </ul>
  </li>
</ul>

<blockquote>
  <p>現在測試的環境通常都是在雲端上，因此可以很容易的進行縮放</p>
</blockquote>

<p><strong>Baseline</strong></p>

<p>在運行時要尊從「Run load test -&gt; Measure performance -&gt; Tune System」 的循環重複進行，直到被測試的系統達到預期的效能為止。</p>
<ul>
  <li>並且要在系統中建立一個 Baseline，這個 Baseline 代表系統在預期的負載下所能達到的效能</li>
  <li>這是為了在 Regression Testing(回歸測試)中，可以透過這個 Baseline 來衡量系統的效能表現</li>
</ul>

<p><strong>Benchmarks</strong></p>

<p>另一種方式是建立一個 Benchmark，這個 Benchmark 代表系統在預期的負載下所能達到的效能，並且這個 Benchmark 會被用來衡量系統的效能表現</p>
<ul>
  <li>這就很像手機每次發表新的型號時，都會使用 AnTuTu Benchmark 來衡量其效能表現一樣</li>
  <li>各家廠商可能會去針對這個 Benchmark 進行優化，好拿到更好的成績</li>
</ul>

<hr />

<h5 id="83-load-testing">8.3 Load Testing</h5>

<p>在測試相關的文章中，Load testing 通常被定義: 「通過向被測系統提供他所能夠操作的最大任務來進行系統練習的過程」</p>

<p>Load testing 有時也被稱作 Volume testing(容量測試)或者 Longevity/Endurance testing (耐久性/持久性測試)</p>

<p><strong>Example of Volume Testing</strong></p>

<ul>
  <li>通過編輯一個非常大的文件來測試一個文字編輯器</li>
  <li>通過向打印機發送一個非常大的文件來測試一個打印機</li>
  <li>對於一個 Mail Server，通過向其發送大量的郵件來測試容量</li>
  <li>這裡有一個特別的測試是 Zero-volume testing，這是一種測試，用來測試系統被提供空任務時的行為</li>
</ul>

<p><strong>Example of Longevity/Endurance testing</strong></p>

<ul>
  <li>通過長時間內讓 Clien 對 Server 進行循環發送請求來測試一個 Client-Server 系統</li>
</ul>

<blockquote>
  <p>延伸閱讀: <a href="https://testsigma.com/blog/volume-testing/">Volume testing</a></p>
</blockquote>

<p><strong>Goals of Load Testing</strong></p>

<p>Load testing 的目標如下</p>
<ul>
  <li>檢查再粗略的測試中不會被發現的錯誤，例如: Memory management bugs, Memory leaks, Buffer overflows, etc.</li>
  <li>確保系統能滿足性能測試中建立的 Performance baseline</li>
  <li>這些都是通過指定的最大負載下對程式運行回歸測試來完成的</li>
</ul>

<p><strong>Performance vs. Load Testing</strong></p>

<ul>
  <li>Performance testing 通常使用負載測試技術和工具來進行測量與建立 Baseline, 或使用 Benchmark 來進行測量</li>
  <li>Load testing 是在一個預定地負載下對系統進行測試，通常是系統在仍然能正常運作下可以承受的最大負載</li>
</ul>

<blockquote>
  <p>注意 Load testing 並不是通過壓倒性的負載來破壞系統，而是試圖讓系統能像一台嚴密潤滑的機器一樣運作</p>
</blockquote>

<p><strong>Large Datasets</strong></p>

<ul>
  <li>在 Load testing 中<strong>「大型的資料集是非常重要的」</strong></li>
  <li>因為很多重要的錯誤只有在處理非常大的實體時才會出現:
    <ul>
      <li>LDAP/NIS/Active Directory 中成千上萬的使用者，成千上萬的 mailboxes，Database 中的 multi-gigabyte tables，File 上的超深層文件/目錄等等…</li>
    </ul>
  </li>
</ul>

<hr />

<h5 id="84-stress-testing">8.4 Stress Testing</h5>

<p>Stress testing(壓力測試)是試圖通過壓倒系統資源或從系統中奪走資源來破壞被測系統(也被稱為負面測試 Negative testin)。</p>

<ul>
  <li>這種測試的目的是確保系統能夠在壓力下能夠 Graceful Failure(優雅的失敗)並恢復，這種被稱為 Recoverability(可恢復性)的能力。</li>
  <li>在 Performance testing 中，系統的負載是在可控的情況下，但壓力測試則需要引入混亂與不可控性</li>
</ul>

<p><strong>Example of Stress Testing</strong></p>

<ul>
  <li>將一次可以連線的最大用戶數量翻倍</li>
  <li>隨便拔除 Server 中的網路線，例如透過 SNMP 來控制</li>
  <li>斷開 Database 的連線，然後重新啟動</li>
  <li>系統運行中拔掉硬碟，然後重新插入新的硬碟重組 RAID</li>
  <li>在伺服器上運行其他會強佔消耗資源的 Process</li>
</ul>

<p><strong>Goals of Stress Testing</strong></p>

<ul>
  <li>Stress testing 不僅僅是為了破壞系統而破壞他，而是讓測試人員觀察系統對失敗的反應
    <ul>
      <li>是否有保存狀態，還是直接崩潰</li>
      <li>是中斷狀態後恢復，還是優雅的失敗</li>
      <li>重啟後，他能從最後一個狀態恢復嗎</li>
      <li>是否有向用戶提供有用的 Log 訊息，還是顯示難以理解的 Hex dump</li>
      <li>因為意外的失敗，系統的安全性是否受到威脅</li>
    </ul>
  </li>
</ul>

<blockquote class="block-warning">
  <h5 id="last-edit">Last Edit</h5>
  <p>1-2-2024 05:23</p>
</blockquote>]]></content><author><name>Benson Hsu</name></author><category term="Jekyll" /><category term="software" /><category term="software_qualitiy" /><summary type="html"><![CDATA[Software testing course notes from CCU, lecturer Nai-Wei Lin. 前面講的部分都是如何去產生測試案例，這裡會介紹 Mutation Testing，這是一種可以評估測試案例的方法]]></summary></entry><entry><title type="html">Testing | Mutation Testing</title><link href="https://hotshot824.github.io/jekyll/2023-12-12-mutation_testing.html" rel="alternate" type="text/html" title="Testing | Mutation Testing" /><published>2023-12-12T00:00:00+00:00</published><updated>2023-12-12T00:00:00+00:00</updated><id>https://hotshot824.github.io/jekyll/mutation_testing</id><content type="html" xml:base="https://hotshot824.github.io/jekyll/2023-12-12-mutation_testing.html"><![CDATA[<blockquote class="block-tip">
  <p>Software testing course notes from CCU, lecturer Nai-Wei Lin.<br />
前面講的部分都是如何去產生測試案例，這裡會介紹 Mutation Testing，這是一種可以評估測試案例的方法</p>
</blockquote>

<h3 id="mutation-testing">Mutation Testing</h3>

<h5 id="71-what-is-mutation-testing">7.1 What is Mutation Testing?</h5>

<ul>
  <li>Mutation testing(突變測試)是一種基於錯誤的測試技術</li>
  <li>如果一個測試能檢測出大部分簡單的錯誤，那麼這個程式就已經被充分的測試了</li>
</ul>

<p>突變測試會去產生一組原程式的缺陷版本，這些缺陷版本被稱為突變體(mutant)，每個突變體都只包含一個簡單的錯誤，
通常是針對程式的一些運算子做修改，讓他與原程式不同造成錯誤。</p>

<p><img src="/image/2023/12-12-mutation_testing/1.png" alt="" /></p>

<blockquote>
  <p>上圖是一個突變的範例，把 <code class="language-plaintext highlighter-rouge">&gt;</code> 改成 <code class="language-plaintext highlighter-rouge">&lt;</code>, <code class="language-plaintext highlighter-rouge">&gt;=</code>, <code class="language-plaintext highlighter-rouge">&lt;=</code>, <code class="language-plaintext highlighter-rouge">==</code>, <code class="language-plaintext highlighter-rouge">!=</code> 這樣就產生了五個突變體</p>
</blockquote>

<p><strong>Mutation Analysis Process</strong></p>

<p>突變分析的流程:</p>

<ol>
  <li>首先以一組測試案例來測試原程式，這裡應該要確保原程式是能通過測試的</li>
  <li>同樣的一組測試案例來測試突變體</li>
  <li>如果一個突變體產生了與原程式不同的輸出，那麼這個突變體就被殺死了</li>
  <li>一個測試案例如果至少殺死了一個突變體，那麼這個測試案例就被認為是足夠有效的</li>
</ol>

<p><strong>Equivalent Mutants</strong></p>

<p>Equivalent Mutants(等價突變體):</p>

<ul>
  <li>如果兩個程式在每個輸入上始終保持相同的輸出，那我們可以說這兩個程式在功能上是等價的</li>
  <li>突變體也是一樣，如果突變體與原程式是等價的，那麼這個突變體就是等價突變體
    <ul>
      <li>因為是等價的，所以突變體並不會被測試案例集殺死</li>
    </ul>
  </li>
</ul>

<p>以之前的例子為例，下面這個原始程式與突變體就是等價的，這邊要注意到即使輸入兩個相同的變數，原始程式返回 b，突變體返回 a，但這個程式在輸出上始終保持相同的輸出，所以這兩個程式是等價的。</p>
<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="nf">max</span><span class="p">(</span><span class="kt">int</span> <span class="n">a</span><span class="p">,</span> <span class="kt">int</span> <span class="n">b</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">a</span> <span class="o">&gt;</span> <span class="n">b</span><span class="p">)</span> <span class="p">{</span>
        <span class="k">return</span> <span class="n">a</span><span class="p">;</span>
    <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
        <span class="k">return</span> <span class="n">b</span><span class="p">;</span>
    <span class="p">}</span>
<span class="p">}</span>

<span class="kt">int</span> <span class="nf">max</span><span class="p">(</span><span class="kt">int</span> <span class="n">a</span><span class="p">,</span> <span class="kt">int</span> <span class="n">b</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">a</span> <span class="o">&gt;=</span> <span class="n">b</span><span class="p">)</span> <span class="p">{</span>
        <span class="k">return</span> <span class="n">a</span><span class="p">;</span>
    <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
        <span class="k">return</span> <span class="n">b</span><span class="p">;</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<h5 id="72-mutation-score">7.2 Mutation Score</h5>

<ul>
  <li>Mutation Score(突變分數)，就是一組測試案例所可以殺死的非等價突變體的比例
    <ul>
      <li>P: Program</li>
      <li>T: Test Case Set</li>
      <li>K: Number of killed mutants</li>
      <li>M: Number of mutants</li>
      <li>E: Number of equivalent mutants</li>
    </ul>
  </li>
  <li>以下是突變分數的公式，分數越高代表測試案例集越有效，可以殺死更多的突變體
\begin{flalign}
&amp; \text{MS(P, T)} = \frac{\text{K}}{\text{M - E}} &amp;
\end{flalign}</li>
</ul>

<p><strong>Measuring Mutation Score of a Test Case Set</strong></p>

<p>如果一組測試案例集的突變分數為 100% 就代表是 Mutation adequate(突變充分的)，這組測試案例集可以殺死所有的非等價突變體。
但是在真實的開發中，要擁有一組突變分數到達 95% 以上的測試案例集是非常困難的。</p>

<h5 id="73-mutation-operators">7.3 Mutation Operators</h5>

<p>Mutation Operators(突變運算子)是對原程式做修改的規則:</p>
<ul>
  <li>突變運算子的集合取決於所使用的程式語言</li>
  <li>Mothra 是一個用於 Fortran 77 程式的突變測試工具，他提供了 22 種突變運算子</li>
</ul>

<blockquote>
  <p>延伸閱讀: <a href="https://ieeexplore.ieee.org/document/48002">The Mothra tool set (software testing)</a></p>
</blockquote>

<p>這裡列出 Mothra 提供的突變運算子:</p>

<p>AAR: array reference for array reference replacement<br />
ABS: absolute value insertion<br />
ACR: array reference for constant replacement<br />
AOR: arithmetic operator replacement<br />
ASR: array reference for scalar variable replacement<br />
CAR: constant for array reference replacement<br />
CNR: comparable array name replacement<br />
CRP: constant replacement<br />
CSR constant for scalar variable replacement<br />
DER: DO statement end replacement<br />
DSA: DATA statement alternations<br />
GLR: GOTO label replacement<br />
LCR: logical connector replacement<br />
ROR: relational operator replacement<br />
RSR: RETURN statement replacement<br />
SAN: statement analysis<br />
SAR: scalar variable for array reference replacement<br />
SCR: scalar for constant replacement<br />
SDL: statement deletion<br />
SRC: source constant replacement<br />
SVR: scalar variable replacement<br />
UOI: unary operator insertion</p>

<p><strong>Categories of Mutation Operators</strong></p>

<p>我們可以把突變運算子分成三種類別:</p>
<ol>
  <li>Replacement of Operandoperators:
    <ul>
      <li>把程式中的每個 Operand 都替換成其他的 Operand</li>
      <li>AAR, ACR, ASR, CAR, CNR, CSR, SAR, SCR, SRC, SVR</li>
    </ul>
  </li>
  <li>Expression Modification Operators:
    <ul>
      <li>把程式中的每個 Operator 都替換成其他的 Operator</li>
      <li>ABS, AOR, ROR, UOI</li>
    </ul>
  </li>
  <li>Statement Modification Operators:
    <ul>
      <li>把程式中的每個 Statement 都替換成其他的 Statement</li>
      <li>DER, DSA, GLR, LCR, SDL</li>
    </ul>
  </li>
</ol>

<h3 id="high-cost-of-mutation-testing">High Cost of Mutation Testing</h3>

<ul>
  <li>突變測試的主要缺點是運行大量突變體對抗測試案例集的成本非常高</li>
  <li>為一個程式產生的突變體數量與數據引用數和數據對象數的乘積成正比</li>
  <li>即使是一個小程式，這個數字也是相當大的</li>
</ul>

<p><strong>Reducing Cost of Mutation Testing</strong></p>

<p>這裡提出幾種可能減少突變測試成本的方法:</p>
<ol>
  <li>Selective Mutation: 做更少的方法</li>
  <li>Weak Mutation: 做的更聰明的方法</li>
  <li>Schema-Based Mutation: 做的更快的方法</li>
</ol>

<p><a href="https://cs.gmu.edu/~offutt/mujava/">MuJava</a> 是一個針對 Java 的突變測試系統，實現了這三種方法，同時在 Eclipse 上也有提供插件 <a href="https://muclipse.sourceforge.net/">MuClipse</a></p>

<h5 id="75-selective-mutation">7.5 Selective Mutation</h5>

<ul>
  <li>Selective Mutation(選擇性突變): 是指只針對一些特定的方法做突變，忽略那些會造成大量突變體的 Operator</li>
  <li>N-Selective Mutation(N-選擇性突變): 忽略最常導致突變體產生的 N 個 Operator
    <ul>
      <li>像是 Mothra 來說就省略了 SVR、ASR 兩種 Operator</li>
    </ul>
  </li>
</ul>

<p><img src="/image/2023/12-12-mutation_testing/2.png" alt="" /></p>

<blockquote>
  <p>SVR、ASR 兩種 Operator 是會產生最多突變體的前兩名 Operator</p>
</blockquote>

<p><strong>Compare Selective Mutation with Non-Selective Mutation</strong></p>

<p>假如我們使用了「一組測試案例」去針對 Selective Mutation 來達到 100% 的突變分數，如果這組測試案例在 Non-Selective Mutation(非選擇性突變)的突變分數也能表現良好，
那麼就能以此來證明 Selective Mutation 是有效的。</p>

<ul>
  <li>Non-Selective Mutation: 針對所有的 Operator 做突變</li>
</ul>

<p><strong>Experiments</strong></p>

<p>來自 <a href="https://dl.acm.org/doi/10.1145/227607.227610">An experimental determination of sufficient mutant operators</a> 的實驗結果，它們使用多個程式來進行突變，並在之後比較四種不同的突變方法:</p>

<div style="display: flex; flex-direction: row; align-items: center;">
    <img src="/image/2023/12-12-mutation_testing/3.png" width="50%" height="50%" />
    <img src="/image/2023/12-12-mutation_testing/4.png" width="50%" height="50%" />
</div>

<p>最終的結論是，實際上只要保留五個關鍵的 Mutation Operator 就能提供與 Mothra 中的 22 個 Operator 幾乎相同的覆蓋率，
並且成本在小型程式中減少了四倍，在大型程式中減少了五十倍。</p>

<ul>
  <li>這五個關鍵的 Operator 是: ABS, AOR, LCR, ROR, UOI</li>
</ul>

<h5 id="76-weak-mutation">7.6 Weak Mutation</h5>

<p>Weak Mutation(弱突變) 是一種近似的突變測試方法，他在程式的突變位置執行後立刻去比較突變體與原程式的內部狀態，
已經有實驗證明，Weak Mutation 與 Strong Mutation(強突變) 一樣有效，並且成本降低至少 50%。</p>

<p><strong>Four Potential Points for State Comparison</strong></p>

<p>那我們需要考慮的就是在什麼時間點去比較突變體與原程式的內部狀態，這裡提出了四個可能的時間點:</p>
<ol>
  <li>在突變符號的 Innermost Expression 進行第一次評估之後</li>
  <li>在突變 Statement 進行第一次評估之後</li>
  <li>在包含突變語句的 Basic Block 進行第一次評估之後</li>
  <li>在包含突變語句的 Basic Block 全部評估之後</li>
</ol>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">while</span><span class="p">()</span> <span class="p">{</span>
    <span class="k">if</span><span class="p">()</span> <span class="p">{</span>
        <span class="n">y</span> <span class="o">=</span> <span class="n">x</span> <span class="o">+</span> <span class="cm">/* 1 */</span> <span class="mi">1</span><span class="p">;</span> <span class="cm">/* 2 */</span>
    <span class="p">}</span> <span class="cm">/* 3 */</span><span class="k">else</span> <span class="p">{</span>
        <span class="p">...</span>
    <span class="p">}</span>
    <span class="cm">/* 4 */</span>
<span class="p">}</span>
</code></pre></div></div>

<p><strong>Schema-Based Mutation Analysis</strong></p>

<p>Schema-Based Mutation Analysis(基於模式的突變分析)是一種弱突變的方法</p>
<ul>
  <li>將所有突變體編碼到一個可以參數化的 Source-level program(源程式)中，稱為 Meta-Mutant(元突變體)</li>
  <li>元突變體使用與編譯原始程式相同的編譯器進行編譯</li>
  <li>元突變體具有運行時充當任何突變體的能力</li>
  <li>這樣成本就能減少到一個數量級</li>
</ul>

<p><strong>Metaoperators</strong></p>

<p>Metaoperators(元運算子)是一種用來產生元突變體的突變運算子，例如:</p>
<ul>
  <li>AOR metaoperator: 用來產生 AOR 元突變體</li>
  <li>對於 Statement <code class="language-plaintext highlighter-rouge">Result = A - B</code>，可以產生四種突變體:
    <ul>
      <li><code class="language-plaintext highlighter-rouge">Result = A + B</code></li>
      <li><code class="language-plaintext highlighter-rouge">Result = A * B</code></li>
      <li><code class="language-plaintext highlighter-rouge">Result = A / B</code></li>
      <li><code class="language-plaintext highlighter-rouge">Result = A % B</code></li>
    </ul>
  </li>
  <li>那麼這些突變體可以通用的表示為 <code class="language-plaintext highlighter-rouge">Result = A ArithOp B</code>，其中 ArithOp 是 Metaoperator</li>
</ul>

<p><strong>Metaprocedures</strong></p>

<ul>
  <li>把上面的方法重寫為 Result = AOrr(A, B)，其中 AOrr 是 Metaprocedure
    <ul>
      <li>在 AOrr 中會執行 <code class="language-plaintext highlighter-rouge">Result = A ArithOp B</code> 這五種可能的突變體</li>
    </ul>
  </li>
  <li>這樣就可以把所有的突變體都編碼到一個可以參數化的 Source-level program 中</li>
</ul>

<p><strong>Class Level Mutation Operators in MuClipse</strong></p>
<ul>
  <li>Access control: AMC</li>
  <li>Inheritance: IHD, IHI, IOD, IOP, IOR, ISK, IPC</li>
  <li>Polymorphism: PNC, PMD, PPD, PRV</li>
  <li>Overloading: OMR, OMD, OAO, OAN</li>
  <li>Java-specific features: JTD, JSC, JID, JDC</li>
  <li>Common-programming mistakes: EOA, EOC, EAM, EMM</li>
</ul>

<p>這些是 MuClipse 所提供的 Class-level 突變運算子，這些突變運算子都是針對 Java 語言所設計的</p>

<h5 id="77-equivalent-mutants">7.7 Equivalent Mutants</h5>

<p>這裡做了一個實驗，對於 6 個程式一共 866 個 Class 進行不同層級的突變，來查看等價突變體的比例:</p>
<ul>
  <li>在 Method-level 突變測試中，Equivalents Mutants 的比例約為 5% ~ 15%</li>
  <li>在 Class-level 突變測試中，Equivalents Mutants 的比例平均為 75%</li>
</ul>

<blockquote class="block-warning">
  <h5 id="last-edit">Last Edit</h5>
  <p>1-2-2024 02:51</p>
</blockquote>]]></content><author><name>Benson Hsu</name></author><category term="Jekyll" /><category term="software" /><category term="software_qualitiy" /><summary type="html"><![CDATA[Software testing course notes from CCU, lecturer Nai-Wei Lin. 前面講的部分都是如何去產生測試案例，這裡會介紹 Mutation Testing，這是一種可以評估測試案例的方法]]></summary></entry><entry><title type="html">OS | Main Memory</title><link href="https://hotshot824.github.io/jekyll/2023-12-10-main_memory.html" rel="alternate" type="text/html" title="OS | Main Memory" /><published>2023-12-10T00:00:00+00:00</published><updated>2023-12-10T00:00:00+00:00</updated><id>https://hotshot824.github.io/jekyll/main_memory</id><content type="html" xml:base="https://hotshot824.github.io/jekyll/2023-12-10-main_memory.html"><![CDATA[<blockquote class="block-tip">
  <p>Operating System: Design and Implementation course notes from CCU, lecturer Shiwu-Lo.</p>
</blockquote>

<ul>
  <li>Physical configuration of DRAM on PC
    <ul>
      <li><code class="language-plaintext highlighter-rouge">DRAM + BIOS + MMIO</code></li>
    </ul>
  </li>
  <li>Three main functions of DRAM</li>
  <li>Segmentation memory management</li>
  <li>Paging memory management</li>
  <li>Linux memory layout</li>
  <li>External fragmentation</li>
</ul>

<h5 id="x86-start---bios-address">x86 Start - BIOS Address</h5>

<blockquote class="block-tip">
  <p>在一台 PC 剛開機後唯一能讀取的記憶體就是 BIOS(ROM)，必須透過 BIOS 才能完成後續 OS 的內容載入到 DRAM 中</p>
</blockquote>

<ul>
  <li>x86 在啟動的時候，執行的第一個指令是在 <code class="language-plaintext highlighter-rouge">0xFFFFFFF0</code> 這個位址
    <ul>
      <li>因此主機板上必須使用 Address decoder 將 <code class="language-plaintext highlighter-rouge">0xFFFFFFF0</code> 對應到 BIOS 的 Entry point</li>
    </ul>
  </li>
  <li>BIOS 設定完畢後，會進入 OS 的 Entry point
    <ul>
      <li>例如: Linux 的 <code class="language-plaintext highlighter-rouge">start_kernel</code></li>
    </ul>
  </li>
</ul>

<h5 id="bios-in-memory-usage">BIOS in Memory Usage</h5>

<p>BIOS 雖然不需要電力就能保存，但是通常速度會比 DRAM 慢，所以大部分的 BIOS 會把自己複製到 DRAM 中，
並且配置 data, stack, heap section，然後在 DRAM 中執行。</p>

<blockquote>
  <p>為什麼不使用 Flash memory，因為 Flash memory 是 Block device 而 CPU 能讀取的必須是 byte addressable</p>
</blockquote>

<hr />

<h3 id="memory-layout">Memory Layout</h3>

<blockquote>
  <p>下圖是如果記憶體只有 256MB 或 512MB 的 Memory Layout，在 32-bit 的 CPU 中，最多只能使用 4G 的定址空間</p>
</blockquote>

<p><img src="/image/2023/12-10-main_memory/1.png" alt="" /></p>

<ul>
  <li>DRAM 之外的空間就是 MMIO，這部分的空間通常只能透過 Kernel 存取</li>
  <li>注意到他把 BIOS 映射到最上層的位置，這樣 BIOS 就不會去占用 DRAM 的定址空間</li>
</ul>

<blockquote>
  <p>延伸閱讀: <a href="https://resources.infosecinstitute.com/topics/hacking/system-address-map-initialization-in-x86x64-architecture-part-1-pci-based-systems/#gref">System address map initialization in x86/x64 architecture part 1: PCI-based systems</a></p>
</blockquote>

<p><img src="/image/2023/12-10-main_memory/2.png" alt="" height="75%" width="75%" /></p>

<ul>
  <li>假如一個 4G 定址空間的機器裝上 4G 的 DRAM，那麼扣除 PCI 的空間後，只會剩下 3.25G 的空間可用</li>
  <li>最好的解決方式就是邁入 64-bit 的時代，不只解決了 Memory address 的問題，也解決了檔案不能超過 2G 的問題</li>
</ul>

<blockquote>
  <p>延伸閱讀: <a href="https://www.quora.com/Why-does-my-motherboard-see-only-3-25-GB-out-of-4-GB-RAM-installed-in-my-PC">Why does my motherboard see only 3,25 GB out of 4 GB RAM installed in my PC?</a></p>
</blockquote>

<p><strong>7.1 BIOS - Memory Map</strong></p>

<p>使用 <code class="language-plaintext highlighter-rouge">dmesg</code> 可以看到 Linux 在啟動時的 <strong><a href="https://en.wikipedia.org/wiki/E820">BIOS-e820</a> Physical Memory</strong> 配置，可以看到幾個區域</p>
<ul>
  <li>usable: 可以使用的記憶體</li>
  <li>reserved: 保留給 BIOS 或是其他的 Device 使用</li>
  <li>ACPI data/NVS: 配置給 ACPI 或 NVS (Non-Volatile Storage)使用</li>
</ul>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">[</span>    0.000000] BIOS-provided physical RAM map:
<span class="o">[</span>    0.000000] BIOS-e820: <span class="o">[</span>mem 0x0000000000000000-0x000000000009f7ff] usable    <span class="c"># 0.62 MB</span>
<span class="o">[</span>    0.000000] BIOS-e820: <span class="o">[</span>mem 0x000000000009f800-0x000000000009ffff] reserved
<span class="o">[</span>    0.000000] BIOS-e820: <span class="o">[</span>mem 0x00000000000ca000-0x00000000000cbfff] reserved
<span class="o">[</span>    0.000000] BIOS-e820: <span class="o">[</span>mem 0x00000000000dc000-0x00000000000fffff] reserved
<span class="o">[</span>    0.000000] BIOS-e820: <span class="o">[</span>mem 0x0000000000100000-0x00000000bfeeffff] usable    <span class="c"># 3069.94 MB</span>
<span class="o">[</span>    0.000000] BIOS-e820: <span class="o">[</span>mem 0x00000000bfef0000-0x00000000bfefefff] ACPI data
<span class="o">[</span>    0.000000] BIOS-e820: <span class="o">[</span>mem 0x00000000bfeff000-0x00000000bfefffff] ACPI NVS
<span class="o">[</span>    0.000000] BIOS-e820: <span class="o">[</span>mem 0x00000000bff00000-0x00000000bfffffff] usable    <span class="c"># 1 MB</span>
<span class="o">[</span>    0.000000] BIOS-e820: <span class="o">[</span>mem 0x00000000f0000000-0x00000000f7ffffff] reserved
<span class="o">[</span>    0.000000] BIOS-e820: <span class="o">[</span>mem 0x00000000fec00000-0x00000000fec0ffff] reserved
<span class="o">[</span>    0.000000] BIOS-e820: <span class="o">[</span>mem 0x00000000fee00000-0x00000000fee00fff] reserved
<span class="o">[</span>    0.000000] BIOS-e820: <span class="o">[</span>mem 0x00000000fffe0000-0x00000000ffffffff] reserved
<span class="o">[</span>    0.000000] BIOS-e820: <span class="o">[</span>mem 0x0000000100000000-0x000000023fffffff] usable    <span class="c"># 5120 MB</span>
</code></pre></div></div>

<p>實際分配給電腦的記憶體是 8192 MB，而經由 BIOS 分配以後為 8191 MB，可以發現這些 usable 的地址其實是不連續的，
有些地方映射到 DRAM，有些地方映射到 MMIO。CPU 會去透過 Address decoder 將 Physical address 映射到不同裝置的記憶體。</p>

<ul>
  <li>DMA 與 DMA32 是保留給 16/32-bit 的 DMA controller 使用
    <ul>
      <li>Normal 就是任何能夠使用 64-bit address 的裝置</li>
    </ul>
  </li>
  <li>Movable zone 是用來處理 Hotplug 的記憶體，例如: USB Keyboard</li>
  <li>unavailable ranges 代表該區域的記憶體不可使用
    <ul>
      <li>可能因為 BIOS 或是其他的裝置使用了這些記憶體</li>
    </ul>
  </li>
</ul>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">[</span>    0.014535] Zone ranges:
<span class="o">[</span>    0.014536]   DMA      <span class="o">[</span>mem 0x0000000000001000-0x0000000000ffffff]
<span class="o">[</span>    0.014539]   DMA32    <span class="o">[</span>mem 0x0000000001000000-0x00000000ffffffff]
<span class="o">[</span>    0.014541]   Normal   <span class="o">[</span>mem 0x0000000100000000-0x000000023fffffff]
<span class="o">[</span>    0.014543]   Device   empty
<span class="o">[</span>    0.014545] Movable zone start <span class="k">for </span>each node
<span class="o">[</span>    0.014548] Early memory node ranges
<span class="o">[</span>    0.014548]   node   0: <span class="o">[</span>mem 0x0000000000001000-0x000000000009efff]
<span class="o">[</span>    0.014550]   node   0: <span class="o">[</span>mem 0x0000000000100000-0x00000000bfeeffff]
<span class="o">[</span>    0.014553]   node   0: <span class="o">[</span>mem 0x00000000bff00000-0x00000000bfffffff]
<span class="o">[</span>    0.014554]   node   0: <span class="o">[</span>mem 0x0000000100000000-0x000000023fffffff]
<span class="o">[</span>    0.014558] Initmem setup node 0 <span class="o">[</span>mem 0x0000000000001000-0x000000023fffffff]
<span class="o">[</span>    0.014577] On node 0, zone DMA: 1 pages <span class="k">in </span>unavailable ranges
<span class="o">[</span>    0.014636] On node 0, zone DMA: 97 pages <span class="k">in </span>unavailable ranges
<span class="o">[</span>    0.027855] On node 0, zone DMA32: 16 pages <span class="k">in </span>unavailable ranges
...
<span class="o">[</span>    0.102320] Kernel/User page tables isolation: enabled
</code></pre></div></div>

<blockquote class="block-warning">
  <p><a href="https://en.wikipedia.org/wiki/Kernel_page-table_isolation">Kernel/User page tables isolation</a> 是用來避免 SPECTRE 之類的攻擊，讓 Kernel 有自己的 Page table，缺點是會降低 System call 的效能</p>
</blockquote>

<blockquote>
  <p>延伸閱讀: <a href="https://zhuanlan.zhihu.com/p/590013141">实模式启动阶段：从bios获取内存信息</a></p>
</blockquote>

<p><code class="language-plaintext highlighter-rouge">cat /proc/iomem</code> 可以看到 Physical address 的配置</p>

<ul>
  <li>與之前的 <code class="language-plaintext highlighter-rouge">dmesg</code> 顯示的內容相同</li>
  <li>並且 kernel 會配置自己的 code, rodata, data, bss section</li>
</ul>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>00001000-0009f7ff : System RAM      <span class="c"># 0.62 MB</span>
00100000-bfeeffff : System RAM      <span class="c"># 3069.94 MB</span>
bff00000-bfffffff : System RAM      <span class="c"># 1 MB</span>
100000000-23fffffff : System RAM    <span class="c"># 5120 MB</span>
8f400000-90201b41 : Kernel code
90400000-90cd5fff : Kernel rodata
90e00000-91046d3f : Kernel data
916fe000-927fffff : Kernel bss
</code></pre></div></div>

<hr />

<h3 id="dram-main-functions">DRAM Main Functions</h3>

<p><a href="#72-buffercache">7.2 Buffer/Cache</a><br />
<a href="#73-program-layout">7.3 Program Layout</a><br />
<a href="#74-memory-segmentation">7.4 Memory Segmentation</a><br />
<a href="#75-memory-paging">7.5 Memory Paging</a><br />
<a href="#76-fragmentation-problem">7.6 Fragmentation Problem</a></p>

<p>這裡先介紹 DRAM 的特性:</p>
<ul>
  <li>DRAM 的特性是只要 CPU 開始工作，DRAM 就會全速運轉
    <ul>
      <li>即使 DRAM 有多個插槽，OS 會把他們視為一體</li>
    </ul>
  </li>
  <li>DIMM (Dual In-line Memory Module)
    <ul>
      <li>通常插滿記憶體代表容量變大，頻寬變大，但 OS 無法獨立控制每個記憶體</li>
    </ul>
  </li>
  <li>NUMA (Non-Uniform Memory Access)
    <ul>
      <li>有多個 CPU 的情況下，每個 CPU 都有自己的 DRAM，但是 CPU 之間可以透過 QPI 互相存取</li>
      <li>這種情況下 OS 可以獨立控制每個 CPU 的 DRAM，例如: 雙路的 Server 的架構</li>
    </ul>
  </li>
  <li>記憶體無論如何都要耗電
    <ul>
      <li>DRAM 不管是讀取或寫入或儲存都需要耗電，所以 OS 會把 DRAM 全部用完</li>
      <li>如果某一個區段沒有真正的用途，那還是要耗電保存這部分 garbage</li>
    </ul>
  </li>
  <li><strong>記憶體的主要用途</strong>
    <ul>
      <li>執行程式的時候使用</li>
      <li>充當 Cache Memory 的角色，主要是當作 HDD, SSD 的 Cache</li>
      <li>充當 Buffer 的角色，再與周邊 Device 溝通時，需要保留一塊 Memory 讓裝置可以存取，例如: DMA</li>
    </ul>
  </li>
</ul>

<h5 id="72-buffercache">7.2 Buffer/Cache</h5>

<p>這裡使用 <code class="language-plaintext highlighter-rouge">top</code> 查看記憶體使用情況</p>
<ul>
  <li>OS 會盡力把記憶體用完，例如: 拿來當作 Cache, Buffer 讓 I/O 更快</li>
  <li>類似 memory cleaner 這樣的軟體，去釋放記憶體反而可能導致 I/O 變慢</li>
</ul>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>MiB Mem :   7939.9 total,   7495.9 free,    357.6 used,    314.9 buff/cache
MiB Swap:    953.0 total,    953.0 free,      0.0 used.   7582.3 avail Mem
</code></pre></div></div>

<h5 id="73-program-layout">7.3 Program Layout</h5>

<p>這裡說明 Program 在記憶體中的配置，以及他們的用途這部分就不多做介紹了，可以參考下圖。OS 在這裡的任務就是將執行檔從儲存裝置中載入到 Memory 中，配置好執行的環境。</p>

<p><img src="/image/2023/12-10-main_memory/3.png" alt="" height="75%" width="75%" /></p>

<blockquote>
  <p>如果想觀察 Kernel 的 code, data, bss section 可以使用 <code class="language-plaintext highlighter-rouge">cat /proc/kallsyms</code>，但是需要 root 權限</p>
</blockquote>

<h5 id="74-memory-segmentation">7.4 Memory Segmentation</h5>

<p>記憶體的區段機制(<a href="https://en.wikipedia.org/wiki/Memory_segmentation">Memory Segmentation</a>)是很直覺以程式的區段作為管理的單度，每個 seg. 都對應到程式的一個邏輯上的 seg.(text, data, bss, heap, stack)，
通常 seg. 是不可以被拆分，要拆分的話需要硬體支援，有固定的拆分大小。</p>

<blockquote>
  <p>例如: x86 的 ds. fs. gs. 三個 data 的 segmentation，其中 fs. gs. 是額外的 data seg.</p>
</blockquote>

<p><img src="/image/2023/12-10-main_memory/4.png" alt="" height="75%" width="75%" /></p>

<p>同一個程式的 seg. 可以在 Physical memory 中不連續，這樣可以更有效率的使用記憶體，但是這樣就有可能產生 Fragmentation Problem(碎片化問題)。</p>

<p><strong>Management of Segmentation</strong></p>

<p>既然是基於 Segmentation 的管理機制，那麼他所使用的定址方式就會是基於 Base + Offset 的方式(相對定址)。</p>
<ul>
  <li>例如這樣的指令: reg<sub>target</sub> = reg<sub>base</sub> + offset
    <ul>
      <li>這個 reg<sub>base</sub> 是 OS 能夠管理的</li>
    </ul>
  </li>
  <li>例如設計三個 Register 可以分別指向 code, data, stack 的開始位置，這樣就能分開存放這三個 seg.</li>
</ul>

<blockquote>
  <p>在這種機制下 Context switch 的時候除了切換 general register 以外，還需要切換 base reg. file</p>
</blockquote>

<p>這種 base reg. file 的設計同時還要引入 limit(長度)用來限制存取的範圍，如果存取錯誤的記憶體就會觸發 Segmentation Fault。
以 ARM Cortex-M 為例，<a href="https://www.keil.com/pack/doc/CMSIS/Core/html/group__mpu8__functions.html#gafe39c2f98058bcac7e7e0501e64e7a9d">MPU_RBAR</a> 是用來設定 base address，MPU_RASR 是用來設定 limit。</p>

<blockquote>
  <p>MPU: <a href="https://en.wikipedia.org/wiki/Memory_protection_unit">Memory Protection Unit</a></p>
</blockquote>

<blockquote class="block-warning">
  <p>Memory Segmentation 的機制並不太適合在動態的環境下，要新增程式或減少程式，容易造成碎片化問題，但在靜態的環境下，例如: 嵌入式系統，就很適合使用這種機制</p>
</blockquote>

<h5 id="75-memory-paging">7.5 Memory Paging</h5>

<blockquote class="block-tip">
  <p>Memory Paging 是目前主流的記憶體管理機制，相較於 Memory Segmentation，他的管理單位是 Page，而不是 Seg.</p>
</blockquote>

<ul>
  <li>Page 是固定大小的記憶體區塊，通常是 4KB</li>
  <li>OS 指定每個 Page 的用途，例如: code, data, stack, heap
    <ul>
      <li>不一定是連續分配的 Page</li>
    </ul>
  </li>
</ul>

<p><strong><a href="https://en.wikipedia.org/wiki/Page_table">Page Table</a></strong></p>

<p>為了確保程式在執行時不會忽鄉干擾，OS 要能很輕鬆的切換正在執行的程式，所以硬體上會支援 Mapping Table 的機制</p>

<ul>
  <li>假如系統最多有 48 個 Page，那麼這個 Table 就起碼要有 48 個 entry</li>
  <li>要能針對每個 Page 設定不同的權限，例如: stack(rw-), code(r–), data(rw-)
    <ul>
      <li>即使 stack, data 是相同權限，但是 OS 會給他們不同的功能，例如: stack 可以隨程式增加大小</li>
    </ul>
  </li>
</ul>

<blockquote>
  <p>下圖是一個程式從 CPU 使用 Virtual address 到 Physical address 的過程，其中 OS 會透過 Mapping Table 來做轉換，
實際上這樣的轉換是在 CPU 中完成的，是已經設計好的硬體線路</p>
</blockquote>

<p><img src="/image/2023/12-10-main_memory/5.png" alt="" height="100%" width="100%" /></p>

<p>可以看到表格中會有 Vir. address, Phy. address 的對應，與屬性權限等等，如果 Context switch 的時候，OS 只需要切換這個 Page Table 就可以了。</p>

<ul>
  <li>同時 OS 會知道這個 Page 是什麼類型，例如: stack 假如碰觸到邊界，OS 就會去增加 stack 的大小。</li>
  <li>在虛擬記憶體上，OS 會把這些資訊記錄在 Task Control Block(TCB) 中，使用 RBtree 來管理</li>
</ul>

<blockquote>
  <p>Linux Kernel 中使用 mm_struct 來描述 Task 的 Virtual Memory Layout</p>
</blockquote>

<ul>
  <li>相較於 Segmentation，Paging 的優點是:
    <ul>
      <li>在記憶體分配上 Paging 比較有彈性，只要有足夠的 Page 就可以</li>
      <li>在 Hardware 上並沒有 Segmentation 的概念，是由 OS 來實現的</li>
      <li>每一個 Page 的大小是固定的，分配演算法會比較好實現</li>
    </ul>
  </li>
  <li>缺點:
    <ul>
      <li>Page 的數量遠超過 Segmentation，所以 Page Table 會很大，可能會造成硬體 Mapping 上的效能負擔</li>
      <li>如果使用 Assembly 寫程式，Paging 的程式碼會比較難寫，難懂</li>
    </ul>
  </li>
</ul>

<h5 id="76-fragmentation-problem">7.6 Fragmentation Problem</h5>

<blockquote class="block-tip">
  <p>只要會造成 Memory 動態分配的情況，就必然會有 Fragmentation Problem，這是一個無法避免的問題</p>
</blockquote>

<ul>
  <li><strong><a href="外部碎片">External Fragmentation</a></strong>: 如果一塊記憶體中的 Free space 足夠，但無法滿足程式的需求，這些 Free space 就是 External Fragmentation</li>
  <li><strong><a href="內部碎片">Internal Fragmentation</a></strong>: 因為配置演算法，本來只需要 X size 的記憶體，但是分配了 X + Y size 的記憶體，其中 Y size 就是 Internal Fragmentation
    <ul>
      <li>例如: OS 分配空間時覺得剩下的空間太小了，乾脆把整個空間都分配給程式，那就是 Internal Fragmentation</li>
    </ul>
  </li>
</ul>

<p>這裡有一些解決 External Fragmentation 的方法，例如:</p>
<ul>
  <li>Memory Compaction(記憶體壓縮): 搬移記憶體使 Free space 變成連續的，但是成本很高</li>
  <li>更好的分配演算法: Best Fit, Worst Fit, First Fit</li>
</ul>

<p><strong>Fragmentation in Paging/Segmentation</strong></p>

<ul>
  <li>在 Pagging 的情況下，由於每個 Page 的大小一致，並且透過 Mapping 的方式使用 Page，所以不會有 External Fragmentation
    <ul>
      <li>但因為程式所需要的記憶體大小不會剛好是 Page 的大小，所以會有 Internal Fragmentation</li>
    </ul>
  </li>
</ul>

<hr />

<h3 id="memory-paging-hardware">Memory Paging Hardware</h3>

<p><a href="#77-mmu-and-mpu">7.7 MMU and MPU</a><br />
<a href="#78-tlb">7.8 TLB</a><br />
<a href="#79-tlb-miss">7.9 TLB Miss</a><br />
<a href="#710-translation-table-structure">7.10 Translation table structure</a><br />
<a href="#711-hardware-handling-of-tlb-miss">7.11 Hardware Handling of TLB Miss</a><br />
<a href="#712-software-handling-of-tlb-miss">7.12 Software Handling of TLB Miss</a></p>

<blockquote class="block-tip">
  <p>這裡會介紹 Mapping Table 的基本形式，即是 TLB(Translation Lookaside Buffer)，如果 TLB 不夠大要怎麼處理</p>
</blockquote>

<h5 id="77-mmu-and-mpu">7.7 MMU and MPU</h5>

<ul>
  <li>MPU(Memory protection unit): 可以設定某段記憶體的權限，例如: code(r–), data(rw-)</li>
  <li>MMU(Memory management unit): 除了 MPU 的功能以外，還可以透過 MMU 使 OS 更有效的分配記憶體的使用</li>
</ul>

<p><strong>MMU management unit</strong></p>

<ul>
  <li>以 Segment 為最小管理單位
    <ul>
      <li>較早期的電腦會以 Segment 為最小管理單位</li>
      <li>在 Protect 上會比較直觀</li>
      <li>因為 Segment 的特性，所以對於記憶體管理的幫助不大</li>
    </ul>
  </li>
  <li>以 Page 為最小管理單位
    <ul>
      <li>在 Protect 上會比較複雜，因為每個程式都會有很多 Page</li>
      <li>但在管理上因為 Page 固定大小，因此可以更有效率的管理記憶體</li>
      <li>因為 Page 很多，所以需要很多的 Mapping entries，例如: 4GB 的記憶體就要 1M 個 entries(4GB / 4KB)，這會成為硬體的負擔</li>
    </ul>
  </li>
</ul>

<p><strong>MMU Architecture</strong></p>

<p>實際上 MMU 會建立在 CPU 內部，下面是一個簡單的架構表示 CPU 中 MMU, Address decoder, DRAM 之間的關係:</p>

<p><img src="/image/2023/12-10-main_memory/6.png" alt="" height="100%" width="100%" /></p>

<ul>
  <li>L1 Cache 與 MMU 誰在前誰在後，取決於 CPU 的設計
    <ul>
      <li>要注意的是 L1 Cache 如果在 MMU 前面 Context switch 的時候，就要把 L1 Cache 清空</li>
    </ul>
  </li>
</ul>

<p><strong>MMU Function</strong></p>

<blockquote>
  <p>這裡介紹下 MMU 應該會是什麼樣子，這下面舉的是抽象簡化的例子</p>
</blockquote>

<p>假如有以下編碼 reg<sub>target</sub> = reg<sub>base</sub> + offset</p>
<ul>
  <li>Segment 機制下:
    <ul>
      <li>reg<sub>base</sub> 可能是 base<sub>code</sub> 這樣的管理方式，代表是 code seg. + offset</li>
    </ul>
  </li>
  <li>Paging 機制下:
    <ul>
      <li>reg<sub>base</sub> 代表的是 Page Number，所以是 Page Number + offset</li>
      <li>例如: 一個 32 bit 的系統，Page number 會是 20 bit，offset 會是 12 bit，因此 offset 最多只能是 4KB，因此不需要 limit 的設計</li>
    </ul>
  </li>
</ul>

<h5 id="78-tlb">7.8 TLB</h5>

<p>其實 TLB(<a href="https://en.wikipedia.org/wiki/Translation_lookaside_buffer">Translation Lookaside Buffer</a>) 就是一個在 CPU 中的高速 Buffer(SRAM)，把 MMU 的 Page Table 存放在 TLB 中，利用這種方式可以加速地址轉換的速度。</p>

<p><img src="/image/2023/12-10-main_memory/7.png" alt="" height="100%" width="100%" /></p>

<ul>
  <li>p(page number): 程式在 Logical address 中的 Page number</li>
  <li>f(frame number): 實際在 Physical address 中的 Page number</li>
  <li>d(displacement, offset): 在 Page 中的 offset</li>
</ul>

<p>這裡來看一個實際的例子 <a href="https://en.wikipedia.org/wiki/Sandy_Bridge">Intel Sandy Bridge</a> 的 TLB 規格:</p>

<ul>
  <li>Level 1 TLB
    <ul>
      <li>i-TLB: 72 entries, i means instruction</li>
      <li>d-TLB: 100 entries, d means data</li>
    </ul>
  </li>
  <li>Level 2 TLB (如果在 L1 TLB 中沒有找到，就會到 L2 TLB 中查找)
    <ul>
      <li>1024 entries</li>
    </ul>
  </li>
</ul>

<p><strong>TLB Size</strong></p>

<p>那麼一個 Page 的大小是 4KB，4KB * 1024 = 4MB，但是一個程式不可能只有 4MB 大小，這裡再看另一個例子，MIPS R8000-style TLB，一共有 384 entries，
這樣也才 384 * 4KB = 1536KB，都是遠遠小於實際程式需求的。</p>

<p>解決方法:</p>
<ul>
  <li>Hardware: 增加 TLB 的數量，但是這樣會增加硬體成本(CPU 變大，成本變高，搜尋時間變長)</li>
  <li>Dynamic: 增加動態載入 TLB 的方式</li>
</ul>

<blockquote>
  <p>要動態載入 TLB 就要思考這是怎樣的資料結構，同時要硬體支援</p>
</blockquote>

<h5 id="79-tlb-miss">7.9 TLB Miss</h5>

<blockquote>
  <p>TLB 有大小的限制，因此會發生 TLB Miss，當 Miss 發生時就會需要動態載入 TLB</p>
</blockquote>

<p>增加 TLB 的大小:</p>
<ul>
  <li>TLB 基本上是一個平行搜尋的硬體
    <ul>
      <li>Page number 會同時與 TLB 中的數百到數千個 entries 做比對</li>
    </ul>
  </li>
  <li>擴充 TLB 會增加硬體成本，例如: 電晶體數量，時脈降低，耗電量增加</li>
</ul>

<blockquote>
  <p>既然這樣的話增加 Page size 也是一種方法，但是就會降低軟體在管理上的彈性，例如: huge page</p>
</blockquote>

<p><strong>Dynamic Loading TLB entries</strong></p>

<p>當 MMU 發現某一個 Page number 在 TLB 找不到對應的 entries 時，觸發 TLB Miss 將當下要使用的 TLB 載入，
Main Memory 足夠放入所有的 TLB entries，但是這部分應該要由 Hardware 來實現還是 Software 來實現?</p>

<h5 id="710-translation-table-structure">7.10 Translation table structure</h5>

<blockquote class="block-tip">
  <p>這裡介紹如何把 TLB 的 entries 放到 Main Memory 中，使用什麼樣的 Data structure 來管理</p>
</blockquote>

<p>假如我們要把 TLB 的 entries 放到 Main Memory 中，那假設以下情況:</p>
<ol>
  <li>500MB 的程式，需要 500KB 的記憶體空間放 Page Table</li>
  <li>700MB 的程式，需要 700KB 的記憶體空間放 Page Table</li>
  <li>1500MB 的程式，需要 1500KB 的記憶體空間放 Page Table</li>
</ol>

<p>以上這些程式一樣會動態載入記憶體，這樣的話久而久之也會造成 External Fragmentation，這樣就反其道而行了，Paging 本來是用來解決 Fragmentation 問題的，
但是為了這些大小不一的 Page Table，反而造成了 Fragmentation 問題。</p>

<p><strong>Hierarchical Paging</strong></p>

<p>這裡就使用一種 Hierarchical Paging(階層式分頁)的方式來解決這個問題，一個 4KB 大小的 Page 可以管理 1024 entries，第二層也是以 4KB 還分層管理，
這樣兩層就能放入 1024 * 1024 = 1M 個 entries，這樣就能解決上面的問題。</p>

<p><img src="/image/2023/12-10-main_memory/8.png" alt="" height="100%" width="100%" /></p>

<p>這樣的話定址的格式就會改成以下的格式:</p>

<p><img src="/image/2023/12-10-main_memory/9.png" alt="" height="100%" width="100%" /></p>

<ul>
  <li>LV1 PTE(Level 1 Page Table Entry): 第一層的 Page Table Index</li>
  <li>LV2 PTE(Level 2 Page Table Entry): 第二層的 Page Table Index</li>
  <li>d(displacement, offset): 在 Page 中的 offset</li>
</ul>

<p>注意到這樣的指令格式剛好是 2<sup>10</sup> x 2<sup>10</sup> x 2<sup>12</sup> = 2<sup>32</sup>，也就是 32-bit 的 CPU 可以使用這樣的指令格式。</p>

<blockquote>
  <p>在這裡 LV1, LV2 在沒有映射出去的時候，會設定為 Invalid bit</p>
</blockquote>

<blockquote>
  <p>延伸閱讀: Andrew S. Tanenbaum, Modern Operating Systems, Chapter 3, 3.3.2 Page Tables 有更多種類的 Page Table 結構可以參, <a href="https://pages.cs.wisc.edu/~solomon/cs537-old/last/paging.html">Marvin Solomon CS 537</a></p>
</blockquote>

<h5 id="711-hardware-handling-of-tlb-miss">7.11 Hardware Handling of TLB Miss</h5>

<p>目前大部分的 CPU 都是用硬體來處理 TLB Miss，硬體會透過之前提過的 Hierarchical Paging 的指令方式來查找 Page Table，
在 Logical address 中會以逗號隔開的方式記錄: <code class="language-plaintext highlighter-rouge">0xpte1, 0xpte2, 0xoffset</code></p>

<p>下圖是一個簡單的架構表示如何處理 TLB Miss:</p>

<p><img src="/image/2023/12-10-main_memory/10.png" alt="" height="100%" width="100%" /></p>

<ol>
  <li>首先 CPU 會先去 TLB Search，TLB 中沒有的話就會觸發 TLB Miss</li>
  <li>PTBR 會記錄 LV1 Page Table 的 Physical address，以供 CPU 找到 LV1 Page Table
    <ul>
      <li>PTBR(Page Table Base Register): 在 Intel x86 中是 CR3</li>
    </ul>
  </li>
  <li>找到第二層的 LV2 Page Table，然後在 LV2 Page Table 中找到對應的 Frame number</li>
  <li>將一個 TLB Entry 置換成剛剛找到的 Frame number</li>
  <li>重新啟動 TLB Search，這次就會找到對應的 TLB Entry</li>
</ol>

<h5 id="712-software-handling-of-tlb-miss">7.12 Software Handling of TLB Miss</h5>

<p>MIPS 處理器使用了 software-managed TLB，這種方式會比較慢，當 TLB miss 時將會觸發 exception，然後 OS Kernel 會去進行後續處理。</p>

<p>這裡列出三類型的 TLB exception:</p>
<ol>
  <li>TLB Refill: TLB 中沒有相對應的 entry 時就會發生</li>
  <li>TLB Invalid: Virtual address 使用被設定為 Invalid 的 entry 時就會發生</li>
  <li>TLB Modified: TLB 有對應的 entry，但是該 entry 的權限不符合(No dirty bit)時就會發生</li>
</ol>

<blockquote class="block-warning">
  <p>MIPS 會告知 OS TLB Miss 的原因與位置，並根據 CPU 正在執行哪一個 task 來使用適當的演算法找出對應的 entry</p>
</blockquote>

<p>要注意軟體處理 TLB Miss 有兩種可能:</p>
<ul>
  <li>Real Error: 也就是程式碼出錯，例如: 存取了不該存取的記憶體</li>
  <li>TLB Miss: 也就是程式碼沒有出錯，只是 TLB 中沒有對應的 entry</li>
</ul>

<p>OS Kernel 在這裡可以像硬體一樣去使用 <strong>Paged Page Table</strong>(分頁分頁表)，或使用 <strong>Hash Table</strong> 來管理 TLB entries。
並且在軟體上會清楚正在執行哪些 Task，所以可以提前把共用的 TLB entries 載入到 TLB 中，這樣就可以減少 TLB Miss 的發生。</p>

<ul>
  <li><strong>Advantages:</strong>
    <ul>
      <li>透過軟體的話，可以更有效的搜尋，甚至修改搜尋演算法</li>
      <li>可以提前載入共用的 TLB entries，減少 TLB Miss 的發生</li>
    </ul>
  </li>
  <li><strong>Disadvantages:</strong>
    <ul>
      <li>要去執行 Exception 會牽涉到 Mode change，會有額外的成本</li>
    </ul>
  </li>
</ul>

<blockquote>
  <p>部分 MIPS 也有硬體機制來走訪 Page Table，部分 SPARC、POWER 處理器允許軟體來處理 TLB Miss</p>
</blockquote>

<blockquote class="block-warning">
  <p>填寫 TLB 的功能必須由 Kernel 來做，由 User space 來做的話會有安全性的問題</p>
</blockquote>

<hr />

<h3 id="fragmentation">Fragmentation</h3>

<p><a href="#713-os-memory-allocation-architecture">7.13 OS memory allocation architecture</a><br />
<a href="#714-kernel-management-of-frames">7.14 Kernel Management of Frames</a><br />
<a href="#715-slab-allocation">7.15 Slab Allocation</a><br />
<a href="#716-malloc">7.16 Malloc</a></p>

<p>Linux Kernel 幾乎不會修改與 Kernel space 相關的 Page Table，因此 Kernel 會盡可能地去使用大的 Page，例如: 1GB，
來降低對於 TLB entries 的使用，因為在 User space 中 Kernel 的部分是共用的，修改 Kernel 的 Page Table 可能會造成額外的 TLB Miss。</p>

<p>User space 的部分就會使用 4KB 的 Page，這裡要討論的是 malloc 怎麼分配記憶體給 Task 使用，malloc 不會每次都剛好等於 4KB 的倍數，
硬體上可以解決的是 4KB Page 為單位的 Fragmentation 問題，但是程式自由配置的大小問題還是需要由軟體來解決。</p>

<h5 id="713-os-memory-allocation-architecture">7.13 OS memory allocation architecture</h5>

<blockquote class="block-tip">
  <p>下圖展示一個作業系統的記憶體分配架構，怎麼初始化到分配記憶體給 Task 使用</p>
</blockquote>

<p><img src="/image/2023/12-10-main_memory/11.png" alt="" height="100%" width="100%" /></p>

<p>在這個架構之下這裡要討論的是:</p>
<ol>
  <li>Kernel 如何管理分配 Frame</li>
  <li>Kernel lib 會提供 <code class="language-plaintext highlighter-rouge">kmalloc()</code> 來提供給 Kernel 的 Task 使用</li>
  <li><code class="language-plaintext highlighter-rouge">libc</code> 如何實現 <code class="language-plaintext highlighter-rouge">malloc()</code> 來提供給 User space 的 Task 使用
    <ul>
      <li>這裡雖然記憶體最小的管理單位是 4KB，但是 libc 透過 syscall 來向 Kernel 一次要求多個 page
        <ul>
          <li>例如: sbrk(), brk(), mmap()</li>
        </ul>
      </li>
      <li>而 malloc() 會在這些 page 中分配記憶體給 Task 使用，以此做到更細微的記憶體分配</li>
    </ul>
  </li>
</ol>

<h5 id="714-kernel-management-of-frames">7.14 Kernel Management of Frames</h5>

<p>Kernel 透過 MMU 的機制，可以把記憶體都視為 4KB 大小的 Page(Frame) 來管理</p>

<ul>
  <li>Kernel 將所有的 DRAM 都 Mapping 到 Kernel space
    <ul>
      <li>這代表 Kernel 可以直接存取所有的 DRAM，即使已經分配給 User Task 使用</li>
      <li>因為記憶體幾乎是完全交由 Kernel 來管理，所以 Kernel 內部必須小心地處理記憶體</li>
    </ul>
  </li>
  <li>因此開機系統啟動時會去探測記憶體大小和狀況，這裡會使用 <a href="https://www.kernel.org/doc/gorman/html/understand/understand022.html">bootmem</a>
    <ul>
      <li>bitmap 只會在最初使用，因為在怎麼優化效率都不高</li>
    </ul>
  </li>
</ul>

<blockquote class="block-warning">
  <p>延伸閱讀: <a href="https://hackmd.io/@sysprog/linux-memory">Linux 核心設計: 記憶體管理</a></p>
</blockquote>

<p><strong>Buddy system</strong></p>

<blockquote class="block-tip">
  <p><a href="https://en.wikipedia.org/wiki/Buddy_memory_allocation">buddy system</a> 的目的是更快的搜尋到可以分配的記憶體，並且盡量保持記憶體的連續性</p>
</blockquote>

<p>假設在分配時，kernel 已經知道有一塊 2M 的連續記憶體可以分配，並且 task 也可以用掉這 2M 的記憶體就可以直接分配這 2M 給 task 使用，
同時保持記憶體的連續性，可以使 MMU 做更有效率的優化。</p>

<p><img src="/image/2023/12-10-main_memory/12.png" alt="" height="100%" width="100%" /></p>

<p>在上面這個例子中，我們假設 kernel 已經知道有 32 個 frame 可以分配，藍色的部分代表已經被使用的 frame:</p>
<ul>
  <li>合併的條件是來自相同的 parent
    <ul>
      <li>橘色與黃色的 frame 來自同一個 parent A，所以可以合併</li>
      <li>黃色的 parent 是 A，棕色的 parent 是 B，所以不能合併</li>
      <li>只有橘色與黃色合併後，parent 變為 B，才能與棕色合併</li>
    </ul>
  </li>
  <li>如果某 Kernel task 需要 2 個 page，那麼就優先把 2 個 page 的 leaf 分配給他
    <ul>
      <li>同理如果需要 4 個 page，就優先把 4 個 page 的 leaf 分配給他</li>
    </ul>
  </li>
</ul>

<blockquote>
  <p>在 Linux kernel 中實現 buddy system 的方式是透過 bitmap 來實現樹，而不是真的使用一個樹的資料結構</p>
</blockquote>

<h5 id="715-slab-allocation">7.15 Slab Allocation</h5>

<blockquote class="block-tip">
  <p>Linux kernel 中有許多頻繁使用的資料結構，例如: task_struct, file, inode 等等，如果直接使用 buddy system 來分配記憶體，
會很容易造成 Fragmentation，所以使用 slab 來解決這個問題</p>
</blockquote>

<p>雖然 buddy system 可以每次以 page(4KB) 為單位分配記憶體了，但是系統執行時絕大部分的資料結構都是小的，因此 Linux 使用 slab 來解決小物件的分配。</p>

<ul>
  <li>slab 會去向 buddy system 要數個 page，在這裡會被稱為 cache(kmem_cache, 並不是指 CPU cache)
    <ul>
      <li>slab 描述 kmem_cache 的定義在 <code class="language-plaintext highlighter-rouge">mm/slab.h</code></li>
      <li><code class="language-plaintext highlighter-rouge">cat /proc/slabinfo</code> 可以看到目前系統中有哪些 cache</li>
    </ul>
  </li>
  <li>slab allocator 是由很多個不同大小的 cache 組成，這些 cache 通常是為了分配特定 object 的大小
    <ul>
      <li>例如: 在 Linux kernel 中 task_struct 大約只需要 1.7KB 就可以使用一個 cache 來服務這個 object</li>
    </ul>
  </li>
</ul>

<p><img src="/image/2023/12-10-main_memory/13.png" alt="" height="100%" width="100%" /></p>

<ul>
  <li>cache 中使用三種 list 來管理: full, partial, free</li>
</ul>

<p>因此 slab 會達到以下的目的:</p>
<ol>
  <li>可以比 buddy system 分配更小的記憶體</li>
  <li>cache 常用的 object 可以被重複使用，不需要重新分配記憶體(allocating, initialising, destroying)</li>
  <li>把這些常用 object 與 L1 或 L2 cache 對齊，可以更好的利用 hardware cache</li>
</ol>

<p>這裡可以想像 slab 就是把常用的 object 當成牛奶瓶，cache 就是牛奶箱，牛奶瓶可以重複去牛奶箱中拿取放回。</p>

<blockquote>
  <p>延伸閱讀: <a href="https://www.kernel.org/doc/gorman/html/understand/understand011.html">Chapter 8  Slab Allocator</a>, <a href="https://zhuanlan.zhihu.com/p/358891862">内存管理 slab 分配器</a></p>
</blockquote>

<h5 id="716-malloc">7.16 Malloc</h5>

<p>通常程式都會有 heap，而 malloc 幾乎都是從 heap 中分配閒置的記憶體給程式使用。</p>

<ul>
  <li>heap 是連續配置的記憶體空間，大小為 page 的倍數</li>
  <li>malloc 這裡會使用一些演算法來從 heap 分配記憶體給程式使用
    <ul>
      <li>例如: <a href="https://en.wikipedia.org/wiki/C_dynamic_memory_allocation#dlmalloc_and_ptmalloc">ptmalloc</a></li>
    </ul>
  </li>
</ul>

<blockquote>
  <p>延伸閱讀: <a href="https://goo.gl/ALHVoh">Glibc 内存管理</a>, <a href="https://sourceware.org/glibc/wiki/MallocInternals">Overview of Malloc</a></p>
</blockquote>

<blockquote>
  <p>malloc 實現這邊打算之後另外寫一篇文章，這邊只要知道 task 使用 malloc 也不是直接向 kernel 索要 page 就可以了</p>
</blockquote>

<blockquote class="block-warning">
  <h5 id="last-edit">Last Edit</h5>
  <p>1-26-2024 19:15</p>
</blockquote>]]></content><author><name>Benson Hsu</name></author><category term="Jekyll" /><category term="OS" /><summary type="html"><![CDATA[Operating System: Design and Implementation course notes from CCU, lecturer Shiwu-Lo.]]></summary></entry></feed>