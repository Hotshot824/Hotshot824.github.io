<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://hotshot824.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://hotshot824.github.io/" rel="alternate" type="text/html" /><updated>2026-09-07T15:39:29+00:00</updated><id>https://hotshot824.github.io/feed.xml</id><title type="html">Home</title><subtitle>Build Jekyll site with the GitBook style.
</subtitle><author><name>Benson Hsu</name></author><entry><title type="html">Note | Exception Handling Best Practices</title><link href="https://hotshot824.github.io/jekyll/2026-09-03-Exception_Practices.html" rel="alternate" type="text/html" title="Note | Exception Handling Best Practices" /><published>2026-09-03T00:00:00+00:00</published><updated>2026-09-03T00:00:00+00:00</updated><id>https://hotshot824.github.io/jekyll/Exception_Practices</id><content type="html" xml:base="https://hotshot824.github.io/jekyll/2026-09-03-Exception_Practices.html"><![CDATA[<blockquote class="block-tip">
  <p>在設計例外處理機制時，我們應該將其劃分為兩個獨立的維度：<strong>Exception Message</strong>（拋出時附加的 context）與 <strong>try-catch</strong>（攔截異常的 interception point）。
一個好的 Exception Message 告訴你 <em>what</em> failed；而一個好的 catch 則告訴你 <em>why</em> you’re allowed to handle it。</p>
</blockquote>

<blockquote class="block-warning">
  <p>這篇文章主要探討何時應該為例外加入訊息，以及何時與何地應該撰寫 try-catch 區塊。文章內容會使用 Java 程式碼作為範例，
但核心概念在大多數語言中都是通用的。</p>
</blockquote>

<h3 id="1-overview">1. Overview</h3>

<blockquote class="block-tip">
  <p>這兩個維度在設計上是正交（orthogonal）的：Message（at throw time）× Catch（at handle time）。拋出例外的當下與處理例外的階段應該分開思考。</p>
</blockquote>

<p>在實際開發中，我們常常看到將這兩者混為一談的糟糕設計。</p>

<blockquote class="block-danger">
  <p>最常見的錯誤就是隨意拋出 <code class="language-plaintext highlighter-rouge">throw new Exception("error")</code>，然後在不知情的外層盲目地 <code class="language-plaintext highlighter-rouge">catch (Exception e)</code>。這樣不僅讓系統丟失了真正的錯誤細節，還可能掩蓋了致命的問題。</p>
</blockquote>

<p>為了更好地理解，我們可以根據系統的內部邏輯（Internal）與邊界（Boundary），來判斷是否需要附加訊息與是否需要捕捉。簡單一句話：你需要根據當下所處的位置來決定例外的處理策略。以下是一個 2×2 的預覽表格：</p>

<table>
  <thead>
    <tr>
      <th style="text-align: left">Scope</th>
      <th style="text-align: left">Needs Message?</th>
      <th style="text-align: left">Needs Catch?</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: left"><strong>Internal</strong></td>
      <td style="text-align: left"><strong>Sometimes.</strong> 取決於 Exception Type 的命名是否已經足夠明確。</td>
      <td style="text-align: left"><strong>No.</strong> 除非你能就地修復它，否則應該讓它往上拋。</td>
    </tr>
    <tr>
      <td style="text-align: left"><strong>Boundary</strong></td>
      <td style="text-align: left"><strong>Yes.</strong> 必須包含足夠的 context 讓呼叫端能理解並回應。</td>
      <td style="text-align: left"><strong>Yes.</strong> 必須捕捉所有例外，並轉換為標準的回應格式。</td>
    </tr>
  </tbody>
</table>

<hr />

<h3 id="2-when-to-add-an-exception-message">2. When to Add an Exception Message</h3>

<blockquote class="block-tip">
  <p><strong>核心原則</strong>：Exception Message 的目標不是「記錄錯誤」，而是「提供上下文」。好的 Message 應該要讓維護者在看到 Log 的當下，就能準確判斷發生了什麼，而不需要重新執行或 Debug。</p>
</blockquote>

<h4 id="21-什麼是好的-message--3w-原則">2.1 什麼是好的 Message — 3W 原則</h4>

<p>要寫出有意義的 Message，建議遵循 3W 原則：</p>

<ul>
  <li><strong>What</strong>: 發生了什麼具體錯誤？</li>
  <li><strong>Where</strong>: 發生在哪個特定的 Context 下？</li>
  <li><strong>Why</strong>: 為什麼這會導致問題？（如果不是顯而易見的話）</li>
  <li><strong>Actionable info</strong>: 提供導致錯誤的 Offending value 或關鍵參數。</li>
</ul>

<p>讓我們比較一下兩者的差異：</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Bad: too vague, tells us nothing</span>
<span class="k">throw</span> <span class="k">new</span> <span class="nf">IllegalArgumentException</span><span class="o">(</span><span class="s">"Invalid ID"</span><span class="o">);</span>

<span class="c1">// Good: includes What (ID format error), Where (User update),</span>
<span class="c1">//       Why (too short) and Offending Value</span>
<span class="k">throw</span> <span class="k">new</span> <span class="nf">IllegalArgumentException</span><span class="o">(</span>
    <span class="nc">String</span><span class="o">.</span><span class="na">format</span><span class="o">(</span><span class="s">"Failed to update user: ID '%s' is too short. Expected at least 8 chars."</span><span class="o">,</span> <span class="n">userId</span><span class="o">)</span>
<span class="o">);</span>
</code></pre></div></div>

<p>簡單來說，當你寫 Message 時，試著想像你是在給「未來的自己」留言。如果他在深夜收到這條 Error Log，他會感謝你提供了這些關鍵資訊，而不是罵你為什麼只留下一句 meaningless 的廢話。</p>

<h4 id="22-應該加-message-的情境">2.2 應該加 Message 的情境</h4>

<p>並非所有 Exception 都需要冗長的解釋，但在以下場景中，Message 是不可或缺的：</p>

<ul>
  <li><strong>Boundary crossing (Public API / Library)</strong>: 給外部使用者看的錯誤，必須解釋為什麼他們的請求被拒絕。</li>
  <li><strong>Async / Logging scenarios</strong>: 當 Exception 會被丟到外部系統（如 ELK, Sentry）時，因為你無法直接 Debug 記憶體，Message 就是唯一的診斷資料。</li>
  <li><strong>Validation failures</strong>: 必須明確指出導致驗證失敗的那個值。</li>
</ul>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Boundary crossing: clear error message for API callers</span>
<span class="k">if</span> <span class="o">(</span><span class="n">input</span> <span class="o">==</span> <span class="kc">null</span><span class="o">)</span> <span class="o">{</span>
    <span class="k">throw</span> <span class="k">new</span> <span class="nf">IllegalArgumentException</span><span class="o">(</span>
        <span class="s">"Request payload cannot be null at OrderService.createOrder()"</span><span class="o">);</span>
<span class="o">}</span>

<span class="c1">// Async scenario: no immediate Stack trace available, must carry its own context</span>
<span class="nc">CompletableFuture</span><span class="o">.</span><span class="na">runAsync</span><span class="o">(()</span> <span class="o">-&gt;</span> <span class="o">{</span>
    <span class="k">if</span> <span class="o">(</span><span class="n">dbConnection</span> <span class="o">==</span> <span class="kc">null</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">throw</span> <span class="k">new</span> <span class="nf">IllegalStateException</span><span class="o">(</span>
            <span class="s">"Database connection lost during background batch processing"</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">});</span>

<span class="c1">// Validation failure: include the offending value</span>
<span class="k">if</span> <span class="o">(</span><span class="n">email</span> <span class="o">!=</span> <span class="kc">null</span> <span class="o">&amp;&amp;</span> <span class="o">!</span><span class="n">email</span><span class="o">.</span><span class="na">contains</span><span class="o">(</span><span class="s">"@"</span><span class="o">))</span> <span class="o">{</span>
    <span class="k">throw</span> <span class="k">new</span> <span class="nf">ValidationException</span><span class="o">(</span>
        <span class="nc">String</span><span class="o">.</span><span class="na">format</span><span class="o">(</span><span class="s">"Invalid email format: '%s' does not contain '@'"</span><span class="o">,</span> <span class="n">email</span><span class="o">));</span>
<span class="o">}</span>
</code></pre></div></div>

<h4 id="23-不需要冗長-message-的情境">2.3 不需要冗長 Message 的情境</h4>

<p>有時候，Less is more。在某些情況下，過度描述反而是一種負擔：</p>

<ul>
  <li><strong>Internal private helpers</strong>: 這些方法的作用域很小，直接丟出 Exception 通常就夠了。</li>
  <li><strong>Well-named Custom Exception Types</strong>: 如果你的 Exception 名稱已經是 <code class="language-plaintext highlighter-rouge">UserAlreadyExistsException</code>，那麼 Message 寫 “User already exists” 就只是重複資訊。</li>
  <li><strong>Performance-sensitive Hot Paths</strong>: 頻繁觸發 Exception 時，String concatenation 的效能開銷不容小覷。</li>
</ul>

<blockquote class="block-warning">
  <p><strong>注意權衡</strong>：在極度要求效能的 Hot Paths 中，如果 Exception 經常被作為控制流程（Control flow）的一部分，建議使用 <code class="language-plaintext highlighter-rouge">static final</code> 的字串作為 Message，以避免重複的字串建立開銷。但這不代表可以完全省略有意義的 Message。</p>
</blockquote>

<h4 id="24-anti-patterns">2.4 Anti-patterns</h4>

<p>有些寫法不但沒幫上忙，反而是在干擾除錯：</p>

<ul>
  <li><strong>模糊語意</strong>: 像是 “Error occurred” 或 “Something went wrong”，這等於什麼都沒說。</li>
  <li><strong>洩漏敏感資訊</strong>: 錯誤訊息中帶有 SQL 查詢字串、密碼或用戶機敏資料（PII）。</li>
  <li><strong>重複 Stack Trace</strong>: 把錯誤的路徑寫進 Message 裡，這完全是多此一舉，因為 Log 系統已經記錄了 Stack Trace。</li>
</ul>

<blockquote class="block-danger">
  <p><strong>千萬別這樣做</strong>：不要在訊息中包含環境敏感資訊，或寫下毫無意義的廢話。</p>
</blockquote>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Bad: leaking internals and meaningless info</span>
<span class="k">try</span> <span class="o">{</span>
    <span class="c1">// ...</span>
<span class="o">}</span> <span class="k">catch</span> <span class="o">(</span><span class="nc">Exception</span> <span class="n">e</span><span class="o">)</span> <span class="o">{</span>
    <span class="k">throw</span> <span class="k">new</span> <span class="nf">RuntimeException</span><span class="o">(</span>
        <span class="s">"Something went wrong at com.myapp.internal.UserDAO.findById(): "</span>
        <span class="o">+</span> <span class="s">"SELECT * FROM users WHERE password = '"</span> <span class="o">+</span> <span class="n">password</span> <span class="o">+</span> <span class="s">"'"</span><span class="o">,</span> <span class="n">e</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div>

<p>這樣的寫法除了讓 Log 變得更亂，還洩漏了資料庫查詢與密碼，對於解決問題完全沒有幫助。</p>

<hr />

<h3 id="3-when-to-write-try-catch">3. When to Write try-catch</h3>

<blockquote class="block-tip">
  <p>簡單一句話：<strong>只攔截你能處理的例外，並且要在正確的系統分層去攔截</strong>。如果當下的方法無法修復這個錯誤，那就應該讓它繼續往上拋。</p>
</blockquote>

<h4 id="31-只攔截你能處理的">3.1 只攔截你能處理的</h4>

<p>在撰寫 <code class="language-plaintext highlighter-rouge">try-catch</code> 時，最常見的壞習慣就是為了編譯通過而隨便接住所有東西。我們應該優先攔截特定型別的 Exception，而不是一網打盡。</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Good: Catch specific exceptions</span>
<span class="k">try</span> <span class="o">{</span>
    <span class="n">processPayment</span><span class="o">(</span><span class="n">orderId</span><span class="o">);</span>
<span class="o">}</span> <span class="k">catch</span> <span class="o">(</span><span class="nc">PaymentNetworkException</span> <span class="n">e</span><span class="o">)</span> <span class="o">{</span>
    <span class="c1">// Retry or fallback</span>
    <span class="n">handleNetworkFailure</span><span class="o">(</span><span class="n">orderId</span><span class="o">);</span>
<span class="o">}</span> <span class="k">catch</span> <span class="o">(</span><span class="nc">InsufficientFundsException</span> <span class="n">e</span><span class="o">)</span> <span class="o">{</span>
    <span class="c1">// Alert the user</span>
    <span class="n">notifyUserInsufficientFunds</span><span class="o">(</span><span class="n">orderId</span><span class="o">);</span>
<span class="o">}</span>

<span class="c1">// Bad: Catch generic Exception</span>
<span class="k">try</span> <span class="o">{</span>
    <span class="n">processPayment</span><span class="o">(</span><span class="n">orderId</span><span class="o">);</span>
<span class="o">}</span> <span class="k">catch</span> <span class="o">(</span><span class="nc">Exception</span> <span class="n">e</span><span class="o">)</span> <span class="o">{</span>
    <span class="c1">// We don't know what actually went wrong!</span>
    <span class="n">log</span><span class="o">.</span><span class="na">error</span><span class="o">(</span><span class="s">"Something went wrong"</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div>

<p>於是聰明的你立刻想到了，如果直接 <code class="language-plaintext highlighter-rouge">catch (Exception e)</code>，我們根本無法判斷這個錯誤是網路斷線、餘額不足還是記憶體爆掉 <code class="language-plaintext highlighter-rouge">OutOfMemoryError</code>。這會讓後續的錯誤恢復機制變得不可能。</p>

<blockquote class="block-danger">
  <p>絕對不要 <code class="language-plaintext highlighter-rouge">catch (Exception)</code>，除非你正處於系統的最高層級（例如 API 的 Global Exception Handler），那才是真正適合把所有漏網之魚全部轉換成 HTTP 500 的地方。</p>
</blockquote>

<h4 id="32-在哪裡攔截--分層邊界原則">3.2 在哪裡攔截 — 分層邊界原則</h4>

<p>在現代軟體架構中，我們會將系統分層。每一層對於例外的處理職責完全不同：</p>

<pre><code class="language-mermaid">%%{init: {'themeVariables': { 'fontSize': '16px' }}}%%

graph TD
    Infra["Infrastructure / Adapter&lt;br/&gt;(DB / HTTP Client)"]
    Domain["Domain Layer&lt;br/&gt;(Business Logic)"]
    App["Application Boundary&lt;br/&gt;(Controller / Global Handler)"]
    User((User))

    Infra --&gt;|"1. Map external errors&lt;br/&gt;to Domain errors"| Domain
    Domain --&gt;|"2. Propagate upward"| App
    App --&gt;|"3. Translate to&lt;br/&gt;HTTP Response &amp; Log"| User
</code></pre>

<ul>
  <li><strong>Domain Layer</strong>：這裡只負責核心商業邏輯，通常直接 Propagate upward。遇到問題直接拋出自定義的商業例外，不需要寫一堆 <code class="language-plaintext highlighter-rouge">try-catch</code>。</li>
  <li><strong>Infrastructure/Adapter Boundary</strong>：這裡負責跟外部系統（資料庫、第三方 API）溝通。遇到例如 <code class="language-plaintext highlighter-rouge">SQLException</code> 應該要在這裡攔截，並轉換成 Domain Layer 看得懂的例外，例如 <code class="language-plaintext highlighter-rouge">UserNotFoundException</code>。</li>
  <li><strong>Application Boundary</strong>：系統的最外層（像是 Spring 的 <code class="language-plaintext highlighter-rouge">@ControllerAdvice</code>）。負責統一攔截所有未處理的例外，記錄錯誤日誌（Log），並轉譯成友善的對外回應（如 HTTP Status Code）。</li>
</ul>

<h4 id="33-攔截後的三種命運">3.3 攔截後的三種命運</h4>

<p>當你真的寫下 <code class="language-plaintext highlighter-rouge">catch</code> 區塊時，這個例外只有三種合法的命運：</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kt">void</span> <span class="nf">handleExceptionFates</span><span class="o">()</span> <span class="o">{</span>
    <span class="c1">// 1. Propagate: Rethrow as-is</span>
    <span class="k">try</span> <span class="o">{</span>
        <span class="n">doSomethingRisky</span><span class="o">();</span>
    <span class="o">}</span> <span class="k">catch</span> <span class="o">(</span><span class="nc">SpecificException</span> <span class="n">e</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">throw</span> <span class="n">e</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="c1">// 2. Translate: Wrap with context</span>
    <span class="k">try</span> <span class="o">{</span>
        <span class="n">readConfigFile</span><span class="o">();</span>
    <span class="o">}</span> <span class="k">catch</span> <span class="o">(</span><span class="nc">IOException</span> <span class="n">e</span><span class="o">)</span> <span class="o">{</span>
        <span class="c1">// Wrap the original exception to preserve stack trace</span>
        <span class="k">throw</span> <span class="k">new</span> <span class="nf">ConfigurationLoadException</span><span class="o">(</span><span class="s">"Failed to load app.yml"</span><span class="o">,</span> <span class="n">e</span><span class="o">);</span>
    <span class="o">}</span>

    <span class="c1">// 3. Swallow: Ignore safely</span>
    <span class="k">try</span> <span class="o">{</span>
        <span class="n">closeResource</span><span class="o">();</span>
    <span class="o">}</span> <span class="k">catch</span> <span class="o">(</span><span class="nc">IOException</span> <span class="n">e</span><span class="o">)</span> <span class="o">{</span>
        <span class="c1">// Ignored intentionally: resource is already closing</span>
        <span class="n">log</span><span class="o">.</span><span class="na">debug</span><span class="o">(</span><span class="s">"Resource close failed, safe to ignore"</span><span class="o">,</span> <span class="n">e</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<ul>
  <li><strong>Propagate</strong>（向上拋出）：當前方法無法處理，直接原封不動往上丟。</li>
  <li><strong>Translate</strong>（轉譯並包裝）：遇到低階的技術例外，轉譯成帶有商業語境的高階例外。記得要把原本的 Exception 當作 <code class="language-plaintext highlighter-rouge">cause</code> 傳進去，以免遺失 Stack Trace。</li>
  <li><strong>Swallow</strong>（吞噬）：直接把錯誤吃掉，當作沒發生過。</li>
</ul>

<blockquote class="block-warning">
  <p>Swallow（吞噬例外）是非常危險的操作！只有在「你百分之百確定這個錯誤不影響系統運行，且不需要任何恢復動作」時（例如關閉資源時的例外），才可以把它吞掉。即便如此，也建議至少保留一行 <code class="language-plaintext highlighter-rouge">log.debug</code>。</p>
</blockquote>

<h4 id="34-anti-patterns">3.4 Anti-patterns</h4>

<p>很多時候我們會不自覺寫出有問題的例外處理，以下是幾種絕對要避免的 Anti-patterns：</p>

<ul>
  <li><strong>Empty catch block</strong>：什麼都不做，連 Log 都沒有，這會讓錯誤完全消失在異次元。</li>
  <li><strong>Log-and-rethrow</strong>：先印出 Log 然後又把例外丟出去，這會導致上層再印一次，造成日誌污染（Duplicate logs）。</li>
  <li><strong>Swallowing at the wrong layer</strong>：在底層偷偷把例外吞掉，讓上層以為一切正常，最後引發難以追蹤的 Bug。</li>
</ul>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kt">void</span> <span class="nf">badPractices</span><span class="o">()</span> <span class="kd">throws</span> <span class="nc">Exception</span> <span class="o">{</span>
    <span class="c1">// Anti-pattern 1: Empty catch block</span>
    <span class="k">try</span> <span class="o">{</span>
        <span class="nc">Files</span><span class="o">.</span><span class="na">readString</span><span class="o">(</span><span class="nc">Path</span><span class="o">.</span><span class="na">of</span><span class="o">(</span><span class="s">"data.txt"</span><span class="o">));</span>
    <span class="o">}</span> <span class="k">catch</span> <span class="o">(</span><span class="nc">IOException</span> <span class="n">e</span><span class="o">)</span> <span class="o">{</span>
        <span class="c1">// Nothing here... the error vanishes into thin air</span>
    <span class="o">}</span>

    <span class="c1">// Anti-pattern 2: Log-and-rethrow</span>
    <span class="k">try</span> <span class="o">{</span>
        <span class="n">connectToDatabase</span><span class="o">();</span>
    <span class="o">}</span> <span class="k">catch</span> <span class="o">(</span><span class="nc">SQLException</span> <span class="n">e</span><span class="o">)</span> <span class="o">{</span>
        <span class="c1">// This causes duplicate logs when the caller also logs it</span>
        <span class="n">log</span><span class="o">.</span><span class="na">error</span><span class="o">(</span><span class="s">"Database connection failed"</span><span class="o">,</span> <span class="n">e</span><span class="o">);</span>
        <span class="k">throw</span> <span class="n">e</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="c1">// Anti-pattern 3: Swallowing at the wrong layer</span>
    <span class="k">try</span> <span class="o">{</span>
        <span class="n">validateUser</span><span class="o">(</span><span class="n">userId</span><span class="o">);</span>
    <span class="o">}</span> <span class="k">catch</span> <span class="o">(</span><span class="nc">ValidationException</span> <span class="n">e</span><span class="o">)</span> <span class="o">{</span>
        <span class="c1">// Swallowed! The caller thinks validation passed!</span>
        <span class="n">log</span><span class="o">.</span><span class="na">warn</span><span class="o">(</span><span class="s">"Validation failed"</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<blockquote class="block-danger">
  <p>上述的壞習慣會讓系統變得極難除錯。尤其 Log-and-rethrow 是最常見的錯誤，請記住：你要麼處理並記錄它，要麼拋出它，絕對不要兩者都做！</p>
</blockquote>

<hr />

<h3 id="4-real-examples">4. Real Examples</h3>

<blockquote class="block-tip">
  <p>讓我們用一個完整的例子來串連前面所有的概念：一個訂單服務在處理付款時可能遇到的各種例外。</p>
</blockquote>

<h4 id="bad-example">Bad Example</h4>

<p>以下是一個典型的「什麼都做錯了」的範例：</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">class</span> <span class="nc">BadOrderService</span> <span class="o">{</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">processOrder</span><span class="o">(</span><span class="nc">String</span> <span class="n">orderId</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">try</span> <span class="o">{</span>
            <span class="nc">Order</span> <span class="n">order</span> <span class="o">=</span> <span class="n">findOrder</span><span class="o">(</span><span class="n">orderId</span><span class="o">);</span>
            <span class="n">chargePayment</span><span class="o">(</span><span class="n">order</span><span class="o">);</span>
            <span class="n">sendConfirmation</span><span class="o">(</span><span class="n">order</span><span class="o">);</span>
        <span class="o">}</span> <span class="k">catch</span> <span class="o">(</span><span class="nc">Exception</span> <span class="n">e</span><span class="o">)</span> <span class="o">{</span>
            <span class="c1">// Anti-pattern: catch-all at the wrong layer</span>
            <span class="c1">// Anti-pattern: log-and-rethrow</span>
            <span class="n">log</span><span class="o">.</span><span class="na">error</span><span class="o">(</span><span class="s">"Error"</span><span class="o">,</span> <span class="n">e</span><span class="o">);</span>
            <span class="k">throw</span> <span class="k">new</span> <span class="nf">RuntimeException</span><span class="o">(</span><span class="s">"Something went wrong"</span><span class="o">);</span>
        <span class="o">}</span>
    <span class="o">}</span>

    <span class="kd">private</span> <span class="nc">Order</span> <span class="nf">findOrder</span><span class="o">(</span><span class="nc">String</span> <span class="n">orderId</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">try</span> <span class="o">{</span>
            <span class="k">return</span> <span class="n">orderRepository</span><span class="o">.</span><span class="na">findById</span><span class="o">(</span><span class="n">orderId</span><span class="o">);</span>
        <span class="o">}</span> <span class="k">catch</span> <span class="o">(</span><span class="nc">SQLException</span> <span class="n">e</span><span class="o">)</span> <span class="o">{</span>
            <span class="c1">// Anti-pattern: swallowing at the wrong layer</span>
            <span class="k">return</span> <span class="kc">null</span><span class="o">;</span>
        <span class="o">}</span>
    <span class="o">}</span>

    <span class="kd">private</span> <span class="kt">void</span> <span class="nf">chargePayment</span><span class="o">(</span><span class="nc">Order</span> <span class="n">order</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">if</span> <span class="o">(</span><span class="n">order</span> <span class="o">==</span> <span class="kc">null</span><span class="o">)</span> <span class="o">{</span>
            <span class="c1">// Anti-pattern: meaningless message</span>
            <span class="k">throw</span> <span class="k">new</span> <span class="nf">RuntimeException</span><span class="o">(</span><span class="s">"error"</span><span class="o">);</span>
        <span class="o">}</span>
        <span class="n">paymentGateway</span><span class="o">.</span><span class="na">charge</span><span class="o">(</span><span class="n">order</span><span class="o">.</span><span class="na">getAmount</span><span class="o">());</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>這段程式碼有多處問題：<code class="language-plaintext highlighter-rouge">findOrder</code> 在 Infrastructure 層吞掉了 <code class="language-plaintext highlighter-rouge">SQLException</code> 導致上層收到 <code class="language-plaintext highlighter-rouge">null</code>，<code class="language-plaintext highlighter-rouge">chargePayment</code> 的 Message 完全沒有意義，最外層用 <code class="language-plaintext highlighter-rouge">catch (Exception)</code> 一網打盡卻只留下 “Something went wrong”。</p>

<h4 id="good-example">Good Example</h4>

<p>接下來看看正確的寫法：</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">class</span> <span class="nc">OrderService</span> <span class="o">{</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">processOrder</span><span class="o">(</span><span class="nc">String</span> <span class="n">orderId</span><span class="o">)</span> <span class="o">{</span>
        <span class="c1">// Application Boundary: catch specific exceptions, translate to response</span>
        <span class="k">try</span> <span class="o">{</span>
            <span class="nc">Order</span> <span class="n">order</span> <span class="o">=</span> <span class="n">findOrder</span><span class="o">(</span><span class="n">orderId</span><span class="o">);</span>
            <span class="n">chargePayment</span><span class="o">(</span><span class="n">order</span><span class="o">);</span>
            <span class="n">sendConfirmation</span><span class="o">(</span><span class="n">order</span><span class="o">);</span>
        <span class="o">}</span> <span class="k">catch</span> <span class="o">(</span><span class="nc">OrderNotFoundException</span> <span class="n">e</span><span class="o">)</span> <span class="o">{</span>
            <span class="c1">// Translate: convert to user-facing response</span>
            <span class="k">throw</span> <span class="k">new</span> <span class="nf">ApiException</span><span class="o">(</span><span class="nc">HttpStatus</span><span class="o">.</span><span class="na">NOT_FOUND</span><span class="o">,</span> <span class="n">e</span><span class="o">.</span><span class="na">getMessage</span><span class="o">(),</span> <span class="n">e</span><span class="o">);</span>
        <span class="o">}</span> <span class="k">catch</span> <span class="o">(</span><span class="nc">PaymentFailedException</span> <span class="n">e</span><span class="o">)</span> <span class="o">{</span>
            <span class="k">throw</span> <span class="k">new</span> <span class="nf">ApiException</span><span class="o">(</span><span class="nc">HttpStatus</span><span class="o">.</span><span class="na">PAYMENT_REQUIRED</span><span class="o">,</span> <span class="n">e</span><span class="o">.</span><span class="na">getMessage</span><span class="o">(),</span> <span class="n">e</span><span class="o">);</span>
        <span class="o">}</span>
    <span class="o">}</span>

    <span class="kd">private</span> <span class="nc">Order</span> <span class="nf">findOrder</span><span class="o">(</span><span class="nc">String</span> <span class="n">orderId</span><span class="o">)</span> <span class="o">{</span>
        <span class="c1">// Infrastructure Boundary: map external error to domain error</span>
        <span class="k">try</span> <span class="o">{</span>
            <span class="k">return</span> <span class="n">orderRepository</span><span class="o">.</span><span class="na">findById</span><span class="o">(</span><span class="n">orderId</span><span class="o">);</span>
        <span class="o">}</span> <span class="k">catch</span> <span class="o">(</span><span class="nc">SQLException</span> <span class="n">e</span><span class="o">)</span> <span class="o">{</span>
            <span class="k">throw</span> <span class="k">new</span> <span class="nf">OrderNotFoundException</span><span class="o">(</span>
                <span class="nc">String</span><span class="o">.</span><span class="na">format</span><span class="o">(</span><span class="s">"Order '%s' not found or database unavailable"</span><span class="o">,</span> <span class="n">orderId</span><span class="o">),</span> <span class="n">e</span><span class="o">);</span>
        <span class="o">}</span>
    <span class="o">}</span>

    <span class="kd">private</span> <span class="kt">void</span> <span class="nf">chargePayment</span><span class="o">(</span><span class="nc">Order</span> <span class="n">order</span><span class="o">)</span> <span class="o">{</span>
        <span class="c1">// Domain Layer: validate and throw with context</span>
        <span class="k">if</span> <span class="o">(</span><span class="n">order</span><span class="o">.</span><span class="na">getAmount</span><span class="o">()</span> <span class="o">&lt;=</span> <span class="mi">0</span><span class="o">)</span> <span class="o">{</span>
            <span class="k">throw</span> <span class="k">new</span> <span class="nf">IllegalArgumentException</span><span class="o">(</span>
                <span class="nc">String</span><span class="o">.</span><span class="na">format</span><span class="o">(</span><span class="s">"Cannot charge non-positive amount %d for order '%s'"</span><span class="o">,</span>
                    <span class="n">order</span><span class="o">.</span><span class="na">getAmount</span><span class="o">(),</span> <span class="n">order</span><span class="o">.</span><span class="na">getId</span><span class="o">()));</span>
        <span class="o">}</span>
        <span class="k">try</span> <span class="o">{</span>
            <span class="n">paymentGateway</span><span class="o">.</span><span class="na">charge</span><span class="o">(</span><span class="n">order</span><span class="o">.</span><span class="na">getAmount</span><span class="o">());</span>
        <span class="o">}</span> <span class="k">catch</span> <span class="o">(</span><span class="nc">PaymentGatewayException</span> <span class="n">e</span><span class="o">)</span> <span class="o">{</span>
            <span class="k">throw</span> <span class="k">new</span> <span class="nf">PaymentFailedException</span><span class="o">(</span>
                <span class="nc">String</span><span class="o">.</span><span class="na">format</span><span class="o">(</span><span class="s">"Payment of %d failed for order '%s': %s"</span><span class="o">,</span>
                    <span class="n">order</span><span class="o">.</span><span class="na">getAmount</span><span class="o">(),</span> <span class="n">order</span><span class="o">.</span><span class="na">getId</span><span class="o">(),</span> <span class="n">e</span><span class="o">.</span><span class="na">getMessage</span><span class="o">()),</span> <span class="n">e</span><span class="o">);</span>
        <span class="o">}</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>對比一下兩者的差異：</p>

<table>
  <thead>
    <tr>
      <th style="text-align: left">面向</th>
      <th style="text-align: left">Bad Example</th>
      <th style="text-align: left">Good Example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: left"><strong>Message</strong></td>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">"error"</code>, <code class="language-plaintext highlighter-rouge">"Something went wrong"</code></td>
      <td style="text-align: left">包含 3W：orderId, amount, 具體失敗原因</td>
    </tr>
    <tr>
      <td style="text-align: left"><strong>Catch 層級</strong></td>
      <td style="text-align: left">最外層 catch-all</td>
      <td style="text-align: left">每層各司其職，Infrastructure 轉譯，Application 回應</td>
    </tr>
    <tr>
      <td style="text-align: left"><strong>Exception Type</strong></td>
      <td style="text-align: left">全部用 <code class="language-plaintext highlighter-rouge">RuntimeException</code></td>
      <td style="text-align: left">自定義 <code class="language-plaintext highlighter-rouge">OrderNotFoundException</code>, <code class="language-plaintext highlighter-rouge">PaymentFailedException</code></td>
    </tr>
    <tr>
      <td style="text-align: left"><strong>Stack Trace</strong></td>
      <td style="text-align: left">被 <code class="language-plaintext highlighter-rouge">new RuntimeException</code> 切斷</td>
      <td style="text-align: left">透過 <code class="language-plaintext highlighter-rouge">cause</code> 參數完整保留</td>
    </tr>
  </tbody>
</table>

<hr />

<h3 id="5-conclusion">5. Conclusion</h3>

<blockquote class="block-tip">
  <p>Exception Handling 不是「讓程式不要 crash」的防禦機制，而是「讓系統在出錯時仍然可觀察、可診斷、可恢復」的設計策略。</p>
</blockquote>

<p>這裡我們做一下總結：</p>

<ol>
  <li><strong>Exception Message 的原則</strong>：
    <ul>
      <li>遵循 3W 原則：What（發生什麼）、Where（在哪裡）、Why（為什麼）</li>
      <li>在 Boundary crossing、Async 場景、Validation 失敗時必須加上有意義的 Message</li>
      <li>如果 Exception Type 名稱已經足夠明確，就不需要重複的 Message</li>
      <li>永遠不要寫 “Error occurred” 這種廢話，也不要洩漏敏感資訊</li>
    </ul>
  </li>
  <li><strong>try-catch 的原則</strong>：
    <ul>
      <li>只攔截你能處理的特定型別，不要 <code class="language-plaintext highlighter-rouge">catch (Exception)</code></li>
      <li>在正確的分層攔截：Infrastructure 轉譯、Domain 傳播、Application 回應</li>
      <li>攔截後只有三種合法命運：Propagate、Translate、Swallow</li>
      <li>避免 Empty catch、Log-and-rethrow、在錯誤層級吞噬例外</li>
    </ul>
  </li>
  <li><strong>兩者的關係</strong>：
    <ul>
      <li>Message 和 Catch 是正交的兩個維度，應該獨立思考</li>
      <li>Message 決定「拋出時帶什麼資訊」，Catch 決定「在哪裡以什麼方式處理」</li>
      <li>好的設計是：在拋出點提供充分的上下文，在正確的邊界做適當的處理</li>
    </ul>
  </li>
</ol>

<blockquote class="block-warning">
  <p>後續可以延伸研究的方向：</p>
  <ul>
    <li><strong>Custom Exception Hierarchy</strong>：如何為你的專案設計一個合理的例外層級結構</li>
    <li><strong>Global Exception Handler</strong>：在 Spring Boot 中如何使用 <code class="language-plaintext highlighter-rouge">@ControllerAdvice</code> 統一處理例外</li>
    <li><strong>Checked vs Unchecked Exceptions</strong>：Java 中兩種例外的設計哲學與取捨</li>
  </ul>
</blockquote>

<h3 id="references">References</h3>

<p><a href="https://www.oracle.com/java/technologies/effective-java-3e.html">Effective Java - Chapter 10: Exceptions</a></p>

<p><a href="https://docs.oracle.com/javase/tutorial/essential/exceptions/">Oracle Java Tutorials - Exceptions</a></p>

<p><a href="https://spring.io/blog/2013/11/01/exception-handling-in-spring-mvc">Spring Error Handling</a></p>

<blockquote class="block-warning">
  <h5 id="last-edit">Last Edit</h5>
  <p>07-09-2026 23:30</p>
</blockquote>]]></content><author><name>Benson Hsu</name></author><category term="Jekyll" /><category term="software" /><category term="exception" /><category term="best practices" /><summary type="html"><![CDATA[在設計例外處理機制時，我們應該將其劃分為兩個獨立的維度：Exception Message（拋出時附加的 context）與 try-catch（攔截異常的 interception point）。 一個好的 Exception Message 告訴你 what failed；而一個好的 catch 則告訴你 why you’re allowed to handle it。]]></summary></entry><entry><title type="html">IoT | MQTT Introduction</title><link href="https://hotshot824.github.io/jekyll/2026-07-08-MQTT_introduction.html" rel="alternate" type="text/html" title="IoT | MQTT Introduction" /><published>2026-07-08T00:00:00+00:00</published><updated>2026-07-08T00:00:00+00:00</updated><id>https://hotshot824.github.io/jekyll/MQTT_introduction</id><content type="html" xml:base="https://hotshot824.github.io/jekyll/2026-07-08-MQTT_introduction.html"><![CDATA[<blockquote class="block-tip">
  <p>MQTT (Message Queuing Telemetry Transport) 是一種輕量級的訊息傳輸協定，專為物聯網 (IoT) 設計。它基於發布/訂閱模式，適合在低帶寬、不穩定的網路環境下使用。
MQTT 的優勢在於為惡劣環境下確保傳輸成功而設計的輕量級協定，並且支援 QoS (Quality of Service) 等級，讓開發者根據需求決定「訊息到底要多可靠」。</p>
</blockquote>

<blockquote>
  <p>尤其是在 IoT 裝置中，Header 最小可以只需要 2 bytes <a href="https://mqtt.org/">MQTT</a>，與之相對的 HTTP 協定 Header 動輒就需要 200 bytes 以上，
對於資源有限的裝置來說，MQTT 是一個更好的選擇。</p>
</blockquote>

<blockquote>
  <p>Reference: <a href="https://mqtt.org/">MQTT</a>, <a href="https://www.oasis-open.org/">OASIS</a></p>
</blockquote>

<h3 id="1-what-is-mqtt">1. What is MQTT?</h3>

<blockquote class="block-tip">
  <p>MQTT 的全名是 <strong>Message Queuing Telemetry Transport</strong>，但請注意：它跟傳統的 Message Queue（如 RabbitMQ、Kafka）雖然名字都有 Queue，設計哲學卻完全不同。MQTT 並不會把訊息「排隊」在 Broker 上等你去領，而是透過發布/訂閱的模式主動把訊息推給你</p>
</blockquote>

<p><a href="https://mqtt.org/">MQTT</a> 誕生於 1999 年，由 IBM 的 Andy Stanford-Clark 與 Arlen Nipper（當時在 Eurotech）為了<strong>監控橫跨荒野的石油管線</strong>而設計。當時的場景是：透過昂貴、不穩定、頻寬極低的衛星連結，把分散在各處的感測器資料傳回控制中心。</p>

<p>這個背景很重要，因為它決定了 MQTT 的全部設計取向：</p>

<ul>
  <li><strong>極小的封包開銷</strong>：Header 最小只要 2 bytes，能在窄頻網路上跑。</li>
  <li><strong>能容忍網路斷斷續續</strong>：連線不穩時不會直接崩潰。</li>
  <li><strong>為資源受限的裝置設計</strong>：MCU 等嵌入式設備也能跑得動。</li>
</ul>

<p>後來 MQTT 在 2013 年交由 <a href="https://www.oasis-open.org/">OASIS</a> 標準化（目前主流為 3.1.1 與 5.0 版本），也因為實在太適合物聯網，逐漸成為 IoT 領域的事實標準（de facto standard）。</p>

<blockquote class="block-warning">
  <p>簡單一句話：<strong>MQTT 是為「網路很差、裝置很弱、電量很少」的場景而生的輕量級通訊協定</strong>，而這恰好就是絕大多數 IoT 應用的真實處境</p>
</blockquote>

<hr />

<h3 id="2-the-publishsubscribe-model">2. The Publish/Subscribe Model</h3>

<blockquote class="block-tip">
  <p>MQTT 的核心精神是 <strong>Decouple（解耦）</strong>：發送訊息的人不需要知道誰在接收，接收訊息的人也不需要知道誰在發送。雙方唯一的交集，就是「主題（Topic）」</p>
</blockquote>

<p>傳統的 Request/Response（如 HTTP）是點對點的：A 直接呼叫 B。但 IoT 場景裡，一個溫度感測器可能要同時通知手機 App、雲端資料庫、與告警系統，若讓裝置自己維護這些連線，很快就會吃不消。</p>

<p>MQTT 引入了一個中間人——<strong>Broker</strong>，把所有裝置都變成「只跟 Broker 講話」。這就是經典的 <strong>Publish/Subscribe（發布/訂閱）</strong> 模式：</p>

<pre><code class="language-mermaid">%%{init: {'themeVariables': { 'fontSize': '16px' }}}%%

graph LR
    Sensor["Temperature Sensor&lt;br/&gt;(Publisher)"] --&gt;|Publish: home/livingroom/temperature| Broker((MQTT Broker))
    App["Mobile App&lt;br/&gt;(Subscriber)"] --&gt;|Subscribe: home/+/temperature| Broker
    DB["Cloud DB&lt;br/&gt;(Subscriber)"] --&gt;|Subscribe: home/#| Broker
    Alert["Alert System&lt;br/&gt;(Subscriber)"] --&gt;|Subscribe: home/livingroom/temperature| Broker
    Broker --&gt;|Push to matched subscribers| App
    Broker --&gt;|Push to matched subscribers| DB
    Broker --&gt;|Push to matched subscribers| Alert
</code></pre>

<h4 id="21-three-core-concepts">2.1 Three Core Concepts</h4>

<dl>
  <dt><strong>Broker（代理人 / 中介伺服器）</strong></dt>
  <dd>整個系統的核心，負責接收所有發布的訊息，並依據 Topic 轉發給有訂閱的客戶端。常見的開源實作有 <a href="https://mosquitto.org/">Mosquitto</a>、<a href="https://www.emqx.io/">EMQX</a>（後面第六章會再提到）。</dd>
  <dt><strong>Topic（主題）</strong></dt>
  <dd>用 <code class="language-plaintext highlighter-rouge">/</code> 分隔的階層式路徑字串，用來標示「這則訊息是關於什麼的」。例如：

    <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>home/livingroom/temperature
</code></pre></div>    </div>

    <p>這條路徑可讀作「家 / 客廳 / 溫度」。訂閱時可以使用萬用字元：</p>
    <ul>
      <li><code class="language-plaintext highlighter-rouge">+</code>：單層萬用字元，例如 <code class="language-plaintext highlighter-rouge">home/+/temperature</code> 可匹配客廳、臥室、廚房的溫度。</li>
      <li><code class="language-plaintext highlighter-rouge">#</code>：多層萬用字元，例如 <code class="language-plaintext highlighter-rouge">home/#</code> 可匹配 home 底下所有子主題。</li>
    </ul>
  </dd>
  <dt><strong>Payload（酬載 / 訊息內容）</strong></dt>
  <dd>真正要傳遞的資料本體。MQTT 對 Payload 的格式<strong>完全不關心</strong>，它可以是 JSON、純文字、甚至是二進位資料。例如：

    <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>{ "value": 26.5, "unit": "C", "ts": 1788920000 }
</code></pre></div>    </div>
  </dd>
</dl>

<blockquote class="block-warning">
  <p>這裡有個常被忽略的重點：<strong>Topic 只是「地址」而不是「資料」</strong>。資料全部都放在 Payload 裡，Broker 只負責根據地址轉信，不會去解析你的內容</p>
</blockquote>

<h4 id="22-mqtt-packet-format">2.2 MQTT Packet Format</h4>

<blockquote class="block-tip">
  <p>MQTT 之所以「輕量」，關鍵就在封包格式非常精簡：一則訊息由 <strong>Fixed Header + Variable Header + Payload</strong> 三段組成，Header 最小只要 2 bytes</p>
</blockquote>

<h5 id="221-fixed-header">2.2.1 Fixed Header</h5>

<p>每個 MQTT 封包<strong>一定</strong>有這個結構：</p>

<table>
  <thead>
    <tr>
      <th style="text-align: center">Field</th>
      <th style="text-align: center">Size</th>
      <th style="text-align: center">Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center">Control Type</td>
      <td style="text-align: center">4 bits (7–4)</td>
      <td style="text-align: center">封包類型，例如 <code class="language-plaintext highlighter-rouge">1</code> = CONNECT、<code class="language-plaintext highlighter-rouge">3</code> = PUBLISH</td>
    </tr>
    <tr>
      <td style="text-align: center">Flags</td>
      <td style="text-align: center">4 bits (3–0)</td>
      <td style="text-align: center">依封包類型而定，PUBLISH 用來設定 DUP、QoS、Retain</td>
    </tr>
    <tr>
      <td style="text-align: center">Remaining Length</td>
      <td style="text-align: center">1–4 bytes</td>
      <td style="text-align: center">編碼後方資料的總長度，最小 0、最大 256 MB</td>
    </tr>
  </tbody>
</table>

<p>長度欄位按需佔用 1~4 bytes，用來說明後面的 Variable Header + Payload 總共多長。這種可變長度的設計讓小封包幾乎不浪費空間，
和 HTTP 那種固定且龐大的 Header 結構相比，差距就出來了</p>

<h5 id="222-variable-header--payload-by-packet-type">2.2.2 Variable Header &amp; Payload by Packet Type</h5>

<blockquote class="block-warning">
  <p>MQTT 有幾種 Packet Type 可以參考 <a href="https://docs.oasis-open.org/mqtt/mqtt/v5.0/os/mqtt-v5.0-os.html#_Toc3901022">MQTT Control Packet Types</a>，每種封包的 Variable Header 與 Payload 結構都不一樣，
總共有 15 種，透過一個 Value 來表示</p>
</blockquote>

<p>不同封包類型有不同的 Variable Header 與 Payload：</p>

<table>
  <thead>
    <tr>
      <th style="text-align: center">Type</th>
      <th style="text-align: center">Variable Header</th>
      <th style="text-align: center">Payload</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center"><strong>CONNECT</strong></td>
      <td style="text-align: center">Protocol Name (“MQTT”)、Protocol Level、Connect Flags、Keep Alive</td>
      <td style="text-align: center">Client ID、Will Topic/Message、Username、Password</td>
    </tr>
    <tr>
      <td style="text-align: center"><strong>CONNACK</strong></td>
      <td style="text-align: center">Session Present Flag、Connect Reason Code</td>
      <td style="text-align: center">無</td>
    </tr>
    <tr>
      <td style="text-align: center"><strong>PUBLISH</strong></td>
      <td style="text-align: center">Topic Name、Packet Identifier (QoS 1/2)</td>
      <td style="text-align: center">Message Payload (JSON、text、binary)</td>
    </tr>
    <tr>
      <td style="text-align: center"><strong>SUBSCRIBE</strong></td>
      <td style="text-align: center">Packet Identifier</td>
      <td style="text-align: center">Topic Filter + Requested QoS</td>
    </tr>
    <tr>
      <td style="text-align: center"><strong>PINGREQ / PINGRESP</strong></td>
      <td style="text-align: center">None</td>
      <td style="text-align: center">None</td>
    </tr>
  </tbody>
</table>

<blockquote class="block-tip">
  <p>從這張表可以看出：<strong>Topic 是寫在 Variable Header 裡的「地址」</strong>，真正的資料放在 Payload。Broker 只要解析到 Variable Header 就知道該轉給誰，
完全不需要打開 Payload，這就是 MQTT 高效又能保持格式中立的原因</p>
</blockquote>

<h5 id="223-real-example--thingsboard-temperature-report">2.2.3 Real Example – ThingsBoard Temperature Report</h5>

<p>延續 2.1 的溫度感測器範例，實際用 ThingsBoard 上傳一筆溫度的 PUBLISH 封包在 wire 上長這樣：</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Byte 0:      0x30          (PUBLISH, QoS 0, No Retain)
Byte 1:      0x37          (Remaining Length = 55)
Byte 2–3:    0x00 0x19     (Topic Length = 25)
Byte 4–28:   v1/devices/me/telemetry  (Topic Name, 25 bytes)
Byte 29–83:  {"value":26.5,"unit":"C","ts":1788920000}  (Payload, 55 bytes)
</code></pre></div></div>

<blockquote class="block-tip">
  <p>整個封包只要 <strong>84 bytes</strong>，其中 Fixed Header 僅佔 <strong>2 bytes</strong>。相比 HTTP POST 同類資料光 Header 就輕鬆超過 200 bytes，輕量差距一目了然</p>
</blockquote>

<p>如果使用 ThingsBoard 閘道器模式，多筆感測資料可以打包一起送：</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Topic:   v1/gateway/telemetry
Payload: [{"ts":1788920000,"values":{"temperature":26.5,"humidity":60}}]
</code></pre></div></div>

<blockquote class="block-warning">
  <p>注意 Broker 只解析到 Variable Header 的 Topic 就知道要路由給 <code class="language-plaintext highlighter-rouge">v1/gateway/telemetry</code> 的訂閱者，完全不打開 Payload 檢查內容——這正是分層設計的高明之處：Payload 是 JSON 還是純二進位，Broker 一律不在乎</p>
</blockquote>

<p>完整的封包結構定義可對照官方規格 <a href="https://docs.oasis-open.org/mqtt/mqtt/v3.1.1/mqtt-v3.1.1.html">MQTT 3.1.1</a> 與 <a href="https://docs.oasis-open.org/mqtt/mqtt/v5.0/mqtt-v5.0.html">MQTT 5.0</a>。</p>

<h3 id="3-key-features">3. Key Features</h3>

<blockquote class="block-tip">
  <p>如果只能記三個 MQTT 的王牌特色，那就是 <strong>QoS（可靠性）</strong>、<strong>Retained Messages（新訂閱者立刻有資料）</strong>、以及 <strong>LWT（設備掛掉會通知大家）</strong>。這三個機制加起來，才讓 MQTT 在不可靠網路下依然「可靠」</p>
</blockquote>

<h4 id="31-qos-quality-of-service">3.1 QoS (Quality of Service)</h4>

<p>QoS 決定了一則訊息「到底要被保證送到什麼程度」。MQTT 提供三個等級：</p>

<table>
  <thead>
    <tr>
      <th style="text-align: center">QoS Level</th>
      <th style="text-align: left">Definition</th>
      <th style="text-align: left">Delivery Guarantee</th>
      <th style="text-align: left">Packet Flow</th>
      <th style="text-align: left">Use Cases</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center"><strong>0</strong></td>
      <td style="text-align: left">At most once</td>
      <td style="text-align: left">Best-effort delivery; message may be lost</td>
      <td style="text-align: left">PUBLISH (No ACK)</td>
      <td style="text-align: left">High-frequency telemetry (e.g., per-second temp), where occasional loss is fine.</td>
    </tr>
    <tr>
      <td style="text-align: center"><strong>1</strong></td>
      <td style="text-align: left">At least once</td>
      <td style="text-align: left">Guaranteed delivery; message may duplicate</td>
      <td style="text-align: left">PUBLISH ⇄ PUBACK</td>
      <td style="text-align: left">Standard status updates, where the receiver can handle duplicate messages.</td>
    </tr>
    <tr>
      <td style="text-align: center"><strong>2</strong></td>
      <td style="text-align: left">Exactly once</td>
      <td style="text-align: left">Guaranteed delivery with no duplicates</td>
      <td style="text-align: left">PUBLISH ⇄ PUBREC ⇄ PUBREL ⇄ PUBCOMP</td>
      <td style="text-align: left">Critical operations like billing or control commands that cannot afford errors.</td>
    </tr>
  </tbody>
</table>

<blockquote class="block-warning">
  <p>QoS 2 最可靠但也最慢（需要四次握手），所以<strong>不是等級越高越好</strong>。實務上絕大多數遙測資料用 QoS 0 或 1 就夠了，只有真正不能出錯的控制指令才會考慮 QoS 2</p>
</blockquote>

<h4 id="32-retained-messages">3.2 Retained Messages</h4>

<p>一般情況下，如果你在「還沒人訂閱」的時候發布訊息，那則訊息就直接消失。但 <strong>Retained Message</strong> 會讓 Broker 把這則訊息「釘」在該 Topic 上：之後任何新訂閱者一連上來，立刻就能收到最新狀態，不需要乾等下一筆資料。</p>

<p>這在 IoT 裡非常實用，例如 <code class="language-plaintext highlighter-rouge">home/livingroom/light</code> 的開關狀態設為 Retained，新的 App 一連線就知道燈現在是開還是關，不用等感測器主動再報一次。</p>

<h4 id="33-last-will-and-testament-lwt">3.3 Last Will and Testament (LWT)</h4>

<blockquote class="block-tip">
  <p>LWT 是 MQTT 的「遺書」：裝置在連線時先預先留一句話在 Broker，如果它<strong>不正常斷線</strong>（沒好好說再見），Broker 就會自動把這句話發布出去，通知其他人「這台裝置掛了」</p>
</blockquote>

<p>正常斷線（發送 <code class="language-plaintext highlighter-rouge">DISCONNECT</code>）不會觸發 LWT。只有當連線意外中斷（網路掉、電力耗盡、程式崩潰）時，Broker 才會替它把遺言發出來。LWT 的 Will Topic 與 Will Payload 是在連線階段（CONNECT 封包內的欄位）就註冊在 Broker 上的，例如一台感測器連線時設定：</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Will Topic:   home/livingroom/sensor/status
Will Payload: offline
</code></pre></div></div>

<p>那麼當它突然斷線，所有訂閱該主題的系統就會立刻收到 <code class="language-plaintext highlighter-rouge">offline</code>，可以馬上觸發告警或把設備標記為離線——這正是傳統輪詢架構很難優雅做到的事。</p>

<hr />

<h3 id="4-mqtt-vs-http">4. MQTT vs. HTTP</h3>

<blockquote class="block-tip">
  <p><strong>IoT 不用 HTTP 當主力協定，不是因為 HTTP 不好，而是因為它的設計背景（穩定網路、充足電力、雙方可直連）在 IoT 場景裡並不成立</strong></p>
</blockquote>

<table>
  <thead>
    <tr>
      <th style="text-align: left">Comparison</th>
      <th style="text-align: left">MQTT</th>
      <th style="text-align: left">HTTP</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: left"><strong>通訊模式</strong></td>
      <td style="text-align: left">發布/訂閱（Push，Broker 主動推）</td>
      <td style="text-align: left">請求/回應（Pull，客戶端輪詢）</td>
    </tr>
    <tr>
      <td style="text-align: left"><strong>連線機制</strong></td>
      <td style="text-align: left">長連線（TCP 一直保持）</td>
      <td style="text-align: left">短連線（每次請求建立/斷開）</td>
    </tr>
    <tr>
      <td style="text-align: left"><strong>Header 開銷</strong></td>
      <td style="text-align: left">極小，最小 2 bytes</td>
      <td style="text-align: left">較大，動輒 200+ bytes</td>
    </tr>
    <tr>
      <td style="text-align: left"><strong>耗電量</strong></td>
      <td style="text-align: left">低（連線常駐，不用頻繁握手）</td>
      <td style="text-align: left">高（每次都要 TCP + TLS 握手）</td>
    </tr>
    <tr>
      <td style="text-align: left"><strong>即時性</strong></td>
      <td style="text-align: left">高，事件發生即刻推送</td>
      <td style="text-align: left">低，依賴輪詢間隔</td>
    </tr>
    <tr>
      <td style="text-align: left"><strong>雙向傳輸</strong></td>
      <td style="text-align: left">原生支援（Server → Client 也行）</td>
      <td style="text-align: left">主要靠客戶端輪詢才能拿到更新</td>
    </tr>
    <tr>
      <td style="text-align: left"><strong>適合對象</strong></td>
      <td style="text-align: left">嵌入式裝置、電池供電設備</td>
      <td style="text-align: left">瀏覽器、REST API、一般伺服器</td>
    </tr>
  </tbody>
</table>

<p>最關鍵的差異在於<strong>長連線 vs. 短連線</strong>：HTTP 每次請求都要重新握手（尤其加上 TLS 後更耗電），對用電池的感測器是致命傷；而 MQTT 連上後就一直保持 TCP 連線，Broker 有訊息立刻推過來，裝置大部分時間可以休眠，只在收到推送時醒來處理。</p>

<blockquote class="block-warning">
  <p>當然，這不代表 MQTT 要取代 HTTP。實務上常見的做法是：<strong>裝置端用 MQTT 跟 Broker 溝通，再由 Broker 背後的橋接層把資料轉成 HTTP/REST 給後端系統或 Dashboard 使用</strong>，兩者各司其職</p>
</blockquote>

<hr />

<h3 id="5-quick-start">5. Quick Start</h3>

<blockquote class="block-tip">
  <p>學任何協定最快的方式就是動手連一次。下面用 Python + <a href="https://www.eclipse.org/paho/">paho-mqtt</a> 連上公開測試 Broker <code class="language-plaintext highlighter-rouge">broker.hivemq.com</code>，
同時做到「訂閱」與「發布」，跑完後你就能理解 MQTT 的運作模式</p>
</blockquote>

<p>Install paho-mqtt：</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pip <span class="nb">install </span>paho-mqtt
</code></pre></div></div>

<p>以下是一個最小運行的範例：它會訂閱 <code class="language-plaintext highlighter-rouge">home/livingroom/temperature</code>，並在連線成功後發布一筆溫度資料，隨後印出收到的訊息。</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">paho.mqtt.client</span> <span class="k">as</span> <span class="n">mqtt</span>

<span class="c1"># --- Configuration Constants ---
</span><span class="n">BROKER</span> <span class="o">=</span> <span class="sh">"</span><span class="s">broker.hivemq.com</span><span class="sh">"</span>          <span class="c1"># Public test broker (no authentication required)
</span><span class="n">PORT</span> <span class="o">=</span> <span class="mi">1883</span>                            <span class="c1"># Default MQTT port (1883 = Unencrypted, 8883 = TLS)
</span><span class="n">TOPIC</span> <span class="o">=</span> <span class="sh">"</span><span class="s">home/livingroom/temperature</span><span class="sh">"</span>


<span class="c1"># --- Callback Functions ---
</span>
<span class="k">def</span> <span class="nf">on_connect</span><span class="p">(</span><span class="n">client</span><span class="p">,</span> <span class="n">userdata</span><span class="p">,</span> <span class="n">flags</span><span class="p">,</span> <span class="n">rc</span><span class="p">,</span> <span class="n">properties</span><span class="o">=</span><span class="bp">None</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    Callback triggered when the client successfully connects to the broker.
    </span><span class="sh">"""</span>
    <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Connected with result code </span><span class="si">{</span><span class="n">rc</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
    
    <span class="c1"># Subscribe to the topic (defaults to QoS 0)
</span>    <span class="n">client</span><span class="p">.</span><span class="nf">subscribe</span><span class="p">(</span><span class="n">TOPIC</span><span class="p">)</span>              
    
    <span class="c1"># Publish an initial message immediately after connecting (Payload as a JSON string)
</span>    <span class="n">client</span><span class="p">.</span><span class="nf">publish</span><span class="p">(</span><span class="n">TOPIC</span><span class="p">,</span> <span class="sh">'</span><span class="s">{</span><span class="sh">"</span><span class="s">value</span><span class="sh">"</span><span class="s">: 26.5, </span><span class="sh">"</span><span class="s">unit</span><span class="sh">"</span><span class="s">: </span><span class="sh">"</span><span class="s">C</span><span class="sh">"</span><span class="s">}</span><span class="sh">'</span><span class="p">,</span> <span class="n">qos</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>


<span class="k">def</span> <span class="nf">on_message</span><span class="p">(</span><span class="n">client</span><span class="p">,</span> <span class="n">userdata</span><span class="p">,</span> <span class="n">msg</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    Callback triggered when a message is received from a subscribed topic.
    </span><span class="sh">"""</span>
    <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">[</span><span class="si">{</span><span class="n">msg</span><span class="p">.</span><span class="n">topic</span><span class="si">}</span><span class="s">] </span><span class="si">{</span><span class="n">msg</span><span class="p">.</span><span class="n">payload</span><span class="p">.</span><span class="nf">decode</span><span class="p">()</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>


<span class="c1"># --- Main Client Initialization &amp; Loop ---
</span>
<span class="c1"># Initialize the MQTT client using the modern Callback API Version 2
</span><span class="n">client</span> <span class="o">=</span> <span class="n">mqtt</span><span class="p">.</span><span class="nc">Client</span><span class="p">(</span><span class="n">mqtt</span><span class="p">.</span><span class="n">CallbackAPIVersion</span><span class="p">.</span><span class="n">VERSION2</span><span class="p">)</span>

<span class="c1"># Bind the defined callback functions to the client events
</span><span class="n">client</span><span class="p">.</span><span class="n">on_connect</span> <span class="o">=</span> <span class="n">on_connect</span>
<span class="n">client</span><span class="p">.</span><span class="n">on_message</span> <span class="o">=</span> <span class="n">on_message</span>

<span class="c1"># Establish connection to the broker
</span><span class="n">client</span><span class="p">.</span><span class="nf">connect</span><span class="p">(</span><span class="n">BROKER</span><span class="p">,</span> <span class="n">PORT</span><span class="p">,</span> <span class="n">keepalive</span><span class="o">=</span><span class="mi">60</span><span class="p">)</span>

<span class="c1"># Start the network loop to continuously block, listen, and handle traffic
</span><span class="n">client</span><span class="p">.</span><span class="nf">loop_forever</span><span class="p">()</span>
</code></pre></div></div>

<p>跑起來後你會看到：程式先 <code class="language-plaintext highlighter-rouge">Connected</code>，接著因為自己發、自己也訂閱了同一個 Topic，
所以會馬上從 <code class="language-plaintext highlighter-rouge">on_message</code> 收到自己剛才發的那筆 <code class="language-plaintext highlighter-rouge">{"value": 26.5, "unit": "C"}</code>。</p>

<blockquote class="block-warning">
  <p>小技巧：開兩個終端機各跑一份，A 發 B 收、B 發 A 收，你就能親眼看到 Broker 是如何把訊息從一個發布者轉發給另一個訂閱者，
這正是第二章講的 Decouple 在真實運作</p>
</blockquote>

<hr />

<h3 id="6-conclusion--next-steps">6. Conclusion &amp; Next Steps</h3>

<blockquote class="block-tip">
  <p>MQTT 之所以能成為 IoT 標準，本質上就是做對了一件事：<strong>用最少的資源，在不可靠的網路上把訊息可靠地送達</strong>。
發布/訂閱解耦了裝置、QoS 解決了可靠性、Retained 與 LWT 解決了狀態與離線感知</p>
</blockquote>

<p>這裡我們做一下總結：</p>

<ul>
  <li><strong>架構上</strong>：
    <ul>
      <li>Broker 居中協調，裝置之間完全解耦，只靠 Topic 地址溝通</li>
    </ul>
  </li>
  <li><strong>可靠性上</strong>：
    <ul>
      <li>QoS 0/1/2 按需求取捨速度與保證；Retained 與 LWT 補齊了「新訂閱者」與「異常離線」兩個常見痛點</li>
    </ul>
  </li>
  <li><strong>對比上</strong>：
    <ul>
      <li>相較 HTTP 的短連線輪詢，MQTT 的長連線 + 主動推送在耗電與即時性上全面勝出</li>
    </ul>
  </li>
</ul>

<blockquote class="block-tip">
  <p>後續可以研究的方向：</p>
</blockquote>

<ul>
  <li><strong>安全性（TLS/SSL）</strong>：
    <ul>
      <li>本文用的是 <code class="language-plaintext highlighter-rouge">1883</code> 明文連線，PROD 請改用 <code class="language-plaintext highlighter-rouge">8883</code>（MQTT over TLS），並搭配帳號密碼或 Certificate 做驗證</li>
    </ul>
  </li>
  <li><strong>認證與權限</strong>：
    <ul>
      <li>研究 Broker 的 ACL（Access Control List），限制每個裝置只能讀寫自己被允許的 Topic</li>
    </ul>
  </li>
  <li><strong>開源 Broker 推薦</strong>：
    <ul>
      <li><a href="https://mosquitto.org/">Mosquitto</a>：輕量、易上手，適合單機或嵌入式閘道器</li>
      <li><a href="https://www.emqx.io/">EMQX</a>：分散式、高併發，適合大規模雲端部署，支援 MQTT 5.0 與多種 Bridge</li>
    </ul>
  </li>
  <li><strong>協定版本</strong>：有空可以研究 <a href="https://docs.oasis-open.org/mqtt/mqtt/v5.0/mqtt-v5.0.html">MQTT 5.0</a> 新增的 Reason Code、User Properties、Shared Subscription 等進階特性</li>
</ul>

<blockquote class="block-warning">
  <h5 id="last-edit">Last Edit</h5>
  <p>07-08-2026 22:15</p>
</blockquote>]]></content><author><name>Benson Hsu</name></author><category term="Jekyll" /><category term="mqtt" /><category term="embedded" /><category term="iot" /><summary type="html"><![CDATA[MQTT (Message Queuing Telemetry Transport) 是一種輕量級的訊息傳輸協定，專為物聯網 (IoT) 設計。它基於發布/訂閱模式，適合在低帶寬、不穩定的網路環境下使用。 MQTT 的優勢在於為惡劣環境下確保傳輸成功而設計的輕量級協定，並且支援 QoS (Quality of Service) 等級，讓開發者根據需求決定「訊息到底要多可靠」。]]></summary></entry><entry><title type="html">Note | NextAuth &amp;amp; OIDC Practical Example</title><link href="https://hotshot824.github.io/jekyll/2026-06-11-nextauth.html" rel="alternate" type="text/html" title="Note | NextAuth &amp;amp; OIDC Practical Example" /><published>2026-06-11T00:00:00+00:00</published><updated>2026-06-11T00:00:00+00:00</updated><id>https://hotshot824.github.io/jekyll/nextauth</id><content type="html" xml:base="https://hotshot824.github.io/jekyll/2026-06-11-nextauth.html"><![CDATA[<blockquote class="block-tip">
  <p>這篇文章是 <a href="./2026-06-09-SSO_introduction.html">SSO Introduction</a> 的延續，我們已經了解了 SSO 與 OIDC 的理論基礎，
現在我們將透過 NextAuth 與一個實作範例（Keycloak + Next.js + Spring Boot）來看看這些概念如何實現</p>
</blockquote>

<blockquote class="block-warning">
  <p>實際的範例可以參考 <a href="https://github.com/Hotshot824/OIDC-Example">OIDC-Example GitHub Repository</a> 在這個 Repo 中，我實作了一個更完整的 SSO 流程，包含了前端、後端與 IdP 的整合與登出應該有的機制，
如 Token Refresh、前端與後端的安全性驗證。</p>
</blockquote>

<h3 id="1-why-nextauth">1. Why NextAuth?</h3>

<blockquote class="block-tip">
  <p>NextAuth.js 讓開發者不用自己手刻 OIDC 流程，只需要透過簡單的設定就能完成 SSO 登入，並且自動處理安全性驗證與 Session 管理。</p>
</blockquote>

<p>在上一篇文章中，我們提到實作 OIDC 需要處理複雜的 Redirect、Token Exchange 以及各式各樣的安全性驗證（iss, aud, exp, nonce 等）。
對於前端開發者來說，如果每一項都要手刻，不僅耗時且容易產生安全漏洞。</p>

<p><a href="https://next-auth.js.org/">NextAuth.js</a> (現已更名為 Auth.js) 正是為了簡化這一切而生。它是一個為 Next.js 量身打造的驗證解決方案，
內建支持多種 OAuth/OIDC Provider (如 Google, GitHub, Keycloak)，並且自動幫我們處理了：</p>
<ul>
  <li>前端與 IdP 之間的導向流程</li>
  <li>回調後的 Token 交換 (Back-channel communication)</li>
  <li>Session 的建立與管理 (可選 JWT 或 Database 模式)</li>
  <li>安全性標頭與 CSRF 防護</li>
</ul>

<hr />

<h3 id="2-oidc-practical-walkthrough-the-oidc-example-project">2. OIDC Practical Walkthrough: The “OIDC-Example” Project</h3>

<p>為了讓大家更有體感，我們準備了一個包含三個組件的練習專案：</p>
<ol>
  <li><strong>Identity Provider (IdP)</strong>: 使用 Keycloak (基於 Docker 運行)</li>
  <li><strong>Frontend (RP)</strong>: 使用 React + Next.js + NextAuth</li>
  <li><strong>Resource Server (Backend)</strong>: 使用 Spring Boot (Java)</li>
</ol>

<h4 id="21-nextauth-configuration-the-frontend">2.1 NextAuth Configuration (The Frontend)</h4>

<p>在 Next.js 中，我們透過定義 <code class="language-plaintext highlighter-rouge">[...nextauth]/route.ts</code> 來設定 OIDC Provider。以下是連接 Keycloak 的關鍵代碼：</p>

<div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">import</span> <span class="nx">NextAuth</span><span class="p">,</span> <span class="p">{</span> <span class="nx">NextAuthOptions</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">"</span><span class="s2">next-auth</span><span class="dl">"</span><span class="p">;</span>
<span class="k">import</span> <span class="nx">KeycloakProvider</span> <span class="k">from</span> <span class="dl">"</span><span class="s2">next-auth/providers/keycloak</span><span class="dl">"</span><span class="p">;</span>

<span class="k">export</span> <span class="kd">const</span> <span class="nx">authOptions</span><span class="p">:</span> <span class="nx">NextAuthOptions</span> <span class="o">=</span> <span class="p">{</span>
  <span class="na">providers</span><span class="p">:</span> <span class="p">[</span>
    <span class="nc">KeycloakProvider</span><span class="p">({</span>
      <span class="na">clientId</span><span class="p">:</span> <span class="nx">process</span><span class="p">.</span><span class="nx">env</span><span class="p">.</span><span class="nx">KEYCLOAK_CLIENT_ID</span><span class="o">!</span><span class="p">,</span>
      <span class="na">clientSecret</span><span class="p">:</span> <span class="nx">process</span><span class="p">.</span><span class="nx">env</span><span class="p">.</span><span class="nx">KEYCLOAK_CLIENT_SECRET</span><span class="o">!</span><span class="p">,</span>
      <span class="na">issuer</span><span class="p">:</span> <span class="dl">"</span><span class="s2">http://localhost:8081/realms/demo</span><span class="dl">"</span><span class="p">,</span> <span class="c1">// Keycloak server URL</span>
      <span class="na">authorization</span><span class="p">:</span> <span class="p">{</span> <span class="na">params</span><span class="p">:</span> <span class="p">{</span> <span class="na">scope</span><span class="p">:</span> <span class="dl">"</span><span class="s2">openid profile email</span><span class="dl">"</span> <span class="p">}</span> <span class="p">},</span>
    <span class="p">}),</span>
    <span class="nc">GoogleProvider</span><span class="p">({</span>
      <span class="na">clientId</span><span class="p">:</span> <span class="nx">process</span><span class="p">.</span><span class="nx">env</span><span class="p">.</span><span class="nx">GOOGLE_CLIENT_ID</span><span class="o">!</span><span class="p">,</span>
      <span class="na">clientSecret</span><span class="p">:</span> <span class="nx">process</span><span class="p">.</span><span class="nx">env</span><span class="p">.</span><span class="nx">GOOGLE_CLIENT_SECRET</span><span class="o">!</span><span class="p">,</span>
    <span class="p">}),</span>
  <span class="p">],</span>
  <span class="na">callbacks</span><span class="p">:</span> <span class="p">{</span>
    <span class="c1">// Step 1: Save the Access Token from Keycloak into the encrypted JWT cookie upon successful login</span>
    <span class="k">async</span> <span class="nf">jwt</span><span class="p">({</span> <span class="nx">token</span><span class="p">,</span> <span class="nx">account</span> <span class="p">})</span> <span class="p">{</span>
      <span class="k">if </span><span class="p">(</span><span class="nx">account</span><span class="p">)</span> <span class="p">{</span>
        <span class="nx">token</span><span class="p">.</span><span class="nx">accessToken</span> <span class="o">=</span> <span class="nx">account</span><span class="p">.</span><span class="nx">access_token</span><span class="p">;</span>
      <span class="p">}</span>
      <span class="k">return</span> <span class="nx">token</span><span class="p">;</span>
    <span class="p">},</span>
    <span class="c1">// Step 2: Expose the Access Token to the frontend session so it can be used for API requests</span>
    <span class="k">async</span> <span class="nf">session</span><span class="p">({</span> <span class="nx">session</span><span class="p">,</span> <span class="nx">token</span> <span class="p">})</span> <span class="p">{</span>
      <span class="nx">session</span><span class="p">.</span><span class="nx">accessToken</span> <span class="o">=</span> <span class="nx">token</span><span class="p">.</span><span class="nx">accessToken</span> <span class="kd">as </span><span class="kr">string</span><span class="p">;</span>
      <span class="k">return</span> <span class="nx">session</span><span class="p">;</span>
    <span class="p">},</span>
  <span class="p">},</span>
<span class="p">};</span>

<span class="kd">const</span> <span class="nx">handler</span> <span class="o">=</span> <span class="nc">NextAuth</span><span class="p">(</span><span class="nx">authOptions</span><span class="p">);</span>
</code></pre></div></div>

<ol>
  <li><code class="language-plaintext highlighter-rouge">providers</code>: 透過這個 Array 來宣告你的 NextAuth 將要使用的登入方式，這裡我們使用 Keycloak、 Google 兩種方式
    <ul>
      <li>以 Keycloak 為例應該要帶入 <code class="language-plaintext highlighter-rouge">clientId</code>, <code class="language-plaintext highlighter-rouge">clientSecret</code>, <code class="language-plaintext highlighter-rouge">issuer</code>，以及 <code class="language-plaintext highlighter-rouge">authorization</code></li>
    </ul>
  </li>
  <li><code class="language-plaintext highlighter-rouge">callbacks</code>: 這裡可以定義登入後的回調函式，每種 callback 都有不同的用途
    <ul>
      <li><code class="language-plaintext highlighter-rouge">jwt</code> callback 會在登入成功後被呼叫，我們可以在這裡把 Keycloak 回傳的 <code class="language-plaintext highlighter-rouge">access_token</code> 存入</li>
      <li><code class="language-plaintext highlighter-rouge">session</code> callback 會在前端呼叫 <code class="language-plaintext highlighter-rouge">getSession()</code> 時被呼叫，我們可以把 <code class="language-plaintext highlighter-rouge">access_token</code> 從 JWT Cookie 取出，並放入 Session 物件中</li>
    </ul>
  </li>
</ol>

<blockquote>
  <p>要了解一個 Callback Context 裡面有哪些屬性可以使用，最好直接去看 Source Code，官方文件只會列出部分屬性 <a href="https://next-auth.js.org/configuration/callbacks#jwt-callback">JWTCallbackContext</a></p>
</blockquote>

<p>NextAuth 已經內建了 <strong>Authorization Code Flow</strong> 所需要的 <code class="language-plaintext highlighter-rouge">/api/auth/signin</code> Endpoint，
點擊後會引導使用者至 Keycloak 登入，成功後帶回 <code class="language-plaintext highlighter-rouge">code</code> 並在後端換取 <code class="language-plaintext highlighter-rouge">id_token</code> 與 <code class="language-plaintext highlighter-rouge">access_token</code>。</p>

<blockquote class="block-warning">
  <p>如果你要自己寫 OIDC 的登入流程，就要自己實作各種的 Endpoint 與 Token 交換，例如 <code class="language-plaintext highlighter-rouge">/api/auth/callback</code></p>
</blockquote>

<hr />

<h4 id="22-backend-security-the-resource-server">2.2 Backend Security (The Resource Server)</h4>

<p>當 Frontend 拿到 <code class="language-plaintext highlighter-rouge">access_token</code> 後，它會將其放入 HTTP Header (<code class="language-plaintext highlighter-rouge">Authorization: Bearer &lt;token&gt;</code>) 來請求後端數據。
Spring Boot 端的配置如下：</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Configuration</span>
<span class="nd">@EnableWebSecurity</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">SecurityConfig</span> <span class="o">{</span>
    <span class="nd">@Bean</span>
    <span class="kd">public</span> <span class="nc">SecurityFilterChain</span> <span class="nf">securityFilterChain</span><span class="o">(</span><span class="nc">HttpSecurity</span> <span class="n">http</span><span class="o">)</span> <span class="kd">throws</span> <span class="nc">Exception</span> <span class="o">{</span>
        <span class="n">http</span>
            <span class="o">.</span><span class="na">authorizeHttpRequests</span><span class="o">(</span><span class="n">auth</span> <span class="o">-&gt;</span> <span class="n">auth</span>
                <span class="o">.</span><span class="na">requestMatchers</span><span class="o">(</span><span class="s">"/api/public/**"</span><span class="o">).</span><span class="na">permitAll</span><span class="o">()</span>
                <span class="o">.</span><span class="na">anyRequest</span><span class="o">().</span><span class="na">authenticated</span><span class="o">()</span>
            <span class="o">)</span>
            <span class="o">.</span><span class="na">oauth2ResourceServer</span><span class="o">(</span><span class="n">oauth2</span> <span class="o">-&gt;</span> <span class="n">oauth2</span>
                <span class="o">.</span><span class="na">jwt</span><span class="o">(</span><span class="nc">Customizer</span><span class="o">.</span><span class="na">withDefaults</span><span class="o">())</span>
            <span class="o">);</span>
        <span class="k">return</span> <span class="n">http</span><span class="o">.</span><span class="na">build</span><span class="o">();</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>後端不需要自己保存使用者密碼，它只需要信任 IdP 發出的公鑰 (JWKS)，驗證傳入的 JWT 是否合法即可。這就是 SSO 中「權限驗證外包」的具體展現。</p>

<hr />

<h3 id="3-key-concepts-re-visited-in-practice">3. Key Concepts Re-visited in Practice</h3>

<p>在實作這個範例時，有幾個細節對應到了理論篇：</p>

<ol>
  <li><strong>Front-channel vs Back-channel</strong>:
    <ul>
      <li>前端跳轉到 Keycloak 登入頁面是 <strong>Front-channel</strong>。</li>
      <li>NextAuth 伺服器端拿到 <code class="language-plaintext highlighter-rouge">code</code> 後，在後端呼叫 Keycloak <code class="language-plaintext highlighter-rouge">/token</code> 端點是 <strong>Back-channel</strong>。</li>
    </ul>
  </li>
  <li><strong>Scope</strong>:
    <ul>
      <li>在設定中我們要求了 <code class="language-plaintext highlighter-rouge">scope: "openid profile email"</code>，這決定了 <code class="language-plaintext highlighter-rouge">id_token</code> 裡面會包含哪些 User Claim。</li>
    </ul>
  </li>
  <li><strong>JWT Verification</strong>:
    <ul>
      <li>Spring Boot 後端透過 <code class="language-plaintext highlighter-rouge">issuer-uri</code> 自動向 Keycloak 抓取 <code class="language-plaintext highlighter-rouge">.well-known/openid-configuration</code>，進而找到 <code class="language-plaintext highlighter-rouge">jwks_uri</code> 來驗證 Token 內容。</li>
    </ul>
  </li>
</ol>

<hr />

<h3 id="4-summary">4. Summary</h3>

<p>透過 NextAuth，我們可以在極短的時間內建構出符合安全標準的 SSO 登入流程。它不僅隱藏了 OIDC 的複雜實作，還提供了靈活的 Hook (如 <code class="language-plaintext highlighter-rouge">jwt</code> 與 <code class="language-plaintext highlighter-rouge">session</code> callbacks) 讓我們能根據需求處理 Token。</p>

<p>下一步，建議讀者可以嘗試在同一個 Keycloak Realm 下接入第二個不同的應用程式，體驗「在應用 A 登入後，應用 B 無須再輸入密碼」的無縫體驗。</p>

<blockquote class="block-tip">
  <h5 id="延伸閱讀">延伸閱讀</h5>
  <p><a href="./2026-06-09-SSO_introduction.html">SSO Introduction</a></p>
</blockquote>

<blockquote class="block-warning">
  <h5 id="last-edit">Last Edit</h5>
  <p>06-11-2026 14:00</p>
</blockquote>]]></content><author><name>Benson Hsu</name></author><category term="Jekyll" /><category term="software" /><category term="authentication" /><category term="jwt" /><category term="sso" /><category term="nextauth" /><summary type="html"><![CDATA[這篇文章是 SSO Introduction 的延續，我們已經了解了 SSO 與 OIDC 的理論基礎， 現在我們將透過 NextAuth 與一個實作範例（Keycloak + Next.js + Spring Boot）來看看這些概念如何實現]]></summary></entry><entry><title type="html">Note | SSO Introduction</title><link href="https://hotshot824.github.io/jekyll/2026-06-09-SSO_introduction.html" rel="alternate" type="text/html" title="Note | SSO Introduction" /><published>2026-06-09T00:00:00+00:00</published><updated>2026-06-09T00:00:00+00:00</updated><id>https://hotshot824.github.io/jekyll/SSO_introduction</id><content type="html" xml:base="https://hotshot824.github.io/jekyll/2026-06-09-SSO_introduction.html"><![CDATA[<blockquote class="block-tip">
  <p>這篇文章會從 SSO（Single Sign-On，單一登入）的基本概念開始介紹，接著說明 JWT 的結構，然後進入 OIDC（OpenID Connect）</p>
</blockquote>

<blockquote class="block-warning">
  <p>本文的目標是依照協定堆疊（Protocol Stack）的層次，由上而下逐步說明，讓讀者能夠清楚理解各個技術之間的關係，逐步了解概念到實作的過程</p>
</blockquote>

<h3 id="1-sso-introduction">1. SSO Introduction</h3>

<blockquote class="block-tip">
  <p>SSO 的重點是把登入集中到同一個 IdP (Identity Provider)，讓多個應用共用同一組驗證結果，而不是讓每個系統自己各做一套登入流程</p>
</blockquote>

<p><a href="https://en.wikipedia.org/wiki/Single_sign-on">Single Sign-On (SSO)</a> 的核心不是 <strong>「一次登入就永遠不用登入」</strong>，而是 <strong>「讓多個系統共用同一個身分提供者（Identity Provider, IdP）與同一組登入結果」</strong>。
對使用者來說，這表示少輸入一次密碼；對系統來說，這表示各服務不必各自處理帳密驗證、找回密碼、風險控管與登入狀態同步。</p>

<p>因此 SSO 的主要解決的問題是：</p>
<ul>
  <li>使用者在多個產品間切換時，不需要重複登入</li>
  <li>每個應用程式不必自己保存密碼驗證邏輯</li>
  <li>登入政策可以集中在 IdP 上做，例如 MFA、裝置信任、封鎖風險帳號</li>
</ul>

<p><strong>在這個流程裡面，主要的角色與概念如下：</strong></p>

<table>
  <thead>
    <tr>
      <th style="text-align: center">Role</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center">IdP (Identity Provider)</td>
      <td>負責驗證使用者身份的服務，例如 Google、Microsoft Entra ID、Auth0、Keycloak 等。</td>
    </tr>
    <tr>
      <td style="text-align: center">RP (Relying Party) / Backend</td>
      <td>想要使用 IdP 提供的登入結果的應用程式，例如你的網站或應用。</td>
    </tr>
    <tr>
      <td style="text-align: center">Session</td>
      <td>IdP 或應用程式內部保存的登入狀態，用來記錄使用者是否已經登入，以及相關的資訊。</td>
    </tr>
    <tr>
      <td style="text-align: center">User</td>
      <td>最終使用者，透過 IdP 進行身份驗證，並希望在多個應用程式間無縫切換。</td>
    </tr>
  </tbody>
</table>

<p>實際上依然會使用到傳統的 Cookie、Session 等技術，但 SSO 的重點是把「驗證」這件事交給一個大家都信任的 IdP，
讓應用程式專注在「授權」和「使用者體驗」上，而不是自己處理帳密驗證的細節。</p>

<blockquote>
  <p>因此如果本來就理解傳統的登入流程設計，在理解 SSO 的機制上會更快上手，因為基本的概念並沒有改變</p>
</blockquote>

<p><strong>典型的 SSO 工作流程：</strong></p>

<ol>
  <li>使用者打開網站 (Backend)，網站發現自己沒有該使用者的登入狀態</li>
  <li>網站把使用者導向 IdP 的登入頁面</li>
  <li>使用者在 IdP 完成驗證與同意，並且 IdP 建立自己的 session
    <ul>
      <li>IdP 的 session 在之後登入其他的 Backend 時會被重用，此時就能直接省略第三步的驗證過程，直接進到第四步</li>
    </ul>
  </li>
  <li>IdP 把結果帶回 Backend，通常會帶一個 token 或 code</li>
  <li>應用程式建立自己的 session，之後就不用再問 IdP 一次</li>
</ol>

<blockquote>
  <p>如果把它講得更直白一點，SSO 其實就是 <strong>「把登入這件事外包給一個大家都信任的地方」</strong>。後面的 OIDC 只是把這個外包流程標準化</p>
</blockquote>

<hr />

<h3 id="2-jwt-format">2. JWT format</h3>

<blockquote class="block-tip">
  <p>JWT 真正重要的部分是簽章，因為它決定了這個 token 是不是可信的</p>
</blockquote>

<p><a href="https://en.wikipedia.org/wiki/JSON_Web_Token">JWT (JSON Web Token)</a> 是 OIDC 裡最容易被誤解的部分。它看起來像一串亂碼，但本質上只是三段式字串：header、payload、signature。
JWT 本身不是「加密格式」，大多數情況下它只是可驗簽的 JSON 封裝；如果需要保密，才會進到 JWE 的領域。這裡的定義可對照 <a href="https://www.rfc-editor.org/rfc/rfc7519">RFC 7519</a>。</p>

<h4 id="21-jwt-structure">2.1 JWT Structure</h4>

<blockquote class="block-tip">
  <p>實際上 JWT 就只是兩個 JSON 物件（header 和 payload）加上一個簽章的一個結構，然後用 Base64url 編碼起來，最後用點號 . 串在一起，
這樣就能以 Text 的形式在 HTTP header 或 URL 裡面傳遞</p>
</blockquote>

<p>JWT 其實就跟傳統的 Cookie + Session 沒有本質上的差別；同樣是存放在 Frontend Cookie 裡面。
所以如果 JWT 被人完整取走，同樣可以被用來冒充使用者；但如果 JWT 沒有被竊取，
幾乎是無法被偽造的，因為簽章是用 IdP 的私鑰簽的，沒有私鑰就無法產生有效的簽章。</p>

<p><strong>header.payload.signature</strong></p>

<p>實際上每一段都是 Base64url 編碼後的內容：</p>

<ul>
  <li>header：
    <ul>
      <li>描述簽章演算法，例如 <code class="language-plaintext highlighter-rouge">RS256</code>，這樣就知道要用什麼算法來驗證簽章</li>
      <li>通常還會有 <code class="language-plaintext highlighter-rouge">typ</code>，表示這是一個 JWT</li>
    </ul>
  </li>
  <li>payload：
    <ul>
      <li>放 claims，例如 <code class="language-plaintext highlighter-rouge">iss</code>、<code class="language-plaintext highlighter-rouge">sub</code>、<code class="language-plaintext highlighter-rouge">aud</code>、<code class="language-plaintext highlighter-rouge">exp</code></li>
      <li>這些內容完全是由 IdP 去定義需要有哪些欄位，Backend 只能選擇相信或不相信裡面的資料
        <ul>
          <li>主要有幾個標準欄位是 RFC 定義的，稱為 <a href="https://www.rfc-editor.org/rfc/rfc7519#section-4.1">Registered Claim</a></li>
        </ul>
      </li>
    </ul>
  </li>
  <li>signature：用來驗證內容沒有被改過
    <ul>
      <li>將前兩段（header 和 payload）用點號串起來，然後用 IdP 的私鑰簽章，產生 signature</li>
      <li>Backend 拿到 JWT 後，可以用 IdP 的公開金鑰來驗證簽章，確保內容沒有被竄改過</li>
    </ul>
  </li>
</ul>

<p><strong>header</strong></p>
<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
</span><span class="nl">"alg"</span><span class="p">:</span><span class="s2">"RS256"</span><span class="p">,</span><span class="w">
</span><span class="nl">"typ"</span><span class="p">:</span><span class="s2">"JWT"</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>
<p><strong>payload</strong></p>
<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
</span><span class="nl">"iss"</span><span class="p">:</span><span class="s2">"https://idp.example.com"</span><span class="p">,</span><span class="w">
</span><span class="nl">"sub"</span><span class="p">:</span><span class="s2">"248289761001"</span><span class="p">,</span><span class="w">
</span><span class="nl">"aud"</span><span class="p">:</span><span class="s2">"client-id-123"</span><span class="p">,</span><span class="w">
</span><span class="nl">"exp"</span><span class="p">:</span><span class="mi">1910000000</span><span class="p">,</span><span class="w">
</span><span class="nl">"iat"</span><span class="p">:</span><span class="mi">1909996400</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p>前面兩個做完 Base64url 後，再加上簽章，就會變成一串像這樣的 JWT 放到 Frontend Cookie 或 HTTP header 裡面：</p>

<p><code class="language-plaintext highlighter-rouge">eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJodHRwczovL2lkcC5leGFtcGxlLmNvbSIsInN1YiI6IjI0ODI4OTc2MTAwMSIsImF1ZCI6ImNsaWVudC1pZC0xMjMiLCJleHAiOjE5MTAwMDAwMDAsImlhdCI6MTkwOTk5NjQwMH0.SflKxwRJSMeKKF2QT4fwpMeJf36POk6yJV</code></p>

<h4 id="22-common-claims">2.2 Common Claims</h4>

<ul>
  <li><code class="language-plaintext highlighter-rouge">iss</code>：這顆 token 是誰發的</li>
  <li><code class="language-plaintext highlighter-rouge">sub</code>：這顆 token 指向哪個使用者</li>
  <li><code class="language-plaintext highlighter-rouge">aud</code>：這顆 token 是發給誰看的</li>
  <li><code class="language-plaintext highlighter-rouge">exp</code>：過期時間</li>
  <li><code class="language-plaintext highlighter-rouge">iat</code>：簽發時間</li>
  <li><code class="language-plaintext highlighter-rouge">nonce</code>：把登入請求和回應綁在一起，避免 replay</li>
</ul>

<p>其中最重要的是 <code class="language-plaintext highlighter-rouge">iss + sub</code> 的組合。<code class="language-plaintext highlighter-rouge">iss</code> 先把識別範圍限定在同一個 issuer，<code class="language-plaintext highlighter-rouge">sub</code> 再當作這個 issuer 底下穩定的使用者識別子；<code class="language-plaintext highlighter-rouge">email</code>、<code class="language-plaintext highlighter-rouge">name</code>、<code class="language-plaintext highlighter-rouge">preferred_username</code> 都不適合直接當主鍵。這個判斷可直接對照 <a href="https://openid.net/specs/openid-connect-core-1_0.html">OpenID Connect Core</a> 的 ID Token 與 subject 定義。</p>

<h4 id="23-what-to-check-when-verifying-jwts">2.3 What to Check When Verifying JWTs</h4>

<blockquote class="block-tip">
  <p>驗證 JWT 的重點不是「讀一讀裡面的資料」，而是「驗簽章確定這個 token 是可信的」，
然後再去檢查 <code class="language-plaintext highlighter-rouge">iss</code>、<code class="language-plaintext highlighter-rouge">aud</code>、<code class="language-plaintext highlighter-rouge">exp</code>、<code class="language-plaintext highlighter-rouge">nonce</code> 等欄位來確保這個 token 是合法的、沒有過期的、而且是給自己的</p>
</blockquote>

<p><strong>以下是一個實際驗證 JWT 的流程：</strong></p>
<ol>
  <li>先驗簽章，確認內容真的是該 IdP 發的</li>
  <li>再驗 <code class="language-plaintext highlighter-rouge">iss</code>、<code class="language-plaintext highlighter-rouge">aud</code>、<code class="language-plaintext highlighter-rouge">exp</code></li>
  <li>如果流程有 <code class="language-plaintext highlighter-rouge">nonce</code>，也要比對 <code class="language-plaintext highlighter-rouge">nonce</code></li>
  <li>不要只要拿到 payload 就直接相信裡面的資料</li>
</ol>

<blockquote>
  <p>JWT 是整個 SSO 流程中最關鍵的部分，因為它決定了這個 token 是不是可信的；如果 JWT 被竊取了，攻擊者就可以冒充使用者；如果 JWT 沒有被竊取，但驗簽章有一定的難度，那麼就幾乎不可能被偽造</p>
</blockquote>

<hr />

<h3 id="3-oidc">3. OIDC</h3>

<blockquote class="block-tip">
  <p>OIDC 是把 OAuth 2.0 的授權流程補上身分，讓系統不只知道「有沒有權限」，也知道「現在登入的人是誰」。</p>
</blockquote>

<p>OIDC 是建立在 OAuth 2.0 上的 identity layer。OAuth 2.0 本來主要處理「授權」，也就是你能不能存取資源；OIDC 則是在這個流程上再補上「身份驗證」，
讓 Backend 可以知道使用者到底是誰。OpenID Connect Core 直接把它定義成 OAuth 2.0 上的一層簡單 identity layer。</p>

<h4 id="31-sso-main-protocols-comparison">3.1 SSO Main Protocols Comparison</h4>

<p>雖然我們常在 SSO 的語境下提到這三者，但它們的設計初衷與應用場景有所不同：</p>

<table>
  <thead>
    <tr>
      <th style="text-align: left">Feature</th>
      <th style="text-align: left">SAML 2.0</th>
      <th style="text-align: left">OAuth 2.0</th>
      <th style="text-align: left">OpenID Connect (OIDC)</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: left"><strong>核心目的</strong></td>
      <td style="text-align: left">身份驗證 (AuthN) &amp; 授權 (AuthZ)</td>
      <td style="text-align: left">授權 (AuthZ)</td>
      <td style="text-align: left">身份驗證 (AuthN)</td>
    </tr>
    <tr>
      <td style="text-align: left"><strong>主要角色</strong></td>
      <td style="text-align: left">「使用者是誰」與「權限」</td>
      <td style="text-align: left">「Backend 能不能拿到資源」</td>
      <td style="text-align: left">「使用者是不是已被驗證」</td>
    </tr>
    <tr>
      <td style="text-align: left"><strong>資料格式</strong></td>
      <td style="text-align: left">XML (Assertion)</td>
      <td style="text-align: left">JSON (Access Token)</td>
      <td style="text-align: left">JSON (ID Token + Access Token)</td>
    </tr>
    <tr>
      <td style="text-align: left"><strong>傳輸協定</strong></td>
      <td style="text-align: left">較重 (SOAP, HTTP POST)</td>
      <td style="text-align: left">輕量 (REST / JSON)</td>
      <td style="text-align: left">輕量 (REST / JSON)</td>
    </tr>
    <tr>
      <td style="text-align: left"><strong>主流場景</strong></td>
      <td style="text-align: left">企業級應用 (Enterprise/B2B)</td>
      <td style="text-align: left">API 存取、第三方授權</td>
      <td style="text-align: left">現代 Web/Mobile、社交登入</td>
    </tr>
    <tr>
      <td style="text-align: left"><strong>關鍵標識</strong></td>
      <td style="text-align: left">XML Signature / Encryption</td>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">access_token</code></td>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">id_token</code> + <code class="language-plaintext highlighter-rouge">scope=openid</code></td>
    </tr>
  </tbody>
</table>

<p>這也是為什麼 OIDC 的 authentication request 必須包含 <code class="language-plaintext highlighter-rouge">scope=openid</code>。少了 <code class="language-plaintext highlighter-rouge">openid</code>，流程就只是純粹的 OAuth 2.0 授權，
無法提供標準化的身份資訊（Identity Layer）。可對照 <a href="https://openid.net/specs/openid-connect-core-1_0.html">OpenID Connect Core</a>。
OAuth 2.0 是一個<strong>授權框架</strong>，只關心 Backend 能不能獲取 IdP 的資源；OIDC 則是在 OAuth 2.0 的基礎上加了一層<strong>身份驗證</strong>，
讓 Backend 不只知道「有沒有權限」，也知道「現在登入的人是誰」，誰在獲取資源。</p>

<blockquote>
  <p>因此在 OIDC 上我們能知道使用者的身份（<code class="language-plaintext highlighter-rouge">id_token</code> 裡的 <code class="language-plaintext highlighter-rouge">sub</code>），就一併完成了 Login 的功能，Backend 可以確認登入者是 IdP 所認證的使用者</p>
</blockquote>

<h4 id="32-authorization-code-flow">3.2 Authorization Code Flow</h4>

<blockquote class="block-tip">
  <p>最簡單說明 OIDC 的方式就是用 Squence Diagram 把整個流程畫出來，這樣就能清楚看到前端、後端、IdP 之間的互動，以及 token 是在哪裡交換的</p>
</blockquote>

<p>對後端來說，OIDC 最常見、也最值得講清楚的就是 Authorization Code Flow。它的優點是 token 不會直接暴露在 user agent 裡面；相較之下，<a href="https://openid.net/specs/openid-connect-core-1_0.html#ImplicitFlowAuth">implicit flow</a> 主要是歷史包袱，現在多半不建議新系統再採用。</p>

<p>下方介紹的流程就是 OIDC 的 Authorization Code Flow，這個流程的重點是把 token 的交換放在後端的安全通道裡面，避免 token 在前端暴露，增加安全性</p>

<blockquote>
  <p>implicit flow 的流程則是直接在前端拿到 token，會少去掉後端交換 token 的步驟，但同時也會讓 token 在前端暴露，增加被竊取的風險，因此現在多半不建議新系統再採用 implicit flow</p>
</blockquote>

<p><a href="#phase-1-authorization-request-front-channel">Phase 1: Authorization Request (Front-channel)</a><br />
<a href="#phase-2-authentication--consent">Phase 2: Authentication &amp; Consent</a><br />
<a href="#phase-3-authorization-response-front-channel">Phase 3: Authorization Response (Front-channel)</a><br />
<a href="#phase-4-token-exchange-back-channel">Phase 4: Token Exchange (Back-channel)</a><br />
<a href="#phase-5-session-establishment">Phase 5: Session Establishment</a></p>

<pre><code class="language-mermaid">%%{init: {'themeVariables': { 'fontSize': '16px' }}}%%

sequenceDiagram
    autonumber
    participant User as User
    participant Frontend as Frontend
    participant Backend as Backend
    participant IdP as Identity Provider

    rect rgb(240, 248, 255)
    Note over User, Backend: Phase 1: Authorization Request (Front-channel)
    User-&gt;&gt;Frontend: Visit Client Frontend website
    Frontend-&gt;&gt;Backend: Request protected resource (or click login)
    Backend-&gt;&gt;Frontend: 302 Redirect to IdP
    Note right of Backend: With client_id, scope=openid,&lt;br/&gt;redirect_uri, state, nonce
    Frontend-&gt;&gt;IdP: GET /authorize?params...
    end

    rect rgb(255, 250, 240)
    Note over User, IdP: Phase 2: Authentication &amp; Consent
    IdP-&gt;&gt;User: Display login page
    User-&gt;&gt;IdP: Enter credentials for authentication
    IdP-&gt;&gt;User: Display consent screen
    User-&gt;&gt;IdP: Approve authorization
    end

    rect rgb(240, 255, 240)
    Note over Frontend, IdP: Phase 3: Authorization Response (Front-channel)
    IdP-&gt;&gt;Frontend: 302 Redirect to Backend Callback
    Note left of IdP: With Authorization Code
    Frontend-&gt;&gt;Backend: GET /callback?code=...&amp;state=...
    end

    rect rgb(255, 240, 245)
    Note over Backend, IdP: Phase 4: Token Exchange (Back-channel)
    Backend-&gt;&gt;IdP: POST /token (Using code + client_secret)
    Note right of Backend: Secure back-channel communication
    IdP-&gt;&gt;Backend: Return ID Token &amp; Access Token
    end

    rect rgb(245, 245, 245)
    Note over Frontend, Backend: Phase 5: Session Establishment
    Backend-&gt;&gt;Backend: Verify ID Token (Signature &amp; Claims)
    Backend-&gt;&gt;Frontend: Establish Session / Set Secure Cookie
    Frontend-&gt;&gt;User: Login successful, access protected page
    end
</code></pre>

<h5 id="phase-1-authorization-request-front-channel">Phase 1: Authorization Request (Front-channel)</h5>
<ol>
  <li>首先 User 造訪 Frontend</li>
  <li>Frontend 發現沒有登入狀態</li>
  <li>於是 Backend 回應一個 302 Redirect</li>
  <li>把 User 導向 IdP 的 <code class="language-plaintext highlighter-rouge">/authorize</code> 端點</li>
</ol>

<p>通常在這個階段會看到一個類似下面的 HTTP 請求，這是 Backend 把 User 導向 IdP 的 <code class="language-plaintext highlighter-rouge">/authorize</code> 端點，並且帶上必要的參數：</p>

<div class="language-http highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">GET</span> <span class="nn">/authorize?response_type=code&amp;scope=openid%20email%20profile&amp;client_id=client-id-123&amp;redirect_uri=https%3A%2F%2Fapp.example.com%2Fcallback&amp;state=abc123&amp;nonce=n-0S6_WzA2Mj</span> <span class="k">HTTP</span><span class="o">/</span><span class="m">1.1</span>
<span class="na">Host</span><span class="p">:</span> <span class="s">idp.example.com</span>
</code></pre></div></div>

<p>類似上面這段 Http 將使用者導向 IdP 的 <code class="language-plaintext highlighter-rouge">/authorize</code> 端點，並且帶上必要的參數，例如 <code class="language-plaintext highlighter-rouge">response_type=code</code>、<code class="language-plaintext highlighter-rouge">scope=openid</code>、<code class="language-plaintext highlighter-rouge">client_id</code>、<code class="language-plaintext highlighter-rouge">redirect_uri</code>、<code class="language-plaintext highlighter-rouge">state</code> 說明 Backend 的身份、回調 URL、以及防止 CSRF 的 state 和 nonce</p>

<h5 id="phase-2-authentication--consent">Phase 2: Authentication &amp; Consent</h5>

<blockquote class="block-tip">
  <p>這一步其實就是我們常見的跳轉到 Google、Facebook 等社交登入頁面，輸入帳號密碼，然後同意授權的流程</p>
</blockquote>

<p>使用者抵達 IdP 後，IdP 會先檢查使用者是否已經登入。若未登入，則引導使用者完成身份驗證（如輸入帳密、MFA 等）。驗證通過後，IdP 會詢問使用者是否同意授權該應用程式存取指定的資訊（如 email, profile），這就是所謂的「同意頁面」</p>

<blockquote>
  <p>同樣的我同意第三方應用程式存取我的基本資訊，但不代表我同意他們可以存取我的所有資料，例如 Drive、Photos 等，這就是 OAuth 2.0 的授權範圍（scope）概念</p>
</blockquote>

<h5 id="phase-3-authorization-response-front-channel">Phase 3: Authorization Response (Front-channel)</h5>

<p>當使用者同意授權後，IdP 會透過瀏覽器（Front-channel）發送一個 302 Redirect，將使用者導回 Backend 原先指定的 <code class="language-plaintext highlighter-rouge">redirect_uri</code>。
此時 URL 中會包含一個臨時的 <code class="language-plaintext highlighter-rouge">code</code> (Authorization Code) 以及原先的 <code class="language-plaintext highlighter-rouge">state</code>。
Backend 收到後必須立即比對 <code class="language-plaintext highlighter-rouge">state</code> 是否與發起請求時一致，以防止 CSRF 攻擊。</p>

<blockquote class="block-warning">
  <p>這個 Code 可以讓 Backend 在後端安全地向 IdP 換取真正的 Token，而不會直接暴露在前端，這是 Authorization Code Flow 的核心安全設計</p>
</blockquote>

<h5 id="phase-4-token-exchange-back-channel">Phase 4: Token Exchange (Back-channel)</h5>

<p>這是 OIDC 最安全的步驟之一。Backend 取得 <code class="language-plaintext highlighter-rouge">code</code> 後，會直接從後端（Back-channel）向 IdP 的 <code class="language-plaintext highlighter-rouge">/token</code> 端點發送 POST 請求，
用授權碼換取真正的 Token。由於這是伺服器對伺服器的通訊，<code class="language-plaintext highlighter-rouge">client_secret</code> 可以安全地包含在請求中而不被瀏覽器看見。</p>

<div class="language-http highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">POST</span> <span class="nn">/token</span> <span class="k">HTTP</span><span class="o">/</span><span class="m">1.1</span>
<span class="na">Host</span><span class="p">:</span> <span class="s">idp.example.com</span>
<span class="na">Content-Type</span><span class="p">:</span> <span class="s">application/x-www-form-urlencoded</span>

grant_type=authorization_code&amp;code=SplxlOBeZQQYbYS6WxSbIA&amp;redirect_uri=https%3A%2F%2Fapp.example.com%2Fcallback&amp;client_id=client-id-123&amp;client_secret=secret
</code></pre></div></div>

<p>上面是一個 Client Backend 向 IdP 換取 Token 的範例請求，IdP 驗證這個請求無誤後會回傳 <code class="language-plaintext highlighter-rouge">id_token</code> (JWT) 與 <code class="language-plaintext highlighter-rouge">access_token</code>，
給予 Backend 用來確認使用者身份與存取資源的權限。這個過程完全在後端進行，增加了安全性，因為 token 不會暴露在前端。</p>

<h5 id="phase-5-session-establishment">Phase 5: Session Establishment</h5>

<p>最後，Backend 會解析並驗證 <code class="language-plaintext highlighter-rouge">id_token</code> 的內容與簽章。確認使用者身份後，Backend 會在自己的系統中建立登入狀態（Session），
通常是發送一個加密且具備 <code class="language-plaintext highlighter-rouge">HttpOnly</code> 屬性的 Cookie 給瀏覽器。至此，使用者成功登入並可以存取受保護資源。</p>

<p>目前通常不會建議把 JWT 直接放在前端 Non-HttpOnly Cookie 裡面，因為這樣會增加被竊取的風險，攻擊者可以直接使用 JWT 來冒充使用者。
相對地，如果把 JWT 放在後端的 Session 裡面，前端只拿到一個 session cookie，這樣如果想要竊取 JWT 就必須同時攻擊到 Backend 的 session 管理，難度會大幅增加；</p>

<blockquote class="block-warning">
  <p>但很多實作依然將 JWT 放在前端 Cookie 裡面，這部分涉及很多 Cookie 上的安全因素，最主要的因素還是 Session ID 相較於 JWT 來說殺傷力較小，有興趣的可以去另外查閱這部分的內容</p>
</blockquote>

<p>目前主流的最佳實務有以下兩種：</p>
<ol>
  <li>BFF (Backend For Frontend)：
    <ul>
      <li>把 JWT 放在 Backend 的 Session 裡面，前端只拿到一個 session cookie，這樣如果想要竊取 JWT 就必須同時攻擊到 Backend 的 session 管理，難度會大幅增加。</li>
    </ul>
  </li>
  <li>JWT in HttpOnly Cookie：
    <ul>
      <li>把 JWT 放在前端 Cookie 裡面，但一定要加上 <code class="language-plaintext highlighter-rouge">HttpOnly</code>, <code class="language-plaintext highlighter-rouge">Secure</code>, <code class="language-plaintext highlighter-rouge">SameSite</code> 等屬性，來防止 JWT 被 XSS 攻擊竊取。</li>
    </ul>
  </li>
</ol>

<blockquote>
  <p><a href="https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Set-Cookie#httponly">HttpOnly</a> 是一個 Cookie 屬性，表示這個 Cookie 只能被伺服器讀取，無法透過 Frontend 執行 JavaScript 來存取</p>
</blockquote>

<blockquote>
  <p><a href="https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Set-Cookie#secure">Secure</a> 是一個 Cookie 屬性，表示這個 Cookie 只能在 HTTPS 連線中傳送</p>
</blockquote>

<hr />

<h4 id="33-key-oidc-endpoints">3.3 Key OIDC Endpoints</h4>

<blockquote class="block-tip">
  <p>在 OIDC 官方標準規範 <a href="https://openid.net/specs/openid-connect-core-1_0.html">OIDC Connect core</a>、<a href="https://openid.net/specs/openid-connect-discovery-1_0.html">OIDC-Discovery</a>中定義了幾個重要的端點，或許實作上會有些差異，但基本上這些端點是 OIDC 流程中不可或缺的部分</p>
</blockquote>

<p>最重要的是 <code class="language-plaintext highlighter-rouge">.well-known/openid-configuration</code> 這個 discovery 端點，所有的 OIDC 實作都必須提供這個端點，
讓 Backend 可以自動發現 IdP 的相關資訊，例如 issuer、authorization endpoint、token endpoint、userinfo endpoint、jwks_uri 等等。
其餘端點可能會有路徑上的差異，但基本上都會有類似的功能。</p>

<ul>
  <li><strong><code class="language-plaintext highlighter-rouge">/.well-known/openid-configuration</code> (Discovery)</strong>
    <ul>
      <li>這是 OIDC 的進入點（遵循 <a href="https://www.rfc-editor.org/rfc/rfc8414">RFC 8414</a>），IdP 會在這個路徑提供一個 JSON 檔案，列出所有支援的端點</li>
      <li>這讓 Backend 只需要知道 Issuer 的 URL 就能自動完成大部分設定。</li>
    </ul>
  </li>
  <li><strong><code class="language-plaintext highlighter-rouge">/authorize</code> (Authorization Endpoint)</strong>
    <ul>
      <li>Backend 將 User 導向此處並帶上 <code class="language-plaintext highlighter-rouge">client_id</code>、<code class="language-plaintext highlighter-rouge">response_type=code</code>、<code class="language-plaintext highlighter-rouge">scope</code> 等參數</li>
      <li>它的任務是完成身分驗證（AuthN）並取得使用者的授權，通常 Client 跳轉就是到這個端點</li>
    </ul>
  </li>
  <li><strong><code class="language-plaintext highlighter-rouge">/token</code> (Token Endpoint)</strong>
    <ul>
      <li>屬於 Back-channel 的通訊端點。Backend 在取得 <code class="language-plaintext highlighter-rouge">code</code> 後，會在此端點使用 <code class="language-plaintext highlighter-rouge">client_secret</code> 進行身分驗證，
  並換取 <code class="language-plaintext highlighter-rouge">id_token</code>（身分資訊）、<code class="language-plaintext highlighter-rouge">access_token</code>（資源存取權限）及可能的 <code class="language-plaintext highlighter-rouge">refresh_token</code></li>
    </ul>
  </li>
  <li><strong><code class="language-plaintext highlighter-rouge">/userinfo</code> (UserInfo Endpoint)</strong>
    <ul>
      <li>這是一個受保護的資源端點。當 Backend 拿到 <code class="language-plaintext highlighter-rouge">access_token</code> 後，可以向此端點請求更多關於使用者的詳細資料（如完整的 profile 或自定義 claims），
  這些資料可能因為體積太大而沒有直接放在 <code class="language-plaintext highlighter-rouge">id_token</code> 中。</li>
    </ul>
  </li>
  <li><strong><code class="language-plaintext highlighter-rouge">jwks_uri</code> (JWKS Endpoint)</strong>
    <ul>
      <li>回傳 <strong>JSON Web Key Set</strong>。這是一組公開金鑰，Backend 用它來驗證 <code class="language-plaintext highlighter-rouge">id_token</code> 的簽章。
  由於 IdP 可能會定期輪替（Rotate）金鑰，Backend 通常會快取這些金鑰並在簽章驗證失敗時重新抓取</li>
    </ul>
  </li>
</ul>

<h4 id="34-what-the-backend-should-verify">3.4 What the Backend Should Verify</h4>

<blockquote class="block-tip">
  <p>後端在驗證 OIDC 流程中收到的 <code class="language-plaintext highlighter-rouge">id_token</code> 時，必須嚴格按照 OIDC 的規定來驗證內容，這些驗證不是裝飾，而是確保安全性的核心防線</p>
</blockquote>

<p>OIDC 規格要求 Backend 驗證 <code class="language-plaintext highlighter-rouge">iss</code> 必須和 discovery 回來的 issuer 一致，<code class="language-plaintext highlighter-rouge">aud</code> 必須包含自己的 client_id，
<code class="language-plaintext highlighter-rouge">exp</code> 不能過期，<code class="language-plaintext highlighter-rouge">nonce</code> 也要對得上。這些不是裝飾，而是避免 token substitution、replay、和錯誤受眾使用的核心防線。</p>

<p>最實際的做法是：</p>

<ol>
  <li>用 library 讀 discovery document</li>
  <li>用 JWKS 驗證 <code class="language-plaintext highlighter-rouge">id_token</code></li>
  <li>檢查 <code class="language-plaintext highlighter-rouge">iss</code>、<code class="language-plaintext highlighter-rouge">aud</code>、<code class="language-plaintext highlighter-rouge">exp</code>、<code class="language-plaintext highlighter-rouge">nonce</code></li>
  <li>驗證通過後，只把你真正需要的 user identity 存進自己系統的 session</li>
</ol>

<blockquote class="block-warning">
  <p>這樣我們就能了解 OIDC 的整個流程，從前端的授權請求，到 IdP 的驗證與同意，再到後端的 token 交換與 session 建立，
每一步都有其安全考量與驗證要求，確保整個 SSO 流程的安全性與可靠性，後續會再寫一篇文章說明 NextAuth 的實作細節，
這些 Library 都是幫我們把這些驗證流程包裝起來，但理解底層的機制還是很重要的</p>
</blockquote>

<blockquote class="block-warning">
  <h5 id="last-edit">Last Edit</h5>
  <p>06-09-2026 23:44</p>
</blockquote>]]></content><author><name>Benson Hsu</name></author><category term="Jekyll" /><category term="software" /><category term="authentication" /><category term="oauth" /><category term="jwt" /><category term="sso" /><summary type="html"><![CDATA[這篇文章會從 SSO（Single Sign-On，單一登入）的基本概念開始介紹，接著說明 JWT 的結構，然後進入 OIDC（OpenID Connect）]]></summary></entry><entry><title type="html">Backend | Database Scale Out</title><link href="https://hotshot824.github.io/jekyll/2026-03-31-database_scale_out.html" rel="alternate" type="text/html" title="Backend | Database Scale Out" /><published>2026-03-31T00:00:00+00:00</published><updated>2026-03-31T00:00:00+00:00</updated><id>https://hotshot824.github.io/jekyll/database_scale_out</id><content type="html" xml:base="https://hotshot824.github.io/jekyll/2026-03-31-database_scale_out.html"><![CDATA[<blockquote class="block-tip">
  <p>後端處理中心的 Database Scale Out 是一個重要的議題，相較於 Web Application 無狀態的水平擴展，
Database 的水平擴展會困難的多，Database 需要維持 ACID 的特性。</p>
</blockquote>

<h3 id="1-overview">1. Overview</h3>

<blockquote class="block-tip">
  <p><a href="https://en.wikipedia.org/wiki/CAP_theorem">CAP Theorem</a> (Consistency, Availability, Partition Tolerance)</p>
</blockquote>

<p>既然在多個節點上使用 Database 也就是分散式儲存，對於分散式儲存的設計來說核心原則就是 <a href="https://en.wikipedia.org/wiki/CAP_theorem">CAP Theorem</a>，也就是說在分散式系統中，無法同時滿足一致性 (Consistency)、可用性 (Availability) 和分區容錯性 (Partition Tolerance)，只能選擇其中的兩個特性來優先考慮。</p>

<p><a href="https://en.wikipedia.org/wiki/ACID">ACID</a> 是 Database 在關聯式資料庫中維持資料一致性和可靠性的四個基本特性，分別是:</p>

<ol>
  <li>Atomicity (原子性): 交易中的所有操作要麼全部完成，要麼全部不完成
    <ul>
      <li>如果交易中的任何一個操作失敗，整個交易都會被回滾，資料庫將保持不變</li>
      <li>交易沒有中間狀態，要麼完全成功，要麼完全失敗</li>
    </ul>
  </li>
  <li>Consistency (一致性): 交易必須使資料庫從一個一致的狀態轉換到另一個一致的狀態
    <ul>
      <li>當前一個交易完成後，資料庫應該處於一致的狀態，下一個交易開始前，資料庫也應該處於一致的狀態</li>
    </ul>
  </li>
  <li>Isolation (隔離性): 交易的執行不應該受到其他交易的干擾
    <ul>
      <li>交易應該像是獨立執行的一樣，即使多個交易同時執行，也不應該互相干擾</li>
    </ul>
  </li>
  <li>Durability (持久性): 一旦交易完成，對資料庫的修改應該是永久性的，即使系統崩潰也不會丟失</li>
</ol>

<p>如果要把 Database 分散到多個節點上，要維持上面所說的特性非常困難，尤其是在分散式系統中，
網路延遲、節點失效、資料同步等問題都會對 ACID 的維持造成挑戰。</p>

<p>Database 遵從 <a href="https://en.wikipedia.org/wiki/ACID">ACID</a> 代表優先考慮資料庫的一致性而非可用性，當然也可以遵從 <a href="https://en.wikipedia.org/wiki/Eventual_consistency">BASE</a>，優先考慮可用性，資料只保證最終會達到一致性，
因此可能會有不一致的狀態存在，但這樣就不適合用在需要強一致性的場景，例如金融交易系統。</p>

<blockquote>
  <p><a href="https://en.wikipedia.org/wiki/Eventual_consistency">BASE</a> (Basically Available, Soft state, Eventual consistency) 強調系統的可用性和彈性，而不是強一致性</p>
</blockquote>

<blockquote class="block-warning">
  <p>我們可以說 BASE 是選擇 CAP 中的 AP 模式，而 ACID 是選擇 CP 模式</p>
</blockquote>

<hr />

<h3 id="2-master-slave-replication">2. Master-Slave Replication</h3>

<blockquote class="block-tip">
  <p>從資料庫的行為來看，可以分為讀取和寫入兩種操作，Master-Slave 就是針對這兩種需求的明確分工</p>
</blockquote>

<p>Master-Slave Replication 是一種常見的資料庫擴展策略，其中 Master 節點負責處理所有的寫入操作，
而 Slave 節點則負責處理讀取操作。在常見的 CRUD 操作中，讀取操作通常比寫入操作更頻繁，
因此這種架構可以有效地分散讀取負載，提高系統的整體性能。</p>

<p><img src="/image/2026/03-31-database_scale_out/1.jpg" alt="" /></p>

<blockquote>
  <p>當 Master 故障或不可用時，Slave 節點可以被提升為新的 Master 節點，以確保系統的可用性</p>
</blockquote>

<p><strong>Advantages:</strong></p>
<ul>
  <li>架構相對來說簡單，是比較容易實現的 Database Scale Out 策略</li>
  <li>所有節點的資料都是相同的，不需要修改資料模型</li>
</ul>

<p><strong>Disadvantages:</strong></p>
<ul>
  <li>寫入依然受限於 Master 節點的性能，無法真正實現寫入的水平擴展</li>
  <li>Slave 節點的資料是從 Master 節點複製過來因此可能會是 Replication Lag 的狀態</li>
</ul>

<p><strong>Use Cases:</strong></p>
<ul>
  <li>適用於讀取操作遠多於寫入操作的場景</li>
  <li>例如: 不需要即時性的查詢，統計報表、歷史資料查詢等</li>
</ul>

<p>在 MariaDB 通常使用 Binlog 來實現 Master-Slave Replication，Master 節點會將所有的寫入操作記錄在 Binlog 中，Slave 節點則會定期從 Master 節點拉取 Binlog 並執行其中的操作，以保持資料的一致性。</p>

<hr />

<h3 id="21-master-master-replication">2.1 Master-Master Replication</h3>

<blockquote class="block-tip">
  <p>既然 Master-Slave 的架構中寫入依然受限於 Master 節點的性能，那麼我們可以考慮讓多個 Master 節點同時處理寫入操作，這就是 Master-Master Replication</p>
</blockquote>

<p>雖然概念上多個 Master 好像就能解決寫入的瓶頸問題，但實際上 Master-Master Replication 的實現非常複雜，
多個 Writer 之間的資料同步和衝突解決是一個非常大的挑戰，尤其是在高併發的場景下，資料衝突的機率會大大增加。</p>

<ul>
  <li>多個 Master 節點之間的資料一致性非常困難，通常會違背 ACID 的特性或是為了同步而提高寫入的延遲</li>
  <li>越多的 Master 節點，只會導致延遲越高，資料衝突的機率越大</li>
</ul>

<p>在 Master-Slave 保持同步只要保持 Slave 從 Master 拉取 Binlog 就可以了，但在 Master-Master 的架構中有可能產生資料衝突，
例如兩個 Master 節點同時寫入相同的資料，這就需要一個機制來解決衝突，這是一大挑戰。如果處理得不好有可能效能比單一 Master 節點還差。</p>

<p><img src="/image/2026/03-31-database_scale_out/2.jpg" alt="" /></p>

<blockquote>
  <p>加入越多的 Master 節點，資料衝突的機率就會越大，延遲也會越高</p>
</blockquote>

<p>對於 Master-Master Replication 或者 Master-Slave Replication 的架構來說，都會有以下的缺點:</p>
<ul>
  <li>在 Copy 還沒完成之前如果節點故障了，有可能喪失資料</li>
  <li>有越多 Replication 節點，要複製的資料就越多，延遲也會越高</li>
  <li>如果 Master 節點的寫入量非常大，Slave 節點可能會無法跟上 Master 的寫入速度，導致 Replication Lag 的問題
    <ul>
      <li>在現代資料庫通常已支援 Multi-Threaded 並行複製來減少 Replication Lag 的問題，但在高併發的場景下仍然可能會有延遲</li>
      <li>Slave 也可能為了處理資料同步的問題導致當下無法提供太多的讀取服務</li>
    </ul>
  </li>
</ul>

<blockquote>
  <p>雖然透過增加 Replication 節點可以提升系統的可用性與讀取能力，但這並不代表系統可以無限制地擴展，增加過多的 Replication 節點可能會導致系統的性能下降</p>
</blockquote>

<blockquote>
  <p>後續我們將這種以 Replication 為基礎的 Database Scale Out 策略稱為 (M/S/R) 架構，因為它的核心概念就是 Master-Slave Replication</p>
</blockquote>

<hr />

<h3 id="3-federated-database">3. Federated Database</h3>

<p>上面的兩種方法都是以 Replication 的方式來實現 Database Scale Out，這種方式始終無法避免 CAP 中的 CP，
只要增加了 Replication 節點，就會增加資料同步的複雜度和延遲，所以如果想要降低資料同步的複雜度和延遲，我們可以考慮其他方式來分散資料庫的負載。</p>

<blockquote class="block-tip">
  <p><a href="https://en.wikipedia.org/wiki/Federated_database_system">Federated Database</a> (聯邦式資料庫系統) 將多個獨立的資料庫組合成一個邏輯資料庫</p>
</blockquote>

<p>Federated 的核心概念是依照功能來區分資料庫，例如一個線上購物平台可能有:</p>
<ul>
  <li>User Database: 儲存使用者的資料
    <ul>
      <li>帳號、密碼、個人資訊 …</li>
    </ul>
  </li>
  <li>Product Database: 儲存商品的資料
    <ul>
      <li>名稱、價格、庫存 …</li>
    </ul>
  </li>
  <li>Order Database: 儲存訂單的資料
    <ul>
      <li>訂單編號、商品清單、訂單狀態 …</li>
    </ul>
  </li>
</ul>

<p>而在這些獨立的資料庫下，還可以使用 Master-Slave 的架構來進行 Replication，以提升讀取的性能。
這樣的策略是避免 Centralized Database 的瓶頸問題，將資料分散到不同的資料庫中，讓每個資料庫專注於處理特定的功能，
同時有機會做到多個資料庫平行寫入 (Writer in parallel)，例如特定情境下只需要改變 User Database 的資料，
就不需要等待 Product Database 或 Order Database 的資料同步完成，這樣就可以提升吞吐量。</p>

<p><img src="/image/2026/03-31-database_scale_out/3.jpg" alt="" /></p>

<p><strong>Advantages:</strong></p>
<ul>
  <li>依照 Domain 來分散資料庫的負載，讓每個資料庫專注於處理特定的功能</li>
  <li>可以做到多個資料庫平行寫入，提升吞吐量
    <ul>
      <li>在低藕荷的情況下，吞吐量可以隨著資料庫的增加而線性提升</li>
      <li>避免單一資料庫的瓶頸問題，提升系統的可用性和擴展性</li>
    </ul>
  </li>
  <li>降低資料同步的複雜度和延遲，因為每個資料庫只需要處理特定的功能，資料之間的依賴性較低</li>
  <li>故障隔離性較好，當某個資料庫發生故障時，不會影響到其他資料庫的運作</li>
  <li>符合 Domain-Driven Design 的原則，讓資料庫的設計更符合業務需求
    <ul>
      <li>也可以說是一種 Microservices 的資料庫設計</li>
    </ul>
  </li>
  <li>資料庫之間可以異質化，例如加入 NoSQL 的資料庫來處理特定的資料類型，提升系統的彈性和適應性</li>
</ul>

<p><strong>Disadvantages:</strong></p>
<ul>
  <li>跨資料庫 JOIN 的效率非常差，無法像單一資料庫那樣進行高效的 JOIN 操作，這可能會導致查詢性能的下降
    <ul>
      <li>例如 <code class="language-plaintext highlighter-rouge">JOIN user, order, product</code></li>
    </ul>
  </li>
  <li>如果要實現 ACID 的特性，跨資料庫的交易會非常複雜，因為需要確保多個資料庫之間的資料一致性，這可能會導致性能的下降
    <ul>
      <li>2-Phase Commit (2PC), Saga Pattern</li>
    </ul>
  </li>
  <li>查詢效能不穩定，因為跨資料庫的查詢需要進行資料傳輸和整合，這可能會導致查詢的延遲和不穩定性
    <ul>
      <li>最慢的資料庫會成為整個查詢的瓶頸，導致查詢性能的下降</li>
      <li>在中間層進行資料整合的過程中，需要使用 CPU / Memory 來處理資料</li>
    </ul>
  </li>
  <li>系統複雜性增加，因為需要管理多個資料庫的運作和維護
    <ul>
      <li>Query Routing, Schema Mapping, Service Boundary, Failure handling, Observability …</li>
    </ul>
  </li>
  <li>資料模型的設計要更加謹慎
    <ul>
      <li>必須避免大量的 Cross-domain dependency</li>
      <li>常見策略是 denormalization / data duplication 在不同的資料庫中儲存相同的資料，但會增加資料同步的成本</li>
    </ul>
  </li>
</ul>

<blockquote>
  <p>在單一資料庫中做 JOIN 是非常高效的，最大的問題是如果資料量大，在 JOIN 之前勢必得將資料庫抓取至某個節點上進行 JOIN 的操作，
這樣資料傳輸就是無法避免的成本消耗，如何設計 JOIN 的策略也是一個重要的議題</p>
</blockquote>

<p><strong>Use Cases:</strong></p>
<ul>
  <li>Business Logic 可以明確區分 Domain 的場景</li>
  <li>多數請求只需要存取特定資料庫的場景，例如 User Database 的請求只需要存取 User Database 的資料
    <ul>
      <li>不需要大量的 JOIN 操作，例如 OLAP (Online Analytical Processing) 的場景</li>
    </ul>
  </li>
  <li>不需要 Strong Consistency 的場景，例如金融核心業務就不適合使用 Federated Database 的架構</li>
</ul>

<blockquote>
  <p>本質上 Federated Database 是用系統複雜度 (Complexity) 與一致性 (Consistency) 來換取寫入分流 (Write Scalability) 與解耦合 (Decoupling)</p>
</blockquote>

<hr />

<h3 id="4-sharding">4. Sharding</h3>

<blockquote class="block-tip">
  <p><a href="https://en.wikipedia.org/wiki/Shard_(database_architecture)">Sharding</a> 是另一種常見的 Database Scale Out 策略，與 Federated Database 不同的是，Sharding 是將資料水平切分 (Horizontal Partitioning)，
將資料分散到多個節點上，所以每個節點上都會有相同的資料模型</p>
</blockquote>

<p>Sharding (分片) 與 Federated Database 很類似，最主要的差異是 Sharding 是同質化的分散，例如我們有一個 User Database，
我們可以依照 User ID 來將資料分散到不同的 Shard 上，例如:</p>
<ul>
  <li>Shard 1: User ID 1-1000</li>
  <li>Shard 2: User ID 1001-2000</li>
  <li>Shard 3: User ID 2001-3000</li>
</ul>

<p>這樣可以讓每個 Shard 專注於處理特定範圍的資料，提升系統的可用性和擴展性，同時也可以做到多個 Shard 平行寫入，提升吞吐量。
常見的作法是依照用戶的地理位址或者姓名、ID 來作為 Sharding 的依據。</p>

<p><img src="/image/2026/03-31-database_scale_out/4.jpg" alt="" /></p>

<blockquote>
  <p>Sharding 的核心挑戰在於如何設計一個好的 Shard key，這需要對業務和資料的特性有深入的理解，選擇一個好的 Shard key 可以讓資料分布均勻，
提升系統的性能和可用性</p>
</blockquote>

<p><strong>Advantages:</strong></p>
<ul>
  <li>水平擴展能力強，可以隨著 Shard 的增加而線性提升系統的性能
    <ul>
      <li>如果每個 Shard 的資料量較小，可以提升寫入與查詢的性能</li>
    </ul>
  </li>
  <li>儲存成本低，可以分散儲存資料到多個節點上，避免單一節點的儲存大量資料</li>
  <li>同時每個 Shard 都可以配合 (M/S/R) 的架構來提升讀取的性能</li>
  <li>Less Replication，因為每個 Shard 的資料量較小，可以減少 Replication 的成本和延遲</li>
</ul>

<p><strong>Disadvantages:</strong></p>
<ul>
  <li>跨 Shard 的 JOIN 非常複雜，因為資料分散在不同的 Shard 上，這可能會導致查詢性能的下降
    <ul>
      <li>這是 Federated Database 和 Sharding 共同的問題，跨節點的 JOIN 都會有性能的問題</li>
    </ul>
  </li>
  <li>Shard key 設計困難，選擇一個好的 Shard key 是非常重要的，因為它會影響到資料的分布和查詢的性能
    <ul>
      <li>可能會導致資料分布不均，某些 Shard 的負載過重，而其他 Shard 的負載過輕，這會導致性能的下降</li>
      <li>例如台灣人姓氏分布不均，如果以姓氏作為 Shard key，可能會導致熱門姓氏的 Shard 負載過重</li>
    </ul>
  </li>
  <li>Resharding 的成本高，當資料量增加或者 Shard key 的設計不合理時，可能需要進行 Resharding，也就是重新分配資料到不同的 Shard 上，這是一個非常複雜和昂貴的過程
    <ul>
      <li>需要停機維護，或者使用 Online Resharding 的方式來減少停機時間，但仍然會有性能的影響</li>
    </ul>
  </li>
</ul>

<p><strong>Use Cases:</strong></p>
<ul>
  <li>資料量極大的場景，單機資料庫無法承受的場景
    <ul>
      <li>需要高寫入性能的場景，例如社交媒體平台、線上遊戲等</li>
      <li>查詢有明確的 Shard key 的場景，例如用戶 ID、地理位置等</li>
    </ul>
  </li>
  <li>同樣不適合需要大量 Cross-Shard JOIN 的場景，例如 OLAP 的場景</li>
  <li>在 Strong Consistency 不重要的場景，例如社交媒體平台的貼文和留言等</li>
</ul>

<blockquote class="block-warning">
  <p>要注意 Shard 適合的是單一資料表資料量太大，如果確定瓶頸在某個資料表的話，才適合使用 Sharding 的策略，如果是整個資料庫的負載過重，
可能就不適合使用 Sharding 的策略，反而是 Federated Database 的策略比較適合</p>
</blockquote>

<h4 id="41-rebalancing">4.1 Rebalancing</h4>

<blockquote class="block-tip">
  <p>Sharding 的一大挑戰就是當資料量增加或者 Shard key 的設計不合理時，可能需要進行 Rebalancing，也就是重新分配資料到不同的 Shard</p>
</blockquote>

<p>Rebalancing 最主要的目的就是讓資料分布更均勻，Shard 有可能因為 Key 設計或者運行一段時間後，由於資料分布不均導致 Shard 成為新的瓶頸，
Rebalancing 就是將資料重新分配到不同的 Shard 上，讓每個 Shard 的負載更均勻，提升系統的性能和可用性。</p>

<blockquote>
  <p>即使最初 Key 設計得再好，隨著時間的推移，資料庫在運行中其內容也會發生變化，這可能會導致資料分布不均，因此 Rebalancing 是 Sharding 架構中不可避免的一個過程</p>
</blockquote>

<p>這些 Rebalance 方法這裡用分類策略來簡單非為兩類:</p>
<ol>
  <li>Static Rebalancing:
    <ul>
      <li>分類規則是固定的，如果 Shard 改變要重新設計資料分布</li>
      <li>例如使用 Hash 來決定資料要如何分布，當 Hash 改變時就需要重新分布資料</li>
      <li>Full Resharding, Range-based Rebalancing, Hash-based Rebalancing
        <ul>
          <li>比較早期的做法，目前大型系統很少直接使用，部分做法需要停機維護，對於線上服務來說是非常不友善的</li>
        </ul>
      </li>
    </ul>
  </li>
  <li>Dynamic Rebalancing:
    <ul>
      <li>分類規則是動態的，根據資料的特性來決定資料要如何分布</li>
      <li>例如使用 Consistent Hashing 來決定資料要如何分布，當 Shard 改變時只需要重新分布部分資料</li>
      <li>Consistent Hashing, Virtual Nodes</li>
      <li><strong>Auto-Sharding</strong> (MongoDB sharding cluster, DynamoDB fully managed partitioning, etc.)
        <ul>
          <li>目前大型系統常用的做法，可以減少 Rebalancing 的成本和影響，適合線上服務的需求</li>
        </ul>
      </li>
    </ul>
  </li>
</ol>

<p>實際上這種分類是很粗略的，因為 Rebalancing 畢竟是一個分類問題，實際上可以使用的演算法策略非常多，用制式想法來解決 Rebalancing 的問題是非常呆版的，
需要根據業務和資料的特性來設計一個適合的 Rebalancing 策略，這也是 Sharding 架構中非常重要的一個議題。</p>

<blockquote>
  <p>目前很多主流 Database 都已經支援 Auto-Sharding 的功能，這些 Database 會自動根據資料的特性來決定資料要如何分布，並且在需要 Rebalancing 的時候自動進行 Rebalancing</p>
</blockquote>

<h5 id="411-consistent-hashing">4.1.1 <a href="https://en.wikipedia.org/wiki/Consistent_hashing">Consistent Hashing</a></h5>

<p>Consistent Hashing 是一種常見的 Dynamic Rebalancing 的策略，主要的概念是將資料和 Shard 映射到一個環狀的 Hash 空間中，當 Shard 改變時，只需要重新分布部分資料，而不需要重新分布所有資料，這樣就可以減少 Rebalancing 的成本和影響。每次重新分布只需要對 K/n 的資料進行重新分布，其中 K 是資料的總量，n 是 Shard 的數量。</p>

<div style="display: flex; flex-direction: row; align-items: center;">
    <img src="/image/2026/03-31-database_scale_out/5.jpg" width="50%" height="50%" />
    <img src="/image/2026/03-31-database_scale_out/6.jpg" width="50%" height="50%" />
</div>

<p><br /></p>

<p>如上圖我們把 Consistent Hashing 想像成一個環形的 Hash 空間，BLOB 在計算完 Hash 後會儲存在 139 這個 Shard 上，
假如我們之後要擴充 139 Shard 的容量，我們可以新增一個 Shard 170，這樣只有 Hash 值在 139 和 170 之間的資料需要重新分布到 170 上，
其他資料則不需要重新分布，這樣就可以減少 Rebalancing 的成本和影響。</p>

<blockquote class="block-warning">
  <h5 id="last-edit">Last Edit</h5>
  <p>03-31-2026 17:04</p>
</blockquote>]]></content><author><name>Benson Hsu</name></author><category term="Jekyll" /><category term="software" /><category term="database" /><summary type="html"><![CDATA[後端處理中心的 Database Scale Out 是一個重要的議題，相較於 Web Application 無狀態的水平擴展， Database 的水平擴展會困難的多，Database 需要維持 ACID 的特性。]]></summary></entry><entry><title type="html">Backend | Swagger</title><link href="https://hotshot824.github.io/jekyll/2026-02-05-swagger.html" rel="alternate" type="text/html" title="Backend | Swagger" /><published>2026-02-05T00:00:00+00:00</published><updated>2026-02-05T00:00:00+00:00</updated><id>https://hotshot824.github.io/jekyll/swagger</id><content type="html" xml:base="https://hotshot824.github.io/jekyll/2026-02-05-swagger.html"><![CDATA[<blockquote class="block-tip">
  <p>Swagger 是一個非常流行的工具，可以幫助我們以 Specification First 的方式來開發 RESTful API，
Swagger 允許我們使用 YAML 或 JSON 來定義 API 的規格，然後我們可以直接透過 Specification 來產生 Controller 的程式碼，
這樣就能夠讓 API 的定義和實現分離，提升開發效率和 API 的可維護性。</p>
</blockquote>

<blockquote class="block-warning">
  <p>目前 <a href="https://swagger.io/">Swagger</a> 已經更名為 <a href="https://www.openapis.org/">OpenAPI</a>，不過很多人還是習慣稱呼 Swagger，所以兩種名稱都可以使用</p>
</blockquote>

<h3 id="1-overview">1. Overview</h3>

<blockquote class="block-tip">
  <p>依照 Specification First 的方式來開發 RESTful，也可以算是 Specification Driven Development (SDD) 的實踐方式</p>
</blockquote>

<p>如果我們要開發一個 Backend 的 RESTful API，主要有兩種做法:</p>

<ol>
  <li><strong>Code First:</strong>
    <ul>
      <li>我們直接到後端框架中撰寫 Controller 的程式碼，然後在程式碼中使用註解的方式來定義 API 的規格，
例如使用 Spring Boot 的 <code class="language-plaintext highlighter-rouge">@RestController</code> 和 <code class="language-plaintext highlighter-rouge">@RequestMapping</code> 等註解來定義 API 的路徑、HTTP 方法、參數等等</li>
      <li>優點是開發速度快，因為我們直接在程式碼中定義 API 的規格，不需要額外的工具來管理 API 的規格</li>
      <li>有可能導致 API 設計不一致，Docs 不完整或者導致前後端耦合</li>
    </ul>
  </li>
  <li><strong>Specification First:</strong>
    <ul>
      <li>我們先使用 Swagger 的 YAML 或 JSON 來定義 API 的規格，然後透過 Swagger 的工具來產生 Controller 的程式碼，
這樣就能夠讓 API 的定義和實現分離，提升開發效率和 API 的可維護性</li>
      <li>優點是前後端可以同時進行開發，前後端都根據 API 的規格來進行開發</li>
      <li>缺點是需要額外的工具來管理 API 的規格，並且在開發過程中需要保持 API 的規格和實現的一致性</li>
    </ul>
  </li>
</ol>

<p>目前如果是大型專案或者 Microservice 的架構，通常會傾向使用 Specification First 的方式來開發 RESTful API，
因為這樣可以讓 API 的定義更加清晰，並且能夠更好地管理 API 的版本和變更。
而在中小型專案或者是快速開發的情況下，可能會傾向使用 Code First 的方式來開發 RESTful API，
因為這樣可以更快地開始開發，不需要額外的工具來管理 API 的規格。</p>

<h4 id="11-swagger-example">1.1 Swagger Example</h4>

<p>我們來設計一個簡單的範例，假如我們有一個 User Registration 的 API，我們可以使用 Swagger 的 YAML 來定義 API 的規格，如下所示:</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">openapi</span><span class="pi">:</span> <span class="s">3.0.0</span>
<span class="na">info</span><span class="pi">:</span>
  <span class="na">title</span><span class="pi">:</span> <span class="s">User Registration API</span>
  <span class="na">description</span><span class="pi">:</span> <span class="s">API for user registration</span>
  <span class="na">version</span><span class="pi">:</span> <span class="s">1.0.0</span>
<span class="na">servers</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">url</span><span class="pi">:</span> <span class="s">http://example.com/v1</span>
    <span class="na">description</span><span class="pi">:</span> <span class="s">Production server</span>
  <span class="pi">-</span> <span class="na">url</span><span class="pi">:</span> <span class="s">http://test.example.com/v1</span>
    <span class="na">description</span><span class="pi">:</span> <span class="s">Test server</span>
<span class="na">paths</span><span class="pi">:</span>
  <span class="na">/users</span><span class="pi">:</span>
    <span class="na">post</span><span class="pi">:</span>
      <span class="na">summary</span><span class="pi">:</span> <span class="s">Register a new user</span>
      <span class="na">requestBody</span><span class="pi">:</span>
        <span class="na">required</span><span class="pi">:</span> <span class="kc">true</span>
        <span class="na">content</span><span class="pi">:</span>
          <span class="na">application/json</span><span class="pi">:</span>
            <span class="na">schema</span><span class="pi">:</span>
              <span class="na">type</span><span class="pi">:</span> <span class="s">object</span>
              <span class="na">properties</span><span class="pi">:</span>
                <span class="na">username</span><span class="pi">:</span>
                  <span class="na">type</span><span class="pi">:</span> <span class="s">string</span>
                <span class="na">email</span><span class="pi">:</span>
                  <span class="na">type</span><span class="pi">:</span> <span class="s">string</span>
                <span class="na">password</span><span class="pi">:</span>
                  <span class="na">type</span><span class="pi">:</span> <span class="s">string</span>
      <span class="na">responses</span><span class="pi">:</span>
        <span class="na">201</span><span class="pi">:</span>
          <span class="na">description</span><span class="pi">:</span> <span class="s">User created successfully</span>
        <span class="na">400</span><span class="pi">:</span>
          <span class="na">description</span><span class="pi">:</span> <span class="s">Bad request</span>
</code></pre></div></div>

<p>這是一個很簡單的 OpenAPI 的定義，我們從上往下逐一來解釋一下這個定義的內容:</p>

<ol>
  <li><code class="language-plaintext highlighter-rouge">info</code>: API 的基本資訊，包括 API 的標題、描述和版本號</li>
  <li><code class="language-plaintext highlighter-rouge">servers</code>: API 的伺服器資訊，包括伺服器的 URL 和描述
    <ul>
      <li>這裡有兩個 Server 一個用於正式環境，一個用於測試環境</li>
      <li>通常是給 Client 來選擇要使用哪個 Server 來呼叫 API</li>
    </ul>
  </li>
  <li><code class="language-plaintext highlighter-rouge">paths</code>: API 的路徑和 HTTP 方法
    <ul>
      <li><code class="language-plaintext highlighter-rouge">/users</code>: 這個路徑的 API 並且只有 POST 方法</li>
      <li><code class="language-plaintext highlighter-rouge">requestBody</code>: POST 方法的 Body 的內容，必須為 JSON 格式
        <ul>
          <li>包含 <code class="language-plaintext highlighter-rouge">username</code>, <code class="language-plaintext highlighter-rouge">email</code>, <code class="language-plaintext highlighter-rouge">password</code> 這三個欄位</li>
        </ul>
      </li>
    </ul>
  </li>
  <li><code class="language-plaintext highlighter-rouge">responses</code>: API 的回應，包括成功和失敗的回應
    <ul>
      <li>成功的回應是 201，表示使用者創建成功</li>
      <li>失敗的回應是 400，表示請求錯誤</li>
    </ul>
  </li>
</ol>

<p>這樣我們透過 Swagger 可以很清楚的知道 API 的規格為 <code class="language-plaintext highlighter-rouge">POST /users</code>，並且知道這個 API 的請求和回應的格式，
這樣就能夠讓前後端都根據這個規格來進行開發。</p>

<hr />

<h3 id="2-openapi-to-spring-boot-controller">2. OpenAPI to Spring Boot Controller</h3>

<blockquote class="block-tip">
  <p>實際開發直接將 OpenAPI 的定義轉換成 Spring Boot 的 Controller 程式碼，這樣能確保規格和實現的一致性</p>
</blockquote>

<blockquote>
  <p><a href="https://openapi-generator.tech/">OpenAPI Generator</a> 是轉換 OpenAPI 定義成程式碼的工具，支援多種語言和框架，包括 Java 的 Spring</p>
</blockquote>

<p>那我們要先了解那些部分是規格可以定義然後直接轉換成 Controller 的程式碼，主要可以分為以下轉換:</p>
<ol>
  <li><code class="language-plaintext highlighter-rouge">paths</code> 的部分可以轉換成 Controller 的路徑和 HTTP 方法</li>
  <li><code class="language-plaintext highlighter-rouge">components/schemas</code> 的部分可以轉換成 <a href="https://en.wikipedia.org/wiki/Data_transfer_object">DTO</a> 的類別定義</li>
</ol>

<blockquote>
  <p><a href="https://en.wikipedia.org/wiki/Data_transfer_object">DTO</a> 是一種用於在不同層之間傳遞資料的物件，通常用於將資料從 Controller 層傳遞到 Service 層，或者從 Service 層傳遞到 Repository 層，
透過 DTO 我們可以避免直接使用實體類別來傳遞資料，這樣可以避免實體類別的變更對其他層造成影響，並且可以更好地控制資料的格式和內容。</p>
</blockquote>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">openapi</span><span class="pi">:</span> <span class="s">3.0.3</span>

<span class="na">info</span><span class="pi">:</span>
  <span class="na">title</span><span class="pi">:</span> <span class="s">User Registration API</span>
  <span class="na">version</span><span class="pi">:</span> <span class="s">1.0.0</span>

<span class="na">paths</span><span class="pi">:</span>
  <span class="na">/</span><span class="pi">:</span>
    <span class="na">get</span><span class="pi">:</span>
      <span class="na">summary</span><span class="pi">:</span> <span class="s">Root health endpoint</span>
      <span class="na">operationId</span><span class="pi">:</span> <span class="s">getRoot</span>
      <span class="na">responses</span><span class="pi">:</span>
        <span class="na">200</span><span class="pi">:</span>
          <span class="na">description</span><span class="pi">:</span> <span class="s">Hello message</span>
          <span class="na">content</span><span class="pi">:</span>
            <span class="na">application/json</span><span class="pi">:</span>
              <span class="na">schema</span><span class="pi">:</span>
                <span class="na">$ref</span><span class="pi">:</span> <span class="s1">'</span><span class="s">#/components/schemas/RootResponse'</span>

<span class="na">components</span><span class="pi">:</span>
    <span class="na">schemas</span><span class="pi">:</span>
        <span class="na">RootResponse</span><span class="pi">:</span>
            <span class="na">type</span><span class="pi">:</span> <span class="s">object</span>
            <span class="na">properties</span><span class="pi">:</span>
                <span class="na">message</span><span class="pi">:</span>
                    <span class="na">type</span><span class="pi">:</span> <span class="s">string</span>
</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">$ref</code> 是 OpenAPI 中用來引用其他部分定義的語法，透過 <code class="language-plaintext highlighter-rouge">$ref</code> 我們可以在 OpenAPI 定義中引用其他部分的定義
    <ul>
      <li>也可以引用多個檔案，例如 <code class="language-plaintext highlighter-rouge">./schemas.yaml</code> 中定義了 <code class="language-plaintext highlighter-rouge">RootResponse</code> 的 schema</li>
      <li>使用 <code class="language-plaintext highlighter-rouge">$ref: './schemas.yaml#/RootResponse'</code> 來引用這個 schema</li>
    </ul>
  </li>
  <li>這裡我們指定了 <code class="language-plaintext highlighter-rouge">200</code> 的回應內容為 <code class="language-plaintext highlighter-rouge">RootResponse</code> 的 schema，這樣在轉換成 Controller 程式碼時，
就會知道這個 API 的回應格式是 <code class="language-plaintext highlighter-rouge">RootResponse</code> 這個 DTO 類別</li>
</ul>

<h4 id="21-spring-boot-file-structure">2.1 Spring Boot File Structure</h4>

<blockquote class="block-warning">
  <p>這裡我就不準備現成環境了，可以參考 <a href="https://github.com/hendisantika/spring-boot-swagger/tree/master">Spring Boot Swagger Example</a> 這個專案的結構來準備一個 Spring Boot 的專案結構，
不過他的版本有點久，所以自己架設也是很簡單的。</p>
</blockquote>

<p>讓我們準備好一個 Spring Boot 的專案結構，如下所示:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">.</span>
├── HELP.md
├── mvnw
├── mvnw.cmd
├── pom.xml
├── src ── main ── resources ── openapi ── openapi.yaml
└── target ── generated-sources
</code></pre></div></div>

<ul>
  <li>Swagger 相關的 yaml 通常被放置在 <code class="language-plaintext highlighter-rouge">src/main/resources/openapi</code> 這個目錄下</li>
  <li>產生後的檔案被放置在 <code class="language-plaintext highlighter-rouge">target/generated-sources</code> 這個目錄下</li>
</ul>

<p>然後去設定 <code class="language-plaintext highlighter-rouge">pom.xml</code> 來使用 OpenAPI Generator 的 Maven Plugin，如下:</p>

<div class="language-xml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;build&gt;</span>
    <span class="nt">&lt;plugins&gt;</span>
        <span class="nt">&lt;plugin&gt;</span>
            <span class="nt">&lt;groupId&gt;</span>org.openapitools<span class="nt">&lt;/groupId&gt;</span>
            <span class="nt">&lt;artifactId&gt;</span>openapi-generator-maven-plugin<span class="nt">&lt;/artifactId&gt;</span>
            <span class="nt">&lt;version&gt;</span>7.5.0<span class="nt">&lt;/version&gt;</span>
            <span class="nt">&lt;executions&gt;</span>
                <span class="nt">&lt;execution&gt;</span>
                    <span class="nt">&lt;goals&gt;</span>
                        <span class="nt">&lt;goal&gt;</span>generate<span class="nt">&lt;/goal&gt;</span>
                    <span class="nt">&lt;/goals&gt;</span>
                    <span class="nt">&lt;configuration&gt;</span>
                        <span class="nt">&lt;inputSpec&gt;</span>${project.basedir}/src/main/resources/openapi/openapi.yaml<span class="nt">&lt;/inputSpec&gt;</span>
                        <span class="nt">&lt;generatorName&gt;</span>spring<span class="nt">&lt;/generatorName&gt;</span>
                        <span class="nt">&lt;library&gt;</span>spring-boot<span class="nt">&lt;/library&gt;</span>
                        <span class="nt">&lt;output&gt;</span>${project.build.directory}/generated-sources/openapi<span class="nt">&lt;/output&gt;</span>
                        <span class="nt">&lt;apiPackage&gt;</span>com.example.demo.api<span class="nt">&lt;/apiPackage&gt;</span>
                        <span class="nt">&lt;modelPackage&gt;</span>com.example.demo.model<span class="nt">&lt;/modelPackage&gt;</span>
                        <span class="nt">&lt;configOptions&gt;</span>
                            <span class="nt">&lt;interfaceOnly&gt;</span>true<span class="nt">&lt;/interfaceOnly&gt;</span>
                            <span class="nt">&lt;useSpringBoot3&gt;</span>true<span class="nt">&lt;/useSpringBoot3&gt;</span>
                            <span class="nt">&lt;useTags&gt;</span>true<span class="nt">&lt;/useTags&gt;</span>
                        <span class="nt">&lt;/configOptions&gt;</span>
                    <span class="nt">&lt;/configuration&gt;</span>
                <span class="nt">&lt;/execution&gt;</span>
            <span class="nt">&lt;/executions&gt;</span>
        <span class="nt">&lt;/plugin&gt;</span>
    <span class="nt">&lt;/plugins&gt;</span>
<span class="nt">&lt;/build&gt;</span>
</code></pre></div></div>

<p>之後就能在 <code class="language-plaintext highlighter-rouge">build</code> 的過程中自動產生 Controller 的程式碼，但這裡要注意產生的只有 <code class="language-plaintext highlighter-rouge">interface</code> 的程式碼，
因此還是需要自己透過 <code class="language-plaintext highlighter-rouge">implementation</code> 的方式來實現 Controller 的邏輯。</p>

<p><strong>./target/generated-sources/openapi/…/RootApi.java</strong>:</p>
<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">package</span> <span class="nn">com.example.demo.api</span><span class="o">;</span>

<span class="cm">/** ... **/</span>

<span class="nd">@Validated</span>
<span class="nd">@Tag</span><span class="o">(</span><span class="n">name</span> <span class="o">=</span> <span class="s">"root"</span><span class="o">,</span> <span class="n">description</span> <span class="o">=</span> <span class="s">"the root API"</span><span class="o">)</span>
<span class="kd">public</span> <span class="kd">interface</span> <span class="nc">RootApi</span> <span class="o">{</span>
    
    <span class="cm">/** ... **/</span>

    <span class="nd">@RequestMapping</span><span class="o">(</span><span class="n">method</span> <span class="o">=</span> <span class="nc">RequestMethod</span><span class="o">.</span><span class="na">GET</span><span class="o">,</span> <span class="n">value</span> <span class="o">=</span> <span class="s">"/"</span><span class="o">,</span> <span class="n">produces</span> <span class="o">=</span> <span class="o">{</span><span class="s">"application/json"</span><span class="o">})</span>

    <span class="k">default</span> <span class="nc">ResponseEntity</span><span class="o">&lt;</span><span class="nc">RootResponse</span><span class="o">&gt;</span> <span class="nf">getRoot</span><span class="o">()</span> <span class="o">{</span>
        <span class="n">getRequest</span><span class="o">().</span><span class="na">ifPresent</span><span class="o">(</span><span class="n">request</span> <span class="o">-&gt;</span> <span class="o">{</span>
            <span class="k">for</span> <span class="o">(</span><span class="nc">MediaType</span> <span class="n">mediaType</span> <span class="o">:</span> <span class="nc">MediaType</span><span class="o">.</span><span class="na">parseMediaTypes</span><span class="o">(</span><span class="n">request</span><span class="o">.</span><span class="na">getHeader</span><span class="o">(</span><span class="s">"Accept"</span><span class="o">)))</span> <span class="o">{</span>
                <span class="k">if</span> <span class="o">(</span><span class="n">mediaType</span><span class="o">.</span><span class="na">isCompatibleWith</span><span class="o">(</span><span class="nc">MediaType</span><span class="o">.</span><span class="na">valueOf</span><span class="o">(</span><span class="s">"application/json"</span><span class="o">)))</span> <span class="o">{</span>
                    <span class="nc">String</span> <span class="n">exampleString</span> <span class="o">=</span> <span class="s">"{ \"message\" : \"message\" }"</span><span class="o">;</span>
                    <span class="nc">ApiUtil</span><span class="o">.</span><span class="na">setExampleResponse</span><span class="o">(</span><span class="n">request</span><span class="o">,</span> <span class="s">"application/json"</span><span class="o">,</span> <span class="n">exampleString</span><span class="o">);</span>
                    <span class="k">break</span><span class="o">;</span>
                <span class="o">}</span>
            <span class="o">}</span>
        <span class="o">});</span>
        <span class="k">return</span> <span class="k">new</span> <span class="nc">ResponseEntity</span><span class="o">&lt;&gt;(</span><span class="nc">HttpStatus</span><span class="o">.</span><span class="na">NOT_IMPLEMENTED</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>Controller 依然是由開發者透過 <strong>implementation</strong> 與 <strong>Override</strong> 的方式來實現的，
同時我們也有了 DTO 的類別定義，這裡的 <code class="language-plaintext highlighter-rouge">RootResponse</code> 就是從 OpenAPI 定義中轉換過來的 DTO 類別。</p>

<p><strong>./src/main/java/…/RootApiController.java</strong>:</p>
<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">package</span> <span class="nn">com.example.demo.controller</span><span class="o">;</span>

<span class="cm">/** ... **/</span>

<span class="nd">@RestController</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">RootApiController</span> <span class="kd">implements</span> <span class="nc">RootApi</span> <span class="o">{</span>

    <span class="nd">@Override</span>
    <span class="kd">public</span> <span class="nc">ResponseEntity</span><span class="o">&lt;</span><span class="nc">RootResponse</span><span class="o">&gt;</span> <span class="nf">getRoot</span><span class="o">()</span> <span class="o">{</span>
        <span class="nc">RootResponse</span> <span class="n">response</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">RootResponse</span><span class="o">().</span><span class="na">message</span><span class="o">(</span><span class="s">"hello, word"</span><span class="o">);</span>
        <span class="k">return</span> <span class="nc">ResponseEntity</span><span class="o">.</span><span class="na">ok</span><span class="o">(</span><span class="n">response</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>以上就是我們如何從 OpenAPI 的定義來產生 Spring Boot Controller 的程式碼，這份 OpenAPI 不只能用於產生 Controller 的程式碼，
也能用於產生 Client 的程式碼，或者是用於產生 API 的文件。</p>

<blockquote class="block-danger">
  <p>注意一個原則，新手常會犯的錯誤是去修改了自動產生的程式碼，應該要把這些自動產生的程式碼當成是可以被覆蓋的，
然後在自己的 Controller 中去實現這些程式碼的邏輯，不管是 Swagger 或者其他工具都是一樣的原則</p>
</blockquote>

<blockquote class="block-warning">
  <p>這樣我們就大致了解了 Swagger 的使用方式，以及如何從 OpenAPI 的定義來產生 Spring Boot Controller 的程式碼，
下次開發新服務時就可以考慮使用 Specification First 的方式來開發 RESTful API。</p>
</blockquote>

<blockquote class="block-warning">
  <h5 id="last-edit">Last Edit</h5>
  <p>02-07-2026 18:32</p>
</blockquote>]]></content><author><name>Benson Hsu</name></author><category term="Jekyll" /><category term="software" /><category term="restful api" /><category term="swagger" /><summary type="html"><![CDATA[Swagger 是一個非常流行的工具，可以幫助我們以 Specification First 的方式來開發 RESTful API， Swagger 允許我們使用 YAML 或 JSON 來定義 API 的規格，然後我們可以直接透過 Specification 來產生 Controller 的程式碼， 這樣就能夠讓 API 的定義和實現分離，提升開發效率和 API 的可維護性。]]></summary></entry><entry><title type="html">Pattern | Inversion of Control</title><link href="https://hotshot824.github.io/jekyll/2026-02-02-inversion_of_control.html" rel="alternate" type="text/html" title="Pattern | Inversion of Control" /><published>2026-02-02T00:00:00+00:00</published><updated>2026-02-02T00:00:00+00:00</updated><id>https://hotshot824.github.io/jekyll/inversion_of_control</id><content type="html" xml:base="https://hotshot824.github.io/jekyll/2026-02-02-inversion_of_control.html"><![CDATA[<blockquote class="block-tip">
  <p><a href="https://en.wikipedia.org/wiki/Inversion_of_control">Inversion of Control</a> (IoC) 具體來說是一種 Design Principle，而不是 Design Pattern，IoC 的核心思想是將對象的控制權從傳統的流程控制轉移到外部容器或框架，
這樣可以實現更好的模組化、可測試性和靈活性。而 Design Pattern 是更具體的實現 IoC 的方式，例如 Dependency Injection 就是一種實現 IoC 的 Design Pattern。</p>
</blockquote>

<blockquote class="block-warning">
  <p>這篇文章偏向概念性的說明 IoC 的核心思想，以及如何透過一些簡單的範例來實現 IoC</p>
</blockquote>

<blockquote>
  <p>不過如果沒有真的實踐過，其實對於 IoC 的理解可能會流於表面，會覺得 IoC 就是把控制權交給框架或容器，而沒辦法理解這樣做的好處</p>
</blockquote>

<h3 id="1-overview">1. Overview</h3>

<p>首先我們先理解字面意思上「Inversion of Control」的意思，這個反轉的意思其實是基於傳統的程式設計模式，
其控制權發生了「Inversion」，也就是說傳統的程式設計模式中，對象的控制權通常是由程式碼本身來掌握的，而在 IoC 中，這個控制權被反轉了，
由外部的容器或框架來掌握，而不是呼叫者自己來控制。</p>

<blockquote class="block-danger">
  <p>這種說法依然是比較抽象的，想要理解用具體案例來說明會更清楚</p>
</blockquote>

<h4 id="11-example">1.1 Example</h4>

<p>如果有一個 <code class="language-plaintext highlighter-rouge">Class A</code> 他需要透過 <code class="language-plaintext highlighter-rouge">Class B</code> 來印出訊息，傳統的做法會是 <code class="language-plaintext highlighter-rouge">Class A</code> 直接 new 一個 <code class="language-plaintext highlighter-rouge">Class B</code> 的實例，
然後呼叫 <code class="language-plaintext highlighter-rouge">Class B</code> 的方法來印出訊息，如下所示：</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">class</span> <span class="nc">ClassA</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="nc">ClassB</span> <span class="n">classB</span><span class="o">;</span>

    <span class="kd">public</span> <span class="nf">ClassA</span><span class="o">()</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">classB</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ClassB</span><span class="o">();</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">printMessage</span><span class="o">()</span> <span class="o">{</span>
        <span class="n">classB</span><span class="o">.</span><span class="na">printMessage</span><span class="o">();</span>
    <span class="o">}</span>
<span class="o">}</span>

<span class="kd">public</span> <span class="kd">class</span> <span class="nc">ClassB</span> <span class="o">{</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">printMessage</span><span class="o">()</span> <span class="o">{</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"Hello, World!"</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>上面的程式碼問題在於 <code class="language-plaintext highlighter-rouge">Class A</code> 與 <code class="language-plaintext highlighter-rouge">Class B</code> 之間是強耦合的，如果未來需要更換 <code class="language-plaintext highlighter-rouge">Class B</code> 的實現，
我們需要進入 <code class="language-plaintext highlighter-rouge">Class A</code> 的程式碼來修改，那如果再整個系統中有多處使用 <code class="language-plaintext highlighter-rouge">Class B</code> 的地方，那麼修改的成本就會非常高，
這就是傳統的程式設計模式中控制權沒有反轉的問題，<code class="language-plaintext highlighter-rouge">Class A</code> 擁有 <code class="language-plaintext highlighter-rouge">Class B</code> 的實例，並且直接控制 <code class="language-plaintext highlighter-rouge">Class B</code> 的行為。</p>

<blockquote class="block-danger">
  <p>同時有可能 A, B 是循環耦合的關係，例如 B 需要取得 A 的某些資訊來完成他的工作，這樣就會如<strong>下圖左</strong> A, B 是循環依賴的，
當未來需要更換 A 或 B 的實現時，變成兩邊的修改都要進行，並且在測試時需要同時 mock A 和 B</p>
</blockquote>

<blockquote class="block-danger">
  <p>如果有 100 個類別需要使用 Class B 的功能，難道我要修改 100 個類別的硬編碼來更換 Class B 的實現嗎？</p>
</blockquote>

<div style="display: flex; flex-direction: row; align-items: center;">
    <img src="/image/2026/02-02-inversion_of_control/1.jpg" width="50%" height="50%" />
    <img src="/image/2026/02-02-inversion_of_control/2.jpg" width="50%" height="50%" />
</div>

<p>於是聰明的你立刻想到了，有沒有可能由第三方的容器來管理 A 和 B 的實例，這樣就能打破 A 和 B 之間的循環依賴，
讓 A 和 B 都不直接擁有對方的實例，而是由容器來管理並提供給 A 和 B，這樣就實現了控制權的反轉，如<strong>上圖右</strong>所示。</p>

<p>但是這樣其實還是無法看出 IoC 的好處，因為我們只是把 A, B 的實例管理交給了容器，
A 還是需要知道 B 的存在，並且直接呼叫 B 的方法來印出訊息，這樣的設計依然是強耦合的，A 和 B 之間的依賴關係沒有被解決。
<strong>如果要修改 A 或 B 的實現，仍然需要修改 A 的程式碼，這樣就沒有真正實現 IoC 的好處。</strong></p>

<h4 id="12-abstraction-layer">1.2 Abstraction Layer</h4>

<blockquote class="block-danger">
  <p>加入抽象層是實現 IoC 的必要條件，這樣才能使 A 與 B 真正的解耦合</p>
</blockquote>

<p>此時我們需要加入一個抽象層，讓 A 和 B 之間不是直接依賴對方的實現，而是依賴對方的抽象介面，如<strong>下圖</strong>所示：</p>

<p><img src="/image/2026/02-02-inversion_of_control/3.jpg" alt="" height="80%" width="80%" /></p>

<p>這樣 A 與 B 的實作之間完全是解耦合的，A 只依賴 B 的抽象介面，而 B 也只依賴 A 的抽象介面，
底層實作只要滿足抽象介面的定義就可以了，這樣我們就可以隨時替換 A 或 B 的實現，這個替換可以是第三方的程式碼或者設定檔來決定使用哪一種實現。
這樣如果有 100 個類別需要使用不同的 B 實現，我們只需要改變設定檔就能一次改變所有實際類別。</p>

<blockquote class="block-warning">
  <p>這樣的優點是如果我們在某個特定環境，例如測試環境，我們要將 A 獨立單元測試時，對於 B 的依賴就不需要真正的 B 實現，
而是可以使用一個 Mock 的 B 來滿足 A 對 B 的依賴，而不需要依賴 B 的實際行為。</p>
</blockquote>

<hr />

<h3 id="2-implementation">2. Implementation</h3>

<blockquote class="block-tip">
  <p>實際上 IoC 的實現方式有很多種，最常見的就是 Dependency Injection (DI)，還有 Service Locator 等等，不同的實現方式有不同的優缺點，
不過最重要的是要理解 IoC 的核心思想，而不是被具體的實現方式所限制，因為 IoC 的核心思想是將控制權反轉，而不是具體的實現方式。</p>
</blockquote>

<blockquote>
  <p>能講的實現方法太多，可以另外寫一篇文章來介紹不同的實現方式，這裡只會著重在 DI 的實現方式</p>
</blockquote>

<p>在理解 DI 的實現方法之前我們要先了解 A 如何獲得 B 的實例，實際上在 Spring 中，這個賦予通常是一次性的，
因此 A 並不會動態的獲得 B 的實例，而是由 Spring 在啟動時就將 B 的實例注入到 A 中。</p>

<p>在寫程式上我們能透過以下幾種方式來實現 A 獲得 B 的實例:</p>

<ol>
  <li><strong>Constructor Injection:</strong>
    <ul>
      <li>透過建構子來注入 B 的實例到 A 中</li>
    </ul>
  </li>
  <li><strong>Field Injection:</strong>
    <ul>
      <li>直接將 B 的實例注入到 A 的欄位中</li>
    </ul>
  </li>
  <li><strong>Setter Injection:</strong>
    <ul>
      <li>透過 setter 方法來注入 B 的實例到 A 中</li>
    </ul>
  </li>
  <li><strong>Method Injection:</strong>
    <ul>
      <li>透過方法參數來注入 B 的實例到 A 中</li>
    </ul>
  </li>
  <li><strong>ObjectProvider Injection:</strong>
    <ul>
      <li>透過第三方的程式碼來獲得 B 的實例，但 A 會依賴 ObjectProvider 來獲得 B 的實例</li>
      <li>這種方式很接近 [Service Locator ]的實現方式，因為 A 需要依賴 ObjectProvider 來獲得 B 的實例
        <ul>
          <li>這種方法由於 A 需要依賴 ObjectProvider 來獲得 B 的實例，實際在上不一定是更好的設計</li>
        </ul>
      </li>
    </ul>
  </li>
  <li><strong>Lookup Method Injection:</strong>
    <ul>
      <li>可以使用 <a href="https://github.com/cglib/cglib?tab=readme-ov-file">CGLIB</a> 來實現動態代理，讓 A 在呼叫 B 的方法時，實際上是呼叫一個代理物件，這個代理物件會在呼叫時動態的獲得 B 的實例
        <ul>
          <li>這種方式通常自己手動寫起來會比較麻煩，會透過框架來實現，例如 Spring 就提供了這樣的功能</li>
        </ul>
      </li>
    </ul>
  </li>
</ol>

<blockquote>
  <p><a href="https://github.com/cglib/cglib?tab=readme-ov-file">CGLIB</a> 是一個 Java lib，可以用來動態生成 Java 類別，這樣就可以在運行時動態的創建類別，並且可以在類別中定義方法，這樣就可以實現動態代理的功能。</p>
</blockquote>

<blockquote class="block-warning">
  <p>總之無論透過哪種方式，實際上還是會需要執行一個代碼將 B 賦予 A，差別只是由框架提供或者我們可以自己寫一個簡單的程式碼來實現這個賦予的過程</p>
</blockquote>

<h4 id="21-dependency-injection">2.1 Dependency Injection</h4>

<p><strong>Constructor Injection</strong></p>

<p>一個最簡單的實現方式就是透過建構子來注入 B 的實例到 A 中，如下所示:</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">class</span> <span class="nc">ClassC</span> <span class="o">{</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">main</span><span class="o">(</span><span class="nc">String</span><span class="o">[]</span> <span class="n">args</span><span class="o">)</span> <span class="o">{</span>
        <span class="nc">ClassB</span> <span class="n">classB</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ClassB</span><span class="o">();</span>
        <span class="nc">ClassA</span> <span class="n">classA</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ClassA</span><span class="o">(</span><span class="n">classB</span><span class="o">);</span>
        <span class="n">classA</span><span class="o">.</span><span class="na">printMessage</span><span class="o">();</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<blockquote>
  <p>這是手動實現 DI 的方式，雖然很簡單，但這也完成了 IoC 的核心定義，Class B 與 Class A 都是由 Class C 的 main 方法控制的。</p>
</blockquote>

<p>實際上我們也可以透過 Reflection 來實現 DI 的方式，透過例如 <code class="language-plaintext highlighter-rouge">@Autowired</code> 這樣的註解來告訴框架需要注入的類別，
然後由框架來透過 Reflection 來實現 DI 的過程，這樣我們就可以寫一個程式來自動化的實現 DI 的過程，而不需要手動寫一個程式碼來實現這個過程。</p>

<p>當然這樣的方法就不可能是數行程式碼就能實現的了，通常會需要一個完整的框架來實現這樣的功能，
例如 Spring 就提供了這樣的功能，讓我們可以透過註解來實現 DI 的過程。</p>

<p><strong>Field Injection</strong></p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">class</span> <span class="nc">ClassA</span> <span class="o">{</span>
    <span class="nd">@Autowired</span>
    <span class="kd">public</span> <span class="nc">ClassB</span> <span class="n">classB</span><span class="o">;</span>

    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">printMessage</span><span class="o">()</span> <span class="o">{</span>
        <span class="n">classB</span><span class="o">.</span><span class="na">printMessage</span><span class="o">();</span>
    <span class="o">}</span>
<span class="o">}</span>

<span class="kd">public</span> <span class="nc">Class</span> <span class="nc">ClassC</span> <span class="o">{</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">main</span><span class="o">(</span><span class="nc">String</span><span class="o">[]</span> <span class="n">args</span><span class="o">)</span> <span class="o">{</span>
        <span class="nc">ClassA</span> <span class="n">classA</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ClassA</span><span class="o">();</span>
        <span class="n">classA</span><span class="o">.</span><span class="na">classB</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ClassB</span><span class="o">();</span>
        <span class="n">classA</span><span class="o">.</span><span class="na">printMessage</span><span class="o">();</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>Field Injection 的方式是直接將 B 的實例注入到 A 的欄位中，這樣就不需要透過建構子來注入了，這裡為了模擬自己實作而使用 <code class="language-plaintext highlighter-rouge">public</code> 欄位來直接注入，
實際上在 Spring 中是透過 Reflection 來實現，會繞過 Java 的存取控制來注入私有欄位的，因此可以保持欄位為 <code class="language-plaintext highlighter-rouge">private</code> 或者 <code class="language-plaintext highlighter-rouge">protected</code>。</p>

<p>其餘方法如 Setter Injection 是透過 setter 方法來注入 B 的實例到 A 中，Method Injection 是透過方法參數來注入 B 的實例到 A 中，這兩種方式的實現方式與 Constructor Injection 類似，只是注入的方式不同而已，實際上在 Spring 中這兩種方式也是被支持的。</p>

<h4 id="22-spring-implementation">2.2 Spring Implementation</h4>

<blockquote class="block-tip">
  <p><a href="https://docs.spring.io/spring-framework/reference/core/beans/annotation-config/autowired.html">Using @Autowired</a> 是 Spring 中最常見的實現 DI 的方式</p>
</blockquote>

<p>Spring 是依照你的 Class 寫法來決定使用哪一種注入方式，有以下規則:</p>

<ol>
  <li>Class 只有一個 Constructor，並且 Constructor 的參數都是 Spring 管理的 Bean
    <ul>
      <li>Spring 就會使用 Constructor Injection 的方式來注入 Bean。</li>
    </ul>
  </li>
  <li>Class 有多個建構子，而且都有 Bean 作為參數，那麼需要使用 <code class="language-plaintext highlighter-rouge">@Autowired</code> 來指定使用哪一個建構子來注入 Bean。</li>
  <li>只有欄位有 <code class="language-plaintext highlighter-rouge">@Autowired</code> 註解，那麼 Spring 就會使用 Field Injection 的方式來注入 Bean。</li>
  <li>如果有 Method 有 <code class="language-plaintext highlighter-rouge">@Autowired</code> 註解，那麼 Spring 就會使用 Method Injection 的方式來注入 Bean。
    <ul>
      <li>當 Method 是 setter 方法的時候，就稱為 Setter Injection</li>
    </ul>
  </li>
</ol>

<p>雖然有這麼多種注入方式，<strong>但 Spring 官方推薦的是使用 Constructor Injection 的方式來實現 DI</strong>，
也就是只有一個 Constructor，並且 Constructor 的參數都是 Spring 管理的 Bean，
這樣的方式是顯式的，盡量減少 <code class="language-plaintext highlighter-rouge">@Autowired</code> 的使用，這樣就能讓程式碼更清晰，並且更容易測試。</p>

<blockquote>
  <p>Since you can mix constructor-based and setter-based DI, it is a good rule of thumb to use constructors for mandatory dependencies and setter methods or configuration methods for optional dependencies.</p>
</blockquote>

<blockquote>
  <p>一個良好的經驗法則是，對於「必要（mandatory）」的依賴，應使用建構子注入；對於「可選（optional）」的依賴，則使用 setter 方法或設定方法來注入。</p>
</blockquote>

<blockquote>
  <p><a href="https://docs.spring.io/spring-framework/reference/core/beans/dependencies/factory-collaborators.html">Dependency Injection</a></p>
</blockquote>

<p>這裡給一個範例，假設我們有 <code class="language-plaintext highlighter-rouge">stripe</code> 與 <code class="language-plaintext highlighter-rouge">paypal</code> 兩個支付系統的實現，透過 <code class="language-plaintext highlighter-rouge">PaymentService</code> 這個抽象介面來定義支付服務:</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">interface</span> <span class="nc">PaymentService</span> <span class="o">{</span>
    <span class="kt">void</span> <span class="nf">pay</span><span class="o">(</span><span class="kt">int</span> <span class="n">amount</span><span class="o">);</span>
<span class="o">}</span>

<span class="nd">@Service</span>
<span class="nd">@Profile</span><span class="o">(</span><span class="s">"stripe"</span><span class="o">)</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">StripePaymentService</span> <span class="kd">implements</span> <span class="nc">PaymentService</span> <span class="o">{</span>

    <span class="nd">@Override</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">pay</span><span class="o">(</span><span class="kt">int</span> <span class="n">amount</span><span class="o">)</span> <span class="o">{</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"Stripe paid: "</span> <span class="o">+</span> <span class="n">amount</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>

<span class="nd">@Service</span>
<span class="nd">@Profile</span><span class="o">(</span><span class="s">"paypal"</span><span class="o">)</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">PaypalPaymentService</span> <span class="kd">implements</span> <span class="nc">PaymentService</span> <span class="o">{</span>

    <span class="nd">@Override</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">pay</span><span class="o">(</span><span class="kt">int</span> <span class="n">amount</span><span class="o">)</span> <span class="o">{</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"Paypal paid: "</span> <span class="o">+</span> <span class="n">amount</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>

<span class="nd">@Service</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">OrderService</span> <span class="o">{</span>

    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">PaymentService</span> <span class="n">paymentService</span><span class="o">;</span>

    <span class="kd">public</span> <span class="nf">OrderService</span><span class="o">(</span><span class="nc">PaymentService</span> <span class="n">paymentService</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">paymentService</span> <span class="o">=</span> <span class="n">paymentService</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">createOrder</span><span class="o">()</span> <span class="o">{</span>
        <span class="n">paymentService</span><span class="o">.</span><span class="na">pay</span><span class="o">(</span><span class="mi">100</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<div class="language-yml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">spring</span><span class="pi">:</span>
  <span class="na">profiles</span><span class="pi">:</span>
    <span class="na">active</span><span class="pi">:</span> <span class="s">stripe</span>
</code></pre></div></div>

<p>或者透過啟動參數 <code class="language-plaintext highlighter-rouge">-Dspring.profiles.active=paypal</code> 來切換使用 <code class="language-plaintext highlighter-rouge">paypal</code> 的實現，
這樣就能夠在不修改程式碼的情況下切換支付系統的實現了。</p>

<h3 id="3-conclusion">3. Conclusion</h3>

<p>最後我們總結一下 IoC 到底帶來開發上那些好處，主要有以下幾點:</p>

<ol>
  <li><strong>降低耦合度:</strong>
    <ul>
      <li>A 和 B 之間的依賴關係被解決了，這樣就能夠降低 A 和 B 之間的耦合度</li>
      <li>在實作時我們可以關注於 A 和 B 的行為，而不需要關注 A 和 B 之間的依賴關係</li>
    </ul>
  </li>
  <li><strong>提高可測試性:</strong>
    <ul>
      <li>我們不必為了測試而特別去建構 B 的實例，更容易區分測試環境的使用與生產環境的使用</li>
      <li>可以專門為測試環境提供一個 Mock 的 B 的實現，這樣就能夠更容易的測試 A 的行為</li>
    </ul>
  </li>
  <li><strong>生命週期管理:</strong>
    <ul>
      <li>將物件統一交由容器來管理，這樣就能夠統一管理物件的生命週期</li>
      <li>在 Spring 中，Bean 的生命週期是由 Spring 來管理的，這樣就能夠統一管理 Bean 的創建、銷毀等過程
        <ul>
          <li>例如物件銷毀的時候需要釋放資源，這樣就能夠統一管理資源的釋放過程</li>
        </ul>
      </li>
    </ul>
  </li>
</ol>

<blockquote class="block-warning">
  <p>以上說明了 IoC 的核心思想，以及如何透過 DI 的方式來實現 IoC，但其實要實現 Dependency Injection 的方式還有很多可以說明，
之後會考慮要不要說 Spring Boot 如何具體實現 DI，會涉及到 Spring 的 BeanFactory、ApplicationContext、BeanDefinition，
以及三層 Cache 如何去解決 Bean 的循環依賴問題，這些都是實現 DI 的細節，會在之後考慮要不要寫一篇文章來說明。</p>
</blockquote>

<blockquote class="block-warning">
  <h5 id="last-edit">Last Edit</h5>
  <p>02-03-2026 18:32</p>
</blockquote>]]></content><author><name>Benson Hsu</name></author><category term="Jekyll" /><category term="software" /><category term="design pattern" /><summary type="html"><![CDATA[Inversion of Control (IoC) 具體來說是一種 Design Principle，而不是 Design Pattern，IoC 的核心思想是將對象的控制權從傳統的流程控制轉移到外部容器或框架， 這樣可以實現更好的模組化、可測試性和靈活性。而 Design Pattern 是更具體的實現 IoC 的方式，例如 Dependency Injection 就是一種實現 IoC 的 Design Pattern。]]></summary></entry><entry><title type="html">Backend | The 12-Factor App</title><link href="https://hotshot824.github.io/jekyll/2026-01-29-the_12_factor_app.html" rel="alternate" type="text/html" title="Backend | The 12-Factor App" /><published>2026-01-29T00:00:00+00:00</published><updated>2026-01-29T00:00:00+00:00</updated><id>https://hotshot824.github.io/jekyll/the_12_factor_app</id><content type="html" xml:base="https://hotshot824.github.io/jekyll/2026-01-29-the_12_factor_app.html"><![CDATA[<blockquote class="block-tip">
  <p>The Twelve-Factor App 是一套用於構建現代雲端應用程式的最佳實踐方法論，由 Heroku 團隊的聯合創始人 Adam Wiggins 在 2011 年提出，
這是開發 SaaS（Software as a Service）的方法論，旨在幫助開發者構建可擴展、可維護和可部署的應用程式。</p>
</blockquote>

<blockquote class="block-danger">
  <p>不過畢竟是 2011 年提出的原則了，當時的技術環境與現在已經有很大的差異了，例如當時還沒有 Docker、Kubernetes 等容器化技術，
目前很多看來是常識的原則在當時還不是那麼普遍的作法，因此這些原則在當時是非常具有前瞻性的，
並且在當前的 Cloud Native 環境中仍然具有很高的指導意義。</p>
</blockquote>

<blockquote>
  <p><a href="https://12factor.net/">THE TWELVE-FACTOR APP</a></p>
</blockquote>

<h3 id="basic-principles">Basic Principles</h3>

<ul>
  <li><strong>使用宣告式（declarative）的設定格式來進行自動化部署與配置，以降低新成員加入專案所需的時間與成本</strong>
    <ul>
      <li>過去我喜歡用半成品的 Image 來部署應用程式，透過 Entry Script 來完成初始化，但這樣會有以下問題:
        <ul>
          <li>Container 變為半成品，破壞可重現性，有可能在 CI/CD 上影響穩定性</li>
          <li>Shell Script 只可以負責初始化與啟動程式，不應該負責環境與配置的管理，這是 .yaml 的工作</li>
        </ul>
      </li>
      <li>環境變數、連線資訊、容器之間的依賴、捲軸掛載、重啟政策、複製數量都應該在 .yaml 中宣告
        <ul>
          <li>在正式環境中請使用乾淨的 Image，並且在 .yaml 中宣告所有的設定與依賴</li>
          <li>Image 要確保 Immutable，即使是 <a href="https://docs.gitops.weaveworks.org/docs/intro-weave-gitops/">GitOps</a> 的部屬方式，也要確保 Image 的版本是固定的</li>
        </ul>
      </li>
    </ul>
  </li>
</ul>

<blockquote>
  <p><a href="https://docs.gitops.weaveworks.org/docs/intro-weave-gitops/">GitOps</a> 會另外考慮要不要寫一篇文章介紹，但目前中文介紹 GitOps 的相關已經很多了</p>
</blockquote>

<blockquote>
  <p><a href="https://hackmd.io/@mopcon/2023/%2F%40mopcon%2Fr1hRhwdXT">2023 MOPCON Git 和 DevOps：在混亂的流星群開發流程中找到小確幸 高見龍</a></p>
</blockquote>

<ul>
  <li><strong>與底層作業系統維持清晰的契約關係，確保在不同執行環境之間具有高度可攜性</strong>
    <ul>
      <li>不依賴於特定發行版的工具或特性，確保應用程式能夠在任何符合標準的環境中運行</li>
      <li>這裡我們以 Debian 與 Ubuntu 中固定 Timezone 為例:
        <ul>
          <li>Ubuntu 或許有 tzdata 但 Debian 沒有，這樣就會降低移植性</li>
          <li>比較好的做法是例如 Spring 可以設定 time-zone，這樣 TZ 設定是綁定在應用程式內部的，
而不是依賴於底層作業系統的工具，這樣就能確保在不同的 Linux 發行版上都能正常運行</li>
        </ul>
      </li>
    </ul>
  </li>
  <li><strong>適合部署於現代雲端平台，減少對傳統伺服器管理與系統維運的依賴</strong>
    <ul>
      <li>應用程式應該設計為無狀態（stateless），並且將任何持久化的資料存儲在外部服務中，例如資料庫或物件存儲</li>
    </ul>
  </li>
  <li><strong>最小化開發環境與生產環境之間的差異，使持續部署 (Continuous Deployment) 得以實現，提升開發敏捷性</strong>
    <ul>
      <li>開發環境與生產環境的差異越小，部屬風險就越小，部屬頻率才能提高</li>
    </ul>
  </li>
</ul>

<blockquote class="block-danger">
  <p>軟體工程師最常甩鍋的一句話是 “It works on my machine”</p>
</blockquote>

<ul>
  <li><strong>能夠在不大幅改動工具鏈、系統架構或開發流程的情況下進行水平擴展(scale up)</strong>
    <ul>
      <li>水平擴展意味著能夠通過增加更多的實例來處理增加的負載，而不是依賴於升級單一實例的硬體資源</li>
      <li>這需要應用程式設計為無狀態，並且能夠在多個實例之間共享狀態，例如使用外部資料庫或分布式快取</li>
    </ul>
  </li>
</ul>

<hr />

<h3 id="i-codebase">I. Codebase</h3>

<blockquote class="block-tip">
  <p>One codebase tracked in revision control, many deploys</p>
</blockquote>

<blockquote>
  <p>一個代碼庫被版本控制系統追蹤，並且可以部署到多個環境中</p>
</blockquote>

<p><img src="https://12factor.net/images/codebase-deploys.png" alt="" /></p>

<p>有點類似於 <a href="https://docs.gitops.weaveworks.org/docs/intro-weave-gitops/">GitOps</a> 的概念，所有的部署都來自同一個版本控制系統中的單一代碼庫，這樣可以確保部署的一致性和可追溯性。
保持單一來源將程式碼集中在 Git 中，無論部屬多少實例，都來自同一個代碼庫，這樣可以確保所有的實例都運行相同的程式碼版本，並且可以輕鬆地追蹤和回滾變更。</p>

<blockquote class="block-danger">
  <p>即使有可能版本有所不同，例如在不同機器上部署不同版本的應用程式，但這些版本仍然來自同一個代碼庫，並且可以通過 Git 的分支或標籤來管理和追蹤。</p>
</blockquote>

<hr />

<h3 id="ii-dependencies">II. Dependencies</h3>

<blockquote class="block-tip">
  <p>Explicitly declare and isolate dependencies</p>
</blockquote>

<blockquote>
  <p>顯式宣告並且隔離依賴</p>
</blockquote>

<p>所有的依賴都應該明確宣告並且隔離，這樣可以確保應用程式在任何環境中都能夠正確運行，而不會受到底層系統環境的影響。
不過我不太相信 2026 年的當下還有程式開發者會不依賴賴於套件管理工具來管理依賴，這樣會導致開發環境與生產環境之間的差異，增加部署的風險。</p>

<p>例如 Java Maven 中的 pom.xml 就是一個明確宣告依賴的例子，這樣不論是在開發環境還是生產環境中，只要使用相同的 pom.xml，就能確保依賴的一致性。
或者 Python 的 requirements.txt、Go 的 go.mod，這些工具都明確宣告了依賴。</p>

<blockquote>
  <p>在這裡額外有強調隔離性(isolation)，但通常 container 之間都是相互隔離的，所以除非特別設計通常 container 之內不會有汙染問題。
例如一個 pod 只跑一個 python 程式，使用 venv 感覺有點在砸自己的腳</p>
</blockquote>

<blockquote class="block-danger">
  <p>避免使用 curl 或者 ImageMagick 這樣或許系統中存在的工具，如果要用就應該確實的打包在 Image 中</p>
</blockquote>

<hr />

<h3 id="iii-config">III. Config</h3>

<blockquote class="block-tip">
  <p>Store config in the environment</p>
</blockquote>

<blockquote>
  <p>將配置存儲在環境變數中</p>
</blockquote>

<p>很簡單的原則，將配置存儲在環境變數中，而不是硬編碼在程式碼中，這樣可以確保應用程式在不同環境中具有高度的可配置性和靈活性。
一個很簡單的判斷原則是: 如果 codebase 是開源的，但不會洩漏任何敏感資訊，就代表設定正確從程式碼中抽離。</p>

<p>例如某個程式需要使用 Public Key 來連接特定資源，該資源應該在 Runtime 中注入，由部屬工具來管理，
例如 Github Actions 的 Secrets，或者 Kubernetes 的 Secret，這樣就能確保敏感資訊不會洩漏在程式碼庫中。</p>

<blockquote class="block-danger">
  <p>不同環境可能有不同的 env 設定，每個環境就獨立一組 env 設定，這樣就能確保在不同環境中應用程式的行為可以根據需要進行調整，而不需要修改程式碼</p>
</blockquote>

<hr />

<h3 id="iv-backing-services">IV. Backing Services</h3>

<blockquote class="block-tip">
  <p>Treat backing services as attached resources</p>
</blockquote>

<blockquote>
  <p>將後端服務視為附加資源</p>
</blockquote>

<p><img src="https://12factor.net/images/attached-resources.png" alt="" /></p>

<p>後端服務是指在應用程式運行中，所有透過網路存取的外部資源，例如:</p>
<ul>
  <li>Database (MySQL, PostgreSQL, MongoDB)</li>
  <li>Message Queue (RabbitMQ, Kafka)</li>
  <li>Caching Service (Redis, Memcached)</li>
  <li>Mail Service (SendGrid, {Amazon SES})</li>
  <li>External APIs (<a href="https://aws.amazon.com/s3/">AWS S3</a>, <a href="https://cloud.google.com/storage">Google Cloud Storage</a>)</li>
</ul>

<p>程式碼不應該區分是本地提供的服務還是第三方服務，對這些服務都透過 URL 或者其他定位方式來存取，
一個最直接的例子是將本地的 MySQL 服務換為 AWS RDS，但不需要修改程式碼，只需要修改配置中的 URL 即可，
這樣就能確保應用程式的靈活性和可擴展性。</p>

<blockquote>
  <p>這樣每個服務只依賴於接口、只關心自己內部的實現，不依賴於外部資源的實現方式</p>
</blockquote>

<blockquote class="block-danger">
  <p>每一個獨立的服務都應該被視為一個獨立的資源，並且可以通過配置來切換不同的服務提供者，這樣就能確保應用程式在不同環境中具有高度的可配置性和靈活性。</p>
</blockquote>

<hr />

<h3 id="v-build-release-run">V. Build, release, run</h3>

<blockquote class="block-tip">
  <p>Strictly separate build and run stages</p>
</blockquote>

<blockquote>
  <p>嚴格區分構建和運行階段</p>
</blockquote>

<p><img src="https://12factor.net/images/release.png" alt="" /></p>

<p>在 Twelve-Factor App 中，Build、Release 和 Run 是三個獨立的階段，每個階段都有明確的職責和流程，
這樣可以確保應用程式的部署過程具有高度的可控性和可靠性。</p>

<ul>
  <li>Build Stage: 在這個階段，應用程式的原始碼被轉換為一個可執行的 Image 或者 Executable
    <ul>
      <li>Maven, Gradle 等構建工具會在這個階段負責編譯程式碼、打包依賴、生成可執行的 Artifact</li>
    </ul>
  </li>
  <li>Release Stage: 在這個階段，構建好的 Image 或者 Executable 發送到對應的 Registry
    <ul>
      <li>Docker Hub、AWS ECR、Google Container Registry</li>
    </ul>
  </li>
  <li>Run Stage: 在這個階段，構建好的 Image 或者 Executable 被部署到對應的環境中運行
    <ul>
      <li>Kubernetes、AWS ECS、Google Cloud Run</li>
    </ul>
  </li>
</ul>

<p>這三個階段要嚴格的分離，例如在 RunTime 中程式碼已經是不可修改的狀態，不允許在 RunTime 中自行修改 Source Code 或 Compiled Code，
這樣就違反了 Build 和 Run 的分離原則。</p>

<blockquote class="block-danger">
  <p>每個 Release 都應該有其唯一的 ID，這樣就能確保每次部署都是 Rollbackable</p>
</blockquote>

<hr />

<h3 id="vi-processes">VI. Processes</h3>

<blockquote class="block-tip">
  <p>Execute the app as one or more stateless processes</p>
</blockquote>

<blockquote>
  <p>將應用程式作為一個或多個無狀態的進程來執行</p>
</blockquote>

<p>程式應該是無狀態的，這樣就能確保應用程式在不同實例之間具有高度的可擴展性和可靠性。
所有資料應該透過 <a href="./2026-01-29-the_12_factor_app#iv-backing-services">IV. Backing Services</a> 中定義的外部服務來存儲，這樣就能確保應用程式的狀態不會依賴於特定的實例，
並且可以在需要時輕鬆地擴展或縮減實例的數量。</p>

<blockquote>
  <p>無狀態的應用程式可以更容易地實現水平擴展，因為每個實例都不依賴於其他實例的狀態</p>
</blockquote>

<blockquote class="block-danger">
  <p>這裡要特別提到即使是 Session 也要存放在外部服務中，例如 Redis，這樣就能確保在多實例的環境中，
使用者的 Session 可以在不同實例之間共享，提升使用者體驗。而不是放於 <code class="language-plaintext highlighter-rouge">/tmp</code> 或者 Memory</p>
</blockquote>

<hr />

<h3 id="vii-port-binding">VII. Port binding</h3>

<blockquote class="block-tip">
  <p>Export services via port binding</p>
</blockquote>

<blockquote>
  <p>透過端口綁定來導出服務</p>
</blockquote>

<p>應用程式應該要自包含 Port Binding 的能力，這樣就能確保應用程式在不同環境中具有高度的可移植性和靈活性。
最好的反例就是 Apache HTTP Server，這種傳統的 Web Server 需要在配置文件中指定 Port Binding 的方式，
因此要修改 Port Binding 的方式就需要修改 Apache HTTP Server 的配置，這樣就降低了應用程式的靈活性。</p>

<blockquote>
  <p>傳統 Http server 需要自己去特定配置檔案目錄中修改 Port Binding 位置，不方便也容易造成複雜度上升</p>
</blockquote>

<p>以 Spring Boot 為例，現在主流的方式不再是 Tomcat 然後再部署 Spring Boot，而是在 Spring Boot 中內嵌入 Tomcat，
這樣 Spring Boot 本身就具有 Port Binding 的能力，無論在哪個環境中都能夠直接運行，而不需要依賴 Web Server。</p>

<blockquote class="block-danger">
  <p>現代後端框架目前基本都已經內建 Port Binding 的能力了，畢竟 12-Factor App 的原則已經提出了十多年了，這個原則也已經成為現代後端框架的標準設計模式了。</p>
</blockquote>

<hr />

<h3 id="viii-concurrency">VIII. Concurrency</h3>

<blockquote class="block-tip">
  <p>Scale out via the process model</p>
</blockquote>

<blockquote>
  <p>通過進程模型來實現水平擴展</p>
</blockquote>

<p><img src="https://12factor.net/images/process-types.png" alt="" /></p>

<p>水平擴展已經是現代雲端應用程式的基本需求了，由於應用程式是無狀態的，因此水平擴展可以通過增加更多的實例來實現。
目前水平擴展也有其層級，例如以下:</p>
<ol>
  <li>Infrastructure Level: 例如增加更多的 VM 來運行應用程式
    <ul>
      <li>AWS EC2</li>
    </ul>
  </li>
  <li>Orchestration Level: 例如使用 Kubernetes 來管理和擴展 Pod 的數量
    <ul>
      <li>Kubernetes Horizontal Pod Autoscaler</li>
    </ul>
  </li>
  <li>Application Level: 例如在應用程式內部使用多線程或者非同步的方式來處理更多的請求
    <ul>
      <li>通常在 Microservice 的架構下，應用程式內部的水平擴展已經不是主要的擴展方式</li>
    </ul>
  </li>
</ol>

<blockquote>
  <p>IaaS (Infrastructure as a Service) 的架構下程式開發者依然要自己負責 HPA 這個層級的擴展</p>
</blockquote>

<blockquote>
  <p>PaaS (Platform as a Service) 的架構下程式開發者不需要自己負責 HPA 這個層級的擴展</p>
</blockquote>

<blockquote class="block-danger">
  <p>在 Twelve-Factor App 提出的時間 2011，Docker、Kubernetes 等容器化技術還沒有出現，因此當時的水平擴展主要是通過增加更多的進程來實現的，
不過在當前的雲原生環境中，水平擴展更多是通過增加更多的容器實例來實現的，但這個原則仍然適用，因為每個容器實例本質上也是一個獨立的進程。</p>
</blockquote>

<hr />

<h3 id="ix-disposability">IX. Disposability</h3>

<blockquote class="block-tip">
  <p>Maximize robustness with fast startup and graceful shutdown</p>
</blockquote>

<blockquote>
  <p>通過快速啟動和優雅關閉來最大化應用程式的健壯性</p>
</blockquote>

<p>實際上大多數程式開發者能控制的是 Graceful Shutdown 的部分，因為快速啟動通常是由底層的框架或者容器來控制的，
例如 Spring Boot 的啟動時間通常是由 JVM 與 Tomcat 的啟動時間來決定的，開發者能做的優化空間有限。
但是 Graceful Shutdown 是完全由開發者來控制的，這裡的原則是當應用程式接收到終止信號時，應該能夠優雅地關閉，例如:</p>
<ul>
  <li>在接收到 SIGTERM 信號時，目前正在處裡的請求是否要完成?</li>
  <li>如果是交易類的應用程式，應該要確保交易的 Atomicity</li>
  <li>是否要在關閉前釋放資源，例如資料庫連線等 …</li>
</ul>

<blockquote>
  <p><a href="https://en.wikipedia.org/wiki/Crash-only_software">Crash-only software</a> 的設計理念，應用程式應該能夠在崩潰後快速重啟，並且能夠自動恢復到正常狀態，這樣就能確保應用程式的高可用性和可靠性</p>
</blockquote>

<blockquote>
  <p>Docker 在關閉容器時會先發送 SIGTERM 信號給容器內的應用程式，讓它有機會進行優雅關閉，如果應用程式沒有在指定的時間內完成關閉，Docker 才會強制終止容器，這樣就能確保應用程式在關閉時能夠有機會釋放資源並且完成正在處理的請求</p>
</blockquote>

<blockquote class="block-danger">
  <p>除此之外當然也要應對 Sudden Death 的情況，例如當容器被強制終止時，應用程式應該能夠在下次啟動時自動恢復到正常狀態，而不會因為之前的非正常關閉而導致資料損壞或者其他問題</p>
</blockquote>

<hr />

<h3 id="x-devprod-parity">X. Dev/prod parity</h3>

<blockquote class="block-tip">
  <p>Keep development, staging, and production as similar as possible</p>
</blockquote>

<blockquote>
  <p>保持開發、測試和生產環境盡可能相似</p>
</blockquote>

<p>開發環境、測試環境和生產環境之間的差異越小，部署的風險就越小，這樣就能確保應用程式在生產環境中能夠正常運行，而不會因為環境差異而導致問題。
例如在開發環境中使用 Docker Compose 來模擬生產環境中的 Kubernetes 部署，
這樣就能確保在開發環境中測試的配置和依賴與生產環境中的一致，從而降低部署的風險。</p>

<ul>
  <li>Time gap: 開發者要多久才能將代碼使用在生產環境中?</li>
  <li>Personnel gap: 開發者和運維人員之間的溝通和協作是否順暢，是否分離了開發和運維的職責</li>
  <li>Tools gap: 開發環境和生產環境使用的工具和技術是否一致，例如使用相同的容器化技術、相同的配置管理工具等
    <ul>
      <li>開發時使用 SQLite，正式上線卻使用 MySQL 就是一個 Tools gap 的例子</li>
    </ul>
  </li>
</ul>

<table>
  <thead>
    <tr>
      <th> </th>
      <th>Traditional app</th>
      <th>Twelve-Factor App</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Time between deploys</td>
      <td>Weeks</td>
      <td>Hours</td>
    </tr>
    <tr>
      <td>Code author vs code deployer</td>
      <td>Different people</td>
      <td>Same people</td>
    </tr>
    <tr>
      <td>Dev vs production environment</td>
      <td>Divergent</td>
      <td>As similar as possible</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>上述的三個 gap 都會增加部署的風險，因為開發環境和生產環境之間的差異越大，就越有可能在部署時遇到問題，
例如配置錯誤、依賴不一致等，從而導致應用程式在生產環境中無法正常運行</p>
</blockquote>

<p>例如程式可以使用抽象層來支援不同後端服務，但還是要盡量保持與生產環境中使用的服務一致，例如以下的表格:</p>

<table>
  <tbody>
    <tr>
      <td>Type</td>
      <td>Language</td>
      <td>Library</td>
      <td>Adapter</td>
    </tr>
    <tr>
      <td>Database</td>
      <td>Ruby/Rails</td>
      <td>ActiveRecord</td>
      <td>MySQL, PostgreSQL, SQLite</td>
    </tr>
    <tr>
      <td>Queue</td>
      <td>Python/Django</td>
      <td>Celery</td>
      <td>Redis, RabbitMQ, Beanstalkd</td>
    </tr>
    <tr>
      <td>Cache</td>
      <td>Ruby/Rails</td>
      <td>ActiveSupport::Cache</td>
      <td>Memory, Filesystem, Memcached</td>
    </tr>
  </tbody>
</table>

<blockquote class="block-danger">
  <p>其中尤其是 Backing Services 的差異會導致部署的風險增加，例如在開發環境中使用本地的 MySQL 服務，
而在生產環境中使用 AWS RDS，這樣就會導致配置和依賴的差異，從而增加部署的風險。</p>
</blockquote>

<hr />

<h3 id="xi-logs">XI. Logs</h3>

<blockquote class="block-tip">
  <p>Treat logs as event streams</p>
</blockquote>

<blockquote>
  <p>將日誌視為事件流</p>
</blockquote>

<p>Microservice 的架構下，Log 應該有同一管理的方式，例如 ELK, Graylog, Cloudwatch 等等。
開發時期可以透過 console 來查看時間排序下的 log，上到環境則應該透過集中服務來收集、聚合和分析 log。</p>

<p>這裡 Log 應該透過結構化的方式進行儲存 (例如 JSON)，這樣就能確保 log 的可讀性和可分析性，並且可以更容易地進行搜尋和過濾。
但在 stdout 中依然可以保持文字格式的輸出，這樣可以即時查看 log 的內容。</p>

<blockquote>
  <p>傳統作法會是每個服務自己控制 log 的輸出位置，例如寫在本地的檔案系統中，這樣就會導致 log 分散在不同的實例中，難以集中管理和分析</p>
</blockquote>

<blockquote class="block-danger">
  <p>12-Factor App 的原則是將 log 視為事件流，應該將 log 輸出到專用服務，這樣就能確保 log 的集中管理和分析，並且可以更容易地追蹤和調試應用程式的行為。</p>
</blockquote>

<hr />

<h3 id="xii-admin-processes">XII. Admin processes</h3>

<blockquote class="block-tip">
  <p>Run admin/management tasks as one-off processes</p>
</blockquote>

<blockquote>
  <p>將維護/管理任務作為一次性進程來運行</p>
</blockquote>

<p>即使是管理任務也應該遵循前面提到的原則，例如使用相同的代碼庫、相同的配置、相同的依賴等，這樣就能確保管理任務在不同環境中具有高度的一致性和可靠性。
例如在 Kubernetes 中，可以使用 Job 來運行一次性的管理任務，這樣就能確保管理任務的執行環境與應用程式的運行環境一致，從而降低管理任務的風險。</p>

<p>這些一次性任務應該要有例如 Shell Script 的工具來啟動，並且在 Script 中確保使用與應用程式相同的配置和依賴，例如:</p>
<ul>
  <li>Django: <code class="language-plaintext highlighter-rouge">python manage.py migrate</code>
    <ul>
      <li>該指令將 Python 中的模型配置與資料庫中的結構進行同步，確保資料庫的 Schema 與 Django 的模型定義保持一致，這是一個典型的管理任務</li>
    </ul>
  </li>
  <li>Spring Boot: <code class="language-plaintext highlighter-rouge">java -jar app.jar --spring.profiles.active=prod</code>
    <ul>
      <li>該指令將 Spring Boot 應用程式啟動，並且指定使用 prod 的配置，這樣就能確保管理任務在與應用程式相同的環境中運行</li>
    </ul>
  </li>
</ul>

<blockquote>
  <p>這些一次性任務應該也要跟 Release 同樣發布，這樣就能確保管理任務的版本與應用程式的版本一致，從而降低管理任務的風險。</p>
</blockquote>

<blockquote class="block-danger">
  <p>例如每次部署後都需要初始化資料庫，那麼初始化的腳本應該也要跟應用程式的 Release 一起發布，
這樣就能在每次部屬後自動執行初始化腳本，確保資料庫的狀態與應用程式的版本保持一致，從而降低部署的風險。</p>
</blockquote>

<blockquote class="block-warning">
  <p>以上就是 12-Factor App 的十二個原則，這些原則幫助開發者構建可擴展、可維護和可部署的應用程式。
所以如果未來要開發一個 Cloud Native 的應用程式，建議一定要遵循這些原則，這樣就能確保應用程式在不同環境中具有高度的可移植性和可靠性。</p>
  <h5 id="last-edit">Last Edit</h5>
  <p>01-29-2026 00:32</p>
</blockquote>]]></content><author><name>Benson Hsu</name></author><category term="Jekyll" /><category term="software" /><category term="design" /><category term="architecture" /><category term="cloud native" /></entry><entry><title type="html">Note | ACPI States</title><link href="https://hotshot824.github.io/jekyll/2026-01-26-ACPI_States.html" rel="alternate" type="text/html" title="Note | ACPI States" /><published>2026-01-26T00:00:00+00:00</published><updated>2026-01-26T00:00:00+00:00</updated><id>https://hotshot824.github.io/jekyll/ACPI_States</id><content type="html" xml:base="https://hotshot824.github.io/jekyll/2026-01-26-ACPI_States.html"><![CDATA[<blockquote class="block-tip">
  <p><a href="https://en.wikipedia.org/wiki/Advanced_Configuration_and_Power_Interface">ACPI</a> 全名為 Advanced Configuration and Power Interface，是一個開放標準，用於管理電腦的電源與硬體配置。</p>
</blockquote>

<p>與 BIOS 密切相關的另一個重要標準就是 ACPI，ACPI 定義了一套電源管理與硬體配置的規範，讓作業系統能夠更有效地控制硬體資源與電源使用。
定義 ACPI 的目的是不以 BIOS 為中心，而是讓作業系統能夠直接管理硬體資源與電源狀態，提升系統的靈活性與效能。</p>

<blockquote>
  <p>這裡一定會有疑問，既然 UEFI Runtime Services 已經提供 OS 與韌體服務，為什麼要另外定義 ACPI。</p>
</blockquote>

<ol>
  <li>電池與電源管理是一個高度動態與 OS 策略深度綁定的問題
    <ul>
      <li>電池管理會隨著時間頻繁變化，並且會影響 OS 的行為
        <ul>
          <li>例如: 當電池電量低時，OS 可能會選擇降低效能以延長續航時間</li>
        </ul>
      </li>
      <li>如果把這類功能放在韌體層面，會導致韌體變得過於複雜，並且難以適應不同的 OS 策略</li>
    </ul>
  </li>
  <li>跨 OS 的標準化
    <ul>
      <li>韌體僅需要以 ACPI Table 描述硬體能力與電源模型</li>
      <li>如何實作則完全交由 OS 實作</li>
    </ul>
  </li>
</ol>

<p>如果改由 UEFI Runtime Services 來處理這些電源管理功能，就代表 Linux, Windows 都需要依賴於同一組韌體 API，
如果韌體設計不良或者有 Bug，會導致 OS 幾乎無法繞過這些問題，並且電源管理的策略會被韌體的更新速度綁定。</p>

<blockquote>
  <p>ACPI 簡單可以這樣理解，韌體描述事實 (what hardware can do)，OS 決定策略 (what to do)</p>
</blockquote>

<blockquote>
  <p>如果沒有 ACPI，OS 就需要為各種硬體平台時做特定的驅動程式，失去硬體抽象化的好處</p>
</blockquote>

<hr />

<h3 id="acpi-states-overview">ACPI States Overview</h3>

<blockquote class="block-danger">
  <p>這份筆記主要目標僅在描述 ACPI 定義的各種電源狀態，並不會深入探討 ACPI Table 的結構等內容。有興趣可以參考 <a href="https://uefi.org/specifications">ACPI Specification</a>。</p>
</blockquote>

<p>ACPI 所定義的 States 包含以下幾種:</p>
<ul>
  <li>Global System Power States (G-States G0 - G3)</li>
  <li>System Sleeping States (S-States S0 - S5)</li>
  <li>Device Power States (D-States D0 - D3)</li>
  <li>Processor Power States (C-States C0 - Cn)</li>
  <li>Throttling States (T-States)</li>
</ul>

<p><img src="/image/2026/01-26-ACPI_States/1.png" alt="" height="75%" width="75%" /></p>

<h4 id="global-system-power-states-g-states">Global System Power States (G-States)</h4>

<blockquote class="block-tip">
  <p>G-States 定義了整個系統的電源狀態，從完全開啟到完全關閉。</p>
</blockquote>

<ol>
  <li>G0 (Working)：系統完全開啟並運作中。</li>
  <li>G1 (Sleeping)：系統處於睡眠狀態，包含 S1 - S4 狀態。</li>
  <li>G2 (Soft Off)：系統關閉，但仍有部分電源供應給某些元件 (如 Wake-on-LAN)。
    <ul>
      <li>電腦插著電源但卻關機的狀態</li>
    </ul>
  </li>
  <li>G3 (Mechanical Off)：系統完全關閉，沒有任何電源供應。</li>
</ol>

<blockquote>
  <p>G-States 是最高層級的電源狀態，決定了系統整體的電源管理策略。G-State 所解決的是這台機器目前處於什麼狀態，
因此在 G 的層級上，我們不清楚 RAM 或 CPU 的細節，只清楚系統是什麼狀態。</p>
</blockquote>

<h4 id="system-sleeping-states-s-states">System Sleeping States (S-States)</h4>

<blockquote class="block-tip">
  <p>S-States 通常是最常被提及的 ACPI 狀態，定義了系統的睡眠深度。</p>
</blockquote>

<ol>
  <li>S0 (Working)：系統完全開啟並運作中。</li>
  <li>S1 (Sleep)：CPU 停止執行，但 CPU 與 RAM 依然供電</li>
  <li>S2 (Deeper Sleep)：CPU 停止供電，Cache 保存至 RAM
    <ul>
      <li>S1 更常被使用在短暫待機狀態，而 S2 則較少見，因為它在節能效果與恢復速度之間的平衡較差</li>
    </ul>
  </li>
  <li>S3 (Suspend to RAM)：大部分元件關閉，僅 RAM 保持供電，以便快速恢復。
    <ul>
      <li>常用的睡眠狀態，能在低功耗與快速恢復之間取得平衡</li>
      <li>此階段風扇等周邊元件也會關閉，只保留 RAM 讓系統狀態得以保存</li>
    </ul>
  </li>
  <li>S4 (Hibernate)：系統狀態保存到硬碟，然後完全關閉電源
    <ul>
      <li>既然 RAM 都不供電了，系統狀態就必須保存到非揮發性儲存裝置</li>
      <li>很直接能想到回復時間會比 S3 長，因為需要從硬碟讀取系統狀態</li>
    </ul>
  </li>
  <li>S5 (Soft Off)：系統完全關閉，但可以透過特定事件喚醒。</li>
</ol>

<blockquote>
  <p>S-State 處理的是 OS 實作層級的睡眠狀態，OS 並無法直接操作 G-State 這種 G1 = Sleeping 的模糊概念。</p>
</blockquote>

<h4 id="processor-power-states-c-states">Processor Power States (C-States)</h4>

<p><img src="/image/2026/01-26-ACPI_States/2.png" alt="" height="75%" width="75%" /></p>

<p>C-States 描述的是 CPU 在 IDLE 狀態下的不同省電模式，因此 C-States 主要描述的是 CPU 層級的電源管理。
因此 C-States 基本上只會在 G0/S0 狀態下被使用，因為在此之外 IDLE 狀態就沒有意義了。</p>

<ol>
  <li>C0 (Active)：CPU 完全運作中，執行指令。</li>
  <li>C1 (Halt)：CPU 停止執行指令，但幾乎可以瞬間恢復。
    <ul>
      <li>在某些處理器中，額外支援 C1E (Enhanced Halt) 狀態，進一步降低功耗</li>
      <li>實作上通常包含 Pipeline Stall、Clock Gating</li>
      <li>但電壓保持不變 Vcore 維持，Cache、TLB、Register 都保持供電</li>
      <li>在此階段任何 Interrupt、<a href="https://en.wikipedia.org/wiki/Inter-processor_interrupt">IPI</a>、Timer Event 都能讓 CPU 立即恢復 C0 狀態</li>
      <li>可想而知這樣的省電效果有限，但恢復速度非常快</li>
    </ul>
  </li>
  <li>C2 (Stop-Clock)：CPU 停止時鐘訊號，降低更多功耗。
    <ul>
      <li>此階段更多的 Clock Domain 被關閉</li>
      <li>Cache 還是保留，但 <a href="https://en.wikipedia.org/wiki/Phase-locked_loop">PLL</a> 可能會停止</li>
      <li>很多平台不會實作 C2 狀態，被折疊進 C1 / C3</li>
    </ul>
  </li>
  <li>C3 (Sleep)：CPU 會停止對 Register 與 Cache 的供電，進一步降低功耗。
    <ul>
      <li>此階段 CPU 內部大部分的電路都會關閉</li>
      <li>Cache 的資料要保持 coherence，直接 Write Back 到 RAM</li>
      <li>TLB 內容也會被清除</li>
      <li>只有 Uncore 部分保持供電，例如 Memory Controller</li>
      <li>這個階段的喚醒需要較長時間，因為需要重新載入 Cache 與 TLB
        <ul>
          <li>Interrupt -&gt; Power Rail Restore -&gt; Clock Restart -&gt; Cache/TLB Rebuild -&gt; Resume</li>
        </ul>
      </li>
    </ul>
  </li>
</ol>

<p>更深的 C-States (C4, C5, …) 則會進一步關閉更多的 CPU 元件，這部分通常是廠商自定義的，並不在 ACPI 規範中明確定義。</p>

<blockquote>
  <p><a href="https://en.wikipedia.org/wiki/Inter-processor_interrupt">IPI</a> (Inter-processor interrupt) 讓一個處理器核心能夠向另一個核心發送中斷請求。</p>
</blockquote>

<blockquote>
  <p><a href="https://en.wikipedia.org/wiki/Phase-locked_loop">PLL</a> (Phase-locked loop) 是一種電子電路，用於生成穩定的時鐘訊號，確保處理器和其他元件能夠同步運作。</p>
</blockquote>

<h4 id="device-power-states-d-states">Device Power States (D-States)</h4>

<blockquote class="block-tip">
  <p>D-States 定義了個別裝置的電源狀態，通常裝置不一定有所有的 D-States。</p>
</blockquote>

<p>這裡的裝置可以是任何硬體元件，例如: NIC, GPU, Sound Card …</p>

<ol>
  <li>D0 (Fully On)：裝置完全開啟並運作中。</li>
  <li>D1 (Low Power)：裝置進入低功耗狀態。</li>
  <li>D2 (Standby)：裝置進入軟體關閉狀態，但仍可快速恢復。</li>
  <li>D3 (Off)：裝置完全斷電。</li>
</ol>

<h4 id="throttling-states-t-states">Throttling States (T-States)</h4>

<blockquote class="block-tip">
  <p>T-State 全稱 Processor Throttling States，定義了 CPU 在高負載或高溫時的降頻狀態。</p>
</blockquote>

<p>T-State 主要透過調整單位時間內 CPU 的 Clock On / Clock Off 比例來達到降頻效果，以降低功耗與熱量產生。
例如一段時間內只允許一半的 Clock cycle 真正被送入 Pipeline 執行指令，其餘時間強制 IDLE 來達到降頻效果。</p>

<p>T-State 的存在原因是:</p>
<ol>
  <li>反映速度快，相較於 P-State 不需要調整電壓或 PLL，在極短時間內限制 CPU 效能</li>
  <li>平台強制控制，例如 EC / Firmware 監控到過熱時，能夠快速降低 CPU 熱量產生，即使 OS 不配合</li>
</ol>

<blockquote class="block-warning">
  <p>以上就是所有 ACPI 定義的主要電源狀態，這些狀態讓作業系統能夠更靈活地管理硬體資源與電源使用，
提升系統的效能與續航能力。</p>
</blockquote>

<hr />

<h3 id="how-os-read-the-battery-infomation">How OS Read the Battery Infomation</h3>

<p>作業系統通常透過 ACPI 提供的 Methods 來讀取電池資訊，例如 _BIF (Battery Information) 與 _BST (Battery Status)。
這些 Methods 定義在 ACPI Table 中，OS 可以透過 ACPI 介面呼叫這些 Methods 來取得電池的詳細資訊。
並且通常是由 <a href="https://en.wikipedia.org/wiki/ACPI_Machine_Language">AML</a> (ACPI Machine Language) 來實作這些 Methods 的邏輯。</p>

<p>AML 是一種專門用於描述 ACPI 功能的低階語言，用來定義 ACPI Table 中的各種 Methods 與物件。
ACPI Tables 由 BIOS / UEFI 事先定義，系統啟動時由韌體將其放置於記憶體並提供給 OS。
OS 在運行時透過 ACPI Driver 來解析並執行這些 AML 程式碼。</p>

<p>以 Linux 為例子，電池資訊位於 <code class="language-plaintext highlighter-rouge">/sys/class/power_supply/BAT0/</code> 目錄下，OS 透過檔案來表示電池狀態與資訊。
通常會以 polling 或 event-driven 的方式來監控電池狀態變化，並根據電池狀態調整系統行為，例如降低效能以延長續航時間。</p>

<p><strong>ACPI Control Method</strong></p>

<blockquote>
  <p><a href="https://uefi.org/htmlspecs/ACPI_Spec_6_4_html/05_ACPI_Software_Programming_Model/ACPI_Software_Programming_Model.html#control-methods-and-the-acpi-source-language-asl">OEMs and platform firmware vendors write definition blocks using the ACPI Source Language (ASL) and use a translator to produce the byte stream encoding described in Definition Block Encoding .</a></p>
</blockquote>

<p>ACPI 定義了一些標準的 Control Methods，其中以下是與電池相關的 Methods:</p>

<ul>
  <li><strong>_STA</strong>: (Status)<br />
報告電池裝置的狀態 (存在、啟用等)。</li>
  <li><strong>_BIF</strong>: (Battery Information)<br />
提供電池的靜態資訊，例如: 設計容量、電壓、型號、序號 …</li>
  <li><strong>_BST</strong>: (Battery Status)<br />
提供電池的動態狀態資訊，例如: 充放電狀態、剩餘容量、當前電壓、充放電速率 …</li>
  <li><strong>_BTP</strong>: (Battery Trip Point) 
由 OS 設定一個剩餘容量的閾值，當電池容量低於此值時觸發 ACPI Notify。</li>
</ul>

<p>這裡要指出 EC (Embedded Controller) 在電池管理中的角色，EC 負責提供原始電池資訊。
而 AML 是由 BIOS / UEFI 開發者撰寫，並且寫入 ACPI Table 中，OS 透過 ACPI Driver 來解析並執行這些 AML 程式碼。</p>

<p>從 User 可以觀察 DSDT (Differentiated System Description Table) 來了解系統的 ACPI 結構。</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">ls</span> /sys/firmware/acpi/tables/DSDT
<span class="nb">sudo cat</span> /sys/firmware/acpi/tables/DSDT <span class="o">&gt;</span> dsdt.dat
<span class="nb">sudo </span>apt <span class="nb">install </span>acpica-tools
iasl <span class="nt">-d</span> dsdt.dat
</code></pre></div></div>

<blockquote class="block-warning">
  <p>反組譯後的 DSDT 就會是 ASL，可以看到裡面定義了許多 ACPI Methods，例如 _BIF, _BST 等等。</p>
</blockquote>

<pre><code class="language-asl">Device (_SB.EPC)
{
    Name (_HID, EisaId ("INT0E0C"))  // _HID: Hardware ID
    Name (_STR, Unicode ("Enclave Page Cache 1.0"))  // _STR: Description String
    Name (_CRS, ResourceTemplate ()  // _CRS: Current Resource Settings
    {
        VendorShort ()      // Length = 0x07
        {
                0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF         // .......
        }
    })
    Method (_STA, 0, NotSerialized)  // _STA: Status
    {
        Return (0x0F)
    }
}
</code></pre>

<blockquote>
  <p>手邊只有虛擬機可以測試，實體機器的 DSDT 會更複雜，這裡以 SGX Enclave Page Cache 為例子</p>
</blockquote>

<p>會看到 DSDT 裡面定義了一個 Device (_SB.EPC)，並且有一個 Method (_STA) 用來回傳裝置狀態。
該 _STA Method 回傳 0x0F，表示該裝置永遠存在且啟用。
kernel sapce 可以透過 <code class="language-plaintext highlighter-rouge">#include &lt;linux/acpi.h&gt;</code> 來呼叫這些 ACPI Method。</p>

<p>例如寫一個簡單的 Kernel Module 來讀取 _SB.EPC 裝置的 _STA 狀態:</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;linux/module.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;linux/kernel.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;linux/init.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;linux/acpi.h&gt;</span><span class="cp">
</span>
<span class="k">static</span> <span class="kt">int</span> <span class="n">__init</span> <span class="nf">epc_sta_init</span><span class="p">(</span><span class="kt">void</span><span class="p">)</span>
<span class="p">{</span>
    <span class="k">struct</span> <span class="n">acpi_device</span> <span class="o">*</span><span class="n">adev</span><span class="p">;</span>
    <span class="kt">unsigned</span> <span class="kt">long</span> <span class="n">sta</span><span class="p">;</span>
    <span class="kt">int</span> <span class="n">ret</span><span class="p">;</span>

    <span class="n">pr_info</span><span class="p">(</span><span class="s">"epc_sta: init</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>

    <span class="cm">/* Find the EPC device by its HID */</span>
    <span class="n">adev</span> <span class="o">=</span> <span class="n">acpi_dev_get_first_match_dev</span><span class="p">(</span><span class="s">"INT0E0C"</span><span class="p">,</span> <span class="nb">NULL</span><span class="p">,</span> <span class="o">-</span><span class="mi">1</span><span class="p">);</span>
    <span class="k">if</span> <span class="p">(</span><span class="o">!</span><span class="n">adev</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">pr_err</span><span class="p">(</span><span class="s">"epc_sta: EPC device not found</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>
        <span class="k">return</span> <span class="o">-</span><span class="n">ENODEV</span><span class="p">;</span>
    <span class="p">}</span>

    <span class="cm">/* Get the _STA status */</span>
    <span class="n">ret</span> <span class="o">=</span> <span class="n">acpi_bus_get_status</span><span class="p">(</span><span class="n">adev</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">sta</span><span class="p">);</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">ret</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">pr_err</span><span class="p">(</span><span class="s">"epc_sta: acpi_bus_get_status failed: %d</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">ret</span><span class="p">);</span>
        <span class="k">return</span> <span class="n">ret</span><span class="p">;</span>
    <span class="p">}</span>

    <span class="n">pr_info</span><span class="p">(</span><span class="s">"epc_sta: EPC _STA = 0x%lx</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">sta</span><span class="p">);</span>

    <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>

<span class="k">static</span> <span class="kt">void</span> <span class="n">__exit</span> <span class="nf">epc_sta_exit</span><span class="p">(</span><span class="kt">void</span><span class="p">)</span>
<span class="p">{</span>
    <span class="n">pr_info</span><span class="p">(</span><span class="s">"epc_sta: exit</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>
<span class="p">}</span>

<span class="n">module_init</span><span class="p">(</span><span class="n">epc_sta_init</span><span class="p">);</span>
<span class="n">module_exit</span><span class="p">(</span><span class="n">epc_sta_exit</span><span class="p">);</span>

<span class="n">MODULE_LICENSE</span><span class="p">(</span><span class="s">"GPL"</span><span class="p">);</span>
<span class="n">MODULE_AUTHOR</span><span class="p">(</span><span class="s">"benson"</span><span class="p">);</span>
<span class="n">MODULE_DESCRIPTION</span><span class="p">(</span><span class="s">"Test ACPI _SB.EPC _STA"</span><span class="p">);</span>
</code></pre></div></div>

<p>剩下的要寫 Kernel Module 這邊就不做測試了，主要是了解如何透過 ACPI Method 來取得裝置資訊。</p>

<blockquote class="block-danger">
  <p>以上就是 ACPI 定義的各種電源狀態與 OS 讀取電池資訊的方式，如果有後續研究再補充相關內容。</p>
</blockquote>

<blockquote class="block-warning">
  <h5 id="last-edit">Last Edit</h5>
  <p>01-27-2026 20:32</p>
</blockquote>]]></content><author><name>Benson Hsu</name></author><category term="Jekyll" /><category term="bios" /><category term="acpi" /><summary type="html"><![CDATA[ACPI 全名為 Advanced Configuration and Power Interface，是一個開放標準，用於管理電腦的電源與硬體配置。]]></summary></entry><entry><title type="html">Note | BIOS Introduction</title><link href="https://hotshot824.github.io/jekyll/2026-01-24-BIOS_introduction.html" rel="alternate" type="text/html" title="Note | BIOS Introduction" /><published>2026-01-24T00:00:00+00:00</published><updated>2026-01-24T00:00:00+00:00</updated><id>https://hotshot824.github.io/jekyll/BIOS_introduction</id><content type="html" xml:base="https://hotshot824.github.io/jekyll/2026-01-24-BIOS_introduction.html"><![CDATA[<blockquote class="block-tip">
  <p>BIOS 全名為 (Basic Input/Output System)，負責在電腦開機時初始化硬體並呼叫 Bootloader 來載入作業系統。</p>
</blockquote>

<blockquote class="block-warning">
  <p>PC 電源按下去難道 OS 會自己飛進 DRAM 自己啟動嗎？當然不會，這時間點 DRAM 還沒初始化也無法使用，這時候就需要 BIOS 做最初的初始化工作。</p>
</blockquote>

<h3 id="1-pc-architecture-overview">1. PC Architecture Overview</h3>

<blockquote class="block-tip">
  <p>通常 Computer Introduction 或者 Computer Architecture 會把這部分講的很詳細，
這裡還是整理一下簡單複習，算盤本通常會更加強調硬體架構與運作原理。</p>
</blockquote>

<p>在開始介紹 BIOS 之前我們應該要先清楚現代電腦的基本架構，以下是一個簡化的 Notebook / Desktop 架構圖：</p>

<p><img src="/image/2026/01-24-BIOS_introduction/1.svg" alt="" /></p>

<blockquote>
  <p>這張圖蠻難得把 BIOS 跟南北橋都畫出來的，現代架構通常會把北橋整合進 CPU 裡面，或不畫出 Flash ROM。</p>
</blockquote>

<p><strong>CPU Level</strong></p>

<ol>
  <li>CPU (Central Processing Unit)：負責執行程式指令與處理資料，是電腦的核心運算單元。</li>
  <li>Register：CPU 內部的高速儲存單元，用於暫存指令與資料。</li>
  <li>Cache：位於 CPU 與主記憶體之間的高速緩存，用於加速資料存取。
    <ul>
      <li>L1、L2、L3 Cache：即使是 Cache 也有分層，L1 最快但容量最小，L3 最慢但容量最大。</li>
    </ul>
  </li>
</ol>

<p><strong>Memory Hierarchy</strong></p>

<ol>
  <li>主記憶體 (RAM)：用於暫存正在執行的程式與資料，速度較快但斷電後資料會消失。</li>
  <li>次級儲存裝置 (SSD/HDD)：用於長期儲存資料，速度較慢但容量較大且斷電後資料不會消失。</li>
  <li>ROM / Flash Memory：用於儲存韌體 (Firmware)，如 BIOS，資料在斷電後仍然保留。
    <ul>
      <li>ROM 就真的是 Read-Only Memory，無法寫入資料。</li>
      <li>但 BIOS 通常會使用 Flash Memory，可以透過韌體更新來修改 BIOS 內容。</li>
    </ul>
  </li>
</ol>

<p><strong>Bus Architecture / Chipset</strong></p>

<p><img src="/image/2026/01-24-BIOS_introduction/2.png" alt="" height="50%" width="50%" /></p>

<ol>
  <li>數據匯流排 (Data Bus)：用於傳輸資料的通道，連接 CPU、記憶體與周邊裝置。</li>
  <li>地址匯流排 (Address Bus)：用於傳輸記憶體地址的通道，CPU 使用地址匯流排來定位記憶體位置。</li>
  <li>控制匯流排 (Control Bus)：用於傳輸控制訊號的通道，協調各個元件之間的操作。</li>
</ol>

<p><strong>Chipset</strong></p>

<ol>
  <li>北橋 (Northbridge)：負責連接 CPU、記憶體與高速周邊裝置，如顯示卡。</li>
  <li>南橋 (Southbridge)：負責連接較慢的周邊裝置，如硬碟、USB 裝置與音效卡。</li>
</ol>

<p>現代架構通常把 北橋整合進 CPU 裡面，這很合理因為北橋控制的基本上就是 CPU 相關的高速周邊元件，
而南橋則通常較複雜，會負責更多週邊裝置的管理。</p>

<blockquote class="block-tip">
  <p>Bus 定義了系統中資料、位址與控制訊號的傳輸規範與通道，而 Chipset 則是實體硬體，負責解碼、仲裁並實作這些 Bus 間的連接與轉換。</p>
</blockquote>

<p><strong>I/O Devices</strong></p>

<p>剩餘的就是各種周邊 I/O 裝置 (Input/Output Devices)，像是鍵盤、滑鼠、顯示器、網路卡、音效卡等等。</p>

<hr />

<h3 id="2-bios-overview">2. BIOS Overview</h3>

<blockquote>
  <p>BIOS (Basic Input/Output System)</p>
</blockquote>

<blockquote class="block-tip">
  <p>現代 PC 通常運行在 OS 環境下執行程式，OS 同時也是一個程式，在 Von Neumann 架構下，
程式必須先被載入到記憶體中才能執行，在 OS 載入之前，PC 就是一個沒有作業系統的硬體平台，
此時運行的第一個程式就是 BIOS。</p>
</blockquote>

<p>因此 BIOS 需要在開機後尚未載入 OS 之前，完成硬體初始化與檢測，其包含以下主要功能：</p>

<ol>
  <li>硬體初始化 (Hardware Initialization)：<br />
BIOS 在電腦開機時會初始化 CPU、記憶體、顯示卡等硬體元件，確保它們能正常運作。</li>
  <li>自檢程序 (Power-On Self Test, POST)：<br />
BIOS 會執行一系列的自檢程序，檢查硬體是否正常運作，若有問題會發出錯誤訊號或顯示錯誤訊息。</li>
  <li>引導程序 (Bootloader)：<br />
BIOS 會尋找並載入作業系統的引導程式，通常是從硬碟、SSD 或其他可引導裝置中尋找，並將控制權交給作業系統。</li>
  <li>硬體設定 (Hardware Configuration)：<br />
BIOS 提供一個介面讓使用者可以設定硬體參數，如系統時間、啟動順序等。</li>
  <li>提供基本 I/O 服務 (Basic I/O Services)：<br />
BIOS 提供一組基本的輸入/輸出服務，這樣在 OS 載入之前，程式仍然可以透過 BIOS 來存取硬體資源。</li>
</ol>

<p><img src="/image/2026/01-24-BIOS_introduction/3.png" alt="" /></p>

<blockquote>
  <p>BIOS/UEFI 的主要任務是在系統開機時初始化硬體、建立啟動環境，並將控制權交給 bootloader，由 bootloader 載入並啟動作業系統。</p>
</blockquote>

<p>BIOS 的執行階段是沒有 DRAM 可以使用的，此時 PC 上的唯一可用記憶體是 CPU 內部的 Register 與 Cache，
指令與資料都必須從 Flash ROM 中讀取，因此 BIOS 程式碼通常會被設計得非常精簡，以確保能在有限的資源下順利執行。</p>

<p>這與我們所認知的 C 執行環境有很大差異，因此幾乎沒有或者非常少的 Stack 可以使用，不使用 Heap，
這階段叫做 Cache-as-RAM (CAR)，BIOS 會把 CPU Cache 當作暫時的記憶體來使用。</p>

<p>直到 BIOS 初始化完畢並啟動 DRAM 後，才會切換到正常的記憶體環境，此時才會有完整的 Stack 與 Heap 可用。</p>

<blockquote class="block-tip">
  <p>這裡會依照 BIOS 的實作不同而有所差異，但大致上都是這樣的流程。</p>
</blockquote>

<p>BIOS 作為 PC 架構中的關鍵韌體，扮演著硬體與作業系統之間的橋樑。在 PC 開機的最初階段，
BIOS 必須在極度受限的環境下完成硬體初始化與自檢程序，為作業系統的載入建立起必要的執行環境。</p>

<blockquote class="block-warning">
  <p>這樣就大致說明了 BIOS 的基本概念與功能，接下來有興趣再進一步探討 BIOS 的工作流程與技術細節。</p>
</blockquote>

<hr />

<h3 id="3-uefi-stages-overview">3. UEFI Stages Overview</h3>

<blockquote class="block-tip">
  <p>UEFI (Unified Extensible Firmware Interface) 是較於 Legacy BIOS 更加現代化的韌體介面。</p>
</blockquote>

<blockquote>
  <p>Reference: <a href="https://uefi.org/specifications">UEFI Specification</a></p>
</blockquote>

<p>Leagcy BIOS 的執行環境都是在 16-bit 下運行，因此即使 CPU 支援 64-bit 還是必須額外實作 16-bit 的執行環境來支援 BIOS，
因此廠商開始想改變這個狀況。16-bit 的 Memory Addressing 極限就是 1MB，這就大大限制了 BIOS 可以做的事情。
並且對開發者來說 Legacy BIOS 的開發多以 Assembly 為主，開發難度較高且不易維護。</p>

<p>當然 UEFI 作為現代化的韌體介面，當然要劃分多個 Phase 來處理不同的任務，以下是 UEFI 的主要階段：</p>

<p><img src="/image/2026/01-24-BIOS_introduction/4.png" alt="" /></p>

<p><strong>SEC (Security Phase)：</strong></p>
<ul>
  <li>主要功能:
    <ul>
      <li>UEFI 的第一個階段，在 Power 或者 Reset 後執行</li>
      <li>這個階段的主要任務驗證 Firmware 的完整性與安全性，確保韌體未被篡改</li>
      <li>建立最小的執行環境，初始化 CPU 基本狀態</li>
      <li>傳遞控制權到下一個階段 PEI</li>
    </ul>
  </li>
  <li>執行環境:
    <ul>
      <li>在這個階段必須建立一塊臨時的記憶體區域 (Temporary RAM)
        <ul>
          <li>通常使用 CAR (Cache-as-RAM) 或者 SRAM</li>
        </ul>
      </li>
      <li>此時的執行代碼位於 ROM 中，並且只能使用非常有限的資源</li>
      <li>CPU 從 Reset Vector (0xFFFFFFF0) 開始執行，這是 CPU 啟動後的預設位置</li>
    </ul>
  </li>
</ul>

<blockquote class="block-tip">
  <p>SEC 階段最初是用 Assembly 編寫的，CPU 在剛啟用時只能運行 Real Mode</p>
</blockquote>

<p><strong>PEI (Pre-EFI Initialization Phase)：</strong></p>
<ul>
  <li>主要功能:
    <ul>
      <li>DRAM 初始化，這樣才能在 DEX 階段使用 DRAM
        <ul>
          <li>通常 DRAM init 會由 Vendor 提供的程式碼來完成</li>
        </ul>
      </li>
      <li>CPU 初始化，將記憶體控制器 (Memory Controller) 設定好
        <ul>
          <li>重新指向 Stack，設定 Exception</li>
        </ul>
      </li>
      <li>Chipset 初始化，設定南北橋等晶片</li>
      <li>DXE 會被解壓縮搬移到 DRAM 中</li>
      <li>透過 Hand-off Block (HOB) 傳遞系統資訊到 DXE 階段</li>
    </ul>
  </li>
  <li>執行環境:
    <ul>
      <li>在這個階段之後都是用 C 作為主要編寫語言</li>
      <li>在這個階段還是沒有 DRAM 可用，因此必須繼續使用 CAR</li>
    </ul>
  </li>
</ul>

<blockquote>
  <p>這些 Init 基本上都是由 PEI Module (PEIM) 來完成，PEI Code 負責管理</p>
</blockquote>

<blockquote class="block-tip">
  <p>PEI 最主要的目的是初始化 DRAM，這樣後續的階段才能使用記憶體支援</p>
</blockquote>

<blockquote>
  <p><a href="https://uefi.org/specs/PI/1.8/V1_Overview.html#pre-efi-initialization-pei-phase">The design for the Pre-EFI Initialization (PEI) phase of a PI Architecture-compliant boot is as an essentially miniature version of the DXE phase of the PI Architecture and addresses many of the same issues.</a></p>
</blockquote>

<blockquote>
  <p>可以說 PEI 是為了進入 DXE 階段的一個不得已而為之的妥協，其目標是為了準備好 DXE 階段的執行環境</p>
</blockquote>

<p><strong>DXE (Driver Execution Environment Phase)：</strong></p>
<ul>
  <li>主要功能:
    <ul>
      <li>DXE 階段才真正的初始化大部分的硬體元件</li>
      <li>載入並執行各種驅動程式 (Driver) 來支援不同的硬體
        <ul>
          <li>GPU、NIC、SATA/NVMe Controller、USB Controller …</li>
        </ul>
      </li>
      <li>建立 UEFI Boot Services 與 Runtime Services
        <ul>
          <li>提供一組標準化的服務介面給後續的 Bootloader 與 OS 使用</li>
        </ul>
      </li>
      <li>建立 ACPI Table、SMBIOS Table 等系統資訊</li>
    </ul>
  </li>
</ul>

<blockquote>
  <p><a href="https://uefi.org/specs/UEFI/2.9_A/07_Services_Boot_Services.html">Boot Services</a> 提供在 OS 載入前使用的服務，而 <a href="https://uefi.org/specs/UEFI/2.9_A/08_Services_Runtime_Services.html">Runtime Services</a> 則是在 OS 載入後仍然可用的服務</p>
</blockquote>

<ul>
  <li>執行環境:
    <ul>
      <li>在這個階段已經可以使用 DRAM，因此可以使用完整的 C 執行環境</li>
      <li>可以使用 Stack 與 Heap，並且可以使用標準的 C 函式庫</li>
    </ul>
  </li>
</ul>

<blockquote class="block-tip">
  <p>DXE 可以說是 BIOS 的主戰場，在這個階段可以進行大量的硬體初始化與設定</p>
</blockquote>

<p><strong>BDS (Boot Device Selection Phase)：</strong></p>
<ul>
  <li>主要功能:
    <ul>
      <li>根據 Boot Order 選擇開機裝置</li>
      <li>嘗試從選定的裝置載入 Bootloader (GRUB、Windows Boot Manager …)
        <ul>
          <li>可載入的 EFI 並不限於 OS Bootloader，也可以是其他的 EFI 應用程式</li>
          <li>UEFI Shell, Firmware Update Utility, Diagnostics Tool …</li>
        </ul>
      </li>
      <li>這個階段就提供使用者 Boot Menu 來選擇開機裝置</li>
    </ul>
  </li>
  <li>執行環境:
    <ul>
      <li>繼續使用 DXE 階段建立的執行環境</li>
    </ul>
  </li>
</ul>

<blockquote class="block-tip">
  <p>BDS 是使用者與 UEFI 互動的主要階段，包括 BIOS Setup 介面、Boot Menu 都在這裡處理</p>
</blockquote>

<p><strong>TSL (Transient System Load Phase)：</strong></p>
<ul>
  <li>主要功能:
    <ul>
      <li>Bootloader 已經被 BDS 載入並執行</li>
      <li>將控制權交給 Bootloader，但在這個階段 UEFI Boot Services 仍然可用</li>
      <li>為 OS 準備好啟動環境</li>
    </ul>
  </li>
</ul>

<p>在這個階段 Bootloader 可以透過 UEFI 提供的 Boot Services 來存取硬體資源，
並建立 OS 所需的啟動環境，例如 Memory Map、ACPI Table 等。</p>

<blockquote>
  <p>此階段的關鍵是當 ExitBootServices() 被呼叫後，至此進入 Runtime Phase，由作業系統接管系統控制權。</p>
</blockquote>

<blockquote class="block-tip">
  <p>在這個階段是 Bootloader 執行期間、ExitBootServices 之前的過渡階段，其實就是 BIOS 與 OS kernel 的交接點。</p>
</blockquote>

<p><strong>RT (Runtime Phase)：</strong></p>
<ul>
  <li>主要功能:
    <ul>
      <li>OS 已經被載入並執行，在這個階段 UEFI Boot Services 不再可用</li>
      <li>但 UEFI Runtime Services 仍然可用，提供一些系統資訊與服務給 OS 使用
        <ul>
          <li>UEFI Variable Services: 某些廠商的特殊功能變數 (RGB LED Control …)</li>
          <li>RTC (Real-Time Clock) Read/Write</li>
        </ul>
      </li>
    </ul>
  </li>
  <li>執行環境:
    <ul>
      <li>由 OS 接管系統控制權，UEFI 僅提供 Runtime Services</li>
    </ul>
  </li>
</ul>

<blockquote>
  <p>在 ExitBootServices() 被呼叫後 BIOS 在 DRAM 中的區塊會被釋放，僅保留 Runtime Services 所需的部分</p>
</blockquote>

<blockquote class="block-tip">
  <p>RT 階段基本上就是 OS 執行期間，UEFI 僅提供有限的 Runtime Services 給 OS 使用</p>
</blockquote>

<p><strong>AL (After Life Phase)：</strong></p>
<ul>
  <li>主要功能:
    <ul>
      <li>處理 OS 的關機或重啟請求</li>
      <li>S3 / S4 / S5 等電源狀態管理</li>
      <li>在這個階段 UEFI 會協助 OS 進行系統關機或重啟</li>
    </ul>
  </li>
  <li>執行環境:
    <ul>
      <li>由 OS 控制系統電源狀態</li>
    </ul>
  </li>
</ul>

<blockquote class="block-tip">
  <p>AL 階段主要是處理系統關機或重啟的相關工作，只負責 Reset 或 Power Off，為下一次開機做準備</p>
</blockquote>

<hr />

<blockquote class="block-warning">
  <p>以上說明了一個 BIOS 在 PC 中的主要腳色與 UEFI 的各個階段功能概述，後續要研究的話可以從各個階段的細節開始著手，
了解每個階段的工作流程與技術細節。</p>
</blockquote>

<blockquote class="block-warning">
  <h5 id="last-edit">Last Edit</h5>
  <p>01-24-2026 18:36</p>
</blockquote>]]></content><author><name>Benson Hsu</name></author><category term="Jekyll" /><category term="bios" /><summary type="html"><![CDATA[BIOS 全名為 (Basic Input/Output System)，負責在電腦開機時初始化硬體並呼叫 Bootloader 來載入作業系統。]]></summary></entry></feed>